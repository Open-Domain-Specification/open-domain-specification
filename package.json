{
	"name": "open-domain-specification",
	"version": "0.0.0",
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"format": "npx @biomejs/biome check --write",
		"dev:ui": "lerna run build --scope ods-example-ws && lerna run dev --scope ods-ui",
		"build": "lerna run build --stream"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.1.3",
		"lerna": "^8.2.3"
	}
}
