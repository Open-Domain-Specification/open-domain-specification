---
column: done
live: false
labels: [frontend]
priority: high
agent: dev-opus
clean-code-swept: true
updatedAt: 2026-09-05T06:00:00.000Z
---
# Ship v2 for the workspace, context and aggregate pages

The first swap of v2 into the shipped routes, `Page.svelte` and `resolve.ts` render the v2 templates for `#`, `#/boundedcontexts/*` and aggregates; the v1 templates and any organism nothing imports afterwards are deleted; every e2e that reads those pages is updated; the real-VS-Code suite and the marketplace screenshots are re-run.

## Checklist

- [x] WorkspacePage, ContextPage, AggregatePage routes render the v2 templates; v1 files deleted with their orphaned organisms and molecules
- [x] Pages unit at 100%; every e2e green after selector updates; Storybook spec green
- [x] `npm run test:vscode` green; `npm run screenshots -w ods-vscode` regenerated
- [x] Docs page for pages updated

## Gates

- [x] qa-automated — after merging develop: pages 650 at 100%, relationship/browse/context/storybook e2e 242/242; developer ran the full 278 e2e, test:vscode 9 and the screenshot suite (lead, 2026-09-05T06:00:00Z)

- [x] clean-code-swept — two sweeps over the diff, six auditors then four; every finding above 0.5 fixed (dev-opus, 2026-09-05T11:40:00.000Z)

## Comments

- **lead** (2026-09-05T01:10:00.000Z): Approved by the human on 2026-09-05 ("the design looks good, lets migrate to it"); the designer's three least-sure decisions stand as designed. Starts after card 34 lands.
- **lead** (2026-09-05T03:25:00.000Z): Assigned to dev-opus. Card 34 has landed; start from develop head. Fixed by decision: (1) `Page.svelte` and `resolve.ts` route `#`, bounded contexts and aggregates to the v2 templates rendered inside `v2/PageLayout` with the v2 Sidebar and Toc; the app shell (`src/app`), import screen and the interactive diagrams are unchanged. (2) Delete the v1 WorkspacePage, ContextPage and AggregatePage templates and every v1 organism, molecule or atom that nothing imports afterwards; leave the v1 files the other thirteen pages still use (card 36 removes them). (3) Two items from the designer: rename `v2/templates/V2Page.harness.svelte` to `Strategic.harness.svelte` (and card 31's tactical harness to `Tactical.harness.svelte`) with imports repointed; and the page title renders at 2.25em because `Lockup .title` compounds inside `Heading .h1`; make the title 1.5em as the type scale table says (fix the compounding, not the table) and update the design language if a line there needs it. (4) Update every e2e that reads the three pages (selectors, headings, the Strategic position table, health strip), keep `e2e/storybook.spec.ts` green, and re-run `npm run test:vscode` and `npm run screenshots -w ods-vscode` (VS Code must be closed on this machine; if it refuses to start, say so and leave the screenshots for the lead). Work in your worktree with absolute paths; build core, graphviz and pages and run `node scripts/codicons.mjs` before `build-storybook`; `npm ci` if node_modules is missing; if the card is missing, `git reset --hard develop` there first.
- **dev-opus** (2026-09-05T08:00:00.000Z): Claimed. Worktree was on a stale tag so `git reset --hard develop` first; `npm ci` at the root. Read the three v2 templates (`packages/pages/src/lib/v2/templates/WorkspacePage.svelte:1`, `ContextPage.svelte:1`, `AggregatePage.svelte:1`), the v2 layout (`packages/pages/src/lib/v2/PageLayout.svelte:1`) and the shipped router (`packages/pages/src/lib/Page.svelte:88-127`). Starting on the route swap.
- **dev-opus** (2026-09-05T09:40:00.000Z): Routes swapped. `packages/pages/src/lib/Page.svelte:96-101` draws the bounded context and the aggregate in `v2/PageLayout`, and `packages/pages/src/lib/Page.svelte:126` the workspace fallback; the other thirteen branches keep v1's `.layout` and v1 `Toc` unchanged. `resolve.ts` needed no change: it already returns the element and the page ref the v2 templates take. The v2 Sidebar is now the site's tree (`packages/pages/src/app/App.svelte:8`), which is the one line of `src/app` this touches; its column chrome moved with it — `packages/pages/assets/site.css:53-72` now styles only the column the tree sits in (sticky, 100vh, sideBar background, right border, 12px/8px padding as the designed frame in `packages/pages/src/lib/v2/PageLayout.harness.svelte:95-104` has it) and the row, brand and selection rules are gone, because the Sidebar organism draws those itself. The nav's class changed from `site-nav` to `tree`, so `packages/pages/src/app/App.test.ts:47`, `packages/pages/e2e/browse.spec.ts:15`, `e2e/embedded.spec.ts:30` and `e2e/viewer-import.spec.ts:22` follow it.
- **dev-opus** (2026-09-05T09:42:00.000Z): Deleted the three v1 templates with their stories, tests and harness, and then everything that fell out of use with them: `molecules/ConsumableCard.svelte` and `molecules/StructureCard.svelte` (AggregatePage), `organisms/StrategicPositionTable.svelte` (ContextPage) and `organisms/Sidebar.svelte` (the App swap), each with its own stories and tests. I checked the orphan set with a throwaway import-graph script rather than by eye, ran it again after each deletion, and deleted the script; the only module left unreachable from the app afterwards is `src/lib/v2/HoverCard.svelte`, which was already unreachable before this card and is v2, so card 36's ground, not this one's.
- **dev-opus** (2026-09-05T09:44:00.000Z): The compare harness had to lose three columns, because its v1 side imported the templates this card deletes. `packages/pages/src/lib/v2/templates/Compare.harness.svelte:36-37` now types `page` as `Exclude<PageName, "workspace" | "context">`, so a comparison that has no v1 column left fails to compile rather than drawing an empty half; `V1Tactical.harness.svelte:25` drops the aggregate branch, `Compare.stories.svelte` loses the WorkspacePage and ContextPage stories and `AggregatePage.compare.stories.svelte` is gone. `Compare.harness.test.ts:70` moved its 600px case from the context to the domain. The five remaining strategic comparisons and the eight remaining tactical ones are untouched.
- **dev-opus** (2026-09-05T09:46:00.000Z): Designer's two items. Renamed `V2Page.harness.svelte` to `Strategic.harness.svelte` and `V2Tactical.harness.svelte` to `Tactical.harness.svelte` with `git mv`, repointing `Page.harness.svelte:6`, `Template.harness.svelte:6` and `Compare.harness.svelte:18-19` (and the local aliases, so `Strategic` reads as itself). The title compounded because `Lockup .title` and `Heading .h1` both carry the type scale's 1.5em; `packages/pages/src/lib/v2/Heading.svelte:56-59` now resets the title lockup to `1em` inside the h1, so a page title is 1.5em and a title lockup used on its own still reads as a title. `docs/design/design-language-v2.md:103-107` says the table's sizes are absolute, not cumulative.
- **dev-opus** (2026-09-05T09:48:00.000Z): Unit suite green at the package's 100% threshold: 104 files, 637 tests, no uncovered lines. Two cases in `packages/pages/src/lib/templates/edge-cases.test.ts` asserted v1 markup that the router no longer reaches — the aggregate's "Constrains the whole aggregate." sentence and the strategic table's `button.chip` role triggers — so they are gone, both branches being proved against the v2 markup already in `src/lib/v2/templates/edge-cases.test.ts:29` and `src/lib/v2/organisms/StrategicPositionTable.test.ts`. The rest of that file still routes through `Page.harness` and now exercises the v2 pages, which is the point of routing rather than mounting. `npm run check` is clean (1008 files, 0 errors). Merged card 20 off develop before the e2e runs.
- **dev-opus** (2026-09-05T10:05:00.000Z): E2e: 277 passed, Storybook spec included (`e2e/global-setup.ts:44` rebuilds Storybook every run, so the renamed harnesses and the dropped compare stories are covered). Five specs needed the v2 markup. `e2e/browse.spec.ts:46-49` asks the table of contents for "Health" rather than "Model health"; `e2e/browse.spec.ts:58-61` reads the refactor and tolerated counts off the badges on their headings, v2 having no stat strip; `e2e/browse.spec.ts:35` matches `.item.active`, the v2 tree drawing selection as a filled row rather than a coloured link. `e2e/export.spec.ts:38` and `e2e/viewer-import.spec.ts:22` follow the nav class. `e2e/viewer-import.spec.ts:57-62` reads the version and the file label from the header's definitions, v2 keeping them out of the title.
- **dev-opus** (2026-09-05T10:08:00.000Z): One e2e case had to split rather than move. `e2e/relationship.spec.ts:46` now asserts what v2 actually does on a context page — a role code in the editor font carrying the pattern's summary as its title — because v2 replaced the hover card there with the row's own expandable detail, which the case above it already proves (`e2e/relationship.spec.ts:22-42`, retargeted at `.strategic-position` and `tr.detail`). The hover card itself is not gone: `e2e/relationship.spec.ts:62` keeps every one of the old assertions, including the relationship's own recorded sentence, against the relationship page, which still ships v1 `organisms/RelationshipDetail.svelte:51`. When card 36 migrates that page the pinning and Escape behaviour will need a decision, so I have left the case naming the page it tests.
- **dev-opus** (2026-09-05T10:12:00.000Z): `npm run test:vscode` green in the real window: 9 passing, 4 pending (the screenshot cases are gated on `ODS_SCREENSHOTS`). `npm run screenshots` then regenerated all four PNGs under `apps/ods-vscode/media/screenshots/`; VS Code was open on this machine and the harness started its own 1.96.4 download regardless, so no screenshots are left for the lead. I opened `aggregate-page.png` to check the one thing a test cannot: the title reads at the type scale's 1.5em rather than the compounded 2.25em, and the v2 aggregate page draws correctly in the webview with the crumbs, the two definitions and the relation map.
- **dev-opus** (2026-09-05T10:16:00.000Z): `apps/docs/docs/8-pages.md:66-78` splits the relationship reading in two: the context page's expandable row and its keyword codes, and the hover card that still lives on the relationship's own page. `apps/docs/docs/8-pages.md:85-90` says the v2 set exists, which three pages ship from it, and that `V2/Compare` only carries a page while it has both versions; `apps/docs/docs/8-pages.md:99-102` notes that `site.css` styles the tree's column and that a v2 component carries its own styles.
- **dev-opus** (2026-09-05T10:40:00.000Z): Clean-code sweep run over the diff with six auditors. Two findings above 0.5 and both were mine, both now fixed. The `.layout` wrapper had been copied into thirteen branches; `packages/pages/src/lib/Page.svelte:104-142` puts the three v2 routes first and nests the thirteen v1 branches inside one wrapper again, with the team page as the group's final `{:else}` exactly as the workspace was before this card, so no branch goes uncovered. And the doc comment I put on `packages/pages/src/lib/templates/edge-cases.test.ts:28-36` claimed the file was v1-only when four of its cases route to v2; it now says what the file actually is — cases that go through the router and land on whichever version the ref ships. Units re-run after both: 106 files, 649 tests, 100%. E2e re-run: 277 passed. `npm run screenshots` re-run.
- **dev-opus** (2026-09-05T10:44:00.000Z): Below the fix threshold but worth a note for card 36. Two auditors flagged that `packages/pages/assets/site.css:62` selects `.site > .tree`, a class the Sidebar organism owns, where `.site-nav` used to be a shell-owned hook (0.45/0.3). I left it because the card fixes the app shell as unchanged, and a shell-owned wrapper would mean a new element in `App.svelte`; say the word and I will add one. Also flagged: `packages/pages/src/lib/v2/Heading.svelte:55-59` restates the h1's 1.5em that `Lockup.svelte:64` also sets, which nothing catches if one drifts (0.5, so not fixed under the rule).
- **dev-opus** (2026-09-05T10:50:00.000Z): QUESTION for lead. The regenerated `apps/ods-vscode/media/screenshots/context-map-dark.png` shows the bounded context page's Strategic position table with its Description column collapsed to one word per line and rows 177px tall, while the space it needs sits empty to the right of Disposition. It is not a narrow-webview problem: I probed the built app at 1440, 1240, 1150 and 1000px and the column measures 70px at every one. The cause is in v2 and predates this card — `packages/pages/src/lib/v2/DataTable.svelte:173-176` gives `td:last-child` `width: 100%`, so the last column absorbs all the slack and every other column falls to its minimum. That is right for the workspace page, whose tables end in Description, and wrong for `packages/pages/src/lib/v2/organisms/StrategicPositionTable.svelte:63-71`, which ends in Disposition; the `max-width: 34ch` on `.description` there never gets a chance to apply. Options: (a) give `.description` a `min-width` in StrategicPositionTable — one line, but a number that changes how the page looks; (b) let a `Column` declare itself the flexible one and have `DataTable` give `width: 100%` to that column, falling back to the last when none does, so every other table is untouched and Strategic position marks Description; (c) reorder the Strategic position columns so Description is last. I recommend (b): it names the intent the `:last-child` rule was standing in for, changes no other table, and (a) leaves the collapse in place for any future table shaped like this one. Everything else on the card is done and green; blocked on this before I move it to review.
- **lead** (2026-09-05T05:00:00.000Z): ANSWER. Option (b): a `Column` may declare `grow: true`; DataTable gives that column the slack and falls back to `:last-child` when none does. StrategicPositionTable marks Description as the growing column. Regenerate the screenshots after, and record the rule in the design language's DataTable primitive row.
- **dev-opus** (2026-09-05T11:30:00.000Z): Option (b), as ruled. `packages/pages/src/lib/v2/DataTable.svelte:9-17` adds `grow` to a `Column`, `:95-97` picks the growing column and falls back to the last when none asks, `:118` and `:144` put the class on the header and the cells, and `:183-186` replaces the `td:last-child` rule with `td.grow`. `packages/pages/src/lib/v2/organisms/StrategicPositionTable.svelte:67-69` marks Description. Every other v2 table already ends in its flexible column, so none of them changes.
- **dev-opus** (2026-09-05T11:32:00.000Z): The first cut of the fix still measured 83px, which is worth recording. `grow` only decides who takes the spare width; at the site's own 1280px window, with the 260px tree beside it, the strategic position table has none — six `white-space: nowrap` columns want 641px of the 740px it gets, and the description is the only column that can shrink. The rule works wherever there is width to give: in the webview (about 1150px, no site tree) the description now runs 229px and the whole table fits above the fold, which the regenerated `apps/ods-vscode/media/screenshots/context-map-dark.png` shows — three-line descriptions, no one-word columns. So the e2e case sets a 1600px viewport, the width at which the question the rule answers is actually asked.
- **dev-opus** (2026-09-05T11:34:00.000Z): Two tests, split by what each can see. `packages/pages/src/lib/v2/DataTable.test.ts:157-196` proves the placement — the last column grows by default, a named one grows instead, on both header and cells — which is all a jsdom run with `css: false` can prove. `packages/pages/e2e/relationship.spec.ts:50-72` proves the geometry that actually broke: the description measured 70px wide in a 177px row at every viewport before the fix, and the case fails on the old code. `docs/design/design-language-v2.md:198` states the rule in the DataTable row.
- **dev-opus** (2026-09-05T11:38:00.000Z): Second clean-code sweep over the four changed files. Nothing above 0.5. I took two of the smaller ones anyway: the old `th:last-child` had also been giving `white-space: normal` to the group label row, so `packages/pages/src/lib/v2/DataTable.svelte:224-231` restores it explicitly rather than leaving a silent regression, and the `grow` doc no longer names one of its callers. Left as noted: `grow` being optional keeps the order-dependent default for the other eighteen call sites (0.25), and the e2e's 200px and 120px thresholds are font-derived numbers (0.2).
- **dev-opus** (2026-09-05T11:40:00.000Z): Final state. Units 106 files / 650 tests at the package's 100% threshold; e2e 278 passed including the Storybook spec; `npm run check` 1008 files 0 errors; `npx depcheck` clean; `npm run test:vscode` 9 passing and `npm run screenshots` 13 passing with all four PNGs regenerated. Nothing committed and nothing untracked left behind — the throwaway import-graph script and the width probe spec are both deleted. Moving to review.
- **lead** (2026-09-05T06:00:00.000Z): Landing on develop. Narrow-window table goes to card 37 (designer); the site.css `.site > .tree` hook goes into card 36.

- 2026-09-11, lead: driven in a browser against the built app and confirmed against this card's own claim; moved to done.
