---
column: done
labels: [frontend]
priority: high
agent: dev-opus
live: false
clean-code-swept: true
updatedAt: 2026-09-04T20:05:00.000Z
---
# v2 templates, strategic pages (design language v2, part 2 of 3)

WorkspacePage, DomainPage, SubdomainPage, ContextPage, RelationshipPage, TeamPage and the health page, per docs/design/v2-specs/template-*.md, built on the v2 organisms from card 29 (import from `v2/organisms/`; if an organism you need is not there yet, build the minimal version the spec describes under the same path and journal it so card 29 reconciles).

## Checklist

- [x] `v2/templates/`: WorkspacePage, DomainPage, SubdomainPage, ContextPage, RelationshipPage, TeamPage, HealthPage, each rendered inside `v2/PageLayout` in a `V2/Templates/...` story against petstore, light, dark and high contrast
- [x] One `V2/Compare/...` story per template rendering v1 and v2 side by side in two columns at 1200px, for the morning review
- [x] Tests at 100%; `e2e/storybook.spec.ts` green over the new titles

## Gates

- [x] clean-code-swept — clean-code-review over the new v2 files: four findings, all under the 0.5 threshold (two DRY, one naming, one harness duplication); fixed the three cheap ones anyway, left the fourth (a one-line keyword repeated in three organisms) as it stands rather than abstract a single word (dev-opus, 2026-09-04T20:00:00.000Z)
- [x] tests — `npx vitest run --config vitest.config.ts --coverage` in packages/pages: 100 files, 603 tests, 100% statements, branches, functions and lines (dev-opus, 2026-09-04T20:02:00.000Z)
- [x] storybook — `node scripts/codicons.mjs && npm run build-storybook` green, then `npx playwright test storybook.spec.ts`: 109 passed, covering all 79 `V2/` stories including the seven new template titles, `V2/Compare` and the three `V2/Organisms` (dev-opus, 2026-09-04T20:04:00.000Z)

## Comments

- **lead** (2026-09-04T16:00:00.000Z): Assigned to dev-opus. Build the v2 versions as new files under `packages/pages/src/lib/v2/` (organisms under `v2/organisms/`, templates under `v2/templates/`), rendered only by `V2/...` Storybook stories against the petstore model. No shipped file changes; v1 stays as it is. Follow docs/design/design-language-v2.md and the matching spec in docs/design/v2-specs/ exactly; where a spec is silent, choose the denser, plainer option and journal it. Use only the v2 primitives (Keyword, Lockup, Ref, DefinitionList, DataTable, Heading, Comments, Disposition, EmptyState, HoverCard) plus InteractiveDiagram, which is kept as is. Every component: light, dark and high-contrast stories via `v2/Theme.harness.svelte`, a density story where rows are laid out, tests at 100% coverage, `{#snippet template()}` story bodies, and `e2e/storybook.spec.ts` green over the new titles. Work in your worktree with absolute paths for every suite; build core, graphviz and pages first (`npm run build -w packages/core -w packages/graphviz -w packages/pages`) and run `node scripts/codicons.mjs` in packages/pages before `build-storybook`; `npm ci` if node_modules is missing; if the card is missing, `git reset --hard develop` there first. Card 29 is building the organisms in parallel; card 31 builds the tactical templates.
- **dev-opus** (2026-09-04T17:05:00.000Z): Picked this up. The worktree was on the v0.2.0 tag, so `git reset --hard develop` first. Card 29's work is not in my tree — `packages/pages/src/lib/v2/` holds the ten primitives and `Theme.harness.svelte` and nothing else — so per the card I am building the minimal organisms the specs describe under `v2/organisms/` for card 29 to reconcile: Problems, Section, PageHeader, Toc, DiagramFigure, AttributeTable, ProvidesTable, ConsumesTable, RelationshipDetail, StrategicPositionTable, HealthReport, plus `v2/PageLayout.svelte`.
- **dev-opus** (2026-09-04T18:35:00.000Z): First three pages and the shared parts are in and green. `v2/PageLayout.svelte:26-52` is v1's `.layout` carried into the component so a v2 page draws without the v1 stylesheet; `v2/organisms/Toc.svelte:20-26` drops the tracked capitals for plain secondary text and keeps the left rule and the scroll-not-navigate click, which is all `organism-toc.md` asks for (v1 tracks no active row and the spec says nothing else changes, so neither does v2). `v2/organisms/Section.svelte:33-38` is a level-2 `Heading` with `lead` and `count` and the restyled problems above the children; `v2/organisms/Problems.svelte:17-25` is the Problems-panel row — severity codicon in its colour, rule id as a `mono` keyword, message, `Ref "go to"`, no frame. `v2/organisms/PageHeader.svelte:38-46` takes the title as a snippet: only the template knows the kind, id and detail of its lockup, and the health page (a read, not an element) has no lockup at all — journalled as my reading of "Nothing sits inside the h1 but the lockup". `v2/organisms/DiagramFigure.svelte:29-34` is the canvas between two hairlines with the caption under it, no rounded frame.
- **dev-opus** (2026-09-04T18:36:00.000Z): The three pages the specs describe without an expandable table: `v2/templates/DomainPage.svelte:36-45` (subdomains as one badged table, the map after it), `v2/templates/SubdomainPage.svelte:45-60` (classification as the keyword under the title plus one definition in the header — v1's empty Classification section is gone, so `sections` is `serving`/`map` only), `v2/templates/TeamPage.svelte:60-78` (both card grids become tables, the `·`-joined counts line becomes numeric columns). Three small row units the specs call for came out of them and sit under `v2/organisms/`: `ContextLockup.svelte:23-32` (lockup plus the `warn` keyword for a big ball of mud, replacing `ContextPill`), `ContextList.svelte:17` (comma-separated, replacing `RefList`), `TeamLockup.svelte:14` (replacing `TeamLine`), and `SubdomainTable.svelte:34-52`, which is `SubdomainCard` as rows with the classification as the sortable keyword column the spec names as the replacement for the lost colour. Stories: `V2/Templates/{DomainPage,SubdomainPage,TeamPage}` in light, dark and high contrast through `v2/templates/Template.harness.svelte:38-50`, and `V2/Compare` with one story per template through `v2/templates/Compare.harness.svelte:27-56` — the compare canvas is two columns capped at 1200px with v1 on the left as it ships and v2 on the right in the light theme, and neither column gets the table of contents, since what is being compared is the content. Suite: `npx vitest run --config vitest.config.ts --coverage` in packages/pages, 88 files, 554 tests, 100% statements, branches, functions and lines; `npm run check` 0 errors; `npx biome check` clean.
- **dev-opus** (2026-09-04T18:40:00.000Z): QUESTION for lead: two specs need a table row that spans every column *under* a data row — `organism-strategic-position-table.md` ("The expanded row spans all columns and renders `RelationshipDetail`") and `organism-health-report.md` ("the `Comments` for the row render in a full-width row under it, indented by the gutter"). `v2/DataTable.svelte:120-131` renders exactly one `<tr>` per row and offers no way to add one, and DataTable is a shipped primitive from card 28 that card 29 is working next to, so I am not changing its shape on my own. Options: (a) add an optional `detail?: Snippet<[T]>` to `v2/DataTable`, rendered as `<tr class="detail"><td colspan={columns.length}>` after each row when the snippet returns content — purely additive, no existing caller changes, the strategic position renders it only for the expanded row and the health report always; (b) hand-roll the `<table>` inside `v2/organisms/StrategicPositionTable.svelte` and `HealthReport.svelte`, which duplicates DataTable's header, grouping, hover and high-contrast rules twice; (c) render the detail under the table rather than inside it, which reads as a different component and deviates from both specs. I recommend (a), and I am happy for card 29 to own the DataTable edit and for me to build on it instead, if you would rather the primitive change lived there. ContextPage, WorkspacePage, RelationshipPage and HealthPage all wait on this. Blocked until answered.
- **dev-opus** (2026-09-04T19:30:00.000Z): Answered — (a) and `v2/molecules/`, with StrategicPositionTable, RelationshipDetail and HealthReport now mine. `v2/DataTable.svelte:37-56,131-133` gains `detail?: Snippet<[T]>` and `hasDetail?: (row) => boolean`: a row may be followed by one row spanning every column, drawn only for the rows the caller says have any. `DataTable.svelte:206-212` keeps it out of the hover, since a detail row is the row above's content and not a row of its own. Covered by `DataTable.test.ts:142-157` and shown by the new `V2/DataTable` "Detail rows" story.
- **dev-opus** (2026-09-04T19:32:00.000Z): The molecule/organism split, as instructed. `v2/molecules/`: `Problems` (v1 molecule), `ContextLockup` (`ContextPill`), `ContextList` (`RefList`), `TeamLockup` (`TeamLine`), `SubdomainTable` (`SubdomainCard`), `ProvidesTable`, `ConsumesTable`, `AttributeTable` (all three v1 molecules restyled), plus two new ones the pages needed twice each: `Crumbs.svelte:14-16`, shared by `PageHeader` and the relationship page, and `Joined.svelte:12-22`, which draws the comma between items in a list with a `::before` rule. `v2/organisms/`: `PageHeader`, `Section`, `Toc`, `DiagramFigure`, `StrategicPositionTable`, `RelationshipDetail`, `HealthReport`. Card 29 keeps `AttributesSection`, `LanguageSection`, `Sidebar` and `InteractiveDiagram`.
- **dev-opus** (2026-09-04T19:34:00.000Z): The three organisms. `v2/organisms/RelationshipDetail.svelte:70-78` is the title as two context lockups with the arrow between, the type as a keyword and the disposition as the Problems mark — no outer card and no inner cards; roles are a definition list per side (`:90-105`), and a side that plays no role prints the relationship's own summary, as v1 did (the spec is silent). `StrategicPositionTable.svelte:79-129` is the grouped `DataTable` with the chevron and disposition columns present only when the position has something recorded, and the expanded row is the detail block at `:139-141`. `HealthReport.svelte:56-97` makes the three stat tiles into the three count badges and each section a table whose comments are the row's detail row; "No comments" stays collapsed behind the chevron in its heading (`:69-81`) and opens without comment rows.
- **dev-opus** (2026-09-04T19:36:00.000Z): The four remaining pages. `v2/templates/ContextPage.svelte` — aggregates and services as tables whose numeric columns replace the `·`-joined counts line (`:154-202`), schemas as level-3 subsections with their attribute table (`:251-266`), provides/consumes/policies/language as tables. Two silences resolved the plainer way and journalled: the Services heading and table are always drawn (v1 hid them when empty; the spec sketch shows both, and an empty table says "No services."), and the strategic position's generated sentence is plain text rather than v1's dimmed `title="generated"`. `WorkspacePage.svelte` — version and file as two definitions (`:67-72`), one subdomain table per domain under a level-3 heading (`:82-88`), contexts and teams as tables, and v1's two health sections folded into one `Model health` that keeps the structural problems, then the report, then the link to the full page; the tick line is the `pass` codicon in `icon.foreground` with a secondary sentence (`:147`). `RelationshipPage.svelte:34-38` is the crumbs and the detail at `heading="h1"`. `HealthPage.svelte:30-38` draws its own header rather than `PageHeader`: it is a read of the workspace, so it has no lockup and no id.
- **dev-opus** (2026-09-04T19:38:00.000Z): Stories and suites. `V2/Templates/{WorkspacePage,DomainPage,SubdomainPage,ContextPage,RelationshipPage,TeamPage,HealthPage}` in light, dark and high contrast, each inside `v2/PageLayout` via `templates/Template.harness.svelte:60-80`; `V2/Compare` with one story per template via `Compare.harness.svelte:45-90` — v1 as it ships on the left, v2 on the right, two columns capped at 1200px, no table of contents in either, and the elements picked once in `picks.harness.ts` so both columns always show the same page (Sales for the context, since it fills all three position groups; the refactor-marked relationship for the relationship page). Evidence: `npx vitest run --config vitest.config.ts --coverage` in packages/pages — 100 files, 603 tests, 100% statements, branches, functions and lines; `npm run check` 0 errors over 950 files; `npx biome check packages/pages/src/lib/v2` clean; `node scripts/codicons.mjs && npm run build-storybook` green; `npx playwright test storybook.spec.ts` 100 passed, which includes all 70 `V2/` stories.
- **dev-opus** (2026-09-04T19:40:00.000Z): On the lead's note about the two Svelte coverage traps: both were already avoided here after I hit them — `organisms/Toc.svelte:23` writes the anchor as an expression, and `molecules/Problems.svelte:21` puts the message in its own span. Two more of the same family are worth passing on: a caption written as `caption="{d.name} context map"` has the trap in an attribute (now a `$derived` template literal, `templates/DomainPage.svelte:32`), and `{#if i}, {/if}` between items in an `{#each}` costs a branch that only a list of two or more can reach — `molecules/Joined.svelte` draws that comma in CSS instead and removes the branch everywhere.
- **dev-opus** (2026-09-04T20:00:00.000Z): Clean-code sweep over the new files. Four findings, all under 0.5, three fixed: the big-ball-of-mud wording is now one exported constant, `molecules/ContextLockup.svelte:2-8`, read by both the lockup and the context page's header; `organisms/StrategicPositionTable.svelte:79` passes the position groups straight to `DataTable` instead of renaming and re-mapping them into an identically-shaped array; and the seven-branch page switch the two story harnesses each carried is now one file, `templates/V2Page.harness.svelte:57-71`, so the compare story's two columns cannot drift apart. Left alone: a one-line `<Keyword text={type} title={PATTERNS[type].summary} />` that appears in three organisms (0.2) — a component for one word would cost more than it saves. Also added `V2/Organisms/{StrategicPositionTable,RelationshipDetail,HealthReport}` stories in the three themes, since those three came to me from card 29 and would otherwise have shipped without any. Final: 100 test files, 603 tests, 100% coverage; `npm run check` 0 errors 0 warnings over 956 files; `npx playwright test storybook.spec.ts` 109 passed over 79 `V2/` stories.
- **dev-opus** (2026-09-04T18:41:00.000Z): QUESTION for lead (second, smaller): the specs restyle four v1 *molecules* that the strategic pages need — `Problems`, `ProvidesTable`, `ConsumesTable`, `AttributeTable` — plus the three row units above. The card sanctions `v2/organisms/` and `v2/templates/` and nothing else, so I have put them under `v2/organisms/`. Confirm that, or say `v2/molecules/` and I will move them (a rename, nothing else).
- **lead** (2026-09-04T18:10:00.000Z): ANSWERS. (1) Option (a): an optional `detail` snippet on v2/DataTable rendered as a colspan row after each row. Card 30 owns that edit and, to unblock its pages, also owns the v2 StrategicPositionTable, RelationshipDetail and HealthReport organisms; card 29 has been told to drop them. (2) Molecules go under `v2/molecules/`; move the ones you have. Card 29 keeps Sidebar, AttributesSection, InvariantsSection, LanguageSection and the layout shell story; on merge the lead takes card 30's versions of anything both wrote.

- 2026-09-11, lead: driven in a browser against the built app and confirmed against this card's own claim; moved to done.
