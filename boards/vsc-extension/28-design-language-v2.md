---
column: done
labels: [frontend, docs]
priority: high
agent: designer-fable
live: false
clean-code-swept: true
updatedAt: 2026-09-04T16:30:00.000Z
---
# Design language v2: VS Code UX guidelines, information density, and v2 primitives in Storybook

The pages UI grew as a port of a Mantine-style web app: chips and badges for everything, boxed cards, decorative colour. It runs inside VS Code, a static export and a browser, and its readers are architects, developers and product people scanning a lot of structured information. This card produces the design language the v2 views will follow and the v2 primitives (atoms and molecules) in Storybook, beside v1, without touching any shipped template.

## Checklist

- [x] `docs/design/design-language-v2.md`: principles (VS Code UX guidelines for webviews; density; hierarchy by type and space, not boxes; colour only for meaning; codicons for kinds; native-feeling lists, tables and links), the type scale in VS Code units, spacing scale, the token map onto `--vscode-*` variables, and a component-by-component v1 to v2 verdict (keep, restyle, replace, remove) for every file in packages/pages/src/lib/{atoms,molecules,organisms,templates}
- [x] Audit evidence: a screenshot of every v1 story and view in light and dark, saved under `docs/design/audit/`, with the density and clarity problems called out in the doc
- [x] v2 primitives under `packages/pages/src/lib/v2/` with stories titled `V2/...`: label/keyword treatment replacing the chip-for-everything pattern, kind icon plus name lockup, definition list, data table (dense, native look, sortable header optional), inline link and ref treatment, section and subsection headings, a comment list, a disposition treatment, empty state, and the hover card frame; each with light, dark and high-contrast stories and a density story where rows are laid out
- [x] `docs/design/v2-specs/` one short spec per organism and template (all 12 organisms and 16 templates) naming which primitives it uses and sketching its layout in words or ASCII, for the implementation cards
- [x] `e2e/storybook.spec.ts` widened to the `V2/` titles; pages unit coverage unchanged (v2 primitives tested to 100%)

## Gates

- [x] qa-automated — inside the worktree: pages 488 at 100%, Storybook built, 41 V2 stories rendered; lead reviewed DataTable, HoverCard and Lockup captures in dark (lead, 2026-09-04T16:30:00Z)

- [x] clean-code-swept — six-agent audit (SRP, DRY, naming, coupling, dead code, KISS) over packages/pages/src/lib/v2 and e2e/storybook.spec.ts; nothing scored 0.5 or above, so no TODO tags; the sub-0.5 notes were fixed anyway: `n` column key renamed `consumerCount`, HoverCard `title` renamed `heading`, Heading harness `count` renamed `withCounts`, spec `EVIDENCE_TITLES` renamed `DESIGNED_TITLES`, and the high-contrast badge outline moved from the theme harness into Heading.svelte (designer-fable, 2026-09-04T13:55:00.000Z)
- [x] tests-passing — `npm run test:unit -w packages/pages`: 73 files, 488 tests, coverage 100% statements / 100% branches / 100% functions / 100% lines; `npx playwright test e2e/storybook.spec.ts` from packages/pages: 65 passed (24 evidence stories + 41 `V2/` stories); `svelte-check`: 0 errors 0 warnings; Biome clean (designer-fable, 2026-09-04T13:55:00.000Z)

## Comments

- **lead** (2026-09-04T10:40:00.000Z): Assigned to the senior designer (a Fable agent with the frontend-design skill). Fixed by decision: v1 stays shipped and untouched; v2 lives under `src/lib/v2/` and `V2/` stories only. Source of truth for platform conventions is Microsoft's VS Code UX guidelines (https://code.visualstudio.com/api/ux-guidelines/overview, especially the webview page) and the VS Code theme tokens the page already maps in packages/pages/assets/page.css. Readers and their jobs: an architect checking boundaries and patterns, a developer finding what an aggregate owns and what crosses a boundary, a product person reading descriptions, comments and health. Density and clarity beat decoration; a badge is used only where a native VS Code surface would use one. The designer decides everything visual and journals each decision with its reason; the lead decides only scope. Work in your worktree; `npm ci` there first if node_modules is missing; if the card is missing, `git reset --hard develop` there first. Storybook: `npm run build-storybook -w packages/pages`, serve `storybook-static` with `node e2e/static-server.mjs 4190 storybook-static`, screenshot `iframe.html?viewMode=story&id=<id>` with Playwright; story bodies must be `{#snippet template()}`.
- **designer-fable** (2026-09-04T11:05:00.000Z): Claimed the card. Worktree was on the stale v0.2.0 tag without the card; reset to develop (3930a31) and ran `npm ci`. Reading the README pitch, page.css tokens, every lib component, RFC-002 and the VS Code UX guidelines before drawing anything.
- **designer-fable** (2026-09-04T11:50:00.000Z): Audit captured: all 78 v1 stories in light and dark, 156 PNGs under docs/design/audit/ (7.8MB, 1000px wide, device scale 1 to keep the repo small). Method: `npm run build-storybook -w packages/pages` (it needs `node scripts/codicons.mjs` first, which `npm run build` normally runs; the first build failed silently on the missing `assets/codicons/codicon.css` and a `tail` hid it), served with e2e/static-server.mjs:1-30 on 4190, screenshotted with Playwright using `colorScheme` emulation because assets/site.css:33-52 switches on `prefers-color-scheme`. What the audit shows, in one line each, before I design against it: (1) chips carry six different meanings (kind, visibility, pattern, classification, disposition, alias) in the same pill, so nothing stands out, see packages/pages/src/lib/molecules/ConsumableChips.svelte:8-10 and organisms/StrategicPositionTable.svelte:996-999; (2) cards box single facts, a two-line aggregate summary costs 110px of height, see templates/ContextPage.svelte:438-448; (3) five uppercase-tracked label styles (crumb kind, facts dt, h3, th, toc title) fight for the same visual slot, see assets/page.css:157-163,204-209,270-277,367-373; (4) colour is spent on classification (purple/blue/green), the id pill, the root card glow and the `ok` line, none of which a VS Code surface would colour; (5) the same pill is used for a link (ContextPill) and for a label (Chip), so readers cannot tell what is clickable; (6) tables are the one v1 surface that already reads well, so v2 makes the table the default row container.
- **designer-fable** (2026-09-04T13:10:00.000Z): Design decisions, each with its reason. (1) Every screen is a list: rows at the platform's 22px line replace cards and grids, because all three readers scan structured facts and VS Code's own dense surfaces (Outline, Problems, keybindings editor) are lists; packages/pages/src/lib/v2/DataTable.svelte:18-32. (2) A classifier is a word, not a pill: `Keyword` is secondary-colour text with no box, `mono` for codes from a table, a tone only for a diagnostic meaning, because a tree row's description and the Problems panel's rule id are drawn this way and six v1 chip meanings in one shape made nothing stand out; v2/Keyword.svelte:2-12. (3) Kind icons take the Outline's `symbolIcon.*Foreground` tokens and nothing else on a page is coloured except links and diagnostics; the platform colours class/method/field/event icons and leaves the rest in `icon.foreground`, so a reader who knows the Outline already reads it; v2/kinds.ts:6-24. (4) Subdomain classification loses its purple/blue/green: it is a sortable keyword column, because no VS Code surface colours a classification and the chart colours stay in the diagrams where a cluster needs a hue; docs/design/design-language-v2.md section 5. (5) No capitals anywhere: the crumb kind eyebrow becomes the title lockup's detail, facts become a definition list, h3 is bold body text, table headers are sentence case; five competing uppercase styles in v1 were indistinguishable; v2/Heading.svelte:4-13, v2/Lockup.svelte:5-14. (6) The one badge is the count on a heading, because a pane header carries its item count in exactly that badge; v2/Heading.svelte:63-75. (7) Disposition is drawn as the Problems panel draws a diagnostic: warning codicon in the warning colour for refactor, info codicon in the secondary colour for tolerated, nothing for by design; v2/Disposition.svelte:4-14. (8) Hover disclosure uses the editor hover widget's tokens and frame; v2/HoverCard.svelte:4-12. (9) Links: link colour, underline on hover only, `focusBorder` ring, trailing `link-external` codicon for a link that leaves the model; v2/Ref.svelte:4-12. (10) One hairline under a table header and none between rows, hover wash from `list.hoverBackground`, dashed `contrastActiveBorder` outline in high contrast via the `vscode-high-contrast` body class; v2/DataTable.svelte:192-199. (11) Tried and dropped: a dotted help underline under a keyword with a title; the editor marks nothing hoverable and it made a dense table busy, so a keyword keeps only the help cursor; v2/Keyword.svelte:35-40. (12) The v2 primitives reset what v1's global page.css sets on bare `table`, `th`, `h1 .id`, `h3` and `.empty`, because both stylesheets stay loaded while templates migrate one by one; found by screenshotting the V2 stories, which showed capitals, an id pill and italics leaking in; v2/DataTable.svelte:132-157. Stories: 41 under `V2/`, each primitive in light, dark and high contrast from v2/Theme.harness.svelte:1-17 (Dark Modern, Light Modern and Default High Contrast token values), plus density stories for Lockup, DefinitionList, DataTable and Comments. Docs: docs/design/design-language-v2.md (principles, guidelines, type and spacing scales, token map, icon map, primitives, audit call-outs, verdict on every file, least-sure decisions) and 28 specs under docs/design/v2-specs/ (12 organisms, 15 templates and the Page layout, since the checklist's 16th template is the layout shell). e2e widened at packages/pages/e2e/storybook.spec.ts:24-33.
- **designer-fable** (2026-09-04T13:55:00.000Z): Done, moving to review. Final evidence: full pages unit suite 73 files / 488 tests at 100% on every coverage measure (the suite needs `packages/core` and `packages/graphviz` built and `npm run build -w packages/pages` for `src/site.test.ts`, which a fresh worktree lacks); Playwright storybook spec 65 passed including all 41 `V2/` stories with no console problems; svelte-check clean. After the audit I fixed one real defect the screenshots showed: a flex-box disposition mark made its table row taller than 22px, packages/pages/src/lib/v2/Disposition.svelte:26-34. Least sure about, for the lead's review: (1) kind icons in the Outline's symbol colours (four hues on a page whose principle is restraint), packages/pages/src/lib/v2/kinds.ts:6-24; (2) subdomain classification as a plain keyword with no colour; (3) no hairline between table rows, packages/pages/src/lib/v2/DataTable.svelte:163-167. All three are written up with their fallbacks in docs/design/design-language-v2.md section 10. Nothing committed; nothing shipped touched: the working tree shows only the card, e2e/storybook.spec.ts, docs/design/ and src/lib/v2/.
- **lead** (2026-09-04T16:30:00.000Z): Reviewed the design language and the primitives. Audit PNGs stay on disk but out of git (.gitignore), README left in their place. Landing on develop; cards 29, 30, 31 build the organisms and templates from the specs. The three least-sure decisions go to the human in the morning.

- 2026-09-11, lead: driven in a browser against the built app and confirmed against this card's own claim; moved to done.
