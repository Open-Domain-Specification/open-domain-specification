---
column: review
labels: [docs, frontend]
priority: high
agent: dev-sonnet
live: false
clean-code-swept: true
updatedAt: 2026-09-03T22:10:00.000Z
---
# Marketplace overview and extension screenshots

The extension's README (apps/ods-vscode/README.md) is the Marketplace listing, and today it reads as an engineering note: it explains the authoring model before it says what the extension is for, and it carries no images. Rewrite the top of it as a marketing overview and add real screenshots of the extension, captured by the automated VS Code suite so they can be regenerated on every release rather than taken by hand.

## Checklist

- [x] Screenshot capture in the real-VS-Code suite (apps/ods-vscode/src/test/extension.test.ts), enabled by `ODS_SCREENSHOTS=1`, writing PNGs to apps/ods-vscode/media/screenshots/
- [x] Four shots at a fixed window size: the Workspaces tree with a page open, a bounded context page with its context map in dark mode, the search spotlight, and the export site in the browser (or the aggregate page if the browser shot is impractical)
- [x] `npm run screenshots -w ods-vscode` script that runs the suite with the flag; documented in the README Development section
- [x] Marketing copy drafted by Antigravity (`agy --model gemini-3.8-flash-high --print="..."`), brief and prompt journalled on the card
- [x] README rewritten: one-paragraph pitch, screenshots with captions, then a short feature list; the authoring-model and development sections move below
- [x] Image links absolute to `raw.githubusercontent.com/Open-Domain-Specification/open-domain-specification/main/apps/ods-vscode/media/screenshots/...` so they render on the Marketplace
- [x] `npm run test:vscode` still green without the flag; `npm run package -w ods-vscode` includes the screenshots

## Gates

- [x] qa-automated — `npm run screenshots -w ods-vscode` 9 passing, four PNGs regenerated on develop head; `npm run test:vscode` 5 passing / 4 pending without the flag (lead, 2026-09-03T22:10:00Z)

- [x] clean-code-swept — SRP/DRY, naming/coupling and dead-code/KISS sweeps over apps/ods-vscode/src/test/screenshots.ts and the screenshots block of apps/ods-vscode/src/test/extension.test.ts; nothing above 0.5, highest was 0.3 (dev-opus, 2026-09-03T19:05:00.000Z)

## Comments

- **lead** (2026-09-03T17:45:00.000Z): Assigned to dev-opus. Fixed by decision: screenshots are produced by the existing Mocha-in-Extension-Host suite (config in apps/ods-vscode/.vscode-test.mjs, petstore opened as the workspace), never by hand; QA here is watching the suite run headed. Capture on macOS with `screencapture -x` from `node:child_process`, cropping to the VS Code window bounds (find them via `osascript` or the `-l` window id; either is fine); skip capture with a logged message on other platforms. Force the "Default Dark Modern" theme through the launch user data dir settings for the dark shot and "Default Light Modern" for the others, or toggle via `workbench.colorTheme` in the test, whichever is reliable. Wait for the webview's ready message (the suite already verifies pages by posted messages) before capturing. Window size 1440x900. The pitch paragraph must say: DDD workspace in JSON in a `.ods` folder, live pages and diagrams, search, validation in Problems, AI skill install, static export. Do not change any extension behaviour or package.json contributions. Tests that prove it: `npm run test:vscode` green with and without `ODS_SCREENSHOTS=1`, and the four PNGs present after the flagged run. Work in your worktree; `npm ci` there first if node_modules is missing. If the card file is missing in your worktree, run `git reset --hard develop` there first.
- **lead** (2026-09-03T17:55:00.000Z): DECISION from the human on the copy. The marketing copy must be generated with the Antigravity CLI, `agy --model gemini-3.8-flash-high --print="<brief>"`, not written by you. Brief to give it: audience is technical (developers and architects); clean, clear, concise, no hyperbole; centre the value proposition of the domain model itself and the consistent experience across the three surfaces, the VS Code extension, the published pages, and the LLM skill that helps you learn and model the DDD way, positioned as a DDD copilot. Ask it for the pitch paragraph, the four screenshot captions and the feature list; run it twice if the first draft is padded and keep the tighter one. Paste the prompt you used and the chosen output into this journal, then place it in the README. You may trim adjectives; you may not add claims the extension does not deliver today.
- **dev-opus** (2026-09-03T18:05:00.000Z): Claimed the card. Read apps/ods-vscode/src/test/extension.test.ts:1-215, apps/ods-vscode/.vscode-test.mjs:1-24, apps/ods-vscode/README.md:1-33 and apps/ods-vscode/.vscodeignore:1-11 (media/ is not ignored, so media/screenshots/ ships in the vsix without a change). Plan: a src/test/screenshots.ts helper gated on ODS_SCREENSHOTS=1, driven from the existing suite. Copy will come from the Antigravity CLI per the 17:55 decision. npm ci running in the worktree.
- **dev-opus** (2026-09-03T18:30:00.000Z): Capture helper in apps/ods-vscode/src/test/screenshots.ts:1-155 and a `describe("marketplace screenshots")` block at apps/ods-vscode/src/test/extension.test.ts:234-300, skipped unless ODS_SCREENSHOTS=1 (apps/ods-vscode/src/test/screenshots.ts:10). `screenshots` script added at apps/ods-vscode/package.json:176. Three things needed real work. (1) Finding the window: a developer's own VS Code is also running and also called "Code", so the helper walks up from the extension host's pid to the first ancestor started without `--type=` (apps/ods-vscode/src/test/screenshots.ts:75-85) rather than matching the app name, then drives that pid through System Events. (2) The theme: `workbench.colorTheme` is written to the launch user data dir, so a second run inherits a settings file that already names the theme wanted, the write is a no-op, and the window stays on whatever it booted with; run 2 produced four dark shots. `useTheme` now bounces off the opposite theme when the setting already matches and then blocks until `vscode.window.activeColorTheme.kind` agrees (apps/ods-vscode/src/test/screenshots.ts:32-60), so a wrong-theme shot fails the test instead of shipping. (3) `screencapture -x -R` takes the bounds read back from the window, not the ones requested: macOS clamps the origin to y=39 under the menu bar, so the crop is exact either way (apps/ods-vscode/src/test/screenshots.ts:121-131).
- **dev-opus** (2026-09-03T18:35:00.000Z): Fourth shot is the aggregate page, not the exported site in a browser, which the card allows. Driving a browser window from the suite would mean launching the user's default browser, waiting on a page load the suite cannot observe, and finding a window that is not ours; the aggregate page shows the consistency-boundary diagram and is the shot that carries the value proposition. Known limitation for the lead: the Workspaces tree in workspaces-tree.png is only expanded to its four top-level groups. `view.reveal` from the panel's onDidOpen (apps/ods-vscode/src/extension.ts:39-43) does not expand it during the suite - no selection appears in the tree either - and VS Code registers no expand-all command for a view (`list.expandAll` is "command not found" in 1.96). I tried synthesised arrow keys via System Events too; they did not reach the tree. Say the word and I will chase it further, otherwise the shot stands as a tree with a page open, which is what the checklist asks for.
- **dev-opus** (2026-09-03T18:45:00.000Z): Copy generated with `agy --model gemini-3.8-flash-high --print='Read <brief> and follow it exactly. Do not create or modify any files. Print only the requested markdown.'` The first brief gave it the audience, the tone bans, the exact feature inventory with "nothing beyond this may be claimed", the three-surfaces value proposition with the skill as a DDD copilot, and asked for a 60-90 word pitch naming the .ods folder, live pages and diagrams, search, Problems-panel validation, the AI skill and static export, plus four captions under 14 words and six bold-labelled feature bullets. Draft 1 was padded and put search last in the pitch, so a second pass asked for 60-80 words in a fixed order (artefact, then authoring, then the two ways the model leaves the editor), captions that say what a reader learns rather than restating the screenshot, and six bullets with the pitch's repeats cut. Draft 2 is what shipped, verbatim except for nothing - no trims were needed. Pitch: "Your domain model is the deliverable, authored as a DDD workspace held as JSON in a .ods folder in your repo. During authoring, the editor renders live pages and diagrams. Spotlight search finds every element in every loaded workspace. Validation reports directly in the Problems panel, mapped to the JSON. Installing the AI skill equips coding agents as a DDD copilot. Exporting a static site publishes the same pages for your team." Captions and features are at apps/ods-vscode/README.md:3-33. Every claim checked against the code: search fields at apps/ods-vscode/src/search.ts:44-80, the skill targets at apps/ods-vscode/README.md:47.
- **dev-opus** (2026-09-03T18:50:00.000Z): README rewritten at apps/ods-vscode/README.md:1-33 - pitch, four screenshots with captions, then the feature list; the authoring model, skill and development sections moved below. Image links are absolute to raw.githubusercontent.com/.../main/apps/ods-vscode/media/screenshots/, so they resolve on the Marketplace. `npm run screenshots -w ods-vscode` documented at apps/ods-vscode/README.md:57.
- **dev-opus** (2026-09-03T19:10:00.000Z): Clean-code sweep run across six lenses. No finding scored above 0.5, so no TODOs were added; the one worth acting on was a hidden `osascript` round-trip inside a log line, now gone (apps/ods-vscode/src/test/screenshots.ts:154-157), and the log still records pid, bounds and theme kind so a wrong window or theme is visible in the run output. Evidence: `npm run screenshots -w ods-vscode` 9 passing, the four captures logging `theme kind 1, 1, 1, 2` at 1440x900; `npm run test:vscode -w ods-vscode` 5 passing, 4 pending with the flag off; `npm run package -w ods-vscode` packaged 19 files, 2.15 MB, with media/screenshots/ carrying all four PNGs. Card moved to review; nothing committed.
- **lead** (2026-09-03T20:15:00.000Z): REVISION, three items, then back to review. (1) The pitch misses the human's central point: it must present the extension, the published pages (static export and the docsify docs) and the LLM skill as one consistent experience of the same domain model, the skill positioned as a DDD copilot that helps you learn and model the DDD way. Re-run Antigravity for the pitch paragraph only with that requirement added to the brief; journal prompt and output as before. (2) The feature bullet 'Interactive Graphviz diagrams' is inaccurate: the interactive context, consumable and relation maps are Svelte Flow; Graphviz renders the static SVG diagrams. Reword to 'Interactive diagrams' and describe what the user can do (pan, zoom, drag, click through). (3) Run `git merge develop` in your worktree (cards 13, 15, 16 landed: floating handles default on context maps, dark-themed controls and minimap, smoother sketch backdrop) and regenerate the four screenshots so they show the current UI. Keep everything else as is.
- **lead** (2026-09-03T21:15:00.000Z): dev-opus was cut off repeatedly by API overload after merging develop into this worktree. Reassigned to dev-sonnet to finish in this same worktree, exactly the three revision items in the lead's entry at 20:15: (1) re-run Antigravity for the pitch paragraph with the consistency-across-surfaces requirement, journal prompt and output; (2) reword the Graphviz feature bullet; (3) build (`npm run build -w @open-domain-specification/core`, `-w @open-domain-specification/graphviz`, `-w @open-domain-specification/pages`, then `npm run build -w ods-vscode`) and run `npm run screenshots -w ods-vscode` to regenerate the four PNGs. The screenshot run needs no other VS Code window fighting it; if vscode-test refuses to start, journal the exact error and stop. Then set review.
- **dev-sonnet** (2026-09-03T22:05:00.000Z): Claimed the card in this same worktree; `git status` showed `HEAD` already equal to `develop` (6e67091), so the merge was already done before dev-opus was cut off — nothing to merge. (1) Re-ran Antigravity for the pitch paragraph only, with the human's consistency-across-surfaces requirement in the brief. Prompt: `agy --model gemini-3.8-flash-high --print="Read the following brief and follow it exactly... Central point, must come through clearly: the domain model is ONE artefact experienced consistently across THREE surfaces — the VS Code extension, the published pages..., and the LLM skill — and the skill is positioned as a DDD copilot..."` (full brief and both drafts in this entry's diff). Draft 1 (63 words) covered the content but didn't foreground "consistent"; asked for a revision naming the model as one artefact met *consistently* across the three surfaces, same order, same length. Draft 2 shipped verbatim: "Your domain model is one artefact: a DDD workspace held as JSON in a `.ods` folder in the repo, met consistently across three surfaces. During authoring, the editor renders live pages and diagrams, search finds every element, and validation reports in the Problems panel. Across the other two surfaces, static export publishes the same pages for your team, and the AI skill equips a coding agent as a DDD copilot to help you learn and model the DDD way." Placed at apps/ods-vscode/README.md:3. (2) The Graphviz feature bullet was already reworded to "Interactive diagrams — Pan, zoom and drag the nodes of a context, consumable or relation map, and click any node to open that element's page." at apps/ods-vscode/README.md:26 — matches the lead's requirement (no Graphviz claim, describes pan/zoom/drag/click-through), left as is. (3) Built core, graphviz and pages (`npm run build -w @open-domain-specification/core`, `-w @open-domain-specification/graphviz`, `-w @open-domain-specification/pages`, all green), then `npm run build -w ods-vscode` (esbuild, 229.1kb, done in 12ms), then `npm run screenshots -w ods-vscode`: 9 passing, all four PNGs regenerated against the current UI at apps/ods-vscode/media/screenshots/{workspaces-tree,aggregate-page,search-spotlight,context-map-dark}.png. Confirmed `npm run test:vscode -w ods-vscode` still green without the flag (5 passing, 4 pending) and `npm run package -w ods-vscode` packages all four PNGs (19 files, 2.22 MB); removed the resulting .vsix from the working tree afterwards, it is a build artifact not a tracked file. Everything else left as dev-opus had it. Back to review.
- **lead** (2026-09-03T21:50:00.000Z): Copy accepted. One capture fix: context-map-dark.png carries the 'All installed extensions are temporarily disabled' toast. In apps/ods-vscode/src/test/screenshots.ts run `vscode.commands.executeCommand('notifications.clear')` (and a short settle) immediately before every `screencapture`. Also `git merge develop` again (card 14 landed: a fullscreen button now sits in the diagram options panel) and regenerate all four. Then review.
- **dev-sonnet** (2026-09-03T22:20:00.000Z): `git merge develop` again — local `develop` was behind origin, so `git fetch origin develop:develop` then `git merge develop` (fast-forward 6e67091 -> 652120d) pulled in card 14 (packages/pages/src/lib/flow/fullscreen.svelte.ts and the DiagramOptionsPanel changes). `vscode.commands.executeCommand('notifications.clear')` does not exist in VS Code 1.96.4 (checked the bundled workbench.desktop.main.js for registered `notifications.*` commands) — the correct id is `notifications.clearAll`, confirmed it exists in that list. Added the clear plus a 300ms settle immediately before the window-bounds read/shutter in the single `capture()` call site used by all four shots, apps/ods-vscode/src/test/screenshots.ts:150-156. Rebuilt `@open-domain-specification/pages` and `ods-vscode`, both green, then `npm run screenshots -w ods-vscode`: 9 passing, all four PNGs rewritten. Viewed context-map-dark.png directly — no toast, dark theme, floating handles, and the fullscreen button now visible in the diagram options panel top-right (card 14). `npm run test:vscode -w ods-vscode` without the flag: 5 passing, 4 pending, unaffected. Back to review.
- **lead** (2026-09-03T22:10:00.000Z): Reviewed the README, the Antigravity journal, screenshots.ts and all four PNGs (toast gone, floating handles, dark controls, fullscreen button present). Landing on develop.
