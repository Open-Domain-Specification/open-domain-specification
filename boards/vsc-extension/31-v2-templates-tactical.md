---
column: review
labels: [frontend]
priority: high
agent: dev-opus
live: false
clean-code-swept: true
updatedAt: 2026-09-04T19:15:00.000Z
---
# v2 templates, tactical pages (design language v2, part 3 of 3)

AggregatePage, EntityPage, ValueObjectPage, ServicePage, ConsumablePage, SchemaPage, PolicyPage, InvariantPage and TermPage, per docs/design/v2-specs/template-*.md, built on the v2 organisms from card 29 (import from `v2/organisms/`; if an organism you need is not there yet, build the minimal version the spec describes under the same path and journal it so card 29 reconciles).

## Checklist

- [x] `v2/templates/`: the nine templates above, each rendered inside `v2/PageLayout` in a `V2/Templates/...` story against petstore, light, dark and high contrast
- [x] One `V2/Compare/...` story per template rendering v1 and v2 side by side in two columns at 1200px, for the morning review
- [x] Tests at 100%; `e2e/storybook.spec.ts` green over the new titles

## Gates

- [x] clean-code-swept — `clean-code-review` run as three sub-agents over the 24 new files (SRP+DRY, naming+coupling, dead code+KISS); every finding above 0.5 fixed, none deferred as a TODO (dev-opus, 2026-09-04T19:15:00.000Z)

## Comments

- **lead** (2026-09-04T16:00:00.000Z): Assigned to dev-opus. Build the v2 versions as new files under `packages/pages/src/lib/v2/` (organisms under `v2/organisms/`, templates under `v2/templates/`), rendered only by `V2/...` Storybook stories against the petstore model. No shipped file changes; v1 stays as it is. Follow docs/design/design-language-v2.md and the matching spec in docs/design/v2-specs/ exactly; where a spec is silent, choose the denser, plainer option and journal it. Use only the v2 primitives (Keyword, Lockup, Ref, DefinitionList, DataTable, Heading, Comments, Disposition, EmptyState, HoverCard) plus InteractiveDiagram, which is kept as is. Every component: light, dark and high-contrast stories via `v2/Theme.harness.svelte`, a density story where rows are laid out, tests at 100% coverage, `{#snippet template()}` story bodies, and `e2e/storybook.spec.ts` green over the new titles. Work in your worktree with absolute paths for every suite; build core, graphviz and pages first (`npm run build -w packages/core -w packages/graphviz -w packages/pages`) and run `node scripts/codicons.mjs` in packages/pages before `build-storybook`; `npm ci` if node_modules is missing; if the card is missing, `git reset --hard develop` there first. Card 29 is building the organisms in parallel; card 30 builds the strategic templates.
- **dev-opus** (2026-09-04T17:05:00.000Z): Claimed. The worktree was on a stale commit, so `git reset --hard develop` brought it to 5a87299; `npm ci` and `npm run build -w packages/core -w packages/graphviz -w packages/pages` are both green. Read docs/design/design-language-v2.md, the nine tactical template specs and the organism specs they lean on. `packages/pages/src/lib/v2/organisms/` does not exist yet (card 29 is in its own worktree), so per the card I am building the minimal organisms these nine templates need under that path and journalling each one for card 29 to reconcile.
- **dev-opus** (2026-09-04T18:20:00.000Z): Stand-in organisms built, minimal and to their specs, for the owners to replace at merge: `packages/pages/src/lib/v2/organisms/PageHeader.svelte:1-83` (crumbs as `Ref`s, kind word as the title lockup's `detail`, `keywords` snippet for what v1 put in `meta`, facts as a `DefinitionList`), `Section.svelte:1-45` (heading 2 + lead + count + problems, no rule under the header), `Problems.svelte:1-60` (Problems-panel rows: severity codicon in its colour, rule id as a `mono` keyword, `Ref "go to"`), `AttributesSection.svelte:1-22`, `InvariantsSection.svelte:1-60` (rows, with the aggregate page's extra "Constrains" column behind `withTargets`), `LanguageSection.svelte:1-40` (comma-separated `Ref`s, no pills), `DiagramFigure.svelte:1-60` (canvas between two hairlines, caption below), `Toc.svelte:1-60` (plain secondary title) and `v2/PageLayout.svelte:1-52`. Prop shapes follow v1 except `PageHeader`, which now takes `kind` as a `Kind` (for the symbol colour) plus a `kindLabel` string, because v1's `kind`/`icon` pair cannot carry the Outline colour.
- **dev-opus** (2026-09-04T18:25:00.000Z): The tactical row units are mine and, per the lead's note on ownership, live under `packages/pages/src/lib/v2/molecules/`: `AttributeTable.svelte:1-56` (key gutter, name and type in the editor font, description last), `ProvidesTable.svelte:1-49`, `ConsumesTable.svelte:1-50`, `ConsumableKeywords.svelte:1-25` (type, `internal` in the warn tone, pattern as a `mono` keyword titled from `PATTERNS`) and `element-kind.ts:1-46`. `element-kind.ts` is new: v1 drew a generic `symbol-misc` where the model types a target only as referenceable (a term's embodied element, an invariant's targets, an attribute's owner), and v2 says the kind with its codicon everywhere a name appears, so `kindOf` recovers it from the instance.
- **dev-opus** (2026-09-04T18:30:00.000Z): The nine templates are in `packages/pages/src/lib/v2/templates/`, each to its spec: `AggregatePage.svelte:1-215` (entities, value objects and consumables as level-3 subsections, invariants as one table with a Constrains column, relations as keyword + ref + secondary rows), `EntityPage.svelte:1-138` (two relation tables, `aggregate root` as a keyword line), `ValueObjectPage.svelte:1-138`, `ServicePage.svelte:1-63`, `ConsumablePage.svelte:1-187` (keyword line, `Disposition` as a definition only when not by design, comments with the count on the heading), `SchemaPage.svelte:1-75`, `PolicyPage.svelte:1-93`, `InvariantPage.svelte:1-67` and `TermPage.svelte:1-107`. Where a spec was silent I took the denser option and journal it here: section counts are the number of rows the section lists, so an empty section shows a `0` badge; the policy page's "When" table has no Kind column and "Then" does, exactly as the spec sketch draws them; and the consumable page's "Consumed by" keeps the shared consumes table the spec names, even though its Provider and Context columns repeat the page's own provider.
- **dev-opus** (2026-09-04T18:35:00.000Z): Stories. `templates/Page.harness.svelte:1-57` resolves a ref to the right v2 template and wraps it in `v2/PageLayout`, so every story renders the page as a host does; `templates/petstore.harness.ts:1-24` picks the petstore element that fills each template. Nine `V2/Templates/...` story files, each Light, Dark and High contrast through `v2/Theme.harness.svelte`, plus an extra story for a consumable event and for a term with an alias. Nine `V2/Compare/...` files render v1 and v2 in two 1200px columns via `templates/Compare.harness.svelte:1-48`; v1 is shown in the ambient theme because its derived tokens (`--muted`, `--card`, `--accent`) resolve on `:root` in `assets/page.css:1-22` and cannot be repointed by a wrapper, and the v2 column pins Light Modern, which is what v1 shows by default.
- **dev-opus** (2026-09-04T18:40:00.000Z): Tests at 100%. `v2/molecules/element-kind.test.ts:1-41` names every kind; `v2/templates/templates.test.ts:1-186` asserts the v2 treatment on each page against petstore — no `.card`, `.grid`, `.chip`, `.pill` or `.facts` survives on any of them, the invariants table carries its Constrains column, the entity's relations are two tables, the provides and consumes tables sort on their headers, and the TOC scrolls; `v2/templates/edge-cases.test.ts:1-203` covers the shared edge-case fixture's branches; `v2/templates/branches.test.ts:1-203` builds one small workspace for the shapes no shared fixture carries (a relation with no label and one with, an operation raising two events, a service that publishes an event and keeps an operation internal, an element two glossary terms name, and a diagnostic to draw in a section). Three source changes came out of chasing the last branches rather than the tests: `?? ""` guards dropped where Svelte already renders nothing for `undefined`; captions and the TOC `href` built in the script instead of by attribute interpolation, which Svelte compiles with a nullish guard no fixture can reach; and `Problems.svelte:21` puts the message in its own span for the same reason.
- **dev-opus** (2026-09-04T18:45:00.000Z): Suites, all from the worktree: `npm run test:unit -w packages/pages` — 79 files, 561 tests, coverage 100% lines/branches/functions/statements against the package's existing thresholds. `npm run check -w packages/pages` (svelte-check) 0 errors. `node scripts/codicons.mjs && npm run build-storybook` green; the index lists 27 new `V2/Templates/...` and `V2/Compare/...` stories, and `npx playwright test e2e/storybook.spec.ts` is 109 passed, so every new story renders with no console error. `npx biome check packages/pages/src/lib/v2` clean.
- **dev-opus** (2026-09-04T19:00:00.000Z): Took the lead's ownership note. Moved my tactical row units out of `v2/organisms/` into `packages/pages/src/lib/v2/molecules/` (AttributeTable, ProvidesTable, ConsumesTable, ConsumableKeywords, element-kind); `v2/DataTable.svelte` is untouched. The stand-ins for card 30's pieces (PageLayout, Toc, Section, PageHeader, DiagramFigure, Problems) and card 29's (AttributesSection, InvariantsSection, LanguageSection) stay at the paths their owners will land on, so the lead takes the owner's version at merge. Also confirming the two Svelte coverage traps the lead relayed: I hit both before the note arrived and fixed them the same way — `Toc.svelte:9` builds the href in the script, `Problems.svelte:21` makes the message its own element, and the two `DiagramFigure` captions are `$derived` strings in `AggregatePage.svelte:49-50` and `ServicePage.svelte:26` rather than attribute interpolation.
- **dev-opus** (2026-09-04T19:15:00.000Z): Clean-code sweep. Three sub-agents (SRP+DRY, naming+coupling, dead code+KISS) over the 24 new files; eleven findings scored above 0.5 and all eleven are fixed, so no TODO markers are left in the tree. DRY: `v2/molecules/RefList.svelte:1-45` now draws the nine inline comma-separated ref lists (and the three copies of the padded wrapper paragraph) that the specs call "comma-separated `Ref`s"; `v2/molecules/Code.svelte:1-27` replaces the `.mono` `<code>` block that was copy-pasted into AttributeTable, EntityPage and ValueObjectPage; `v2/molecules/crumbs.ts:1-16` replaces the workspace-then-context crumb pair spelled out in five templates (`ownerCrumbs` in the v1 `templates/elements.ts` stays the aggregate-owned variant, and that shipped file is untouched). COUPLING: the six inline `type === "event" ? "event" : "command"` ternaries and the `"entities" in provider` shape sniff in `ConsumesTable` all go through `kindOf` now, so kind derivation lives in one module. SRP: AggregatePage's two inline sub-renderers became `v2/molecules/StructureSubsection.svelte:1-64` and `v2/molecules/ConsumableSubsection.svelte:1-62`, leaving the template at composition only (215 lines down to 133). DEAD: dropped `AttributesSection`'s `problems` relay and `ProvidesTable`'s `empty` relay, neither of which had a caller, and the unreachable `?.` on the total `PATTERNS` record in `ConsumesTable`. NAMING: `InvariantsSection`'s row parameter is `inv`, not `i`. Two findings I did not act on and the lead may want to rule on: `ValueObjectPage.svelte:41` keeps v1's `ownerOf` cast, because core types an attribute's owner as its own `AttributeOwner` and the page needs the local one; and `DiagramFigure`'s `direction` prop is unused by my three call sites but is v1's API and card 30's context and domain maps pass it. Re-ran everything after the sweep: unit suite 79 files, 561 tests, coverage still 100/100/100/100; svelte-check 0 errors; biome clean; `build-storybook` green and `npx playwright test e2e/storybook.spec.ts` 109 passed.
