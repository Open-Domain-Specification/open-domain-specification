---
column: done
labels: [frontend, ddd]
priority: high
agent: dev-opus
live: false
clean-code-swept: true
updatedAt: 2026-09-05T08:30:00.000Z
---
# Pattern hover disclosure on the v2 pages

Card 27's hover (what the DDD keyword means, then this relationship's disposition and comments) was built on v1 parts that card 36 deletes. Rebuild it on the v2 `HoverCard` frame and wire it into the v2 Strategic position table (type and role keywords) and the relationship detail's role cards, with the same behaviour: open on hover after 150 ms and on focus, pin on click, close on Escape and outside click, one open at a time; part 1 always renders (name with abbreviation, summary, architectural nature), part 2 (disposition, comments with links) only when the intent has any.

## Checklist

- [x] `molecules/PatternHover.svelte` (v2 primitives only) with a `hover.svelte.ts` state module, tests at 100%
- [x] Wired into `organisms/StrategicPositionTable.svelte` keywords and `organisms/RelationshipDetail.svelte` role cards
- [x] Storybook stories in light, dark and high contrast for: no comments, comments, tolerated, refactor; render spec green
- [x] e2e: hover `ACL` on the petstore Sales page, read the anti-corruption-layer summary and the PetSummaryClient comment, Escape closes (restore the case card 36 removed)
- [x] Design language: the HoverCard primitive row names this as its first use

## Gates

- [x] qa-automated — in the worktree: pages unit 580 at 100% lines/branches/functions/statements, pages Playwright 241/241 including the ACL hover on the petstore Sales page and the twelve new stories in the render spec, whole repo `npm test` green over 10 projects (dev-opus, 2026-09-05T13:45:00.000Z)

- [x] clean-code-swept — six-agent audit over the authored files; one finding at 0.5 fixed (the trigger reached through `Keyword`'s own class to recolour the word on hover), one at 0.3 fixed, nothing else above threshold (dev-opus, 2026-09-05T13:45:00.000Z)

## Comments

- **lead** (2026-09-05T06:40:00.000Z): Assigned to dev-opus, starts after card 36 lands (the lead will say). Fixed by decision: the HoverCard frame from card 28 is the container; content order is RFC-002 section 4 as card 27 shipped it; no pills or chips anywhere in it, keywords and the Disposition primitive only. Work in your worktree with absolute paths; build core, graphviz and pages and run `node scripts/codicons.mjs` before `build-storybook`; `npm ci` if node_modules is missing; if the card is missing, `git reset --hard develop` there first.
- **dev-opus** (2026-09-05T12:00:00.000Z): Picked this up; the worktree was on a stale commit so it was reset to develop first. Read the v1 parts card 36 deleted (the old `molecules/PatternHoverCard.svelte` and `molecules/hover-card.svelte.ts`), the v2 frame at `packages/pages/src/lib/atoms/HoverCard.svelte:1-20` with its harness at `packages/pages/src/lib/atoms/HoverCard.harness.svelte:1-40`, and the two wiring sites: `packages/pages/src/lib/organisms/StrategicPositionTable.svelte:100-113` (type and role keywords, today plain `Keyword`s carrying a native `title`) and `packages/pages/src/lib/organisms/RelationshipDetail.svelte:84-103` (the heading type keyword and the role codes in the Roles section). Plan: port the state module as `molecules/hover.svelte.ts`, build `molecules/PatternHover.svelte` as a focusable trigger wrapping a `Keyword` with a `HoverCard` positioned under it, and drop the native `title` from the keywords it replaces so the disclosure is the only hover on them.
- **dev-opus** (2026-09-05T13:25:00.000Z): Built. State module at `packages/pages/src/lib/molecules/hover.svelte.ts:1-110`: 150 ms pause on hover (`:79-83`), immediate on focus, pin/unpin on click (`:88-97`), Escape and outside `pointerdown` captured on `document` only while open (`:48-65`), one at a time through module state (`:20`). It is deliberately not card 25's `packages/pages/src/lib/flow/disclosure.svelte.ts:38`, which is per-diagram, in flow coordinates, with no delay and no pinning. `packages/pages/src/lib/molecules/PatternHover.svelte:1-141` renders the frame: `HoverCard` with `{nature.name} ({nature.abbreviation})` as the heading (`:55`, built in the script because interpolating it into the attribute compiles to nullish branches coverage can never reach), the summary and the architectural nature above the rule, and below it — only when `hasEvidence(intent)` (`:83`) — the `Disposition` mark and the `Comments` list. Nothing else: no chip, no pill, and the trigger is a reset `<button>` around a plain `Keyword` (`:69-74`) so the word looks exactly like the keywords beside it that have nothing to say, with `cursor: help` and the focus ring the only marks. The card layer pins itself to body font size (`:130-134`) because the same component opens from an h1 on the relationship page.
- **dev-opus** (2026-09-05T13:26:00.000Z): Wiring. `packages/pages/src/lib/organisms/StrategicPositionTable.svelte:96-106` — the type cell and both role cells are `PatternHover` with the row's relationship as the intent; `PATTERNS`, `roleLabel` and `Keyword` are no longer imported there because nothing in that file spells a pattern out itself any more. `packages/pages/src/lib/organisms/RelationshipDetail.svelte:84` (the title's type) and `:104` (each role in the Roles section) likewise, keeping the inline `Name — summary` line beside the code; the crossings table keeps plain `Keyword`s with a `title`, which is what card 27 shipped and what this card names. The keywords that became hovers lost their native `title`: two hovers on one word is one too many.
- **dev-opus** (2026-09-05T13:27:00.000Z): Tests. Ten cases in `packages/pages/src/lib/molecules/PatternHover.test.ts:1-215` over the four ways in and the four ways out, with two instances in `packages/pages/src/lib/molecules/PatternHover.harness.svelte:1-13` for the one-at-a-time rule. The wiring gets a case each — `packages/pages/src/lib/organisms/StrategicPositionTable.test.ts:50-65` and `packages/pages/src/lib/organisms/RelationshipDetail.test.ts:50-68` open the ACL card and read both the pattern's summary and this relationship's own comment out of it, which is the test that would have caught the wiring being missed. Twelve stories in `packages/pages/src/lib/molecules/PatternHover.stories.svelte:1-93`: no comments, comments, tolerated and refactor, each in light, dark and high contrast, every one opening its card in the meta `play`. e2e: `packages/pages/e2e/relationship.spec.ts:74-99` hovers ACL on the petstore Sales page, reads the anti-corruption-layer summary and the PetSummaryClient comment with its link, then presses Escape; `:101-126` does the relationship page, pins with a click, proves the pointer can leave, and closes it with a click elsewhere. Evidence: pages unit 578 green at 100% lines/branches/functions/statements, pages Playwright 241 green including the twelve new stories in the render spec, and the whole repo (`npm test`, 10 projects) green.
- **dev-opus** (2026-09-05T13:28:00.000Z): Docs. `docs/design/design-language-v2.md:204` names `molecules/PatternHover` as the `HoverCard` row's first use and says what sits either side of the rule; `apps/docs/docs/8-pages.md:65-76` replaces "carries the pattern's meaning as its hover text" with what the card actually discloses and how it opens, closes and pins, and lists the pattern hover under molecules at `:87`.
- **dev-opus** (2026-09-05T13:45:00.000Z): Clean-code sweep. Six agents over SRP, DRY, naming, coupling, dead code and KISS. Two fixes, both mine: the trigger styled `:global(.keyword)` to brighten the word on hover, which reached through `Keyword`'s private class from outside it — dropped, because `docs/design/design-language-v2.md` already settled that a keyword with a meaning to reveal gives the help cursor and nothing else, and the card appearing under it is the feedback (`packages/pages/src/lib/molecules/PatternHover.svelte:100-114`); and the stories' `play` picked the ACL keyword by its index in the row, now by its word (`packages/pages/src/lib/molecules/PatternHover.stories.svelte:52-58`). Nothing else scored above 0.5. Two accepted notes for the record: `hover.svelte.ts` duplicates the Escape/outside-click wiring of `packages/pages/src/lib/flow/disclosure.svelte.ts:40-68` (0.2, deliberate — that one is per-diagram, in flow coordinates, with no delay and no pinning, and the module comment says so), and the module-level "one open at a time" state at `packages/pages/src/lib/molecules/hover.svelte.ts:20` (0.15, the documented design carried over from card 27). Re-ran everything after the fixes: pages unit 580 at 100%, Playwright 241/241. Moving to review.
- **lead** (2026-09-05T08:30:00.000Z): Landing on develop. The card clipping at the viewport edge and the narrow-table alignment go to card 37.

- 2026-09-11, lead: driven in a browser against the built app and confirmed against this card's own claim; moved to done.
