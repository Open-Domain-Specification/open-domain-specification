---
column: todo
labels: [bug, frontend]
priority: low
agent: dev-sonnet
clean-code-swept: true
updatedAt: 2026-09-05T02:30:00.000Z
---
# Pre-existing Storybook stories render the bare component instead of their body

`addon-svelte-csf` treats plain children of `<Story>` as children of the meta component, not as a replacement for it. Card 19 found this while making the Storybook build work for the first time: `atoms-chip--tones` renders one empty chip instead of six and `atoms-markdown--prose` renders nothing. Wrap each affected story body in `{#snippet template()}` as the evidence stories now do, then widen `packages/pages/e2e/storybook.spec.ts` to every story so the whole Storybook is render-checked.

## Checklist

- [x] Audit every `*.stories.svelte` under packages/pages/src/lib for bodies passed as children
- [x] Convert to `template` snippets
- [x] `e2e/storybook.spec.ts` covers all stories; green

## Gates

- [x] qa-automated — after merging develop: Storybook built and the widened render spec green over every story; developer ran pages 662 at 100% (lead, 2026-09-05T02:30:00Z)

- [x] clean-code-swept — self-scan (diff is 19 files but a mechanical, repeated snippet-wrap pattern plus two small harness fixes and the spec widening); no violation scored > 0.5 (dev-sonnet, 2026-09-04T02:15:00.000Z)

## Comments

- **dev-sonnet** (2026-09-04T00:00:00.000Z): Starting card 21. Audited every `*.stories.svelte` under packages/pages/src/lib for `<Story>` bodies not wrapped in `{#snippet template()}`. Found 27 files with plain children: molecules/SubdomainCard, atoms/RefLink, atoms/Markdown, atoms/Chip, organisms/InteractiveDiagram, flow/LegendPanel, flow/ContextEdge, flow/RelationEdge, flow/ContextNode, flow/ConsumableEdge, flow/RelationNode, flow/ConsumableNode, flow/SketchBackdrop, and templates/{SchemaPage,ServicePage,PolicyPage,ConsumablePage,InvariantPage,WorkspacePage,AggregatePage,SubdomainPage,ContextPage,TermPage,ValueObjectPage,TeamPage,EntityPage,DomainPage}. Converting each.
- **dev-sonnet** (2026-09-04T00:45:00.000Z): Converted the 16 files with real `<Story>` children (not self-closing `args={{...}}`) to `{#snippet template()}`: `packages/pages/src/lib/molecules/SubdomainCard.stories.svelte:17-21`, `atoms/Chip.stories.svelte:8-13`, and `templates/{SchemaPage,ServicePage,PolicyPage,ConsumablePage(x2),InvariantPage,WorkspacePage,AggregatePage,SubdomainPage,ContextPage,TermPage,ValueObjectPage,TeamPage,EntityPage,DomainPage}.stories.svelte`. The other files flagged in my first audit pass (RefLink, Markdown, InteractiveDiagram, LegendPanel, the flow node/edge stories, SketchBackdrop) turned out to be self-closing `<Story args={{...}} />` with no children — not affected by the bug, so left as-is. Widened `packages/pages/e2e/storybook.spec.ts:21-41` to run against every story in the index (dropped the `DESIGNED_TITLES` allowlist) and changed the render-check at `e2e/storybook.spec.ts:53-64` to accept text OR at least one `<svg>` in `#storybook-root`, since several stories (diagrams, node/edge harnesses) paint only SVG with no text.
- **dev-sonnet** (2026-09-04T01:00:00.000Z): Built core, graphviz, pages, ran `node scripts/codicons.mjs`, then `npm run build-storybook` (green) and `npx playwright test e2e/storybook.spec.ts`: 231 passed, 8 failed. All 8 failures are pre-existing bugs unrelated to the template-snippet issue this card targets, surfaced only because the suite now covers every story instead of an allowlist:
  - `Atoms/Markdown — Empty` (`packages/pages/src/lib/atoms/Markdown.stories.svelte:9`, component at `atoms/Markdown.svelte:12`): `args={{ text: undefined }}` is an intentional empty-state story — `Markdown.svelte` renders nothing `{#if html}` when there's no text, by design, so it will never satisfy a "must paint something" check.
  - `Flow/LegendPanel — Context map/Consumable map/Relation map` (`packages/pages/src/lib/flow/LegendPanel.harness.svelte:11`): throws `Cannot read properties of undefined (reading 'nodes')` in the browser console — `LegendPanel.svelte` calls a SvelteFlow hook that needs `<SvelteFlowProvider>`, but the harness only wraps it in `<SvelteFlow>`, not a provider. Pre-existing harness bug.
  - `Flow/SketchBackdrop` — all 4 stories (`packages/pages/src/lib/flow/SketchBackdrop.harness.svelte:20-24`): throws `https://svelte.dev/e/props_invalid_value` from inside `SvelteFlow`'s `bind:nodes`, before anything paints. Pre-existing harness/binding bug, unrelated to story wiring.
  QUESTION for lead: the checklist item "`e2e/storybook.spec.ts` covers all stories; green" — should I (a) fix these three underlying bugs (SvelteFlowProvider missing in LegendPanel.harness.svelte, the SketchBackdrop.harness.svelte bind:nodes crash, and decide how Markdown's intentional empty state should pass the check), which is new work beyond "convert to template snippets", or (b) exclude these known-broken/intentionally-empty stories from the widened suite (e.g. a small skip-list) and leave the underlying bugs for separate cards? I recommend (b) for Markdown (it's a correct empty-state story, not a bug) but I'm not sure the same call is mine to make for the two SvelteFlow harness bugs. Blocked until answered; verified all 231 other stories (including everything targeted by the original DESIGNED_TITLES allowlist) pass with the new text-or-svg check.
- **lead** (2026-09-05T00:30:00.000Z): Assigned to dev-sonnet. Fixed by decision: convert every story body to a `{#snippet template()}` (see any V2/ story for the pattern), then widen `e2e/storybook.spec.ts` to every story title and keep the existing content assertions; a story that renders only its bare meta component with empty args counts as a failure, so add a check that each story's root has text or an svg. Work in your worktree with absolute paths; build core, graphviz and pages and run `node scripts/codicons.mjs` before `build-storybook`; `npm ci` if node_modules is missing; if the card is missing, `git reset --hard develop` there first.
- **lead** (2026-09-05T01:40:00.000Z): ANSWER. Fix the two harness bugs in this card: wrap `LegendPanel.harness.svelte` in `SvelteFlowProvider`, and give `SketchBackdrop.harness.svelte` a `$state.raw` nodes array so `bind:nodes` is valid. For `Atoms/Markdown — Empty`, keep an explicit `RENDERS_NOTHING` allow-list in `e2e/storybook.spec.ts` with that one entry and a comment saying why; the check asserts those stories render an empty root, so a future regression that paints something there fails too. No separate cards.
- **dev-sonnet** (2026-09-04T01:50:00.000Z): Implemented the lead's fixes. `flow/LegendPanel.harness.svelte:2,12-15` wraps `<SvelteFlow>` in `<SvelteFlowProvider>` and now guards `{#if graph}` around `<LegendPanel>` — debugged with a throwaway Playwright script hitting the built iframe directly: the real crash was `legendEntries(graph, kind)` at `flow/legend.ts:79` (`graph.nodes`) throwing because `graph` arrives `undefined` for one tick while Storybook resolves args through the custom `render: Harness` path, before the provider was ever reached; rebuilding with only the provider fix left the same 3 stories failing, so I added the `{#if graph}` guard too and reran clean. `flow/SketchBackdrop.harness.svelte:16-22` now defaults the `$state.raw` array to `initial ?? []` and only re-syncs in the `$effect` `if (initial)`, so `bind:nodes` on `SvelteFlow` never receives `undefined` (its `nodes` prop has a fallback, and Svelte rejects binding `undefined` to a prop with a fallback — that's what `props_invalid_value` meant). `e2e/storybook.spec.ts:31-35` adds the `RENDERS_NOTHING` allow-list with the single `atoms-markdown--empty` entry and a comment explaining it's intentional; `e2e/storybook.spec.ts:63-81` now asserts an empty root for allow-listed stories (after a 200ms settle) and a non-empty text-or-svg root for everything else.
- **dev-sonnet** (2026-09-04T02:10:00.000Z): Rebuilt `build-storybook` (green) and reran `npx playwright test e2e/storybook.spec.ts`: 239 passed, 0 failed. Reran `npm run test:unit` (vitest + coverage) twice: 109/109 files, 662/662 tests, 100% coverage across the board; the first run showed one flaky timeout in `src/lib/Page.test.ts` under coverage instrumentation (passes standalone and on immediate rerun, and touches nothing this card changed), the second run was fully green. Ran the clean-code-review self-scan (diff is mechanical: 16 near-identical `{#snippet template()}` wraps matching the existing V2 convention, plus the two small harness fixes and the spec widening) — no violation scored above 0.5, nothing to sweep. All checklist items ticked; moving to review.
- **lead** (2026-09-05T02:30:00.000Z): Landing on develop.

- 2026-09-11, lead: Four `Flow/SketchBackdrop` stories still render an empty canvas, and the Storybook check cannot see it; issue 41. This card's claim that every story renders its own component does not hold for those four.

- 2026-09-11, lead: held back from done. Four `Flow/SketchBackdrop` stories still paint an empty canvas, so this card's claim is not met; issue 41.

- 2026-09-11, lead: held back from done. Four `Flow/SketchBackdrop` stories still paint an empty canvas, so this card's claim is not met; issue 41.
