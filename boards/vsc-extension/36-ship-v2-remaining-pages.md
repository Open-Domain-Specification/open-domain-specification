---
column: done
labels: [frontend]
priority: high
agent: dev-opus
live: false
clean-code-swept: true
updatedAt: 2026-09-05T07:30:00.000Z
---
# Ship v2 for the remaining thirteen pages (after card 35)

Domain, Subdomain, Relationship, Team, Health, Entity, ValueObject, Service, Consumable, Schema, Policy, Invariant and Term pages switch to v2; the rest of v1 (templates, organisms, molecules, atoms, page.css rules only they used) is deleted; the `v2/` folder is promoted to the package's main `lib` layout.

## Checklist

- [x] All routes render v2; v1 deleted; `v2/` promoted and imports repointed
- [x] Pages unit at 100%; every e2e green; Storybook spec green over the renamed titles
- [x] `npm run test:vscode` green; screenshots regenerated; docs updated

## Gates

- [x] qa-automated — after merging develop: pages 568 at 100%, all 229 e2e incl. the 148-story render spec, petstore suite; developer ran test:vscode 9, screenshots 13, assertDocSite on four models (lead, 2026-09-05T07:30:00Z)

- [x] clean-code-swept — six-agent audit over the authored files; five findings above 0.5 fixed, one pre-existing tagged in place, two "dead CSS" findings disproved (dev-opus, 2026-09-05T11:35:00.000Z)

## Comments

- **lead** (2026-09-05T01:10:00.000Z): Approved by the human on 2026-09-05. Starts after card 35 lands.
- **lead** (2026-09-05T06:05:00.000Z): Assigned to dev-opus. Card 35 has landed; start from develop head. Fixed by decision: (1) all thirteen remaining routes render the v2 templates inside v2/PageLayout; `Page.svelte` loses its nested v1 `.layout` block. (2) Delete every v1 template, organism, molecule and atom, and every rule in assets/page.css and site.css that only they used; keep InteractiveDiagram, the flow folder, DisclosureCard, PatternHoverCard and the evidence derivations, moving any of them the v2 pages import into the v2 tree if that reads better. (3) Promote `v2/` to be the package's lib layout: `src/lib/v2/*` become `src/lib/{atoms,molecules,organisms,templates}` (or a flat `src/lib/ui/` if that is simpler; journal the choice), imports repointed, story titles lose the `V2/` prefix, `V2/Compare` stories and the `Compare`, `Strategic`, `Tactical` and `V1Tactical` harnesses are deleted since nothing remains to compare. (4) The app shell gets its own wrapper class for the tree column so `site.css` no longer selects a class the Sidebar owns. (5) Update `packages/pages/src/index.ts` exports, `apps/docs/docs/8-pages.md`, and every e2e; re-run `npm run test:vscode` and `npm run screenshots -w ods-vscode`; `assertDocSite` for the four models must stay green (doc generator is untouched). This is a `feat!:` change. Work in your worktree with absolute paths; build core, graphviz and pages and run `node scripts/codicons.mjs` before `build-storybook`; `npm ci` if node_modules is missing; if the card is missing, `git reset --hard develop` there first.
- **dev-opus** (2026-09-05T07:10:00.000Z): Picked this up; the worktree was cut from a stale commit so I reset it to develop first. Survey before touching anything: the only v1 modules the v2 tree imports are `atoms/Logo.svelte`, `atoms/Markdown.svelte`, `evidence/*`, `flow/graph`, `flow/roles`, `model`, `ModelProvider.svelte`, `resolve`, `fixtures`, `templates/elements`, `xyflow-test-env` and `organisms/InteractiveDiagram.svelte`. Every v1 *template* import from the v2 tree is confined to the two compare harnesses this card deletes (packages/pages/src/lib/v2/templates/V1Tactical.harness.svelte:10-18 and packages/pages/src/lib/v2/templates/Compare.harness.svelte:4-8), so once packages/pages/src/lib/Page.svelte:99-140 is repointed the thirteen v1 templates have no consumer left. All thirteen v2 templates already take the same prop names as their v1 counterparts, so the route table is a like-for-like swap.
- **dev-opus** (2026-09-05T07:25:00.000Z): QUESTION for lead: rule (2) says "keep ... PatternHoverCard" but also "delete every v1 template, organism, molecule and atom", and after this card those two cannot both hold. `PatternHoverCard`'s only consumer is the v1 relationship detail (packages/pages/src/lib/organisms/RelationshipDetail.svelte:14), which rule (1) deletes, and the component is built entirely out of v1 parts this card otherwise removes: `atoms/Chip.svelte` (packages/pages/src/lib/molecules/PatternHoverCard.svelte:8), `atoms/DispositionChip.svelte` (:9), `molecules/CommentList.svelte` (:11) and `molecules/hover-card.svelte.ts` (:12), plus the `.chip` rules in packages/pages/assets/page.css:274-299. `docs/design/design-language-v2.md:270-286` marks every one of those four "replace" (`v2/Keyword`, `v2/Disposition`, `v2/Comments`), and `docs/design/v2-specs/organism-strategic-position-table.md:8` says the v2 keyword hovers are drawn with `v2/HoverCard` — which today ships with stories and no consumer. Note that whichever way this goes, the v2 relationship page loses the rich pattern disclosure: v2 `RelationshipDetail` renders the pattern as a plain `Keyword` with a `title` (packages/pages/src/lib/v2/organisms/RelationshipDetail.svelte:22), so keeping the v1 file on disk does not preserve the behaviour. Options: (a) delete `PatternHoverCard` with `Chip`, `DispositionChip`, `CommentList`, `molecules/hover-card.svelte.ts` and `evidence/DispositionStates.harness.svelte`, leaving `v2/HoverCard` as the frame a later card wires in; (b) keep `PatternHoverCard` exactly as it is and keep the four v1 files and the `.chip` CSS alive under it, as dead-but-tested code; (c) rebuild `PatternHoverCard` on the v2 primitives (`HoverCard` + `Keyword` + `Comments` + `Disposition`), move it into the v2 tree and wire it into v2 `RelationshipDetail` in place of the plain keyword, so the disclosure survives the migration — this is more than the card scopes and changes what a reader sees. I recommend (a): it is the only option that leaves no v1 atom behind, and (c)'s behaviour is a card of its own. Blocked until answered.
- **dev-opus** (2026-09-05T07:25:00.000Z): Two choices rule (3) delegates to me, stated here so you can overrule them in the same reply rather than at review. (i) Layout: `src/lib/{atoms,molecules,organisms,templates}` rather than a flat `src/lib/ui/`. The v2 tree is already three quarters of the way there — `v2/molecules`, `v2/organisms` and `v2/templates` hoist unchanged — so only the twelve flat files at `v2/`'s root need a home; they go to `atoms/` (`Comments`, `DataTable`, `Definition`, `DefinitionList`, `Disposition`, `EmptyState`, `Heading`, `HoverCard`, `Keyword`, `Lockup`, `Ref`, `kinds.ts`), except `PageLayout.svelte`, which composes the `Sidebar`-less page frame and belongs with `templates/`. A flat `ui/` would put 60-odd files in one directory and lose the distinction the design doc's own audit tables are organised by (`docs/design/design-language-v2.md:266-320`). (ii) Story harnesses: with `Strategic`, `Tactical`, `Template` and `Compare` gone, both story styles in `v2/templates` collapse onto one ref-based harness that renders the real route — `ModelProvider > Page ref=...`, which is what `packages/pages/src/lib/Page.harness.svelte:1-9` already does for `Page.test.ts` — with `petstore.harness.ts` growing refs for the workspace, domain, subdomain, context, relationship, team and health routes beside the nine it already names. That makes every template story the actual page a host renders rather than a hand-assembled column, and drops `picks.harness.ts` (its five pickers become refs).
- **lead** (2026-09-05T06:40:00.000Z): ANSWER. Option (a): delete PatternHoverCard with Chip, DispositionChip, CommentList, hover-card.svelte.ts and the DispositionStates harness; the rich pattern disclosure is rebuilt on v2/HoverCard by card 38 straight after this card, so keep the `title` on the Keyword as the interim. Both delegated choices confirmed: `src/lib/{atoms,molecules,organisms,templates}` with PageLayout in templates, and one ref-based harness rendering the real route.
- **dev-opus** (2026-09-05T09:10:00.000Z): Routes and promotion done. `packages/pages/src/lib/Page.svelte:95-131` is now sixteen branches of one shape, each a template inside `PageLayout`; the nested v1 `.layout` block is gone and the team page is still the final `{:else}`. The v2 tree was hoisted with `git mv`: `v2/molecules|organisms|templates` became `lib/molecules|organisms|templates`, the twelve flat primitives at `v2/`'s root became `lib/atoms/*` beside the surviving `Icon`, `Logo` and `Markdown`, `PageLayout.*` went to `lib/templates/` and the story theme harness to `packages/pages/src/lib/Theme.harness.svelte` — that last one keeps `"../Theme.harness.svelte"` correct from both `organisms/` and `templates/`, so nothing there had to be rewritten. Deleted per rule (2) and the lead's option (a): all thirteen v1 templates, ten v1 organisms, the whole v1 `molecules/` folder, six v1 atoms, `PatternHoverCard` with `Chip`, `DispositionChip`, `CommentList`, `molecules/hover-card.svelte.ts` and `evidence/DispositionStates.harness.svelte`, and the compare surfaces (`Compare`, `Strategic`, `Tactical`, `V1Tactical` and `Template` harnesses, `picks.harness.ts`, the `V2/Compare` stories and the eight `*.compare.stories.svelte`). `evidence/Theme.harness.svelte` went too: it was the v1 twin of the v2 one and its only remaining caller, `packages/pages/src/lib/evidence/DispositionMap.stories.svelte:8`, now uses the promoted harness, which is a superset (it adds high contrast and the webview body class).
- **dev-opus** (2026-09-05T09:25:00.000Z): Story harnesses collapsed onto one, as agreed. `packages/pages/src/lib/templates/Page.harness.svelte` was a duplicate of `packages/pages/src/lib/Page.harness.svelte:1-9` once the route table drew every page, so it is deleted and all sixteen template story files import the latter; the seven that went through `Template.harness` are rewritten to the same ref-based shape, and `packages/pages/src/lib/templates/petstore.harness.ts:20-27` gains the seven strategic refs (the relationship one is the Catalog/Inventory shared kernel, the petstore's only `refactor`). Story titles lost the `V2/` prefix: the twelve promoted primitives are `Atoms/…`, the twelve organisms `Organisms/…`, `V2/Layout` is `Templates/PageLayout`. `templates.test.ts` iterates `PETSTORE_REFS`, so adding the strategic refs widened its "no card, grid, chip, pill or facts strip" assertion from nine pages to sixteen (packages/pages/src/lib/templates/templates.test.ts:19-33).
- **dev-opus** (2026-09-05T09:40:00.000Z): `assets/page.css` swept, 133 lines out. Gone: `.toc*` (the promoted `Toc` carries its own, and page.css's `.toc li a` was beating the scoped rule on specificity), `.page-head`, `.crumbs*`, `h1 .codicon`, `h1 .id`, `.facts*`, `section` and `section > header`, `h2`, the uppercase `h3`, `.empty`, `.ok`, `.grid.wide`, `.card.highlight`, `.counts`, `.policy`, the whole chip and pill blocks, the bare `table`/`th`/`td` and `table.attrs` rules, `.relations`, the v1 `.problems` rows and `.problems .rule`, the framed `.diagram` block, and the `--supporting` and `--generic` tokens. Two exceptions to "delete every v1 rule", journalled because they read as leftovers otherwise: `.layout`, `.card`, `.card-head`, `.grid`, `.dim`, `.lead`, `.problems`, `h1`, `code`/`.id`, `--card`, `--radius` and `--gap` stay, because `packages/pages/src/app/ImportScreen.svelte:82-113` and `packages/pages/src/app/WorkspacePicker.svelte:13-18` still use them and `docs/design/design-language-v2.md:266-336` gives neither screen a verdict — they are not templates and this card does not restyle them; and `--core` stays because `packages/pages/src/lib/flow/RelationNode.svelte` uses it. One rule was added rather than deleted: `.flash` (packages/pages/assets/page.css:100-114). It was only ever `.card.flash`, so deleting the cards would have silently dropped the scroll-to-anchor mark that `packages/pages/src/lib/Page.svelte:90` still sets; it is now global and drawn as `docs/design/v2-specs/template-page-layout.md:19` specifies, a `list.hoverBackground` wash fading over the same 1.6s.
- **dev-opus** (2026-09-05T09:45:00.000Z): One behavioural change fell out of the deletions and is worth calling out at review: `organisms/DisclosureCard.svelte:4` imports `./RelationshipDetail.svelte`, which is now the v2 organism, so clicking an evidence badge on the context map opens the v2 detail. The test asserted v1's bare title, so it now asserts the v2 one whole — both lockups, the arrow, the pattern and the disposition (packages/pages/src/lib/organisms/DisclosureCard.test.ts:42-49), which would catch the card opening on the wrong relationship as the old assertion did. Pages unit is green at the package's thresholds: 91 files, 568 tests, 100% statements, branches, functions and lines.
- **dev-opus** (2026-09-05T10:30:00.000Z): Every suite green. Pages unit: 91 files, 568 tests, 100% on all four measures (the package's threshold). Playwright: 229 passed, which includes the Storybook spec walking all 148 stories under the renamed titles — `Atoms/…`, `Organisms/…`, `Templates/…` and `Templates/PageLayout` — with no empty root. Six e2e assertions had to move to the v2 surfaces they now describe, each rewritten to assert the v2 treatment rather than deleted: `main .crumbs .kind` is gone in two specs because v2 says the kind in the title lockup, so `packages/pages/e2e/browse.spec.ts:121-127` now checks the trail and `h1 .detail`, and `packages/pages/e2e/relationship.spec.ts:112-116` asserts the whole crumb trail; the health page's `.summary` strip became the heading count badges (packages/pages/e2e/browse.spec.ts:66-70) and its "No comments" toggle lost its `(0)` because v2 draws no badge at zero, which the spec now asserts explicitly (packages/pages/e2e/browse.spec.ts:77-81); the two disclosure specs matched five headings once the card started drawing the v2 detail, so they select the detail's own `> h3` and assert it whole, contexts, pattern and disposition (packages/pages/e2e/diagrams-context.spec.ts:141-146 and :178-180). The seventh, `relationship.spec.ts:87`, covered the `PatternHoverCard` disclosure this card deletes; it is rewritten as the interim the lead specified — the role code carries the pattern summary as its title and the evidence is the page's own `#comments` section — and names card 38 as what restores the hover (packages/pages/e2e/relationship.spec.ts:87-105). `npm run test:vscode`: 9 passing. `npm run screenshots -w ods-vscode`: 13 passing, all four marketplace PNGs regenerated. `assertDocSite` for petstore, streamline, northbank and rivermart: 27 tests passing, generator untouched. `svelte-check`: 0 errors, 0 warnings. Docusaurus build clean.
- **dev-opus** (2026-09-05T10:35:00.000Z): Docs updated. `apps/docs/docs/8-pages.md` loses the "a second set lives under src/lib/v2" paragraph and the `V2/Compare` mention; the component library section now lists the four layers as they ship and says every page renders through `Page.svelte` into `PageLayout`, and the stylesheet paragraph says what `page.css` is now for — theme tokens, document defaults, the toolbar, the anchor flash and the import screen. The relationship section's hover-card paragraph is replaced by what the page actually does today. Three design docs carried paths and story titles that this card invalidates, so they are corrected in place rather than left to mislead: `docs/design/design-language-v2.md:20-22,157,163,187` (the primitives are `lib/atoms/`, the harness is `lib/Theme.harness.svelte`, the stories are `Atoms/…`), `docs/design/v2-specs/README.md:7`, and `docs/design/MORNING-REVIEW.md:3,13`, which sent a reader to `V2/Compare` — it now says the review is the record of that morning and that the comparison stories went with the last v1 template.
- **dev-opus** (2026-09-05T11:20:00.000Z): Clean-code sweep, six agents over the authored files (the ~200 pure moves were excluded from the audit). Five findings above 0.5 were real and are fixed. (1) `.layout` was defined twice with contradicting comments — the page stylesheet's copy and `templates/PageLayout.svelte`'s scoped one, same grid, gap, cap, gutter and 900px breakpoint. `PageLayout` now owns `.layout` outright; the import screen and the workspace picker, which never had a second column to fill, get their own one-column `.screen` (packages/pages/assets/page.css:84-92, packages/pages/src/app/ImportScreen.svelte:82, packages/pages/src/app/WorkspacePicker.svelte:13). (2) `organisms/LanguageSection.svelte` imported `../templates/elements`, the one upward import across the four layers, and `molecules/crumbs.ts` referenced it too — so `elements.ts` moved to `packages/pages/src/lib/elements.ts`, where the molecules, the organisms, the templates and `src/index.ts` can all read it and the extension can still import it without pulling in Svelte; its header now says why it sits there (packages/pages/src/lib/elements.ts:11-19). (3) The same 88-character pattern summary was pasted into two relationship e2e assertions; both now read it from core, as the sibling diagram spec already did (packages/pages/e2e/relationship.spec.ts:11-12,84,98). (4) and (5) two doc comments made false present-tense claims: `Theme.harness.svelte` called itself the v2 theme wrapper when it now themes every story, and `PageLayout.stories.svelte` described a toolbar its harness does not draw and asked "whether v2 reads as one page" (packages/pages/src/lib/Theme.harness.svelte:5-7, packages/pages/src/lib/templates/PageLayout.stories.svelte:17-21). Two agents called `.toolbar`, `.toolbar .spacer` and `button.icon` dead: they are not — `apps/ods-vscode/src/pages/panel.ts:162-164` renders exactly that markup into the webview shell around the app, which loads `page.css` from the bundle. Left as-is, noted here so it is not re-raised. `atoms/HoverCard.svelte` is flagged unconsumed (0.55) and is correct to keep: it is the frame card 38 wires in. One finding above 0.5 is pre-existing and outside this card, so it is tagged rather than fixed: `packages/pages/src/lib/organisms/PageHeader.svelte:51-57` restates `.md p` from the page stylesheet verbatim. Below the threshold but worth doing while the tree is being renamed: nine `describe("v2 …")` suite names lost the qualifier that now distinguishes nothing, matching the story titles, and `lib/icons.ts:43-47`'s comment no longer cites the deleted chip.
- **dev-opus** (2026-09-05T11:35:00.000Z): Re-ran everything after the sweep. `svelte-check` 0/0; pages unit 91 files, 568 tests, 100% on all four measures; `npm run build` clean; Playwright 229 passed; `npm run screenshots -w ods-vscode` 13 passing with the four PNGs regenerated against the final tree; `assertDocSite` 27 tests across the four models. `biome check` over `packages/pages` reports the same one error and two warnings it reports on develop (`WorkspacePage.test.ts:79`, `page.css`'s flow block, `flow/fullscreen.svelte.ts:35`) — all pre-existing and untouched. Rule (5) asked for `packages/pages/src/index.ts` exports to be updated: nothing was needed beyond the `elements.ts` path, because the barrel never exported a v1 component — it exports the icon maps, `resolvePage`/`pageRefs`/`HEALTH_REF` and the leaf lookups, and all five names `apps/ods-vscode` imports still resolve. Note for review: I staged the tree (`git add -A`) so the ~108 renames render as renames; `git diff --cached -M --stat` is the readable view. Moving to review.
- **lead** (2026-09-05T07:30:00.000Z): Reviewed the staged rename set and the Page.svelte routes. The map disclosure opening the v2 detail is intended. Landing on develop.

- 2026-09-11, lead: driven in a browser against the built app and confirmed against this card's own claim; moved to done.
