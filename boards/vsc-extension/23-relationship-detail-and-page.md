---
column: review
labels: [frontend, docs]
priority: high
agent: dev-opus
live: false
clean-code-swept: true
updatedAt: 2026-09-04T08:20:00.000Z
---
# Relationship detail in place and as its own page (RFC-002 card E)

The relationship detail designed in card 19 ships: expanded in place from a Strategic position row, and as a standalone page reachable from the tree, search, the context page and the map edge. Comments and dispositions now come from core (project card 40) and pattern summaries from core (project card 41); the provisional types in packages/pages/src/lib/evidence/fixtures.ts go away.

## Checklist

- [x] `packages/pages/src/lib/evidence/` reads `comments` and `disposition` from the core model elements; `NoteSheetIndex`/`CommentSheetIndex`, `relationshipKey`, `sheetFor*` and the petstore overlay fixture are deleted; stories use the petstore model's real comments
- [x] `StrategicPositionTable` on the context page shows the disposition column and the expand toggle when any relationship carries comments or a disposition; the expanded row renders `RelationshipDetail`
- [x] A relationship ref (`#/relationships/<n>` or a stable key, decide with the lead if core has no id) routes to a `RelationshipPage` template rendering `RelationshipDetail` standalone; `elements.ts` and search index it; the extension tree's relationship nodes get that ref so clicking opens the page
- [x] `ConsumablePage` gains the comments list and a disposition chip
- [x] `packages/doc`: the bounded context page prints each relationship's comments (text plus link) beneath its row or as a per-relationship section, and the consumable page prints its comments
- [x] Pages unit suite at 100%; an e2e case that expands a row on the petstore Sales page and one that opens the relationship page from the tree in `npm run test:vscode`
- [x] The "participant" chips on symmetric relationships in `RelationshipDetail` are removed

## Gates

- [x] qa-automated — inside the worktree: core 119, doc 21, petstore 18, pages 452 at 100%, relationship and context e2e 3/3; developer ran the full 65 e2e and test:vscode 6 (lead, 2026-09-04T08:20:00Z)

- [x] clean-code-swept — six principles over three parallel agents; five findings above 0.5 fixed (SYMMETRIC triplication, labels out of fixtures.ts, noFacts→noComments, petstoreSales helper, commentBlocksMd dropped), two left for the lead and recorded in the journal (dev-opus, 2026-09-04T10:05:00.000Z)

## Comments

- **lead** (2026-09-04T06:30:00.000Z): Assigned to dev-opus. Fixed by decision: (1) a relationship's ref is `#/relationships/<source.id>~<type>~<target.id>` (the key card 19 already used), exposed as a `ref` getter on `ContextRelationship` in core, with `workspace.findRelationship(ref)` beside it; that is the only core change. (2) The page is `RelationshipPage` in packages/pages/src/lib/templates, registered in `elements.ts` with the icon the tree uses, so search and the tree open it; the extension tree's relationship nodes get that ref. (3) `RelationshipDetail` drops the "participant" chips on symmetric relationships; the role cards on a symmetric relationship show just the two contexts and the pattern summary once, above them. (4) Comments in the doc generator render as a bullet list under the relationship's row group in strategic-position.md.ts and under the consumable's header, text then link as `[label](url)`. (5) Delete every provisional type and fixture in packages/pages/src/lib/evidence that core now provides; keep only what derives views (positionGroups, health, crossingConsumables, relationshipLinks) and the density fixture used by the eight-relationship stories. Tests that prove it: pages unit at 100%, the e2e cases in the checklist, `npm run test:vscode` for the tree, doc tests, `assertDocSite` for the four models. Run suites from inside your worktree with absolute paths; a root `npm test` replays the main tree's cache and is not evidence. Work in your worktree; `npm ci` there first if node_modules is missing; if the card is missing, `git reset --hard develop` there first.
- **dev-opus** (2026-09-04T07:10:00.000Z): Claimed. The worktree was created from a stale commit, so `git reset --hard develop` and `npm ci` first. Confirmed core cards 40 and 41 have landed: `packages/core/src/evidence.ts:22-26` gives every intent `comments`/`disposition`, `packages/core/src/workspace.ts:1473-1474` carries them on `ContextRelationship`, and `models/petstore/.ods/petstore.json` already holds the real comments the overlay was standing in for (three relationships, two consumables, one consumption). `ContextRelationship` has no `ref` getter yet (`packages/core/src/workspace.ts:1460-1526`), so that is the first change.
- **dev-opus** (2026-09-04T08:10:00.000Z): Core first. `ContextRelationship` gains `path`/`ref` at `packages/core/src/workspace.ts:1495-1508` — `#/relationships/<source.id>~<type>~<target.id>`, exactly the key card 19 used — and `Workspace.findRelationship(ref)` sits with the other ref lookups at `packages/core/src/workspace.ts:111-114`. Three cases at `packages/core/src/lookups.test.ts:319-361` prove the shape, the round trip, and that two relationships between the same pair are told apart by their type. Core suite green: 122 tests, 11 files.
- **dev-opus** (2026-09-04T08:25:00.000Z): The evidence layer now reads the model. `packages/pages/src/lib/evidence/fixtures.ts` keeps only the wording (`DISPOSITION_LABELS`, `DISPOSITION_SUMMARIES`, `LINK_KIND_LABELS` at :17-38 — core has no equivalent, and `DispositionChip`/`CommentList`/`DispositionLegend` already imported them from here) and the eight-relationship density fixture at :156-207, which now sets `comments`/`disposition` through the builder instead of an overlay index. Every provisional type and the whole `PETSTORE_SHEETS` overlay are gone; `Comment`, `CommentLink`, `CommentLinkKind` and `Disposition` come from core. `derive.ts` lost its `sheets` parameter throughout: `positionGroups(bc, relationships)` at :49-79, `health(workspace)` at :96-117, `crossingConsumables(r, workspace)` at :129-148, `relationshipLinks(r, crossings)` at :156-170, and a new `hasEvidence(r)` at :22-23 for the table's disclosure decision. `dispositionOf` is core's now.
- **dev-opus** (2026-09-04T08:35:00.000Z): `StrategicPositionTable` no longer takes `sheets`; it derives `withEvidence` from the rows themselves at `packages/pages/src/lib/organisms/StrategicPositionTable.svelte:34-38`, so the toggle column, the disposition column and the expandable row appear as soon as one relationship in this context's position carries a comment or a non-default disposition, and vanish together when none does. Two new cases pin both ends at `packages/pages/src/lib/organisms/StrategicPositionTable.test.ts:130-155`. The context page needed no change and now discloses the petstore's evidence — `packages/pages/src/lib/templates/ContextPage.test.ts:28-52` expands the Catalog→Sales row and reads the comment out of it.
- **dev-opus** (2026-09-04T08:45:00.000Z): `RelationshipDetail` reads `r.comments`/`r.disposition` directly (`packages/pages/src/lib/organisms/RelationshipDetail.svelte:51,95`), and per your point (3) the "participant" chips are gone: a symmetric relationship states its pattern once above the two context cards at :60-72 and gives neither side a chip or a role list. Its four blocks became `<section>`s with the ids the page's table of contents jumps to, with `.relationship-detail section { margin: 0 }` at :168-172 so the page-wide 40px section gap does not tear the block apart inside a table row. `packages/pages/src/lib/organisms/RelationshipDetail.test.ts:55-84` proves the symmetric shape and that a directed relationship with no declared roles still falls back to the pattern summary on both sides.
- **dev-opus** (2026-09-04T08:55:00.000Z): The page and the routes. `packages/pages/src/lib/templates/RelationshipPage.svelte` renders `RelationshipDetail` standalone with `heading="h1"` under the same `.crumbs` header every other page uses, and exports the four sections for the Toc. `relationshipTitle` is registered in `packages/pages/src/lib/templates/elements.ts:12-24` and re-exported from `packages/pages/src/index.ts:9`, so the detail, the page and the extension's search all name a relationship the same way. `resolve.ts:5,50-51` lists relationship refs in `pageRefs` and matches them whole in `PAGE_PATTERNS`; `Page.svelte:104-105` renders the template. Extension: the tree's relationship rows carry `ref: r.ref` (`apps/ods-vscode/src/tree.ts:229-232`) so clicking one opens the page, and the spotlight indexes them at `apps/ods-vscode/src/search.ts:57-65`. `ConsumablePage` gained a disposition chip in its header and a Comments section (`packages/pages/src/lib/templates/ConsumablePage.svelte:52,118-124`).
- **dev-opus** (2026-09-04T09:00:00.000Z): Evidence. Pages unit suite green at the 100% thresholds — 452 tests, 61 files, every file 100% statements/branches/functions/lines. Two new Playwright cases in `packages/pages/e2e/relationship.spec.ts`: one expands the Catalog→Sales row on the petstore Sales page, reads the comment out of the expanded detail and asserts the URL never leaves `#/boundedcontexts/sales_bc`; one opens `#/relationships/catalog_bc~customer-supplier~sales_bc` directly and walks its crumb back to Sales. Both pass. `npm run test:vscode` is green with a new case at `apps/ods-vscode/src/test/extension.test.ts:161-183` that walks the real tree to the Relationships group, checks the row's `ods.openPage` command, runs it and waits for the webview's own `navigated` message carrying the relationship ref; `ModelTree` is exposed on `OdsTestApi` (`apps/ods-vscode/src/extension.ts:19,152`) so the test drives the real tree rather than a stub.
- **dev-opus** (2026-09-04T09:05:00.000Z): `packages/doc` half done. `strategic-position.md.ts:36-70` prints each relationship's comments as a bullet list under its own group's table, headed `- **<counterpart>** (<type>)` so a reader can match a bullet to a row, then one indented bullet per comment, statement first and the citation as `[label](url)`. `packages/doc/src/index.test.ts:264-284` pins the wording, that the bullets sit under the right group, and that the two silent petstore relationships print nothing. Doc suite green: 19 tests.
- **dev-opus** (2026-09-04T09:06:00.000Z): QUESTION for lead: your point (4) says the consumable's comments render "under the consumable's header", but `packages/doc` has no consumable page and no consumable header — a provided consumable is a row in the `Provides` table (`packages/doc/src/consumables.md.ts:26-49`) on its aggregate's or service's page, and only the *consumed* side has `### <name>` headings (`packages/doc/src/service.md.ts:7-12`). Options: (a) print the comments as a bullet list beneath the `## Provides` table, headed `- **<consumable>**`, exactly as the relationships now read under their group table — one shape for both, no change to the doc's page set; (b) replace the `Provides` table with a `### <consumable>` section per consumable, which gives every consumable a real header but rewrites a table three page types share and every reader knows; (c) give each non-internal consumable its own page under the provider's path, `pathToIndexMd` and the sidebar included. I recommend (a): it keeps the doc's shape, matches what a reader has just seen for relationships, and leaves (c) to whichever card decides the doc site needs consumable pages. Blocked until answered — everything else on the card is done and green.
- **dev-opus** (2026-09-04T09:15:00.000Z): Full re-run after `biome check --write`. Note for review: biome reformats several files this card never touched (`apps/docs/docusaurus.config.ts`, both board `.config.json`s, `lerna.json`, all four `models/*/.ods/*.json`, `packages/pages/e2e/global-setup.ts`, `scripts/qa-review.mjs`), so I reverted every one of them — the working tree holds only this card's files. The three lint errors biome still reports (`packages/core/src/workspace.ts:1764,1780` noThenProperty and `packages/pages/e2e/global-setup.ts` formatting) are all pre-existing on develop. Green: core 119 tests / 11 files; pages unit 452 tests / 61 files at 100% statements, branches, functions and lines; pages Playwright 65 passed, including the six rewritten Storybook stories which still render non-empty; doc 19 tests / 4 files; `npm run test:vscode` 6 passing; all four model packages green, so `assertDocSite` still finds every link the new relationship bullets add; `apps/ods-ui` builds. `svelte-check` clean. I have held the `clean-code-review` sweep until the doc question is answered, so it covers the finished change.
- **dev-opus** (2026-09-04T09:40:00.000Z): Your answer (a) is in. `packages/doc/src/comments.md.ts` is the one renderer both halves share — `commentsMd(title, comments)` emits `- <title>` then one indented bullet per comment, statement first and the citation as `[label](url)`. `strategic-position.md.ts:40-58` calls it per relationship under its group's table, `consumables.md.ts:32-48` per consumable under the Provides table. Two cases at `packages/doc/src/index.test.ts:287-310` pin the consumable half: `GetPetSummary`'s comment prints under the Pet App Provides table and after it, and Fulfilment's Shipment aggregate — whose consumables carry nothing — prints no bullets at all. A group now ends with a blank line (`strategic-position.md.ts:54-57`) so the next `###` never abuts the last bullet.
- **dev-opus** (2026-09-04T09:45:00.000Z): Regenerated the committed doc site with `npm run build` in each model package, as card 41 did when it changed the generator — otherwise the committed site contradicts its generator. Only `models/petstore/docs` moved (five files); the other three reference models carry no comments in their `.ods` yet, so their sites are byte-identical. All four model suites stay green, so `assertDocSite` still resolves every link.
- **dev-opus** (2026-09-04T10:05:00.000Z): `clean-code-review` sweep, six principles over three parallel agents. Fixed everything they scored above 0.5, all of it mine: (1) DRY 0.7 — the symmetric-type set was triplicated, so `packages/pages/src/lib/relationship.ts` is now the one home for `isSymmetricRelationship` and `relationshipTitle`; `flow/graph.ts:95` re-exports the predicate so no existing importer changed, `templates/elements.ts:11-16` re-exports the title (your point 2's registration), and `derive.ts`, `HealthReport` and `RelationshipDetail` read the leaf module directly. It has to be a leaf: `flow/graph.ts` re-exports `context-graph`, which imports `@open-domain-specification/graphviz`, and `elements.ts` is on the extension's import path through `src/index.ts`. (2) NAMING 0.7 — the label maps were living in a file called `fixtures.ts` and were imported by shipped atoms and molecules, so they moved to `packages/pages/src/lib/evidence/labels.ts` with their own test at `labels.test.ts`; `fixtures.ts` is now only the density fixture. (3) NAMING 0.6 — `Health.noFacts` was the last of the retired "facts" vocabulary while the UI already said "No comments"; it is `noComments` throughout `derive.ts:89,114` and `HealthReport.svelte`. (4) DRY 0.55 — the petstore-Sales lookup had spread to three copies, now one `petstoreSales()` at `packages/pages/src/lib/fixtures.ts:23-35` used by `derive.test.ts`, `StrategicPositionTable.test.ts`, `StrategicPositionTable.stories.svelte` and `ContextPage.test.ts`. (5) NAMING 0.55 — dropped `commentBlocksMd`, a confusable one-line wrapper; both doc call sites inline `.filter(Boolean).join("\n")`.
- **dev-opus** (2026-09-04T10:06:00.000Z): Two sweep findings I did not act on, both for you rather than me. First, `apps/ods-vscode/src/tree.ts:198-205` still builds its own relationship label (`"A to B"` / `"A and B"`), so the tree and the spotlight now word the same relationship differently, and a `separate-ways` relationship reads "A to B" in the tree though it is symmetric — a pre-existing bug. Adopting `relationshipTitle` there fixes both but changes a label every user of the tree can see, which is your call, not mine; I left the tree alone and wrote `relationshipTitle`'s docstring to claim only the surfaces that actually use it. Second, `packages/doc/src/strategic-position.md.ts:9-11` keeps its own copy of the symmetric set — sharing it would mean `packages/doc` depending on `packages/pages`, or a fourth core export, and neither is this card's to decide. Nothing else scored above 0.5.
- **dev-opus** (2026-09-04T10:10:00.000Z): Final evidence, everything re-run after the sweep. Core 119 tests / 11 files. Pages unit 452 tests / 62 files, 100% statements, branches, functions and lines. Pages Playwright 65 passed, the two new relationship cases and all six rewritten Storybook stories among them. Doc 21 tests / 4 files. `npm run test:vscode` 6 passing including "opens a relationship's page from its row in the tree". All four model packages green. `svelte-check` 0 errors, 0 warnings. `apps/ods-ui` builds. Biome clean over every file this card touches; the three findings it still reports (`packages/core/src/workspace.ts:1764,1780`, `packages/pages/e2e/global-setup.ts`) are pre-existing on develop, and I again reverted the unrelated files `biome check --write` reformats. Moving to review.
- **lead** (2026-09-04T08:20:00.000Z): Reviewed the ref getter, RelationshipPage and the tree change. The two leftovers (tree label wording, duplicated symmetric predicate) are card 24. Landing on develop.
