---
column: done
labels: [bug, infra, frontend]
priority: high
agent: dev-opus
live: false
clean-code-swept: true
updatedAt: 2026-09-05T23:50:00.000Z
---
# Two e2e specs fail on Linux, blocking the release

The new CI e2e job (project card 43) ran the Playwright suite on a Linux runner for the first time and two specs fail there while passing on macOS. `publish` now depends on that job, so this blocks the release. Run 33909581491 on PR 24.

1. `packages/pages/e2e/relationship.spec.ts:275` — the first Strategic position row's height is 88px, asserted `< 80`. The runner's fonts are not the developer's, so the description wraps to one more line and the row is a line taller. The assertion's intent is "the description reads as prose, not a word a line, and the row stays short", and a pixel count tuned on one machine cannot say that.
2. `packages/pages/e2e/diagrams-context.spec.ts:160` ("the shared kernel the model wants refactored is marked on the workspace map") — `locator.click` times out after 30s at `:182`, waiting on the anchored disclosure card's heading. It passes locally. Likely the map's fit differs on the runner (different viewport metrics after the panel-fit change from card 20), leaving the badge outside the view or under a panel.

## Checklist

- [x] Failure 1: express the intent in terms the platform cannot move — line count against the computed line height, or the description's height against a single line's — rather than an absolute pixel threshold; say in the test's comment what it is really asserting
- [x] Failure 2: find why the click never lands on the runner and fix the cause; if the badge is genuinely out of view, the spec should scroll or fit to it the way a reader would, not raise the timeout
- [x] Audit the rest of the suite for the same class of assertion (absolute pixel thresholds tuned on macOS) and fix any others you find, listing them in the journal
- [x] Both specs green locally
- [x] Pages unit suite unchanged at 100%

## Gates

- [x] qa-automated — after merging develop: pages 644 at 100%, full Playwright 254/254 including the new hit-test; the Linux claim is confirmed by the CI e2e job on PR 24 (lead, 2026-09-05T23:50:00Z)

- [x] clean-code-swept — six-agent audit over the diff; one finding above 0.5 (DRY 0.55, the prose assertion pair copy-pasted in two tests) fixed by extracting `expectProseRow` to `packages/pages/e2e/helpers.ts:69-89`; everything else scored 0.4 or below (dev-opus, 2026-09-04T20:05:00.000Z)

## Comments

- **lead** (2026-09-05T23:00:00.000Z): Assigned to dev-opus. Fixed by decision: do not raise the Playwright timeout, do not skip either spec, and do not weaken an assertion to "greater than zero"; each must still fail if the defect it guards comes back. You cannot reproduce the Linux font metrics locally, so reason from the numbers in the run rather than tuning to your own machine: 88px against a 22px line is four lines where three was intended. Verify by pushing to your branch if you want a runner, or by reasoning it through and saying so. Work in your worktree with absolute paths; build core, graphviz and pages and run `node scripts/codicons.mjs` before `build-storybook`; `npm ci` if node_modules is missing; if the card is missing, `git reset --hard develop` there first.
- **dev-opus** (2026-09-04T19:32:10.000Z): Picked this up. The worktree was on tag v0.3.0 and had no card, so `git reset --hard develop` first (now 6a068f6), then `npm ci` and a build of core, graphviz and pages.
- **dev-opus** (2026-09-04T19:34:00.000Z): Failure 2 is not the fit and not the fonts moving the badge out of view — it is a stacking bug that has been there all along, and the runner only made it wide enough to hit. The run's call log names the interceptor: the SK badge's button resolves, is "visible, enabled and stable", and every click lands on `<div class="svelte-flow__node svelte-flow__node-context">` for `#/boundedcontexts/sales_bc` instead. Svelte Flow writes `z-index: 2` on a node (`packages/pages/src/lib/flow/flow-nodes.ts:79` sets only the cluster's own `-1`) and the same `z-index: 2` on an edge label, from its edge, at `node_modules/@xyflow/svelte/dist/lib/components/EdgeLabel/EdgeLabel.svelte:39`; the two layers are siblings under the viewport with no z-index of their own, and `.svelte-flow__nodes` is the later one, so wherever a node's box reaches a badge the node draws over it and takes its pointer. `packages/pages/src/lib/flow/PortBadge.svelte:38` renders the clickable badge as an `EdgeLabel`, so this is every badge that discloses evidence.
- **dev-opus** (2026-09-04T19:36:00.000Z): Reproduced it here, which the click alone never does: a hit test at the SK button's own centre returns the Sales card on macOS too. The Sales node's box is `[702, 318, 87, 38]` and the button's centre `(737.6, 316.9)`, so the overlap is about a pixel — Playwright's synthesised click still squeaks through, and on Linux, where the node's box is measured from wider fonts, it does not. New spec `packages/pages/e2e/diagrams-context.spec.ts:190-224` asks it as a hit test over every `.port.intent` on the context map, and fails on this checkout with `SK is under #/boundedcontexts/sales_bc` — the cause named, where the old spec could only ever report a 30s timeout.
- **dev-opus** (2026-09-04T19:38:00.000Z): Fixed by lifting the label layer over the nodes layer: `packages/pages/assets/page.css:300-313`. The layer and not the badge, because Svelte Flow writes each label's z-index inline (so a rule on the badge would need `!important`, which Biome flags) and it takes it from the edge, so lifting the edge would carry the line over the cards too. Badges now draw above the cards they touch, which is what a badge is for; the disclosure card keeps its own lift above both at `packages/pages/src/lib/organisms/DisclosureCard.svelte:40`. Verified both ways: with the rule the new spec and `e2e/diagrams-context.spec.ts:160` (the shared kernel) pass, and with the rule taken out the new spec fails again on the same badge.
- **dev-opus** (2026-09-04T19:40:00.000Z): Failure 1. Added `wrapOf` at `packages/pages/e2e/helpers.ts:24-67`: it measures how a block of text actually wrapped — the line boxes its content occupies (fragments sharing a line grouped into one band, so an icon beside a name counts as the one line it is), the ink on them, the width they wrapped into, and the computed line height. `packages/pages/e2e/relationship.spec.ts:284-293` now says the two things the 80px stood for, neither of which a font can move: the description fills the width it has, `lines <= ceil(ink / width) + 1` (the `+ 1` is what a word boundary can cost), and the row is those lines and nothing more, `height <= lines * lineHeight + 1`, so nothing else in the row is taller than the prose. On this machine the row is 3 lines of 22px and 66px tall; on the runner it is 4 lines and 88px, and both pass, while the defect the assertion guards — the description crushed to 119px and 111px tall — still fails at the 24ch prose floor on the line above.
- **dev-opus** (2026-09-04T19:42:00.000Z): Audit of the rest of the suite for thresholds a font can move. Two more of the same class, both fixed. `packages/pages/e2e/relationship.spec.ts:75-81` asserted the crossings row is `< 32px` to mean "the icon did not break off its name"; it is 23.5px here with a 22px line height and a 24px line box (the icon is the tallest thing on the line, which is why a line-height sum does not model it) — now `wrapOf(crossing).lines` is 1. `packages/pages/e2e/relationship.spec.ts:193-202` asserted a row `< 120px` where the defect was 177px — now the same prose-and-row pair as above. Left alone as structural rather than typographic: the modal geometry at `:63-73` (centre, margins and max width, all derived from the viewport or a design token), `packages/pages/e2e/diagrams.spec.ts:86` (embedded height against the viewport), and `packages/pages/e2e/diagrams-sketch.spec.ts:95-158` (each measurement compared against another measurement from the same run).
- **dev-opus** (2026-09-04T19:44:00.000Z): Evidence. Full Playwright suite `npx playwright test` in `packages/pages`: 254 passed. Unit suite `npm run test:unit`: 94 files, 644 tests, coverage 100/100/100/100, thresholds unchanged. `npx @biomejs/biome check` over the changed files is clean apart from the pre-existing `noDescendingSpecificity` warning at `packages/pages/assets/page.css:211`. I could not run a Linux runner, so the Linux claim is reasoning, not a run: the fix removes the overlap's effect rather than the overlap, so it does not depend on the runner's metrics, and the two rewritten assertions are satisfied by both the macOS numbers and the numbers the failing run reported.
- **dev-opus** (2026-09-04T20:05:00.000Z): Clean-code sweep, six agents over the diff. One finding above 0.5: DRY 0.55, the prose-and-row assertion pair copy-pasted into two tests, with the `+ 1` tolerance written out twice. Fixed by moving both assertions into `expectProseRow` at `packages/pages/e2e/helpers.ts:69-89`, which explains the pair once; the call sites are now `packages/pages/e2e/relationship.spec.ts:194` and `:278`, and the earlier journal entries' line numbers for those two blocks are superseded by this. Also took two sub-threshold naming notes while I was in the file: the hit test's list is `problems` rather than `covered`, since one of its entries is a badge that is not a button, and the element at the point is named once instead of twice (`packages/pages/e2e/diagrams-context.spec.ts:207-223`). Below threshold and left alone: the CSS rule's dependence on Svelte Flow's layer names (0.4 — the whole of `page.css` is written against them, and the new hit test now catches it if a release moves them), `wrapOf` and `ink` as names (0.35, 0.3), and `data-id` in the failure message only (0.1). Re-ran everything after the refactor: 254 e2e passed, 644 unit tests at 100%, Biome clean but for the pre-existing warning. Card to review; not committed, the tree is yours.
- **lead** (2026-09-05T23:50:00.000Z): Reviewed. Failure 2 was a real defect, not a flake: a node drawing over an edge label takes its pointer, so a badge near a node was unclickable for readers too, and the runner's wider fonts only made it certain. Lifting the label layer fixes the effect regardless of metrics, and the new hit-test guards every clickable badge. Landing on develop; the CI e2e job on PR 24 is the Linux proof.

- 2026-09-11, lead: driven in a browser against the built app and confirmed against this card's own claim; moved to done.
