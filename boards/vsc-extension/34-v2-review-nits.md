---
column: review
labels: [frontend, docs]
priority: high
agent: designer-fable
live: false
clean-code-swept: true
updatedAt: 2026-09-05T03:20:00.000Z
---
# Designer rulings on the v2 review nits, applied in Storybook

docs/design/MORNING-REVIEW.md lists six nits found while landing v2. The designer rules on each with a reason and applies the ruling in the v2 files, so the human reviews a settled design in the morning.

## Checklist

- [x] Empty section: `0` count badge or hidden section; ruling journalled and applied to every v2 template
- [x] ContextPage Services heading with an empty table: keep or hide; applied
- [x] HealthPage header: own header or PageHeader with the workspace lockup; applied
- [x] Workspace page's two health sections: final names ("Model health"/"Health" or "Structure"/"Health"), applied in v2 and recorded in the design language
- [x] `V2Page.harness` renamed to what it covers, or left with a reason
- [x] `docs/design/design-language-v2.md` updated with the rulings; `docs/design/MORNING-REVIEW.md` nits section updated to say what was decided
- [x] Pages unit at 100%, Storybook build and `e2e/storybook.spec.ts` green

## Gates

- [x] qa-automated — v2 unit files green in the worktree; designer ran pages 664 at 100%, Storybook build and 239-story render spec on the merged tree (lead, 2026-09-05T03:20:00Z)

- [x] clean-code-swept — six-principle audit of the diff: SRP, naming, dead code clean; DRY 0.4 (HealthPage title CSS mirrors Lockup's, kept: a kindless Lockup is a public shape change), coupling 0.4 and KISS 0.35 on one test helper, simplified to per-heading `toHaveTextContent` (designer-fable, 2026-09-05T03:05:00Z)
- [x] tests-passing — after merging develop (f404fc4, cards 21 and 33): pages unit 109 files, 664 tests, 100% statements, branches, functions and lines; `build-storybook` green; `e2e/storybook.spec.ts` 239 passed (designer-fable, 2026-09-05T03:10:00Z)

## Comments

- **lead** (2026-09-05T00:40:00.000Z): For the designer (Fable agent with the frontend-design skill). The three least-sure decisions in the morning review are the human's to rule on; do not change them. Cards 20, 21 and 33 are in flight on the diagram fit, the story bodies and the compare harness; do not touch those files, and merge develop before your final run. Work in your worktree with absolute paths; build core, graphviz and pages and run `node scripts/codicons.mjs` before `build-storybook`; `npm ci` if node_modules is missing; if the card is missing, `git reset --hard develop` there first.
- **lead** (2026-09-05T01:10:00.000Z): The human approved the design on 2026-09-05 with the three least-sure decisions as designed; rule on the six nits only. Cards 35 and 36 migrate the shipped routes after you land, so finish promptly.
- **designer-fable** (2026-09-05T01:20:00.000Z): Claimed. Worktree was on the v0.2.0 tag; reset to develop (32ad2c4) and ran `npm ci`. Reading docs/design/MORNING-REVIEW.md, docs/design/design-language-v2.md and the v2 templates before ruling on the six nits. Scope is the six nits only; the three least-sure decisions stand as approved.
- **designer-fable** (2026-09-05T01:45:00.000Z): Rulings, one per nit, before any code moves. (1) **Empty section**: the section stays, the badge goes at zero. The platform never draws its count badge at zero (the Problems tab, the Source Control view, Extensions all hide it; a badge means "there are N" and its absence means none), and the empty sentence already says the zero in words, so `0` beside "No policies." says it twice. The section itself stays because principle 9 says an empty state says what would fill it and because the table of contents anchors it; hiding it would leave a TOC entry pointing at nothing and hide the shape of the model. Applied once, in packages/pages/src/lib/v2/Heading.svelte:29, which covers every template, organism and the health report. (2) **ContextPage Services heading**: keep. A paired level-3 subsection (Aggregates/Services, Provides/Consumes, Entities/Value objects, Outgoing/Incoming, Operations/Events) is the fixed shape of its section, and the shape being visible is the information: a developer learns the context has aggregates and no services rather than wondering whether services live somewhere else. VS Code keeps an empty pane with a sentence ("No symbols found in document", "No editors open"), it does not remove the pane. v1 hid it because an empty card grid was a hole; an empty row list is one heading and one secondary line, and with ruling 1 the heading carries no `0`. The rule for the language: a section or subsection never disappears because it is empty; the badge does. (3) **HealthPage header**: use `PageHeader`, with a plain title and no lockup. The page is a read of the workspace, not an element: a lockup carries a kind, an id and a detail, and Health has none; a workspace lockup would claim the page is the workspace, which the crumb already names as where the reader came from. The title keeps the pulse codicon the tree draws on the health node, at the size a title lockup's icon takes, so the row the reader clicked and the title they land on match. The duplicated header CSS in packages/pages/src/lib/v2/templates/HealthPage.svelte:29-40 goes. (4) **Workspace health names**: v1's two sections are already one in v2, so the naming moves down a level. The section is "Health" (TOC entry, matching the full page's title and the tree's node) and its first heading is "Structure", badge = diagnostic count, followed by the report's Refactor / Tolerated / No comments. The reader's question is one, "what is the model unhappy about", with four kinds of answer, and "Structure" names the source of the findings the way "Refactor" and "Tolerated" do; "Model health" beside "Health" was two names for one thing. Applied in packages/pages/src/lib/v2/templates/WorkspacePage.svelte:6 and :140-152. (5) **`V2Page.harness` naming**: left, with a reason. A rename has to edit packages/pages/src/lib/v2/templates/Compare.harness.svelte:21, which card 33 owns, and a re-export file at the old name is the kind of alias this repo does not keep. Cards 35 and 36 delete v1 and the compare harness, at which point every `V2` prefix is stale; the rename belongs in that pass, as `Strategic.harness.svelte` beside `Tactical.harness.svelte`. Recorded in the review document for those cards. (6) **Compare column collapse**: card 33's, not ruled here; the review document says so.
- **designer-fable** (2026-09-05T02:30:00.000Z): Applied. Ruling 1 is one line, packages/pages/src/lib/v2/Heading.svelte:32 (`{#if count}`), with the reason in its doc comment at packages/pages/src/lib/v2/Heading.svelte:10-14; the harness now counts Value objects at zero so the V2/Heading story shows the rule (packages/pages/src/lib/v2/Heading.harness.svelte:16), and the tests pin it in packages/pages/src/lib/v2/Heading.test.ts:23-35, packages/pages/src/lib/v2/organisms/Section.test.ts:48-61, packages/pages/src/lib/v2/organisms/AttributesSection.test.ts:64-72 (was asserting the `0`) and packages/pages/src/lib/v2/organisms/HealthReport.test.ts:16-29 (the petstore has no uncommented intent, so that heading now rightly carries no badge). Ruling 2 is a test, packages/pages/src/lib/v2/templates/ContextPage.test.ts:89-103: the Services heading stays on the thin context, unbadged. Ruling 3: packages/pages/src/lib/v2/templates/HealthPage.svelte:29-32 renders `PageHeader` with a plain title snippet; the duplicated header CSS is gone; packages/pages/src/lib/v2/templates/HealthPage.test.ts:16-37 asserts the trail, the title inside `.page-header`, no lockup/id/detail, the description as `.md`, no facts. Ruling 4: packages/pages/src/lib/v2/templates/WorkspacePage.svelte:6 (TOC "Health"), :145-151 (section "Health", h3 "Structure" badged with the diagnostic count); packages/pages/src/lib/v2/templates/WorkspacePage.test.ts:18-26, :74-90, :92-108. Docs: docs/design/design-language-v2.md:50-62 (principles 7 and 9 sharpened) and :357-397 (new section 11 recording the rulings); docs/design/MORNING-REVIEW.md:27-36; docs/design/v2-specs/organism-section.md:21-24; docs/design/v2-specs/template-workspace-page.md:38-42. Measured in the built Storybook: element page titles render at 29.25px (the h1's 1.5em compounded by the title lockup's 1.5em) while the Health title was 19.5px before and after the header change, so packages/pages/src/lib/v2/templates/HealthPage.svelte:47-54 gives the title span the lockup's title size; rebuilding to confirm. Note for the lead, outside this card: the type scale in docs/design/design-language-v2.md:95 says the page title is 1.5em, but every shipped v2 title is 2.25em because `Lockup .title` compounds inside `Heading .h1`; either the table or packages/pages/src/lib/v2/Lockup.svelte:65-69 should change, and that is a visible default so I have not touched it. First runs: pages unit 662/663 with the one failure a v1 `src/lib/Page.test.ts` reference-organisation case that passes alone (it ran beside the 184-story Playwright run); `e2e/storybook.spec.ts` 184 passed.
- **designer-fable** (2026-09-05T03:10:00.000Z): Confirmed in the rebuilt Storybook: the Health title now measures 29.25px like every other v2 page title (was 19.5px). Merged origin/develop (f404fc4: cards 21 and 33) with no conflicts; the merge touched none of this card's files. Clean-code sweep run on the diff: the one actionable note was the badge-stripping helper in packages/pages/src/lib/v2/templates/WorkspacePage.test.ts, replaced with four `toHaveTextContent` assertions at packages/pages/src/lib/v2/templates/WorkspacePage.test.ts:81-85. Final runs on the merged tree: pages unit 109 files, 664 tests, 100% statements, branches, functions and lines; `build-storybook` green; `e2e/storybook.spec.ts` 239 passed. Nothing committed. To review; cards 35 and 36 can start.
- **lead** (2026-09-05T03:20:00.000Z): Rulings accepted. The harness rename and the compounding 2.25em title go to card 35. Landing on develop.
