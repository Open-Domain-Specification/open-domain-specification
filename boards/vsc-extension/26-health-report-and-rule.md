---
column: review
labels: [frontend, backend]
priority: high
agent: dev-opus
live: false
clean-code-swept: true
updatedAt: 2026-09-04T11:30:00.000Z
---
# Health report on the workspace page and the comments-required rule (RFC-002 card G)

`HealthReport` (card 19) ships: a section on the workspace page and its own route `#/health`, listing refactor, tolerated and no-comment intents from core's `dispositionOf` and `intentsWithoutComments`. One opt-in validation rule in core warns on strategic intents with no comments. The extension tree shows the three counts on the workspace node and the Problems panel shows the rule's warnings.

## Checklist

- [x] `WorkspacePage.svelte` gains a "Health" section with the summary strip and links to the full report; `HealthPage` template at `#/health` renders the full `HealthReport`; `elements.ts`, `resolve.ts`, search updated
- [x] Core: `comments-required` rule behind a workspace option (`WorkspaceSchema.options?.rules?.commentsRequired: boolean`, or the existing rule-options shape if there is one), emitting one warning per intent with its ref; rule tests
- [x] Extension: workspace tree node description carries "n to refactor, n tolerated, n uncommented" when any is non-zero; Problems maps the rule's warnings to the JSON position of the relationship, consumable or consumption
- [x] Doc generator: the workspace page prints the same three lists under "## Health"
- [x] Pages unit at 100%; e2e case opens `#/health` on petstore and sees "1 to refactor"; `npm run test:vscode` case for the tree counts; core and doc suites green

## Gates

- [x] qa-automated — inside the worktree: core 133, doc 23, extension unit 9, petstore 18, pages 465 at 100%; developer ran test:vscode 9 and 66 e2e (lead, 2026-09-04T11:30:00Z)

- [x] clean-code-swept — SRP/DRY and NAMING/COUPLING/DEAD/KISS sweeps both returned nothing above 0.5, so nothing to refactor; both confirmed the three health lists have one source (`packages/core/src/evidence.ts:88-95`) that core, pages, doc and the extension all read (dev-opus, 2026-09-04T10:15:00.000Z)

## Comments

- **lead** (2026-09-04T08:00:00.000Z): Assigned to dev-opus. Fixed by decision: the report reads core helpers only, no lifecycle, no dates; "No comments" stays collapsed by default; the rule is opt-in and off in the four reference models except petstore, which turns it on so the Problems path is exercised (petstore must then have no uncommented intents, or the two it has get comments). Start after card 23 has landed on develop (the lead will say). Work in your worktree with absolute paths for every suite; `npm ci` there first if node_modules is missing; if the card is missing, `git reset --hard develop` there first.
- **dev-opus** (2026-09-04T09:20:00.000Z): Claimed. Worktree was on the v0.2.0 tag, so `git reset --hard develop` then `npm ci` and `npm run build`. Read the ground: `packages/core/src/evidence.ts:52-79` (`intentsWithoutComments`), `packages/pages/src/lib/evidence/derive.ts:93-115` (`health`), `packages/pages/src/lib/organisms/HealthReport.svelte:24-31`, `packages/pages/src/lib/templates/WorkspacePage.svelte:1-7,101-107`, `packages/core/src/validate.ts:279-360` (`RULES`/`RULE_CATALOG`), `apps/ods-vscode/src/tree.ts:167-206` (workspace root node), `apps/ods-vscode/src/diagnostics.ts:36-63` (Problems mapping), `packages/doc/src/workspace.md.ts:39-88`.
- **dev-opus** (2026-09-04T09:25:00.000Z): QUESTION for lead: "intents with no comments" means two different numbers in the two halves of this card, and petstore is where they collide. `health()` at `packages/pages/src/lib/evidence/derive.ts:113` counts **relationships only** — petstore has 2. Core's `intentsWithoutComments` at `packages/core/src/evidence.ts:52-79` counts relationships **plus non-internal consumables plus consumptions** — petstore has 41 (measured: 5 relationships, 1 refactor, 1 tolerated, 2 uncommented; 41 uncommented intents overall). Checklist line 2 ("one warning per intent") and line 3 ("the JSON position of the relationship, consumable or consumption") pin the rule to the wide reading, so turning it on in petstore emits 41 warnings, not the 2 your comment anticipates, and "petstore must then have no uncommented intents" would mean writing 39 new comments across petstore's consumables and consumptions. Options: **(a)** rule stays wide (41 intents) and the page + tree count widens to match — `health()` and `HealthReport`'s "No comments" list switch to `intentsWithoutComments`, which needs a new row shape for consumables and consumptions (today's `intent` snippet at `HealthReport.svelte:88-101` only renders a `ContextRelationship`), and petstore turns the rule **off** while a smaller model or a core test fixture exercises the Problems path. **(b)** rule stays wide and petstore turns it on and simply carries 41 warnings, which contradicts "the workspace validates clean" at `models/petstore/src/workspace.ts:10`. **(c)** rule is narrowed to relationships only, page and tree keep today's 2, petstore's two uncommented relationships (`salesBC.partnerOf(fulfilmentBC)` at `models/petstore/src/workspace.ts:986-990` and `identityBC.separateWaysFrom(salesBC)` at `models/petstore/src/workspace.ts:993-996`) get comments, and a fake-org model (RiverMart/StreamLine/NorthBank) turns the rule on to exercise Problems. I recommend **(c)**: it keeps the one number consistent across page, tree and rule, keeps card 19's shipped component and its stories intact, keeps petstore clean, and matches your parenthetical — and RFC-002 section 4.5 only ever illustrates the report with relationships. If you want (a) instead, say so and I will widen `derive.ts` and add the consumable/consumption row. Blocked until answered.
- **lead** (2026-09-04T09:40:00.000Z): Answered option (c). The rule and the health report count relationships only; core keeps `intentsWithoutComments` wide for the skill, and gains `relationshipsWithoutComments` for the rule and the page. Petstore's two uncommented relationships get comments and turn the rule on; a core fixture exercises the warning path.
- **dev-opus** (2026-09-04T09:45:00.000Z): Merged card 24 off `origin/develop` first (fast-forward; `relationshipTitle` and `isSymmetricRelationship` now come from core), resolving two import conflicts in `packages/pages/src/lib/evidence/derive.ts:1-10` and `packages/pages/src/lib/organisms/HealthReport.svelte:1-13`. Core: `relationshipsWithoutComments` at `packages/core/src/evidence.ts:88-95`, with `intentsWithoutComments` left wide and its doc comment at `packages/core/src/evidence.ts:50-62` now saying which reading each surface takes and why. Options plumbed end to end: `packages/core/src/schema.ts:345-360` (`RuleOptionsSchema`, `WorkspaceOptionsSchema`, `WorkspaceSchema.options`), `packages/core/src/workspace.ts:47-56,69-71,86-89,492-494` and `packages/core/src/workspace-from-schema.ts:409-418`. The rule itself is `packages/core/src/validate.ts:301-315` with its catalogue entry at `packages/core/src/validate.ts:434-443`; it returns `[]` unless `options.rules.commentsRequired` is set.
- **dev-opus** (2026-09-04T09:50:00.000Z): Pages. `health()` at `packages/pages/src/lib/evidence/derive.ts:100-125` now reads core's `relationshipsWithoutComments` rather than its own local predicate, so the page, the tree and the Problems panel cannot drift apart; `healthCounts` and `healthCountsOf` at `packages/pages/src/lib/evidence/derive.ts:88-105` are the one source of the three numbers. The summary strip moved out of `HealthReport` into `packages/pages/src/lib/molecules/HealthSummary.svelte:1-52` so the workspace section and the full report render the same markup. New route: `HEALTH_REF`/`HEALTH_PAGE` at `packages/pages/src/lib/resolve.ts:3-12`, its pattern at `packages/pages/src/lib/resolve.ts:47-48`, dispatched in `packages/pages/src/lib/Page.svelte:92-94`, rendered by `packages/pages/src/lib/templates/HealthPage.svelte:1-41`. The workspace page's new section is `packages/pages/src/lib/templates/WorkspacePage.svelte:7,110-118` — id `evidence-health`, because `health` was already taken by the structural-diagnostics section. The report's three headings gained ids (`packages/pages/src/lib/organisms/HealthReport.svelte:44,56,65`) so the page's table of contents links to them.
- **dev-opus** (2026-09-04T09:55:00.000Z): Extension. Workspace node description at `apps/ods-vscode/src/tree.ts:63-72,209-215` appends "n to refactor, n tolerated, n uncommented" after the file name, and returns undefined when all three are zero. Problems needed real work for relationships: their ref is `#/relationships/<source>~<type>~<target>` but the file stores them as a keyless array, so `locateRef` fell back to squiggling the whole array. `apps/ods-vscode/src/locate.ts:26-63` now matches the triple against each element (upstream/downstream for directed, participants in order for symmetric) and only then falls back. Consumables already locate by path, and a consumption has no ref of its own so the rule never emits one. Search gained the health row at `apps/ods-vscode/src/search.ts:45-54`.
- **dev-opus** (2026-09-04T10:00:00.000Z): Doc generator prints the same three lists under "## Health" at `packages/doc/src/workspace.md.ts:39-77,101-102`, off the same core helpers. Petstore turns the rule on at `models/petstore/src/workspace.ts:25-28` and its two bare relationships got comments — `models/petstore/src/workspace.ts:989-1003` (partnership, deliberately uncited so the report is exercised with a comment that has no link) and `models/petstore/src/workspace.ts:1006-1027` (separate-ways, with an ADR). Petstore regenerates with 0 diagnostics; RiverMart, StreamLine and NorthBank still report their existing 3 each, so the rule really is off there. Four existing tests moved off petstore's now-commented relationships onto hand-built bare workspaces: `packages/pages/src/lib/evidence/derive.test.ts:33-47`, `packages/pages/src/lib/organisms/HealthReport.test.ts:35`, `packages/pages/src/lib/organisms/RelationshipDetail.test.ts:114-126` and `packages/doc/src/index.test.ts:278-289`.
- **dev-opus** (2026-09-04T10:05:00.000Z): Evidence. Core 133 passing (`packages/core/src/validate.test.ts:290-386` is 7 new cases for the rule, `packages/core/src/evidence.test.ts:133-158` 3 for the helper, and the everything-wrong fixture at `packages/core/src/rule-catalog.test.ts:7-13` opts in so the catalogue's "every rule fires" check covers it). Pages 465 passing at 100% lines/branches/functions/statements, including `packages/pages/src/lib/templates/HealthPage.test.ts:1-68` and the two new workspace-page cases at `packages/pages/src/lib/templates/WorkspacePage.test.ts:23-45`. Doc 23 passing with two new health cases at `packages/doc/src/index.test.ts:400-448`. Extension unit 9 passing (`apps/ods-vscode/src/locate.test.ts:56-101` is 5 new relationship cases). `npm run test:vscode` 9 passing in a real window, including "carries the health report's three counts on the workspace node" and "opens the health report from its row in search" at `apps/ods-vscode/src/test/extension.test.ts:203-236`. Playwright 66 passing, with `packages/pages/e2e/browse.spec.ts:54-80` walking the strip to `#/health` and asserting "1 to refactor". Full `npm test` green across all 10 projects.
- **dev-opus** (2026-09-04T10:15:00.000Z): Clean-code sweep clean, card to review. One sub-threshold note for the lead rather than a change I would make unasked: `packages/pages/src/lib/templates/WorkspacePage.svelte:6-7` now shows two adjacent table-of-contents entries, "Model health" (the structural-rules section, id `health`) and "Health" (the evidence section, id `evidence-health`). The card named the new one "Health", and renaming the old one is a user-visible label change, so I left both as they are — say the word if you want "Structural rules" and "Health" instead. Also worth a look in review: `npm test` at the repo root reports the ods-vscode suite from the Nx cache of the main checkout (4 tests) rather than this worktree's 9; the worktree run itself is green, but the cached line is misleading.
- **lead** (2026-09-04T11:30:00.000Z): Reviewed the opt-in rule, the workspace options schema and the locate fix for relationship refs. The 'Model health' vs 'Health' naming goes to the v2 design pass (card 28). Landing on develop.
