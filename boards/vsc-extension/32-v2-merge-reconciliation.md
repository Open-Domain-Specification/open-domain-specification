---
column: review
live: false
labels: [frontend]
priority: high
agent: dev-opus
clean-code-swept: true
updatedAt: 2026-09-04T23:10:00.000Z
---
# Reconcile the v2 merge: one version of every organism and molecule, suites green

Cards 29, 30 and 31 built v2 in parallel and each carried stand-ins for pieces another card owned. The lead merged them with the owners' versions winning: card 30 owns v2/PageLayout, organisms/{PageHeader,Section,Toc,DiagramFigure,StrategicPositionTable,RelationshipDetail,HealthReport}, molecules/{Problems,ContextLockup,ContextList,TeamLockup,SubdomainTable,ProvidesTable,ConsumesTable,AttributeTable,Crumbs,Joined} and templates/Compare.harness; card 29 owns organisms/{Sidebar,AttributesSection,InvariantsSection,LanguageSection} and the V2/Layout shell story; card 31 owns the nine tactical templates and its other molecules. On develop now: Storybook builds and all 185 V2 stories render, but `npm run check` fails and 38 unit tests fail, all in `v2/templates/branches.test.ts` and `v2/templates/edge-cases.test.ts` (card 31's tests against its own stand-ins' props).

## Checklist

- [x] Card 31's templates and tests use the owners' components (props, slots, snippet names); any duplicate molecule left from the merge is removed so exactly one file exists per name under v2/
- [x] `npm run check` 0 errors; pages unit suite green at 100% coverage
- [x] `npm run build-storybook` green and `e2e/storybook.spec.ts` green; every V2/Templates and V2/Compare story renders the real page (no blank stories)
- [x] Stories that only existed for stand-ins (PageHeader/Section/Toc harnesses from card 29) either match the owners' components or are removed

## Gates

- [x] qa-automated — svelte-check 0, pages 662 at 100%, Storybook built, 184 stories render incl. all 16 V2/Compare with the h1 assertion (lead, 2026-09-04T23:10:00Z)

- [x] tests-passing — `npx svelte-check` 0 errors 0 warnings; pages unit suite 662 passing in 109 files at 100% lines/branches/functions/statements; `npm run build-storybook` exit 0; `npx playwright test e2e/storybook.spec.ts` 184 passing (dev-opus, 2026-09-04T23:10:00.000Z)
- [x] clean-code-swept — `clean-code-review` over the diff in three parallel passes (SRP/DRY, naming/coupling, dead-code/KISS); nothing scored above the 0.5 threshold, highest 0.45; the one latent asymmetry I had introduced was fixed rather than annotated (dev-opus, 2026-09-04T23:10:00.000Z)

## Comments

- **lead** (2026-09-04T21:30:00.000Z): Assigned to dev-opus. Fixed by decision: when a template and an owner's organism disagree, the owner's organism wins and the template adapts; do not fork a second component. Do not redesign anything: this is plumbing. Work in your worktree with absolute paths; build core, graphviz and pages first, run `node scripts/codicons.mjs` before `build-storybook`; `npm ci` if node_modules is missing; if the card is missing, `git reset --hard develop` there first. Report the final counts.
- **dev-opus** (2026-09-04T22:00:00.000Z): Claiming card 32. Worktree reset to develop (53697ed). Surveying v2/ for duplicate molecules and the 38 failing tests in `v2/templates/branches.test.ts` and `v2/templates/edge-cases.test.ts`.
- **dev-opus** (2026-09-04T22:35:00.000Z): Plumbing done where the owner's shape was unambiguous. Deleted the one duplicate, `v2/organisms/Problems.svelte` — nothing imported it and `organisms/Section.svelte:5` already uses card 30's `molecules/Problems.svelte`, so exactly one file per name now exists under `v2/`. Rewrote the nine tactical templates' page headers onto card 30's `organisms/PageHeader.svelte:20-30` (`title`/`meta`/`facts` snippets) in place of the stand-in `kind`/`kindLabel`/`name`/`id`/`keywords` props, following the owner's own pattern at `templates/ContextPage.svelte:82-83`: `templates/AggregatePage.svelte:56-57`, `ConsumablePage.svelte:70-79`, `EntityPage.svelte:57-59`, `InvariantPage.svelte:33-34`, `PolicyPage.svelte:58-59`, `SchemaPage.svelte:38-39`, `ServicePage.svelte:30-32`, `TermPage.svelte:47-49`, `ValueObjectPage.svelte:56-57`. Renamed `withTargets` to the owner's `constrains` at `templates/AggregatePage.svelte:97` (`organisms/InvariantsSection.svelte:28`). Adapted card 29's stand-in harnesses to the owners: `PageLayout.harness.svelte:44-65` drops the `toolbar`/`toc` snippets card 30's `PageLayout.svelte:11-15` does not have and passes `sections`; `organisms/Toc.harness.svelte:4-13` drops the `active` prop card 30's `Toc.svelte:9` does not have, and the "Active section" story went with it (`organisms/Toc.stories.svelte`) — the owner's Toc marks the row under the pointer, not a section. Adapted card 31's tests to the owners' markup: `templates/templates.test.ts:32,246` (`.layout .toc`, not `.layout-v2 .toc-v2`), `:232-234` (aliases are in `.page-header .meta`, not `.keywords`), `:124-152` (card 30's `ConsumesTable` has no sortable column, so the test now asserts the consumes table has no header buttons); `branches.test.ts:155` (`#language .terms` per `organisms/LanguageSection.svelte:26`) and `:187-192` (card 30's `Joined.svelte:16-20` draws its comma from CSS `::before`, so `textContent` carries none — the test now reads the two `a.ref` inside the `.joined`). `npm run check` is down from 33 errors to 20, and the pages unit suite from 38 failures to 1 — 658 passing, and `src/site.test.ts` green again once `npm run build` had written `app/`.
- **dev-opus** (2026-09-04T22:35:00.000Z): QUESTION for lead (three, all "should the owner's component grow a prop, or should the template give the behaviour up?" — I am not making these calls). Blocked until answered.

  **(1) `molecules/ConsumesTable.svelte` empty copy.** The owner fixes it at `molecules/ConsumesTable.svelte:24` as "Depends on nothing outside itself." `templates/ConsumablePage.svelte:165` passes `empty="Nobody consumes this yet."` for its "Consumed by" section, and `templates/edge-cases.test.ts:137` asserts that wording (the one remaining unit failure). Options: (a) give `ConsumesTable` an optional `empty` prop defaulting to today's string — exactly what its sibling `molecules/AttributeTable.svelte:14-16` already does, so no existing render changes; (b) drop the override and change the test, which leaves a "Consumed by" table saying "Depends on nothing outside itself." I recommend (a).

  **(2) `organisms/InvariantsSection.svelte` and diagnostics.** `templates/AggregatePage.svelte:100` passes `problems={invariants.flatMap(...)}`, which the owner's `InvariantsSection.svelte:22-32` does not accept, although the `Section` it wraps does (`organisms/Section.svelte:22`). Options: (a) add an optional `problems: Diagnostic[] = []` pass-through to `InvariantsSection` — nothing renders differently where it is unset, and the other caller `templates/EntityPage.svelte` does not pass it; (b) drop the prop from `AggregatePage`, which loses the invariant diagnostics on the aggregate page. I recommend (a).

  **(3) the nine `*.compare.stories.svelte`.** Card 31's tactical compare stories pass `{model, ref}` (e.g. `templates/EntityPage.compare.stories.svelte:13,18`), but card 30's `templates/Compare.harness.svelte:28-31` takes `{page: PageName, mode}` and its `PageName` (`templates/V2Page.harness.svelte:17-25`) names only the seven strategic pages. That is 18 of the 20 remaining `npm run check` errors, and those nine stories are blank today. Options: (a) extend `PageName`, `sectionsOf` and the `V2Page`/`Compare` dispatches with the nine tactical pages so the stories read `<Compare page="entity" />` — most faithful to card 30's "picked once" rule, but `ConsumablePage`'s sections are `sectionsFor(consumable)` rather than a constant, so the map needs a special case, and the v1 column needs nine more imports; (b) give `Compare.harness` an optional `ref` mode that draws the v1 and v2 tactical page for a `PETSTORE_REFS` ref, leaving the `page` API untouched — least new code, and `templates/petstore.harness.ts` already plays picks.harness's role for tactical elements; (c) delete the nine files, leaving `V2/Compare` to the seven strategic pages. I recommend (b).
- **lead** (2026-09-04T22:20:00.000Z): ANSWERS. (1) a: optional `empty` prop on molecules/ConsumesTable with today's text as the default, as AttributeTable does. (2) a: optional `problems` pass-through on organisms/InvariantsSection into Section. (3) b: an optional `ref` mode on templates/Compare.harness driven by petstore.harness.ts; the `page` API stays as card 30 wrote it. Then the Storybook build and the render spec.
- **dev-opus** (2026-09-04T23:10:00.000Z): Lead answered (a)/(a)/(b); all three built. (1) `molecules/ConsumesTable.svelte:10-19` takes an optional `empty` defaulting to today's "Depends on nothing outside itself.", so `templates/ConsumablePage.svelte:165` keeps "Nobody consumes this yet." on its Consumed-by table and nothing else renders differently. (2) `organisms/InvariantsSection.svelte:26,35,46` passes an optional `problems` through to the `Section` it wraps, restoring the invariant diagnostics `templates/AggregatePage.svelte:100` asks for. (3) `templates/Compare.harness.svelte:37-48,58-59,81` gained the `ref` mode: a strategic page is still named by `page` and picked in `picks.harness.ts`, a tactical one by a `petstore.harness.ts` ref resolved through `resolvePage`. Rather than write a second nine-branch dispatch inline, I lifted the one `templates/Page.harness.svelte` already had into `templates/V2Tactical.harness.svelte` (its module block exports `tacticalSections` for the layout's table of contents) and added its v1 twin `templates/V1Tactical.harness.svelte`; `Page.harness.svelte:6,18-20` is now four lines and both compare columns draw the same resolved element. The nine `*.compare.stories.svelte` drop the `model` arg `Compare` never took and pass `ref` only.
- **dev-opus** (2026-09-04T23:10:00.000Z): The Storybook e2e spec could not have caught what was wrong here — `e2e/storybook.spec.ts:76-82` asserts a story painted *something*, so the nine tactical compare stories passed all along while silently drawing HealthPage in both columns. Added `templates/Compare.harness.test.ts:22-52`, which asserts both columns carry an `h1` and that both name the element the `ref` names. Mutation-checked it: reverting the v2 column to ignore `ref` fails 2 of its 3 cases, and restoring the branch turns them green. Final counts — `npx svelte-check --tsconfig ./tsconfig.app.json` 0 errors 0 warnings (was 33); unit suite 662 passing across 109 files, 0 failing (was 620 passing / 38 failing) at 100% lines, branches, functions and statements; `npm run build-storybook` exit 0; `npx playwright test e2e/storybook.spec.ts` 184 passing, of which 154 are `V2/` stories including all 16 `V2/Compare` (7 strategic + the 9 tactical that were blank). 184 rather than the 185 the card records because the one story removed under checklist item 4 was Toc's "Active section", which drove a stand-in prop card 30's `organisms/Toc.svelte:9` does not have. Biome: I formatted only the files I touched; the 12 pre-existing format/lint findings elsewhere under `packages/pages/src` (including `lib/flow/fullscreen.svelte.ts:35` `noFocusedTests`) are untouched, as they are not this card's. No commits made; working tree is yours to review.
- **lead** (2026-09-04T23:10:00.000Z): Landing on develop. The V2Page naming nit goes to the morning review.
