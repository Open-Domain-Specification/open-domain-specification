---
column: done
labels: [frontend]
priority: high
agent: dev-opus
live: false
clean-code-swept: true
updatedAt: 2026-09-04T19:05:00.000Z
---
# v2 organisms in Storybook (design language v2, part 1 of 3)

The twelve organisms rebuilt on the v2 primitives, per docs/design/v2-specs/organism-*.md: PageHeader, Section, Toc, Sidebar, AttributesSection, InvariantsSection, LanguageSection, DiagramFigure, StrategicPositionTable, RelationshipDetail, HealthReport, and the page layout (template-page-layout.md) as `v2/PageLayout.svelte`. InteractiveDiagram is kept and wrapped, not rebuilt.

Scope narrowed by the lead on 2026-09-04 (see the Comments): card 30 owns
PageLayout, Toc, Section, PageHeader, DiagramFigure, Problems,
StrategicPositionTable, RelationshipDetail and HealthReport. This card is the
four sections below, plus the shell story.

## Checklist

- [x] `v2/organisms/`: Sidebar, AttributesSection, InvariantsSection and LanguageSection (with `v2/molecules/AttributeTable.svelte`), each with stories (light, dark, high contrast, density where rows are laid out) and tests at 100%
- [x] A `V2/Layout/Context page shell` story composing PageLayout, Sidebar, PageHeader, Toc and one Section so the chrome can be judged as a whole
- [x] `e2e/storybook.spec.ts` green over the new titles; pages unit suite at 100%

## Gates

- [x] clean-code-swept — clean-code-review sweep over the new v2 files: two findings, both fixed (`.crumbs` now resets the v1 page stylesheet's colour and size at packages/pages/src/lib/v2/organisms/PageHeader.svelte:59-66, and the reused `.flash` marker class is documented at packages/pages/src/lib/v2/PageLayout.svelte:64-66); no SRP, DRY, coupling or dead-code findings (dev-opus, 2026-09-04T18:48:00.000Z)

## Comments

- **lead** (2026-09-04T16:00:00.000Z): Assigned to dev-opus. Build the v2 versions as new files under `packages/pages/src/lib/v2/` (organisms under `v2/organisms/`, templates under `v2/templates/`), rendered only by `V2/...` Storybook stories against the petstore model. No shipped file changes; v1 stays as it is. Follow docs/design/design-language-v2.md and the matching spec in docs/design/v2-specs/ exactly; where a spec is silent, choose the denser, plainer option and journal it. Use only the v2 primitives (Keyword, Lockup, Ref, DefinitionList, DataTable, Heading, Comments, Disposition, EmptyState, HoverCard) plus InteractiveDiagram, which is kept as is. Every component: light, dark and high-contrast stories via `v2/Theme.harness.svelte`, a density story where rows are laid out, tests at 100% coverage, `{#snippet template()}` story bodies, and `e2e/storybook.spec.ts` green over the new titles. Work in your worktree with absolute paths for every suite; build core, graphviz and pages first (`npm run build -w packages/core -w packages/graphviz -w packages/pages`) and run `node scripts/codicons.mjs` in packages/pages before `build-storybook`; `npm ci` if node_modules is missing; if the card is missing, `git reset --hard develop` there first. Cards 30 and 31 build the templates on top of these organisms in parallel; keep the organisms' props close to the v1 ones so the template cards can start from the specs without waiting on you, and journal any prop you rename.
- **dev-opus** (2026-09-04T17:10:00.000Z): Picked this up. The worktree was created from a stale commit and had no card file, so `git reset --hard develop` first (now at 5a87299), then `npm ci` and `npm run build -w packages/core -w packages/graphviz -w packages/pages`. Baseline `npm run test:unit` in packages/pages is green with 100% lines/branches/functions/statements (packages/pages/vitest.config.ts:38-45 is the threshold I have to keep). Read docs/design/design-language-v2.md, every docs/design/v2-specs/organism-*.md and template-page-layout.md before touching anything.
- **lead** (2026-09-04T18:10:00.000Z): SCOPE CHANGE to avoid building the same organisms twice. Card 30 already built and owns v2/PageLayout, Toc, Section, PageHeader, DiagramFigure, Problems, ContextLockup, ContextList, TeamLockup, SubdomainTable, and now also StrategicPositionTable, RelationshipDetail, HealthReport and a `detail` snippet on v2/DataTable. Card 29 is now: Sidebar, AttributesSection, InvariantsSection, LanguageSection (organisms under v2/organisms, molecules under v2/molecules), each with stories and tests, plus the `V2/Layout/Context page shell` story. Do not edit v2/DataTable. Anything already written from card 30's list stays in the tree but is journalled as superseded; the lead takes card 30's versions at merge.
- **dev-opus** (2026-09-04T18:30:00.000Z): Built the four sections this card now owns. `Sidebar` (packages/pages/src/lib/v2/organisms/Sidebar.svelte:57-68) is the workbench tree: a `Lockup` per row inside a `.item` span at 22px, `ul ul` indenting 16px per level (Sidebar.svelte:101-104, v1 used 14px), the brand line reset off `.toc-title` to body text (Sidebar.svelte:85-95), hover in `list.hoverBackground` and selection in `list.activeSelection*` (Sidebar.svelte:114-131), with the high-contrast dashed `contrastActiveBorder` outline for both (Sidebar.svelte:132-137). `AttributesSection` (organisms/AttributesSection.svelte:15-17) is a `Section` around the restyled attribute table, which is its own file because the spec reuses it in the structure subsections and the schema pages: packages/pages/src/lib/v2/molecules/AttributeTable.svelte:23-28 is the 16px key column, name, type and description, the type a `Ref` when the attribute has a value object (AttributeTable.svelte:38). `InvariantsSection` (organisms/InvariantsSection.svelte:41-53) is rows, not cards, and grows the aggregate page's "Constrains" column behind `constrains` (InvariantsSection.svelte:33-37, 47). `LanguageSection` (organisms/LanguageSection.svelte:26) prints comma-separated term `Ref`s instead of pills.
- **dev-opus** (2026-09-04T18:32:00.000Z): Props kept as v1's except where a v2 primitive forced a change, for cards 30 and 31: `AttributesSection` unchanged (`attributes`, `lead`); `LanguageSection` unchanged (`target`); `Sidebar` unchanged (`current`); `InvariantsSection` keeps `invariants`, `lead`, `emptyText` and gains optional `id`, `title` and `constrains` (organisms/InvariantsSection.svelte:20-31) so the aggregate page can title it "Invariants" and show what each rule names. `v2/molecules/AttributeTable.svelte` takes `attributes` and a required `empty` sentence rather than v1's implicit one, so a schema page can word its own. My `PageHeader` stand-in replaces v1's (`kind: string`, `icon: string`) with (`kind: Kind`, `kindLabel?: string`) because `Lockup` needs the typed kind for its symbol colour — card 30's version governs, this is only noted in case it landed the same way.
- **dev-opus** (2026-09-04T18:34:00.000Z): Choices where the specs are silent, all taken as the denser option. A section that lists things carries a count badge, so `LanguageSection` passes `count={terms.length}` (organisms/LanguageSection.svelte:23) even though organism-language-section.md's sketch shows no badge — organism-section.md names terms as a counted section, and the two specs disagree. `Toc` gained an optional `active` prop (organisms/Toc.svelte:11) so the 2px active marker the spec sketches can be seen; v1 styled `.active` but never set it. Every new component resets what `assets/page.css` and `assets/site.css` would otherwise impose (`.problems`, `.toc-title`, `.site-nav a`, bare `code`), because both stylesheets stay loaded in Storybook via .storybook/preview.ts:2-4 until the last template moves over. No `list.activeSelection*` tokens exist in v2/Theme.harness.svelte, so the sidebar's selected row falls back to the hover wash and the plain foreground per section 5's fallback column (Sidebar.svelte:117-131) — flagged to the lead rather than adding colour constants to another card's harness.
- **dev-opus** (2026-09-04T18:36:00.000Z): SUPERSEDED, kept in the tree per the scope change: packages/pages/src/lib/v2/PageLayout.svelte, organisms/Section.svelte, organisms/Problems.svelte, organisms/PageHeader.svelte and organisms/Toc.svelte (with their harnesses, stories and tests). They were written before the scope change and the four sections this card owns compose them, so they stay as stand-ins; the lead takes card 30's versions at merge. My unfinished `RelationshipDetail.svelte` and `DiagramFigure.svelte` were deleted rather than kept: neither had a story or a test, and an untested file cannot pass the package's 100% threshold. I did not touch `v2/DataTable.svelte`.
- **dev-opus** (2026-09-04T18:40:00.000Z): Two coverage traps worth recording for cards 30 and 31, both from Svelte's compiled output rather than from the components. An attribute written as `href="#{s.id}"` compiles to `${s.id ?? ''}`, and a text expression that has siblings (`{d.message}` beside a component) compiles to `` `${d.message ?? ''}` ``; the `??` fallback is unreachable, so each costs a branch. Writing the attribute as an expression (organisms/Toc.svelte:26) and wrapping the text in its own element so it is an only child (organisms/Problems.svelte:23) removes both. `packages/pages/npm run test:unit` is green at 100% lines, branches, functions and statements over 83 files and 551 tests.
- **dev-opus** (2026-09-04T18:50:00.000Z): Green and ready for review. `packages/pages` `npm run test:unit` exits 0 with 83 files, 551 tests and 100% lines, branches, functions and statements; `npm run check` is 0 errors and 0 warnings over 922 files; biome is clean over src/lib/v2. `node scripts/codicons.mjs && npm run build-storybook && npx playwright test e2e/storybook.spec.ts` is 109 passed, covering the eight new `V2/Organisms/*` and `V2/Layout` stories including `V2/Layout — Context page shell`. Nothing shipped changed: the diff is this card plus new files under packages/pages/src/lib/v2/.
- **dev-opus** (2026-09-04T19:05:00.000Z): Lead answered both questions on the card. (1) Added `--vscode-list-activeSelectionBackground` and `--vscode-list-activeSelectionForeground` to all three themes in packages/pages/src/lib/v2/Theme.harness.svelte:50-51 (Light Modern `#0060c0` on `#ffffff`), :82-83 (Dark Modern `#04395e` on `#ffffff`) and :114-115 (Default High Contrast `#000000` on `#ffffff`, where the dashed `contrastActiveBorder` outline is what actually marks the row), so `V2/Organisms/Sidebar` now shows the real filled selected row instead of the fallback. That harness belongs to card 28 and is the one edit outside this card's scope; the token fallbacks in organisms/Sidebar.svelte:117-131 stay, because a host without those tokens still has to draw something. (2) The count on the terms section is confirmed right, so organisms/LanguageSection.svelte:23 stands as written. Reran everything after both changes: `npm run test:unit` exits 0 at 83 files, 551 tests, 100% on all four metrics; `npm run check` 0 errors 0 warnings over 922 files; biome clean; `node scripts/codicons.mjs && npm run build-storybook && npx playwright test e2e/storybook.spec.ts` 109 passed.

- 2026-09-11, lead: driven in a browser against the built app and confirmed against this card's own claim; moved to done.
