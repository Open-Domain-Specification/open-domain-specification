---
column: done
labels: [frontend]
priority: med
agent: designer-fable
progress: 80
clean-code-swept: true
updatedAt: 2026-09-05T11:00:00.000Z
---
# Strategic position table at narrow widths

With the site tree beside it at a 1280px window, the Strategic position table has about 740px for six no-wrap columns, so Description shrinks to a word per line even with `grow`. In the webview (no tree) it fits. Designer to rule: which columns may wrap or stack below a width (type, roles), whether the table drops to a two-line row layout, or whether the tree collapses first. Apply in `v2/organisms/StrategicPositionTable.svelte` and `v2/DataTable.svelte` if a general rule falls out; e2e assertion at 1280px with the tree.

## Gates

- [x] clean-code-swept — designer's applied rulings, reviewed by the lead (lead, 2026-09-05T11:00:00Z)
- [x] qa-automated — svelte-check 918 files 0 errors, pages 587 at 100%, all 243 e2e; lead screenshotted the Sales page at 1300px with the tree (lead, 2026-09-05T11:00:00Z)

## Comments

- **lead** (2026-09-05T08:30:00.000Z): Two more observations from the shipped Sales page at 1300px with the tree: (1) cells in a tall row are bottom-aligned, so the counterpart lockup sits at the foot of a five-line description; rows should align to the top. (2) The pattern hover card opens to the right of the keyword and runs past the viewport edge; it needs to flip or clamp inside the viewport. Both are the designer's call alongside the column rule.
- **designer-fable** (2026-09-05T09:00:00.000Z): Picked this up. The worktree was on the v0.2.0 tag with no card file, so it was reset to develop and `npm ci` run. Read the card, the lead's two observations, card 38's journal, `docs/design/design-language-v2.md` (the `DataTable` row at `:199` is the rule under test), `packages/pages/src/lib/atoms/DataTable.svelte:1-244`, `packages/pages/src/lib/organisms/StrategicPositionTable.svelte:1-148`, `packages/pages/src/lib/molecules/PatternHover.svelte:1-141` and `packages/pages/src/lib/atoms/HoverCard.svelte:1-43`. Next: build, screenshot the Sales page at 1300px with the tree, and rule on each of the three with a reason.
- **designer-fable** (2026-09-05T09:40:00.000Z): Reproduced all three with a Playwright script at 1300x900 with the tree. The table is 760px; the fixed columns take 641 (With 200 because `Identity BC big ball of mud` sets the column for every row, Type 154 from `upstream-downstream`, Upstream/Downstream/Disposition 75/92/91 from their own header labels), so Description gets 119px. The lockup sits 88px below the cell top because `.description` at `packages/pages/src/lib/organisms/StrategicPositionTable.svelte:127-131` is an inline-block, whose baseline is its last line, and `packages/pages/src/lib/atoms/DataTable.svelte:176` aligns cells on baseline. The ACL card opens at x=869, 491px wide, 60px past the edge. Two mechanism facts before ruling: (1) a cell that is allowed to wrap collapses to one token per line at every width, 1600 included, and dropping the grow column's `width: 100%` changes nothing, because auto layout hands all slack to the prose column; so token wrapping must be gated on the table's own width with a container query. (2) `min-width` on the growing `td` holds as a floor (197px at 1100px) and the table then overflows its container, so a floor needs a scroll frame; and since Chrome 129 `container-type` no longer makes a containing block for fixed descendants (verified here: a fixed layer inside the container landed at viewport 100,100), a fixed hover layer escapes that frame.
- **designer-fable** (2026-09-05T09:41:00.000Z): RULING 1, columns. The tree never collapses for a table: 1300px is a normal laptop browser and the tree is the platform's own sidebar. The table keeps its six columns (RFC-002 4.1 fixes the grouping, the Description column and the hover, not the shape; the two role columns teach the vocabulary and align across rows). Three tiers keyed to the table's width, since the tree and the TOC make the viewport meaningless: at full width nothing changes; under 900px of table width, which is a table beside the tree or an editor tab under about 1150px, a run of tokens in a non-growing cell breaks between tokens, never inside one (a keyword, a lockup, a code, a disposition stay whole), and a context's `big ball of mud` drops under its name; the growing column has a floor of 24ch, the narrowest measure that still reads as prose, and when stacked tokens cannot give it that the table scrolls sideways inside its own frame and the page never does, which is what the Extensions detail page and the markdown preview do with a wide table. Stack first, scroll last. 900 is the layout's own idea of narrow (the TOC's breakpoint). General `DataTable` rules; `ContextLockup` carries the mud wrap; `Lockup` gains `nowrap` so a name never breaks beside its icon.
- **designer-fable** (2026-09-05T09:42:00.000Z): RULING 2, alignment. Cells align to the top. Every cell shares the 22px line and the same strut, so first lines already sit on one baseline; `top` says so directly and cannot be fooled by content whose baseline is not its first line, which is what the inline-block description did. The description becomes a block. General `DataTable` rule.
- **designer-fable** (2026-09-05T09:43:00.000Z): RULING 3, hover. Principle 8 says the editor's hover, so the card does what the editor hover widget does: stays inside the viewport with an 8px margin, shifts left when the word is near the right edge rather than flipping sideways (the card's left edge stays under the word, the anchor the eye expects; a flip would move it 460px away), opens above the word when there is no room below and more above, scrolls inside itself when neither side has room, and closes when the page scrolls, as the editor's does. It is positioned fixed in viewport coordinates so no frame, the table's new scroll frame included, can clip it; the DOM stays inside the trigger's root so pin, Escape and outside-click containment are unchanged. The 4px gap becomes padding on the layer so the pointer can cross into the card.
- **designer-fable** (2026-09-05T10:30:00.000Z): Applied. `packages/pages/src/lib/atoms/DataTable.svelte:110-113` wraps the table in a `.frame` that is the container the width tiers measure against and the one thing that scrolls sideways (`:176-180`); cells align to the top (`:181-194`); the growing column keeps `min-width: 24ch` (`:195-200`); under 900px of frame width a cell's `white-space` goes normal so a run of tokens breaks between tokens (`:201-209`). `packages/pages/src/lib/atoms/Lockup.svelte:39-47` makes the lockup one token with `nowrap`. `packages/pages/src/lib/molecules/ContextLockup.svelte:40-53` gates `flex-wrap` on the same container so the warning word drops under the name only in a narrow table (a wrap-capable flex box collapses at every width otherwise). `packages/pages/src/lib/organisms/StrategicPositionTable.svelte:125-132` makes the description a block. The hover: `packages/pages/src/lib/molecules/hover-placement.ts:1-41` is the editor's placement rule as a pure function (under the word, shift left at the right edge, above when no room below and more above, the larger side with a max height when neither fits); `packages/pages/src/lib/molecules/PatternHover.svelte:64-77` applies it as an action on the layer the moment it mounts, and the layer is `position: fixed` with the 4px gap as padding on both sides (`:134-152`); `packages/pages/src/lib/molecules/hover.svelte.ts:55-74` closes on a scroll outside the disclosure and on resize. Measured again at 1300: With 116, Description 203 (29ch), rows 66/44/66/66 all on their first line, card right edge at 1292; at 1100 the floor holds at 197 and the frame scrolls 194px while the page does not; at 1600 nothing changed (With 200, two-line rows, card clamped at 1591). Tests: `packages/pages/src/lib/molecules/hover-placement.test.ts:1-52` five cases on the geometry; `packages/pages/src/lib/molecules/PatternHover.test.ts:216-275` closes on page scroll and resize but not on the card's own scroll, and places the layer against mocked rects with the max-height cap; `packages/pages/src/lib/atoms/DataTable.test.ts:32-35` the frame. e2e in `packages/pages/e2e/relationship.spec.ts:130-244`: at 1300 with the tree the description holds 24ch, the row is under 80px, the lockup is on the description's first line, the warning word sits under its name and the frame does not scroll; at 1100 the frame scrolls and the page does not; the ACL card ends inside the viewport with its left edge no further right than the word, closes on a wheel scroll, and `separate-ways` near the bottom opens above itself. The relationship-page pin test now clicks the h1 rather than the Links heading, because reaching Links scrolls and a scroll closes the card by rule. Docs: `docs/design/design-language-v2.md:47-56` (principle 6), `:60-66` (principle 8), the `Lockup`, `DataTable` and `HoverCard` rows in section 7; `apps/docs/docs/8-pages.md:72-82`. Evidence so far: pages unit 587 at 100% lines/branches/functions/statements, svelte-check 0 errors, Biome clean; the full Playwright suite is running.
- **lead** (2026-09-05T11:00:00.000Z): The designer applied all three rulings and was cut off by a Fable rate limit mid-verification; the lead ran the verification instead. Confirmed on the shipped Sales page at 1300px with the tree: the description reads in three lines, cells align to the top, and the hover card stays inside the viewport. Landing on develop.

- 2026-09-11, lead: driven in a browser against the built app and confirmed against this card's own claim; moved to done.
