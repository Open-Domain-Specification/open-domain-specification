---
column: done
labels: [backend, ddd, breaking]
priority: high
agent: ironhide
live: false
clean-code-swept: true
updatedAt: 2026-09-05T13:32:00.000Z
---
# An operation names what it rejects with

Implements [decision 25](../../decisions/25-an-operation-names-what-it-rejects-with.md): `ConsumableSchema.rejects?: { $ref }[]` on operations, covered by `schema-context`, refused on events.

## Checklist

- [x] `rejects?: { $ref }[]` on `ConsumableSchema`; workspace model, DSL (`provides(..., { rejects })`), `toSchema`/`fromSchema`, JSON schema regenerated
- [x] `rejects-on-operation` (error) on an event; `schema-context` covers each ref like `schema` and `returns`
- [x] Consumable page: "Rejects with" beside "Returns", one attribute table per rejection; story and test; doc generator prints it; `ConsumableSubsection` names them too, and `browse.spec.ts` walks the rejection
- [x] Skill: DSL reference, interview follow-up for operations "and when it says no, what does it say?", regenerated bundle
- [x] Reference models: at least one honest rejection per model where the discovery notes name a refusal (a declined payment, an over-limit transfer, a failed reservation); `.ods/` and petstore `docs/` regenerated
- [x] Decision 13's "synchronous error shapes are not modelled" line marked superseded by 25; `apps/docs` gains the refusal paragraph and the `rejects-on-operation` row
- [x] Root suites green inside each package in build order; pages at 100% with `npm run check` clean; `cmp` of the petstore schema against core dist silent

## Gates

- `clean-code-swept`: swept 2026-09-05. `rejects` is a list, so it follows `raises` where `returns` (a single ref) could not: an array on the class defaulting to `[]`, `undefined` in `toSchema` when empty, mapped back through `getSchemaByRefOrThrow` in `addProvides`. No new component: the "Rejects with" section reuses `Heading` level 3, `Lockup` and `AttributeTable` in the shape `StructureSubsection` already draws entities in, and the fact row reuses `RefList` because a rejection list is a list of refs like any other. `schema-context` gained a loop beside its two existing `if`s rather than a second rule, and `rejects-on-operation` is `returnsOnOperation`'s twin, named in the same words. `DataSchema.consumables` and `SchemaPage`'s `directionOf` grew a third arm each, which is what keeps a schema that exists only as a refusal off the "nothing carries this" branch. biome clean on every file touched, run file by file (`npx @biomejs/biome check <file>`, no fixes applied); `npx tsc --noEmit` clean in core, doc, graphviz, skill and all four models; `npm run check` clean in pages; pages coverage 100% statements, branches, functions and lines. The second pass added no component either: `ConsumableSubsection` reuses the `RefList` the consumable page's own fact row uses, so the two surfaces name a rejection in the same words, and the full-page tables stay on the page that has room for them. One pre-existing condition left alone: `packages/core/src/workspace.ts` trips `lint/suspicious/noThenProperty` on `Policy.then`, as it does on `origin/develop`.
- `qa-automated`: `bash scripts/verify-all.sh` green end to end from the worktree root —

```
core: 322 tests passed
graphviz: 20 tests passed
doc: 29 tests passed
skill: 26 tests passed
northbank: 3 tests passed
petstore: 18 tests passed
rivermart: 3 tests passed
streamline: 3 tests passed
models/_shared: 9 tests passed
pages: 706 tests passed
apps/docs: 22 tests passed
apps/ods-vscode: 9 tests passed
pages e2e (diagram-panel-fit): passed
schema comparison (petstore vs core dist): match
```

- `qa-automated` (full browser suite): `npx playwright test` in `packages/pages` — 272 passed, including the new `browse.spec.ts` rejection walkthrough and the new "Operation with rejections" story.

## Comments

- **optimus-prime** (2026-09-07T09:00:00.000Z): Ironhide, after card 58 lands (the lead will say); `feat!`.
- **ironhide** (2026-09-05T13:08:00.000Z): Claimed. Reset to f6a5699 ("Merge card 68" at 00c2f5c) and installed. Schema and model: `ConsumableSchema.rejects?: { $ref }[]` sits between `returns` and `raises` (packages/core/src/schema.ts). Card 44 was the pattern, but `returns` is one ref and a rejection is a list, so the model follows `raises` instead: `Consumable.rejects: DataSchema[]` defaults to `[]`, `toSchema` writes `undefined` when the list is empty rather than `[]`, and `addProvides` maps each ref back through `getSchemaByRefOrThrow` in the same pass that resolves `schema` and `returns`, because a rejection is a schema of the provider's own context and every schema exists by then. `DataSchema.consumables` counts a rejection as a third way to depend on a shape, beside sending it and answering with it — without that, a schema that exists only as a refusal reads as carried by nothing, which is the one thing the reference models now have. Round-trip case at packages/core/src/round-trip.test.ts, including that an operation refusing with nothing keeps the field absent.
- **ironhide** (2026-09-05T13:10:00.000Z): Rules. `rejects-on-operation` (error) refuses a rejection on an event and names every schema the event listed, so one edit fixes it; its `why` says an event is a fact that already happened, so it has nobody to refuse and nothing left to refuse them. `schema-context` gained a loop over `rejects` beside its `schema` and `returns` checks and its catalogue summary now names all three; the message reads `"X" rejects with schema "Y" from "Z"`, matching the two already there. Cases at packages/core/src/validate.test.ts (a borrowed rejection alongside a local one, so only the borrowed one trips; an event with two rejections, so the message lists both), and the completeness fixture trips the new rule.
- **ironhide** (2026-09-05T13:13:00.000Z): Surfaces. Consumable page: a "Rejects with" fact under "Returns" listing the schemas as refs, and a "Rejects with" section under Returns drawing one level-3 subsection per rejection — the schema's lockup, its description, its attribute table — which is the shape an aggregate page already draws entities in, so no new component. The section is in `sectionsFor`, so the table of contents lists it only when there is one. Schema page: `directionOf` gained "rejects with" as a third direction on one row, and the "Carried by" lead now says send, answer or refuse. Doc generator: a "Rejects with" column between Returns and Raises, comma-joined links, `-` when there are none. Story "Operation with rejections" on `PETSTORE_REFS.operation`, which is `ReservePetForOrder`; `rejectionSchema` added to the harness for `PetUnavailable`, the one petstore schema that exists only as a refusal. Three cases at packages/pages/src/lib/templates/templates.test.ts and one at packages/doc/src/index.test.ts. Pages coverage stays at 100%.
- **ironhide** (2026-09-05T13:16:00.000Z): Models. One rejection each, each on an operation whose discovery notes already name the refusal, and each a case where nothing happened rather than a failure the model already publishes as an event. Petstore: `ReservePetForOrder` rejects with `PetUnavailable` (petId, status) — the failed reservation, and the one Catalog contract Sales calls synchronously. NorthBank: `InitiatePayment` rejects with `InstructionRefused` (reason, payerAccountId, remainingToday) — the over-limit transfer, matching the `DailyLimit` context invariant that operation already guards; no instruction exists, so there is no payment event to raise. RiverMart: `CancelOrder` rejects with `CancelRefused` (orderId, shipmentId) — "you cancel only before anything ships; after that it's a return", so the storefront is told which shipment blocked it. StreamLine: `StartPlayback` rejects with `PlaybackDenied` (reason, titleId) — "no certification, no playback" and the lapsed subscription, told apart by the reason. Deliberately not touched: RiverMart's `PaymentDeclined` and `StockShort` and StreamLine's `PaymentFailed` are already published events with policies reacting to them, and decision 25 does not ask for those to move. One sentence per rejection in each DISCOVERY.md, in the section whose lead named the refusal; no reformatting, since card 74 is in the same four files.
- **ironhide** (2026-09-05T13:18:00.000Z): Skill: `rejects?` in the `provides` row of the DSL table, a Phase E follow-up after "and what comes back?" — "and when it says no, what does it say?", with the instruction to leave it off when the answer is a status code and nothing else — and a translation-table row for "we decline it", "we refuse it", "they get an error saying why". `model-reference.md` and `validation-rules.md` regenerated from core's build output along with the bundle. Decision 13's "synchronous error shapes are not modelled" line already carried its superseded-by-25 note, so the checklist item needed no edit; the pointer is in place and reads correctly. Regenerated core `dist/workspace.schema.json`, all four `.ods/`, the petstore `docs/` and `apps/docs`' two `toSchema` snapshots (both gained one `"rejects": undefined` line). Ready for review; not pushed.
- **ironhide** (2026-09-05T13:20:00.000Z): QUESTION for lead, not blocking. `apps/docs/docs/3-core/3-tactical-design.md` and `4-validation.md` are now stale: the consumable paragraph says a consumable carries a payload and an operation a `returns`, with nothing about a refusal, and the rules table lists `returns-on-operation` but not `rejects-on-operation`. Card 44 updated both files in the same commit, but this card's checklist does not name them and my brief said scope out is anything else, so I left them alone. Two small prose edits whenever you want them assigned.
- **optimus-prime** (2026-09-05T13:22:00.000Z): Finish on this card: the two `apps/docs` edits, `ConsumableSubsection` with an `edge-cases.test.ts` case, and the `browse.spec.ts` rejection walkthrough with one full `npx playwright test`. `PaymentDeclined`, `StockShort` and `PaymentFailed` stay events — they are facts the provider reports, not this operation's answer, and a policy reacting to them is the right shape.
- **ironhide** (2026-09-05T13:32:00.000Z): All three done. Docs: `3-tactical-design.md` gains a paragraph after the `returns` one — an operation lists the schemas it `rejects` with, a declined payment or an over-limit transfer, nothing happened so none is an event and a transport error stays outside; each is the provider's own context's, checked by `schema-context`; leave it off when there is no shape worth naming, and an event never lists one. `4-validation.md` gains the `rejects-on-operation` row under `returns-on-operation`. Subsection: a "Rejects with" definition under "Returns" using the same `RefList` the consumable page's fact row uses, so an aggregate or service page names the refusals and the consumable's own page draws them in full — the split the returned shape already has. The petstore has no aggregate-provided operation that refuses, so `edgeCaseModel` grew "Refusing Operation" with two rejections (`Refusal Schema`, `Over Limit Schema`), which is also the first two-rejection case anywhere: three cases at packages/pages/src/lib/templates/edge-cases.test.ts cover the subsection naming both, the consumable page drawing a table for each, and a schema that is only ever refused with reading as carried rather than as carried by nothing. E2E: `browse.spec.ts` gains the walkthrough in the returns one's shape — `ReservePetForOrder` shows Payload and Rejects with and no Returns between them, one `h3` for `PetUnavailable` with `status` under it, then the fact's link opens the schema whose one carrier row says `ReservePetForOrder` / `rejects with`. `npx playwright test` in pages: 272 passed. Recorded decision 2: `PaymentDeclined`, `StockShort` and `PaymentFailed` stay events, unchanged. verify-all re-run green, pages 703 → 706 and still 100% on all four coverage metrics with `npm run check` clean. Ready for review; not pushed.
