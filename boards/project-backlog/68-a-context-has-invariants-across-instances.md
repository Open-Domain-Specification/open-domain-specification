---
column: done
labels: [backend, ddd, breaking, pages]
priority: high
agent: senior-dev
live: false
clean-code-swept: true
updatedAt: 2026-09-05T13:05:00.000Z
---
# A context has invariants across instances

Implements [decision 27](../../decisions/27-a-context-has-invariants-across-instances.md): `BoundedContextSchema.invariants`, two rules, the eleven cross-instance invariants of the reference models move to their contexts and name their guards, and the aggregate invariant's rule text finally says what it means.

## Checklist

- [x] `invariants` on `BoundedContextSchema`; workspace model (`Invariant.owner` is an aggregate or a context), DSL `bc.addInvariant`, `toSchema`/`fromSchema`, JSON schema regenerated; refs in the grammar
- [x] `invariant-in-context` and `context-invariant-guarded` (errors) with DDD reasons; `invariant-in-aggregate`'s summary, why and fix rewritten to "holds inside the boundary on every save"; its fix text no longer says "model the guarantee as a policy"
- [x] Reference models: every invariant whose prose says one instance cannot see another (StreamLine `OneActiveSubscriptionPerHousehold`, `WithinStreamLimit`; NorthBank `DailyLimit`, `OneOpenApplicationPerCustomer`; RiverMart `OneActiveOfferPerSellerSku`; and the rest the sweep finds) moves to its context and names its guard; DISCOVERY.md sections that said "ODS has no other place" updated; `.ods/` and petstore `docs/` regenerated — the sweep found the five named and no sixth of that shape; see the QUESTION comment
- [x] Pages: context page Invariants section in the aggregate page's shape; invariant page states its kind and owner; tree and search in the extension; doc generator prints all of it
- [x] Skill: DSL reference, interview question "is this true of one of these, or of all of them together?", regenerated bundle
- [x] Decision 15's "a rule that spans aggregates is not an invariant" section replaced by a pointer to decision 27 (the lead writes it; you confirm the pointer resolves)
- [x] Root suites green inside each package in build order; pages at 100% with `npm run check` clean; `cmp` of the petstore schema against core dist silent

## Gates

- `clean-code-swept`: swept 2026-09-05. `Invariant` gained an `owner` in place of its `aggregate` and two derived reads (`kind`, `boundedcontext`), which is one field rather than a second class: `toSchema`, `guarded` and `constrains` are untouched, and `path` reads `owner.path` because both owners already have one. The two new rules share `contextInvariantsOf`, and `contextOf` is the context-level twin of the existing `scopeOf` rather than a rewrite of it. Pages reuse `InvariantsSection` as-is; the only new prose on `InvariantPage` is one `KIND` table that keeps the two readings side by side instead of scattering ternaries down the template. No new node kind, route table or page component. biome clean on every file touched, run file by file (`npx @biomejs/biome check <file>`, no fixes applied after formatting two); `npx tsc --noEmit` clean in core, doc, graphviz, skill, all four models, `models/_shared`, `apps/docs` and `apps/ods-vscode`; `npm run check` clean in pages. Two pre-existing conditions left alone: `packages/core/src/workspace.ts` trips `lint/suspicious/noThenProperty` on `Policy.then` (it does so on `origin/develop` too), and the generated `.ods/*.json` are left exactly as their generator writes them.
- `qa-automated`: `bash scripts/verify-all.sh` green end to end from the worktree root —

```
core: 318 tests passed
graphviz: 19 tests passed
doc: 29 tests passed
skill: 26 tests passed
northbank: 3 tests passed
petstore: 18 tests passed
rivermart: 3 tests passed
streamline: 3 tests passed
models/_shared: 9 tests passed
pages: 673 tests passed
apps/docs: 22 tests passed
apps/ods-vscode: 9 tests passed
pages e2e (diagram-panel-fit): passed
schema comparison (petstore vs core dist): match
```

## Comments

- **lead** (2026-09-07T10:00:00.000Z): the senior developer, after card 67 lands (the lead will say); `feat!`.
- **senior-dev** (2026-09-05T12:50:00.000Z): Claimed. Reset to 4093761 ("Merge card 67" at d745cfc) and installed. Schema and model: `BoundedContextSchema.invariants` sits beside `aggregates` with the same `InvariantSchema` (packages/core/src/schema.ts:113-120), and `contextInvariantRef(bc, invariant)` joins `invariantRef` at :525-543. In the workspace, `Invariant.owner` is an `Aggregate | BoundedContext` and the class derives `kind` ("aggregate" | "context") and `boundedcontext` from it (packages/core/src/workspace.ts:1373-1456); `BoundedContext` gained the map, `addInvariant` and the `toSchema` line. Two lookups widened rather than forked: `getInvariantByRef` falls through to the contexts, and `Consumable.invariants` now sees its context's rules as well as its aggregate's, which is what makes a guard show on the operation's own page. `fromSchema` builds a context's invariants with its policies and wires their `constrains` after every aggregate has its attributes, because a context rule may count an attribute in any of them (packages/core/src/workspace-from-schema.ts:186-194, :352-366). The visitor walks them from the context node. Round-trip case at packages/core/src/round-trip.test.ts:148-160.
- **senior-dev** (2026-09-05T12:54:00.000Z): Rules. `invariant-in-context` (error) keeps every target inside the context — entity, attribute or value object of any of its aggregates, or one of its consumables — and reports a schema's attribute as "in no bounded context at all", the same way `invariant-in-aggregate` already refuses one (packages/core/src/validate.ts:649-700). `context-invariant-guarded` (error) wants at least one `operation` of that context in `constrains`; an event does not count, because nothing can be refused after the fact, and an application service's operation does, because that is where most of these checks actually run. `invariant-in-aggregate` keeps its id and its check and changes only its words: the message now ends "an aggregate's invariant holds inside the boundary on every save", the `why` says the rule is true again the moment the save returns, and the `fix` sends a cross-instance rule to `bc.addInvariant` instead of telling the author to model it as a policy. Six cases at packages/core/src/validate.test.ts:709-835; the completeness fixture trips both new rules with one context rule that counts another context's entity and names no operation.
- **senior-dev** (2026-09-05T12:58:00.000Z): Surfaces. Context page: an `InvariantsSection` between Model and Value objects, with the `constrains` column the aggregate page uses, so the two tables read alike (packages/pages/src/lib/templates/ContextPage.svelte). Invariant page: a keyword beside the title saying "aggregate invariant" or "context invariant" with the promise each makes as its tooltip, "Enforced by" pointing at whichever owner it is, crumbs that stop at the context for a context rule, and the two section leads swapped per kind — the guard lead now says a rule across instances holds only because something checks it before acting, and its empty state says nothing keeps it. `InvariantsSection`'s "whole aggregate" reads "whole context" for a context rule. Routing: `pageRefs` yields the context's invariants and a pattern below the glossary resolves `#/boundedcontexts/<bc>/invariants/<id>` (the aggregate pattern is matched further up, so neither shadows the other). Extension: an Invariants group on the context node between Services and Value Objects, and search yields them with "across aggregates" as the extra. Doc generator: an Invariants section on the context page with the aggregate page's three columns. The petstore has no context invariant, so the pages fixtures grew two in `edgeCaseModel` — one counting an entity and naming the operation that checks it, one naming nothing, which is the unguarded case a reader has to be shown — and four cases cover them at packages/pages/src/lib/templates/edge-cases.test.ts and packages/doc/src/index.test.ts.
- **senior-dev** (2026-09-05T13:00:00.000Z): Models. Five invariants moved, all five the card names: StreamLine `WithinStreamLimit` (Playback, guarded by `StartPlayback`) and `OneActiveSubscriptionPerHousehold` (Billing & Plans, `StartSubscription`); NorthBank `DailyLimit` (Payments, `InitiatePayment`) and `OneOpenApplicationPerCustomer` (Lending, `SubmitApplication`); RiverMart `OneActiveOfferPerSellerSku` (Offers, `PublishOffer`). Each keeps what it counted and adds the operation, and each declaration moved down the file to sit under the operation that guards it, so the guard is a const rather than a lookup. Edits are invariants and the one `const x =` prefix on each guard's `provides`, so nothing collides with card 72's services and `returns` in the same files. Discovery notes follow: NorthBank's "ODS has no other place for a rule the business states" now says decision 27 gave it one, and StreamLine's two "partially accepted" entries and both "recorded as" summaries say which boundary keeps which rule. Regenerated all four `.ods/`, the petstore `docs/`, core `dist/workspace.schema.json`, the skill references and bundle, and `apps/docs`' visitor snapshot. `packages/skill/skill/examples/minimal.ods.json` gained `"invariants": {}`, since the collection is required.
- **senior-dev** (2026-09-05T13:03:00.000Z): QUESTION for lead, not blocking, but it is the one judgement in this card that is yours. Decision 27 and this card say eleven; the sweep by the card's own criterion — prose that says one instance cannot see another — finds five, and they are exactly the five named. I moved those and stopped. What the sweep also finds, and what I think your other six are, is a second family: rules whose prose admits they are checked before acting because the data is in **another context**, not in a sibling instance. Those are petstore `ApproveOnlyWhenAvailable`, StreamLine `SessionNeedsEntitlement`, NorthBank `FundsAvailableAtInitiation`, `AuthWithinAvailableBalance`, `LinesReconcileToLedger` and `MandateHolderIsVerified` — six, which is exactly the gap. They fit `context-invariant-guarded` perfectly (each has a real guard: `ApproveOrder`, `StartPlayback`, `InitiatePayment`, `AuthoriseCard`, `FileReturn`, and whatever adds a mandate) and they would all pass `invariant-in-context`, because every element they constrain is local. But decision 27's Decision section says a context's invariant holds "across instances or across aggregates of that context", which none of these does, and NorthBank's discovery notes treat the two families as separate complaints. Moving them says something the decision does not quite say, so I did not. Say the word and it is a small follow-up in the same shape. Two things I deliberately left alone while looking: StreamLine `NoOverlappingWindowsPerTerritory` reads like a cross-instance rule but the `LicenseDeal` aggregate's own description says the windows live together precisely so they can be checked against each other, so it stays where it is; and the licensing context has no operation that records a window, so it could not name a guard without inventing one.
- **senior-dev** (2026-09-05T13:05:00.000Z): Decision 15's "A rule that spans aggregates is not an invariant" section is now one line pointing at decision 27; the relative link `27-a-context-has-invariants-across-instances.md` resolves to `decisions/27-a-context-has-invariants-across-instances.md`, which exists. Skill: `bc.addInvariant` in the DSL table, the interview question "is this true of one of these, or of all of them together?" with the follow-up "who checks that before acting?" in Phase E, a context row in the translation table, both readings in the glossary, the required-collection and invariant-ownership rules in SKILL.md, and the ref grammar split into the aggregate's path and the context's. Ready for review; not pushed.
