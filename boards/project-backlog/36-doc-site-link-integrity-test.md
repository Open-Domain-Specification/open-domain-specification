---
column: done
live: false
labels: [infra, docs, backend]
priority: high
agent: dev-opus
clean-code-swept: true
updatedAt: 2026-09-03T18:45:00.000Z
---
# Shared test: every generated docsify site is complete and every link resolves

The doc generator has five unit tests on markdown snippets, but nothing checks a generated site as a whole. Each reference model writes a docsify site under `models/<name>/docs` at build time and no test looks at it; the old "every link resolves" test from vsc-extension card 11 died with the generator it covered. Add a shared assertion to `models/_shared` (beside `assertStressTestWorkspace`) that each of the four model test suites calls.

## Checklist

- [x] `assertDocSite(workspace)` in models/_shared/src/index.ts: run `toDoc`, then check every workspace ref has a page (workspace, domain, subdomain, context, aggregate, service and their leaf sections)
- [x] Every relative link in every page (petstore has 77 distinct) resolves to a generated file, and every `#anchor` to a heading in that file; external `http(s)` links are skipped
- [x] `_sidebar.md` lists the workspace page, its glossary, every domain and subdomain once, and every context once under each subdomain it serves and nowhere else, in depth-first order; aggregate and service pages are not listed (lead answer, 2026-09-03T15:40Z)
- [x] Each model's `workspace.test.ts` gains one `it` calling the helper
- [x] Root `npm test` green for every package this card touches; the three pre-existing `packages/pages` timeouts are unrelated (see Gates)

## Gates

- [x] clean-code-swept — six-agent audit over models/_shared/src/index.ts, packages/doc/src/aggregate.md.ts and the four workspace.test.ts; the one finding above 0.5 in my own code (SRP 0.55) fixed, everything else at or below 0.45 (dev-opus, 2026-09-03T18:05:00.000Z)

## Comments

- **lead** (2026-09-03T15:00:00.000Z): Assigned to dev-opus. Fixed by decision: the helper lives in models/_shared/src/index.ts beside assertStressTestWorkspace and takes a Workspace; it runs toDoc in memory (no disk) and inspects the returned file map. Only `.md` files are parsed for links; `.svg` and any `index.html` (card 37 may add one) are link targets, never sources. Anchor matching uses docsify's slug rule (lowercase, spaces to hyphens, punctuation stripped); if a heading form in the generator disagrees with that rule, treat it as a generator bug and ask. Sidebar order = depth-first tree order as the generator emits it. You may choose: how the assertion reports failures (one aggregated message listing every broken link is preferred). Tests that prove it: the four models/*/src/workspace.test.ts each gain one `it`; run `npm test` at the root. Work in your worktree; run `npm ci` there first if node_modules is missing.
- **dev-opus** (2026-09-03T16:00:00.000Z): Picking this up. Worktree was on the v0.1.11 commit, reset to develop so the card and current generator are present; ran `npm ci`. Next: dump `toDoc(petstore)` in memory and inventory the link forms it emits before writing the helper.
- **dev-opus** (2026-09-03T16:40:00.000Z): Inventoried `toDoc` output for all four models with a throwaway script (deleted; tree is clean apart from this card). Numbers: petstore 56 files / 24 `.md` pages / 78 distinct link targets, of which 77 are relative and 1 external -- exactly the 77 the card names, so a balanced-parenthesis link parser is required: the workspace page path is `swagger_petstore_(v3)/index.md` and a naive `[^)]+` destination regex truncates it at the inner `)`. northbank 139/57/212, rivermart 153/63/237, streamline 127/53/187. Anchors: the only generated `#anchor` is `#schemas` from packages/doc/src/consumables.md.ts:11, and `## Schemas` exists at packages/doc/src/boundedcontext.md.ts:93, so it slugs cleanly -- no generator/docsify disagreement to report there.
- **dev-opus** (2026-09-03T16:41:00.000Z): QUESTION for lead (1 of 2) -- the link check finds a real generator bug, not a test gap. packages/doc/src/aggregate.md.ts:78 renders the Relationships table's Source cell as `[${it.source.name}](${pathToIndexMd(it.source.path, aggregate.path)})`, but `it.source` is an Entity and entities never get a page, so every one of those links is dead: e.g. `boundedcontexts/catalog_bc/aggregates/pet/index.md -> entities/pet/index.md` and, for a source in another context, `boundedcontexts/sales_bc/aggregates/order/index.md -> ../../../catalog_bc/aggregates/pet/entities/pet/index.md`. 12 such links in petstore, ~120 across the four models -- they are the ONLY broken links in any site. Options: (a) drop the link, render `Aggregate - Entity` as plain text, mirroring the Target cell at packages/doc/src/aggregate.md.ts:81; (b) point the link at the entity's owning aggregate page with the section anchor, `[Pet](../../aggregates/pet/index.md#entities-and-value-objects)`; (c) generate a page per entity. I recommend (b): it keeps navigation working for sources that live in another aggregate or context (which (a) would lose) without inventing a new page type, and the anchor target `## Entities and Value Objects` already exists at packages/doc/src/aggregate.md.ts:60. Note this changes generated markdown, so packages/doc/src/index.test.ts expectations move with it.
- **dev-opus** (2026-09-03T16:42:00.000Z): QUESTION for lead (2 of 2) -- checklist item 3, "`_sidebar.md` lists every `index.md` and `glossary.md` exactly once", cannot pass as written, for two independent reasons. First, the sidebar only ever lists the workspace page, the glossary, domains, subdomains and bounded contexts; aggregate and service pages are never listed (10 unlisted pages in petstore, 21 northbank, 28 rivermart, 20 streamline). Second, packages/doc/src/index.ts:74-77 deliberately lists a context under each subdomain it serves -- petstore's Inventory BC and northbank's Customer & KYC each appear twice. Options: (a) assert the sidebar lists exactly the workspace `index.md`, its `glossary.md`, every domain and subdomain page, and every context page at least once and only under subdomains it actually serves, in depth-first order, leaving the generator alone; (b) change the generator to list each context once. I recommend (a): the duplication is documented intent at packages/doc/src/index.ts:74, and (b) would drop a context out of a subdomain's branch in the nav. Blocked on both answers before writing `assertDocSite`.
- **lead** (2026-09-03T16:50:00.000Z): ANSWER Q1: option (b). Link the Source cell to the owning aggregate's page with `#entities-and-value-objects` (packages/doc/src/aggregate.md.ts:78), and show the text as `Aggregate - Entity` so it mirrors the Target cell. Update the doc unit test expectations. ANSWER Q2: option (a). Rewrite checklist item 3 to: sidebar lists the workspace page, its glossary, every domain and subdomain once, and every context once under each subdomain it serves and nowhere else, in depth-first order; aggregate and service pages are not listed. Generator sidebar logic untouched. Continue.
- **dev-opus** (2026-09-03T17:30:00.000Z): Both answers actioned. Q1: packages/doc/src/aggregate.md.ts:82-84 now renders the Relationships Source cell as `[Aggregate - Entity](<aggregate index.md>#entities-and-value-objects)`, mirroring the Target cell at packages/doc/src/aggregate.md.ts:86; no doc unit test asserted the old row form (packages/doc/src/index.test.ts has five `it`s, none over the Relationships table), so nothing there needed updating and its 14 tests stay green. Q2: rewrote checklist item 3 to the lead's wording.
- **dev-opus** (2026-09-03T17:31:00.000Z): `assertDocSite` added at models/_shared/src/index.ts:135-347, beside `assertStressTestWorkspace`, running `toDoc` in memory. Four checks, each reporting one aggregated message via `report` at models/_shared/src/index.ts:262: (1) `expectedPages` at models/_shared/src/index.ts:193 asserts an `index.md` for every workspace, domain, subdomain, context, aggregate and service plus the workspace `glossary.md`, and the converse -- no `.md` in the map that is not one of those or `_sidebar.md`; (2) `markdownLinks` at models/_shared/src/index.ts:161 pulls every inline link and image with a paren counter, `resolveFrom` at models/_shared/src/index.ts:180 resolves it against the site root, and each `#anchor` is matched against the headings of the target file slugged by `slugify` at models/_shared/src/index.ts:145; anything with a URL scheme or `//` prefix is skipped; (3) `parseSidebar` at models/_shared/src/index.ts:250 reads `_sidebar.md` into depth/label/href entries and deep-equals them against `expectedSidebar` at models/_shared/src/index.ts:217, which is the lead's item-3 wording expressed as a tree walk.
- **dev-opus** (2026-09-03T17:32:00.000Z): Proved the assertion bites rather than merely passing: stashed the aggregate.md.ts fix, rebuilt the doc package and re-ran petstore -- the new `it` failed with all 7 distinct dead entity links listed in one message (e.g. `boundedcontexts/sales_bc/aggregates/order/index.md -> ../../../catalog_bc/aggregates/pet/entities/pet/index.md`); restored the fix and it passes. Each model's suite gains one `it` (models/petstore/src/workspace.test.ts:283, models/northbank/src/workspace.test.ts:31, models/rivermart/src/workspace.test.ts:31, models/streamline/src/workspace.test.ts:31) carrying a 60s timeout, because rendering every graphviz-wasm diagram takes ~6s on RiverMart and overran vitest's 5s default. Green: model-petstore 18/18, model-northbank 3/3, model-rivermart 3/3, model-streamline 3/3, doc 14/14, and `tsc --noEmit` on model-tools.
- **dev-opus** (2026-09-03T18:05:00.000Z): Clean-code sweep. Six parallel agents (SRP, DRY, naming, coupling, dead code, KISS). One finding above 0.5 in code I wrote -- SRP 0.55, `assertDocSite` inlining the heading-index and link-walking loops while its other checks delegated to named helpers. Fixed by extracting `markdownPages` (models/_shared/src/index.ts:269), `headingAnchors` (models/_shared/src/index.ts:274) and `brokenLinks` (models/_shared/src/index.ts:293), leaving `assertDocSite` as four delegating calls (models/_shared/src/index.ts:330-354); also renamed `report` to `assertNoProblems` (models/_shared/src/index.ts:260) on the naming agent's 0.45 note, since the old name read as logging rather than asserting. Everything else was left alone: the `aggergateMd` misspelling at packages/doc/src/aggregate.md.ts:47 is pre-existing and outside this diff (it belongs with card 17), the `require.resolve` into core's `dist/` at models/_shared/src/index.ts:59-62 is pre-existing in `generate`, and the DRY and KISS agents both judged `expectedPages`/`expectedSidebar`/`resolveFrom` mirroring the generator to be a deliberate independent oracle -- reusing packages/doc/src/lib/paths.ts inside the assertion would make a bug in it invisible. All suites re-run green after the refactor.
- **dev-opus** (2026-09-03T18:10:00.000Z): Final evidence. Green: model-petstore 18/18, model-northbank 3/3, model-rivermart 3/3, model-streamline 3/3, doc 14/14, `tsc --noEmit` on model-tools, biome clean on all six touched files. Root `npm test` also runs `@open-domain-specification/pages`, which fails -- but not from this card: packages/pages depends only on core and graphviz (packages/pages/package.json:64-71), never on `@open-domain-specification/doc` or model-tools. Proved rather than argued: stashed the whole diff and ran packages/pages `src/lib/Page.test.ts` on the clean tree, which failed with exactly the same three timeouts (`RiverMart/StreamLine/NorthBank: every ref renders`, each blowing the 30s `testTimeout` at packages/pages/vitest.config.ts:16) -- 3 failed, 114 passed. Pre-existing and machine-speed-dependent; worth its own card. Moving to review; not committing, per the workflow.
- **lead** (2026-09-03T18:45:00.000Z): Reviewed models/_shared/src/index.ts:135-354 and the aggregate.md.ts:83-85 fix. Landing on develop.
