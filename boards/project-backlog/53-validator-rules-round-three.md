---
column: done
labels: [backend, ddd]
priority: high
agent: senior-dev
live: false
clean-code-swept: true
updatedAt: 2026-09-04T02:35:00.000Z
---
# Validator rules, round three

From Antigravity run 4 (scratchpad agy-run4.md, issues 12, 13, 18). Each is a rule with a catalogue entry (why, fix), tests for the failing and boundary-passing shapes, and the four models checked.

## Checklist

- [x] `relationship-cycle` (warning): the directed relationships (`upstream-downstream`, `customer-supplier`) form no cycle; the message lists the cycle's contexts in order
- [x] `partnership-backed` (warning): two contexts declaring a partnership have consumable traffic in both directions
- [x] `disposition-needs-comment` (warning): an intent whose disposition is `tolerated` or `refactor` carries at least one comment
- [x] `entity-identity` (warning): a non-root entity with no `identity: true` attribute; the message says an entity without identity is a value object
- [x] `reaction-cycle` (warning): a cycle through policies and consumables (operation raises event, policy on that event issues an operation, and so on back to the start); the message lists the chain in order
- [x] `attribute-relation-coherence`: drop the check that `type` equals the value object's name or `Name[]`; decision 15 promises free-text types, and only the trailing `[]` convention the cardinality check uses survives
- [x] `relationship-roles-backed`: a `published-language` upstream role is backed by any crossing consumable carrying a `schema`, so one open-host-service operation with a schema backs both roles
- [x] Skill references regenerated; validation docs page updated; four models validate with only their declared deliberate diagnostics

## Gates

- `clean-code-swept`: swept 2026-09-04. Folded three copies of the crossing filter into `crossingsBetween` (packages/core/src/validate.ts:586-597), kept one cycle walker for both cycle rules, and removed two stale fixture comments. biome and tsc clean on every file touched; the two `noThenProperty` errors left in packages/core/src/workspace.ts are pre-existing and out of scope.
- `qa-automated`: core 282; petstore 18; northbank 3; rivermart 3; streamline 3; skill 26; pages 660 across 94 files. Each model's `npm run build` regenerates docs/ and .ods/ and emits only its declared deliberate diagnostics (petstore 0, northbank 3, rivermart 3, streamline 4). packages/doc is 1 failed | 23 passed on a snapshot that was already stale on develop (see the journal); packages/doc is out of scope for this card.

## Comments

- **lead** (2026-09-06T22:00:00.000Z): the senior developer, after card 48 lands (the lead will say). No schema change. Work in your worktree with absolute paths; if the card is missing, `git reset --hard develop` there first.
- **senior-dev** (2026-09-04T00:00:00.000Z): Claimed. The worktree sat on tag v0.4.0 so the card was missing; a hard reset to develop put me on 7db8b3b (card 48) and `npm ci` restored node_modules. Read the catalogue at packages/core/src/validate.ts:887-1093, the symmetric-pair helper at packages/core/src/validate.ts:405-416 and `relationship-roles-backed` at packages/core/src/validate.ts:454-496 as the shapes to build on.
- **senior-dev** (2026-09-04T00:20:00.000Z): Added `relationship-cycle` at packages/core/src/validate.ts:502-559 with the rotation helper at packages/core/src/validate.ts:566-572, and `partnership-backed` at packages/core/src/validate.ts:600-623 over the crossing helper at packages/core/src/validate.ts:575-596. Two notes on judgement calls. The cycle rule reports one diagnostic per back edge, the same shape `aggregate-tree` uses at packages/core/src/validate.ts:280-302, and rotates each ring so its lowest context id leads, so the message and the ref do not depend on which context the walk entered from. `partnership-backed` counts a policy subscribed to the other context's event as traffic, because `separate-ways` already treats a subscription as the same exchange as a consumption (packages/core/src/validate.ts:696-716); without that a partnership backed only by events would warn falsely.
- **senior-dev** (2026-09-04T00:25:00.000Z): the lead added four more items from the fifth review run, now on the checklist above: `entity-identity`, `reaction-cycle`, dropping the type-name half of `attribute-relation-coherence` per decision 15, and letting a crossing consumable's `schema` back a `published-language` role.
- **senior-dev** (2026-09-04T01:05:00.000Z): All five rules are in and green. `entity-identity` at packages/core/src/validate.ts:165-186, `relationship-cycle` at packages/core/src/validate.ts:608-643, `partnership-backed` at packages/core/src/validate.ts:674-698 over the crossing helper at packages/core/src/validate.ts:645-668, `reaction-cycle` at packages/core/src/validate.ts:1068-1100 over the step function at packages/core/src/validate.ts:1049-1058, and `disposition-needs-comment` at packages/core/src/validate.ts:1154-1170 with the label and ref helpers at packages/core/src/validate.ts:1128-1148. Both cycle rules share one walker, `cyclesOf` at packages/core/src/validate.ts:241-278, which reports one ring per back edge (the shape `aggregate-tree` already uses) and rotates each ring to its lowest key so neither the message nor the ref depends on where the walk started. `attribute-relation-coherence` lost its type-name half at packages/core/src/validate.ts:369-385, and `carriesUpstreamRole` at packages/core/src/validate.ts:541-547 lets a crossing consumable's schema back a `published-language` role. Catalogue entries for all five are alongside their rules; tests are packages/core/src/validate.test.ts:1044-1420 (26 new cases) and the completeness fixture at packages/core/src/rule-catalog.test.ts:130-135 now plants a ring, an unbacked partnership and a disposition with no comment. `cd packages/core && npm run build && npx vitest run` is green at 277.
- **senior-dev** (2026-09-04T01:10:00.000Z): One bug worth recording, because the first version of `relationship-cycle` shipped it and NorthBank caught it. Walking the relationships as the graph's nodes finds rings of relationships, and a ring of relationships is not always a ring of distinct contexts: NorthBank reported `"Accounts" -> "Cards" -> "Fraud" -> "Cards" -> "Accounts"`, a closed walk threading Cards twice that says nothing the two real rings do not. Walking the contexts instead makes every ring simple by construction (packages/core/src/validate.ts:608-618); the ring still reports at a relationship's ref so the reader lands on something editable. Regression test at packages/core/src/validate.test.ts:1180-1196.
- **senior-dev** (2026-09-04T01:15:00.000Z): Fixture and model defects the new rules found, all fixed as real defects rather than declared away. The rich fixture's Order Line had no identity and its Reporting/Ordering partnership had no traffic at all, both at packages/core/src/makeTestWs.ts:174-177 and packages/core/src/makeTestWs.ts:287-305; backing the partnership needed a Reporting application service and one consumption each way, the outbound one behind an anti-corruption layer because Reporting is a big ball of mud. That moved counts in two neighbouring suites: packages/core/src/derived-maps.test.ts:12-32 and packages/core/src/derived-maps.test.ts:128-129, and packages/core/src/lookups.test.ts:207-210, where the renamed-key test now has to drop the consumption that points into the context it renames. In the models, eight child entities had no identity attribute (models/rivermart/src/workspace.ts:649, 761, 931, 1444, 1492, 1749, 1755, 2010 — Stop takes its route sequence as its identity, the rest an explicit id) and Petstore's DeliveryAttempt took the attempt number its own description already said it was counted by, at models/petstore/src/workspace.ts:771-774.
- **senior-dev** (2026-09-04T01:20:00.000Z): Docs and skill done: apps/docs/docs/3-core/4-validation.md:18, 21, 23-25, 38 and 41 carry the five new rows and the two amended ones, and `npm run generate` in packages/skill rewrote packages/skill/skill/references/validation-rules.md with all five (skill suite green, 26). Also ran the pages suite the brief asked about even though it is out of scope for edits: `cd packages/pages && npm run build && npx vitest run --config vitest.config.ts` is fully green, 660 passed across 94 files, so whatever was red after card 47 is red no longer.
- **senior-dev** (2026-09-04T01:25:00.000Z): QUESTION for lead. The two cycle-and-partnership rules find eleven things in the reference models that I do not think are mine to decide, because every fix rewrites a strategic map rather than filling in a missing attribute. `partnership-backed`: Petstore Sales/Fulfilment, NorthBank Lending/Credit Decisioning, RiverMart Search/Advertising, StreamLine Playback/Devices — each has traffic one way only. `relationship-cycle`: RiverMart 6 rings (Order Management/Payments, Order Management/Warehouse, Fraud/Order Management, Fraud/Seller Onboarding, plus Last Mile -> Order Management -> Warehouse -> Last Mile and Fraud -> Seller Onboarding -> Offers -> Order Management -> Fraud), NorthBank 2 (Cards/Fraud and Accounts -> Cards -> Fraud -> Accounts), StreamLine 1 (Ads Tier/Playback). Two options each way. For a partnership: add the consumable the quiet direction is missing, which invents model content, or demote the relationship to customer-supplier, which contradicts the release-train evidence the models write down — Petstore's own comment at models/petstore/src/workspace.ts:1144 claims traffic crosses both ways, and that claim is simply false today, so something there has to change either way. For a ring: most are a mutual pair of opposing `upstream-downstream` relationships, which DDD would call a partnership, but converting them throws away the upstream and downstream roles those relationships carefully declare and silences `role-coherence` between them, so the model would say less than it does now; the two longer rings cannot be fixed that way at all and need an edge inverted or dropped. Worth saying plainly: a mutual pair of directed relationships is a common and not obviously unhealthy shape, and if you would rather `relationship-cycle` only flagged rings of three or more contexts, that is a change to the rule you specified and yours to make, not mine. I recommend deciding the rule's scope first, then I will do whichever model surgery follows in one pass. Blocked on this; everything else on the card is done and green.
- **senior-dev** (2026-09-04T02:10:00.000Z): Decision 20 read and applied. `relationship-cycle` now walks only the steps a call carries — a directed relationship counts as an edge when an operation of the upstream context is consumed by the downstream one (packages/core/src/validate.ts:599-621, over the new `crossingsBetween` helper at packages/core/src/validate.ts:586-597). Doc comment, message and catalogue entry all say so: the message is now "Calls run in a cycle: ..." and ends with the repair the decision names, turning one call on the ring into an event. Tests rewritten at packages/core/src/validate.test.ts:1108-1265 around a `step` helper that backs each declared relationship with a call or an event, including the pair the decision asks for — the same three-context ring validates clean on events and warns on calls — plus a ring with one event step among calls, and a declared relationship no traffic backs at all. The completeness fixture needed a call each way to keep tripping the rule (packages/core/src/rule-catalog.test.ts:137-146).
- **senior-dev** (2026-09-04T02:15:00.000Z): The ruling wiped out every ring in the reference models: all nine were carried by events, which is the decision's own argument holding up under test. RiverMart therefore has no ring of calls to keep as a deliberate finding, so I did not invent one.
- **senior-dev** (2026-09-04T02:20:00.000Z): The four one-way partnerships, each settled by its own model's prose as the ruling directs. Petstore's ApplicationSubmitted equivalent was `ShipmentDelivered`, a published-language event with a schema that nobody outside Fulfilment consumed; Sales' OrderApp now consumes it (models/petstore/src/workspace.ts:862-869) and the partnership's description and comment at models/petstore/src/workspace.ts:1131-1152 now say what actually crosses, because the old comment claimed a two-way crossing the model did not have. NorthBank was the same shape and even more explicit: `ApplicationSubmitted`'s schema is described as "What decisioning receives" and the event as "decisioning runs", yet no consumption existed, so Credit Decisioning now consumes it (models/northbank/src/workspace.ts:2131-2136). Petstore validates clean again and NorthBank is back to its three declared diagnostics.
- **senior-dev** (2026-09-04T02:25:00.000Z): RiverMart and StreamLine went the other way, and here I had to depart from the letter of the ruling — flagging it rather than quietly choosing. Neither model's prose names a consumable the quiet direction takes: RiverMart's Search/Advertising says only "the results page is one product owned by two teams" and StreamLine's Playback/Devices only "one release train ... certified in the same lab run". Both are organisational ties, not modelled dependencies, so the ruling says demote to customer-supplier. That is not available: `assertStressTestWorkspace` at models/_shared/src/index.ts:84-91 asserts each stress model carries all five relationship types, and each of these models has exactly one partnership, so demoting either one breaks a different declared invariant. Inventing the missing consumable would have been the other way out, and the ruling explicitly forbids that where the prose is silent. So both keep the claim and carry the warning as a deliberate finding — the mechanism the lead offered RiverMart, extended to StreamLine for the same structural reason. DELIBERATE comments at models/rivermart/src/workspace.ts:2337-2343 and models/streamline/src/workspace.ts:1985-1990, `deliberate` entries at models/rivermart/src/workspace.test.ts:19 and models/streamline/src/workspace.test.ts:19 with the suite docblocks updated, and section 7 of both DISCOVERY.md files now lists four findings instead of three, each saying plainly that on the traffic alone the relationship is customer-supplier and whose call it is to change.
- **senior-dev** (2026-09-04T02:30:00.000Z): Clean-code sweep. Three copies of the same "what does `to` consume from `from`" filter had accumulated — the new cycle rule, the new partnership rule and the older `relationship-roles-backed` — so all three now share `crossingsBetween` at packages/core/src/validate.ts:586-597, which also let `relationship-roles-backed` drop its own materialised consumption list. The two cycle rules already share `cyclesOf`. Two stale comments in the completeness fixture went with it: one still promised a "wrong type name" case that decision 15 removed, the other described the new call ring backwards. Biome and tsc are clean on every file I touched; the only two lint errors left in core are the pre-existing `noThenProperty` pair in packages/core/src/workspace.ts, which is out of scope.
- **senior-dev** (2026-09-04T02:35:00.000Z): Full run, each command inside its own package. core `npm run build` + `npx vitest run` 282 passed. petstore 18, northbank 3, rivermart 3, streamline 3, and each `npm run build` regenerates docs/ and .ods/ emitting exactly its declared diagnostics — petstore 0, northbank 3, rivermart 3, streamline 4. skill regenerated, 26 passed. pages `npm run build` + 660 passed across 94 files. doc is 1 failed | 23 passed, and that one is not mine: the inline snapshot at packages/doc/src/index.test.ts:377 is missing two `shipment_app` pages that develop's own committed models/petstore/docs and .ods already contain, so it was stale before this card — card 52's ground, and packages/doc is out of scope here.
