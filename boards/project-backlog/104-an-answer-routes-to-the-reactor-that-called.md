---
column: review
labels: [backend, ddd, breaking]
priority: high
agent: senior-developer-deep
live: false
clean-code-swept: true
updatedAt: 2026-09-10T04:40:00.000Z
---
# An answer routes to the reactor that called; the rules the seventh review reproduced; a local value object's line is derived

The architect's seventh review, each item probed. `routesTo` returns every caller named on every consumption of an operation in a context, so two processes in one context that both call one operation each receive the other's answer, the flow map draws it wrong and `reaction-cycle` reports a ring that does not exist; `hearsAnswerOf` lets a non-caller hear an answer when one consumption exists. A process that raises its own start event is exempt as a lifecycle though each pass spawns a new instance. `relationship-cycle`'s fix text says declare a partnership, and the rule does not read one. `relationship-declared` still says "no relationship says how they stand" on a declared separate-ways pair for consumptions and subscriptions. A precondition may reach an answer it cannot have seen. Every local value-object attribute must be restated as a `uses` relation while a borrowed one's line is derived. And a JSON context must write eight empty maps.

## Checklist

- [x] `routesTo` returns only the callers among the operations the waiting reactor issues (or the reactor itself when named in `by`); `hearsAnswerOf`'s single-consumption clause applies only when that consumption names no `by`; the review's two-processes-one-operation probe validates clean and the flow map draws each answer to its own reactor; decision 23 amended by the lead
- [x] `isProcessLifecycle` does not exempt a ring that re-enters the process through a `starts` trigger; test with `starts(Started).issues(Restart raising Started)`
- [x] `relationship-cycle` treats two contexts joined by a partnership as one for the walk, so a declared partnership clears the ring the fix text promises it clears; test with the review's probe P6
- [x] `relationshipJoins` reports a consumption or subscription across a declared separate-ways pair under `separate-ways` only, never under `relationship-declared`; NorthBank's deliberate quick-quote case reports once
- [x] `guardedSchemas` gives a precondition the request and what it composes, and a postcondition the request, the answer and the rejections; the rule texts and decision 19 say the same (the lead amends 19)
- [x] `attribute-relation-coherence` no longer demands a declared `uses` for a local value-object attribute: the line is derived from the attribute as it is for a borrowed one, a declared `uses` stays legal and adds a label or cardinality, and the rule checks only that a declared relation agrees with its attribute; the relation map draws derived lines; decision 16 noted by the lead
- [x] Every map on `BoundedContextSchema`, `AggregateSchema` and `ServiceSchema` and `relations` on entities and value objects are optional in the schema and default to empty on load; the JSON schema regenerated; the extension's minimal example and the skill's minimal example shrink accordingly
- [x] `mud-needs-acl`'s fix text for an identity says an anti-corruption-layer consumption from the mud clears it, which is what the rule reads
- [x] `bash scripts/verify-all.sh` green; diagnostics per model unchanged or explained; the review's probes P3, P4, P5, P6, P16 rerun

## Gates

- [x] clean-code-swept — swept the diff (22 files, 867 lines added, 377 removed) against the eight principles. Four things it changed. The `by`-filtering that `hearsAnswerOf` and `routesTo` each did for themselves, with different answers, is one `callersFor(reactor, call)` in `reaction-walk.ts`; the two functions still answer different questions and still say so, but they can no longer disagree about who a caller is, which is what the defect was. The attribute↔relation pairing was about to exist twice, in the rule and in the relation map, so it is `Attribute.drawnBy` in `workspace.ts` and both read it. `ReactionChain.stepsFrom` grew a third concern when the answer steps had to be appended for every kind of node, so what a node does of itself is `ownSteps` and `stepsFrom` adds the answers to it. And the loader's twenty `Object.entries(x)` over now-optional maps are `entriesOf(x)`/`listOf(x)`, one place that says an absent collection is an empty one, rather than twenty `?? {}`. Three things it looked at and left. `attributeRelationCoherence` computes `usesOfValueObject(...)` for the count in its ambiguity message and `drawnBy` recomputes the same list for the pairing; passing the count out of `drawnBy` would widen a model getter to serve one message, and the two uses are different questions. `routesTo` now returns `Reactor[]` rather than `Consumable[]` for the sake of a `by` that names the reactor itself, which `consumption-by-operation` reports as an error — the walk reads what the author wrote rather than dropping the answer, and the cost is one map key type. And `movingAsOne` shares one array between a partnership's members instead of a union-find, because the group is read once per relationship and once per ring and the largest reference model has seventeen contexts. (senior-developer-deep, 2026-09-10T04:30:00Z)
- [x] verify-all — `bash scripts/verify-all.sh` exit 0: core 654, graphviz 28, doc 31, skill 26, northbank 3, schema comparison (petstore vs core dist) match, petstore 20, rivermart 3, streamline 3, models/_shared 9, pages 761, apps/docs 23, apps/ods-vscode 15, pages e2e (full suite) passed (senior-developer-deep, 2026-09-10T04:35:00Z)

## Comments

- **the lead** (2026-09-10T01:00:00.000Z): senior-developer-deep, after card 103 lands; `feat!`.
- **senior-developer-deep** (2026-09-10T02:40:00.000Z): Picked it up; worktree reset to origin/develop at d14f478 ("cards 103 and 105 done; decision 27 notes the merged rule; 104 dispatched"), installed. Read the card, the 2026-09-10 notes on decisions 03, 13, 16, 22 and 26 and the amendments on 19, 23 and 27, and the five core files the brief names. Baseline diagnostics before any change: `petstore: 0`, `northbank: 4`, `rivermart: 2`, `streamline: 4`. Then reproduced each item as a probe against the built core dist, and only then changed anything. What each probe did before: P3, two processes of one context each calling one scorer through an operation of their own, drew **four** answer edges where there are two calls — `AskForMotor -> Motor quote`, `AskForMotor -> Home quote`, `AskForHome -> Motor quote`, `AskForHome -> Home quote`. P4, one consumption naming `by: [AskForMotor]` and a second process that issues something else, was silent. P5, `starts(Started).issues(Restart raising Started)`, was silent. P6, two contexts calling each other with a partnership declared beside the two directed relationships, warned. P7, NorthBank's quick-quote shape, reported twice, once truthfully under `separate-ways` and once falsely under `relationship-declared`. P8, a precondition naming an attribute of what its guard returns, was accepted. P9, an entity with an attribute typed by its own context's `Money` and no `uses` relation, warned and drew no line at all.
- **senior-developer-deep** (2026-09-10T03:10:00.000Z): Items 1 and 2, the answer's route and the lifecycle. `routesTo` (`packages/core/src/reaction-walk.ts:325-346`) keeps only the callers that are this reactor's — an operation it issues, or the reactor itself where a `by` names it — through the shared `callersFor` (`packages/core/src/reaction-walk.ts:282-288`), and its silent-inference clause now asks for the silence `hearsAnswerOf` asks for: a lone consumption that names its caller has said who called, so it routes by what it names or not at all. `hearsAnswerOf`'s third clause reads `calls.length === 1 && calls[0].by.length === 0` for the same reason (`packages/core/src/reaction-walk.ts:261-269`). One judgement call, flagged rather than assumed: the card's parenthetical "(or the reactor itself when named in `by`)" cannot be honoured by a function returning `Consumable[]`, since a reactor is not a consumable, so `routesTo` returns `Reactor[]` and `ReactionChain` keys its answer steps by `Reactor` (`packages/core/src/reaction-walk.ts:115-124`, `packages/core/src/reaction-walk.ts:171-180`). That case is only reachable on a model `consumption-by-operation` already reports as an error; the walk draws the answer as the process's own loop rather than losing it, and the loop is exempt as a lifecycle. For item 2, `isProcessLifecycle` (`packages/core/src/validate.ts:3811-3822`) now asks which step closes the ring: `reEntersWhileAlive` (`packages/core/src/validate.ts:3844-3852`) says a deadline, an event or answer the process waits on with `on`, and nothing else. A `starts` trigger begins another instance every pass, so no instance's state holds the ring together and the exemption's reason is gone. NorthBank's onboarding and RiverMart's checkout are untouched — both re-enter through `on` — and the two probes now behave as the card says.
- **senior-developer-deep** (2026-09-10T03:35:00.000Z): Items 3 and 4, the partnership and separate ways. `relationship-cycle` contracts each partnership into one node for the walk (`movingAsOne`, `packages/core/src/validate.ts:2259-2273`, transitive, so three pairwise partners are one release train), a directed relationship inside a group is not a step, and the message names a group as `"A" and "B"` with a clause saying they are partners and move as one context here. QUESTION for lead, decided narrowly and flagged: contraction clears a ring **that is nothing but the partnered pair**, which is the fix text's own promise and probe P6; it does not clear a longer ring the pair is only part of, because merging A and B leaves the merged unit calling C and C calling it, which is a real mutual dependency. I have said so in the rule's `why` and reworded the fix from "declare a partnership where two of them really do move as one" to "between two neighbours on the ring that really do move as one, which makes them one context here", so the text no longer promises more than the walk does; there is a test for each of the two shapes. If you meant a partnership anywhere on a ring to clear the whole ring, that is a one-line change and the fix text goes back. For item 4, `relationshipJoins` (`packages/core/src/validate.ts:1763-1769`) now counts separate ways: the rule asks whether the pair has said how they stand, and a pair declaring separate ways has said it. The crossing is `separate-ways`'s error, at the same element, and it says what is actually wrong. NorthBank's `deliberate` list loses its `relationship-declared` entry and its comment (`models/northbank/src/workspace.test.ts:14-21`); the model itself is unchanged.
- **senior-developer-deep** (2026-09-10T03:55:00.000Z): Items 5 and 6, the precondition's reach and the derived line. `guardedSchemas` (`packages/core/src/validate.ts:1100-1111`) adds the guarded operation's `returns` and `rejects` only for a postcondition; a precondition gets the request and everything it composes, which is all that exists when it runs. The refusal message (`packages/core/src/validate.ts:1151-1165`) says which of the two it is refusing, and the postcondition is asked first so the sentence matches the reach where a model has claimed both flags. The catalogue's `invariant-in-aggregate` and `invariant-in-context` summaries, whys and fixes, and `postcondition-names-operation`'s fix, say the same thing now; the test that asserted a precondition could reach the answer asserts the refusal. For item 6, the pairing of an attribute with its `uses` relation is `Attribute.drawnBy` (`packages/core/src/workspace.ts:2629-2641`), and both the rule (`packages/core/src/validate.ts:893-930`) and the relation map read it: the map's `borrowings` is `derivedUses` (`packages/core/src/relation-map.ts:168-175`, `packages/core/src/relation-map.ts:293-307`) and now collects any attribute whose line no relation draws, foreign or local, so a local value's box is drawn plain and a borrowed one keeps its `foreign_valueobject` mark. The rule no longer asks for the declaration; it checks that a declared relation agrees with its attribute, still reports the ambiguous pairing (the map cannot draw what it cannot pair either), and still reports a relation no attribute is typed by. As the brief says, none of the four models needed a change: every `uses` they declare is legal, carries a label and is still drawn, and none of them gained or lost a diagnostic.
- **senior-developer-deep** (2026-09-10T04:15:00.000Z): Items 7 and 8, and the surfaces. The eight maps on `BoundedContextSchema`, the three on `AggregateSchema`, `provides` on `ServiceSchema` and `relations` on entities and value objects are optional; the loader reads every collection through `entriesOf`/`listOf` (`packages/core/src/workspace-from-schema.ts:48-64`), so "left out" and "written empty" cannot load differently, and the JSON schema is regenerated (`BoundedContextSchema` now requires `description, name, subdomains`). `toSchema` still writes the maps, so no reference model's `.ods` file moves. The skill's `packages/skill/skill/examples/minimal.ods.json` loses the context's `invariants`, `policies` and `processes`, the value object's `relations` and the entity's whole `relations` block — the attribute alone now says the order holds an address — and `packages/skill/skill/examples/minimal.workspace.ts:44-47` loses the matching `uses` call, with the equality test comparing the two through the loader (`packages/skill/src/bundle.test.ts:238-252`), which is what makes the file's silence and the DSL's empty maps the same model. `references/json-mode.md`'s "every required field is present even when empty" is now the truth. Item 8 needed no code: `mud-needs-acl` already clears an identity on any anti-corruption-layer consumption from the mud, and the fix text said so narrowly, in terms of "an operation this context translates"; it now says what the rule reads — a consumption of that context's consumables, anywhere in this one, declaring the layer (`packages/core/src/validate.ts:4399`). The extension has no minimal example of its own: `ods.createWorkspace` writes `new Workspace(...).toSchema()`, which holds no contexts, and it installs the skill bundle for the rest, so the skill's example is the one that shrank.
- **senior-developer-deep** (2026-09-10T04:38:00.000Z): `bash scripts/verify-all.sh` green end to end, exit 0. Summary: core 654 tests passed, graphviz 28, doc 31, skill 26, northbank 3, schema comparison (petstore vs core dist) match, petstore 20, rivermart 3, streamline 3, models/_shared 9, pages 761, apps/docs 23, apps/ods-vscode 15, pages e2e (full suite) passed, 297 e2e specs, no retries. `npx tsc --noEmit` clean in core, graphviz, doc, skill, pages, all four models, models/_shared, apps/docs and apps/ods-vscode; `npm run check` in pages clean, 939 files, 0 errors, 0 warnings; biome clean on every file I touched. Diagnostics per model, against the baseline at the top of this card:

```
petstore:   0 diagnostic(s)                                    unchanged
rivermart:  2 diagnostic(s)                                    unchanged
  [error]   aggregate-root  Wishlist has 2 root entities
  [error]   cross-aggregate-reference  Cart includes WishlistItem
streamline: 4 diagnostic(s)                                    unchanged
  [error]   internal-consumable  RecommendationsAPI consumes BookmarkUpdated
  [error]   internal-consumable  Freshen continue watching reacts to BookmarkUpdated
  [error]   schema-context  PlaybackStarted carries TitleRef from Catalogue
  [warning] policy-complete  Recertify on SDK release issues no command
northbank:  3 diagnostic(s)                                    was 4
  [error]   separate-ways  Branch & Contact Centre consumes Decide from Credit Decisioning
  [error]   consumable-kind  Escalate arrears issues ArrearsNoticeIssued
  [warning] context-serves-subdomain  Identity & Access serves no subdomain
```

  The one change is checklist item 4: the quick-quote consumption reported twice and now reports once, under the rule that says what is wrong with it. The probes, rerun against the built core dist:

```
P3 (two processes, one operation; every rule): 0 diagnostic(s)
  answer edge: AskForMotor -> Motor quote (Verdict)
  answer edge: AskForHome -> Home quote (Verdict)
P4 (a second process the lone consumption does not name): 1 consumable-kind error
  Process "Renewal" waits for "Score returns Verdict", but nothing says process
  "Renewal" made that call ...
P5 (starts(Started).issues(Restart raising Started)): 1 reaction-cycle warning
  "Started" -> "Run" -> "Restart" -> "Started"
P6 (a ring of two with a partnership declared): 0 relationship-cycle diagnostic(s)
P7 (NorthBank's quick-quote shape): 1 diagnostic, separate-ways, at the consumer
P8 (a precondition naming what its guard returns): 1 invariant-in-context error
P9 (a local value-object attribute, no uses relation): 0 coherence diagnostic(s)
  edge: Order -uses-> Money "total"
P16 (a context with none of the eight maps): loaded, sizes 0,0,0,0,0,0,0,0
  BoundedContextSchema required: description, name, subdomains
  AggregateSchema required: consumes, description, name
  ServiceSchema required: consumes, description, name, type
  EntitySchema required: attributes, description, name
  ValueObjectSchema required: attributes, description, invariants, name
```

- **senior-developer-deep** (2026-09-10T04:40:00.000Z): Three things for the lead, none of them blocking. (1) The card leaves `consumes` on `AggregateSchema` and `ServiceSchema`, and `attributes` and `invariants` on `ValueObjectSchema`, required, so a hand-written aggregate still writes `"consumes": []` and a value object still writes `"invariants": {}` — see the P16 output above. I did exactly what the checklist says and did not widen it; say the word and the arrays and the value object's two maps go the same way, which is a schema change and a regenerated JSON schema and nothing else. (2) Decisions 19 and 23 are yours to amend and have no 2026-09-10 entry yet; decision 16's note, decision 03's correction and decision 22's correction are already in and match what shipped. (3) `apps/docs/docs/3-core/4-validation.md` is a hand-written table and six of its rows now describe the old rules: line 28 (`attribute-relation-coherence` "has the matching `uses` relation"), 32 and 33 (a precondition reaching "returns or rejects with"), 38 (`relationship-declared` "separate ways answers nothing"), 40 (`relationship-cycle`, no partnership), 73 (`reaction-cycle`, no starting-event case). The path is outside my brief so I have left it alone; it is six rows and I can take it in a follow-up if you want it in this card.
