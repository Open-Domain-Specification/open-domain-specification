---
column: review
labels: [models, ddd]
priority: medium
agent: developer
live: false
clean-code-swept: true
updatedAt: 2026-09-06T03:20:00.000Z
---
# NorthBank tells the truth about the scheme; the reference set explains its Money

The architect's seventh review. NorthBank's `SubmitToScheme` declares `returns` and `rejects` and says "await the response" while the surrounding text says the scheme answers on its own timings through events and a policy, so the reaction walk draws two disconnected chains; the honest shape is the asynchronous exchange decision 13's second note names. CardCo carries an invented `RequestAuthorisation` operation, the inside of somebody else's machine. Onboarding says it waits for the screening engine's answer but `ScreenParty` returns nothing and `VerifyCustomer` is issued by nothing; `LinesReconcileToLedger` is described as a precondition of filing and carries neither the flag nor the guard. And RiverMart and StreamLine each declare a `Money` per context through a helper without their discovery notes saying why those teams own one each while NorthBank's share a kernel.

## Checklist

- [x] NorthBank: `SubmitToScheme` becomes the returns-less submission it is; the scheme's `SchemeSettlementConfirmed` and `SchemeRejected` events, already declared on the external Payment Scheme, are what the instruction lifecycle waits on; the policy and `RecordSchemeResponse` that bridged the fake answer come out or become the reaction; DISCOVERY.md says the exchange is asynchronous
- [x] CardCo's `RequestAuthorisation` comes out; its feed consumes `AuthoriseCard` with no `by`, which the rules allow for a consumer with no operations
- [x] Onboarding: `ScreenParty` returns the verdict the process waits for, or the process waits on `PartyMatched` and its description stops saying it waits for an answer; `VerifyCustomer` is issued by the step that verifies or comes out; `LinesReconcileToLedger` carries `precondition: true` and names `FileReturn`
- [x] RiverMart's and StreamLine's DISCOVERY.md say why each context owns its own Money (a marketplace's contexts price in different currencies and rounding rules; a streaming service's billing and disc rental keep separate ledgers), or the models share one where the interviews support it
- [x] `.ods/` regenerated; each model's diagnostics unchanged or explained; `bash scripts/verify-all.sh` green

## Gates

- [x] clean-code-swept — swept the diff (5 files) against the eight principles. NorthBank's `workspace.ts` change is entirely subtractive plus one reattachment: `SchemeSettlement`/`SchemeRefusal`/`RecordSchemeResponse`/`Match the scheme's response` and `RequestAuthorisation` come out rather than being replaced by new machinery; the two additions (`paymentsApp.consumes(schemeSettlementConfirmed/schemeRejected)`, `paymentSchemeBC.upstreamOf(paymentsBC, ...)`) are the same shape as an existing pattern two lines above them (`onboardingApp.consumes(partyMatched, {...})` beside `screeningVendorBC.upstreamOf(sanctionsBC, ...)`), no new abstraction. `verifyCustomer` and `fileReturn` are plain `const` captures of operations the DSL already returns, matching every other named operation in the file (`sendToScheme`, `postSettlement`, etc.); moving `LinesReconcileToLedger` below `fileReturn`'s declaration is a reorder forced by the reference, not a rewrite. Four `DISCOVERY.md` edits are prose only, no code. (developer, 2026-09-06T03:19:00Z)
- [x] verify-all — `bash scripts/verify-all.sh` exit 0: core 632, graphviz 27, doc 31, skill 26, northbank 3, schema comparison (petstore vs core dist) match, petstore 20, rivermart 3, streamline 3, models/_shared 9, pages 757, apps/docs 23, apps/ods-vscode 15, pages e2e (full suite, 297) passed (developer, 2026-09-06T03:20:00Z)

## Comments

- **the lead** (2026-09-10T01:00:00.000Z): developer, now; models only. Card 103 touches core in parallel; do not touch `packages/`.
- **developer** (2026-09-06T02:35:00.000Z): Picked it up. `git fetch origin develop && git reset --hard origin/develop` landed on `30e1191` ("docs: architect round 7 rulings; cards 104 and 105; decisions 03, 13, 15, 16, 22, 26 corrected or noted") — matched on the first fetch. `npm install` clean; `packages/core`, `packages/graphviz` and `packages/doc` all build clean. Read the card, decision 13 in full (the 2026-09-10 note is the one this card names: "an asynchronous request whose reply arrives later, correlated, is modelled as the returns-less call plus the events the provider raises, which the caller's process waits on"), decisions 17, 19, 23 and 28 with every amendment, NorthBank's `src/workspace.ts` for Payments Hub, Scheme Gateway, Payment Scheme, CardCo, Cards, Customer onboarding and Regulatory Reporting, its `DISCOVERY.md`, and RiverMart's and StreamLine's `money()` helpers and `DISCOVERY.md`. Not touching `packages/`; card 103 is the lead's concern.
- **developer** (2026-09-06T02:55:00.000Z): NorthBank, item 1. `SubmitToScheme` had `returns: SchemeSettlement` / `rejects: [SchemeRefusal]` while its own comment and the "Instruction lifecycle" description said the scheme answers "on its own timings" — the two disconnected chains the card names. Made it returns-less (just `schema: submissionSchema`); `SchemeSettlement` and `SchemeRefusal` come out (they existed only to be the fake answer); `RecordSchemeResponse` and the "Match the scheme's response" policy come out (they existed only to bridge the fake answer to the waiting call — with no answer to wait on, there is nothing to bridge); `schemeApp`'s two `consumes` of the scheme's events come out with them. The process now waits directly: `.on(schemeSettlementConfirmed, schemeRejected)` in place of `.on(submitToScheme.returned(), submitToScheme.rejected(schemeRefusalSchema))`. That crosses from Payment Scheme (external) to Payments Hub, so it needs its own consumption and relationship, the same shape as onboarding's `partyMatched` (`onboardingApp.consumes(partyMatched, {pattern: "anti-corruption-layer"})` beside `screeningVendorBC.upstreamOf(sanctionsBC, ...)`): added `paymentsApp.consumes(schemeSettlementConfirmed, {pattern: "conformist"})`, same for `schemeRejected`, and `paymentSchemeBC.upstreamOf(paymentsBC, {upstreamRoles: ["published-language"], downstreamRoles: ["conformist"]})` in place of the now-unbacked `paymentSchemeBC.upstreamOf(schemeBC, ...)`. `SchemeMessage` (the aggregate `RecordSchemeResponse` sat on) stays — it is still a legitimate record of one message to or from the scheme per its own description and DISCOVERY, just with no operation on it now, which no rule requires. Added `## Revision (card 105)` to NorthBank's `DISCOVERY.md` covering all four fixes, and corrected the event-storming table's `SchemeSettlementConfirmed / Rejected` row and removed the `SchemeSettlement / SchemeRefusal (answers, not events)` row, since that table describes the current shape rather than a point-in-time interview quote (the "Recorded as" paraphrases under the original interviews are left as the historical record, per the file's own convention — see cards 92/95/98's treatment of the same sections).
- **developer** (2026-09-06T03:00:00.000Z): NorthBank, item 2. `RequestAuthorisation` named a step inside CardCo's own machine (a merchant asking CardCo, CardCo asking the issuer) invented only so the consumption had something to put in `by`. Removed it; `cardCoFeed.consumes(authoriseCard, {})` now carries no `by` — `cardCoFeed` provides nothing, so there is nothing to name (decision 17's amendment: "a context that only subscribes needs an application service to hold the subscription, a node that provides nothing"). No rule in `packages/core/src/validate.ts` requires `by` on a consumption of an operation, confirmed by running the build clean with it absent.
- **developer** (2026-09-06T03:08:00.000Z): NorthBank, item 3. `ScreenParty` was already returns-less and already raised `PartyMatched`, which the process already waited on (`.on(partyMatched)`) — the actual defect was the process's own description saying it "waits for the engine's answer", which claims a return that was never there. Reworded to "waits on the engine's verdict ... a clean screening lets KYC verify the customer". `VerifyCustomer` raised `CustomerVerified` (the process's own `ends` event) but nothing issued it; captured it as `const verifyCustomer` and added it to the process's `.issues(...)`. `LinesReconcileToLedger` carried the precondition's own reasoning in a code comment ("the ledger is another context, so this is a precondition of filing") but neither the flag nor a named guard, and constrained `reportLine, regReturn` instead of the operation; moved it below `FileReturn`'s declaration (captured as `const fileReturn`), added `precondition: true`, and changed `.constrains(reportLine, regReturn)` to `.constrains(fileReturn)` — decision 19's original text ("`constrains` may also name a consumable of the same aggregate") already permits this without needing the later service-guard amendments, since `FileReturn` is on `returnAgg` itself.
- **developer** (2026-09-06T03:12:00.000Z): Item 4, RiverMart and StreamLine. Checked both `DISCOVERY.md` and `BRIEF.md` for any statement backing "different currencies per context" for RiverMart's five `money()` callers (Offers, Cart, Order, Payments, Ads): found none — no mention of currency, territory or international sellers anywhere in either file. For StreamLine's three (Studio, Licensing, Billing): found real grounding — Licensing's deals are per-territory ("a deal is with a licensor, for a term... a territory, a start, an end"), and Billing's invoice is per-household-country with the disc business's charge folded in as a line, not carried in its own currency. Rather than either fabricate an unsupported RiverMart narrative or force a real refactor onto five contexts and their relationships (out of proportion to "smallest change" for a documentation gap), I wrote each `DISCOVERY.md` a `## Revision (card 105)` explaining why the existing five/three-way split stands: each context prices a genuinely different thing in its own aggregate's language (RiverMart: a seller's live listing vs. a cart snapshot vs. a settled order total vs. a processor amount vs. an auction's fractional-cent bid ceiling; StreamLine: a studio's own budget vs. a per-territory licensing fee vs. a consumer's own-country invoice), and decision 16's default — a value object belongs to its own context — covers all of them; neither model has a real shared-library coupling like NorthBank's `@northbank/money`, so neither gets a kernel. Flagging this explicitly below since it's the one item on this card that is editorial judgement rather than a straightforward reading of an existing quote.
- **developer** (2026-09-06T03:20:00.000Z): `.ods/northbank.json` regenerated via `node src/generate.ts` inside `models/northbank`; RiverMart's and StreamLine's `.ods/` are untouched since their `src/workspace.ts` did not change. Diagnostics compared against each model's own DISCOVERY "deliberate diagnostics" list: NorthBank 4 (unchanged: `relationship-declared` warning + `separate-ways` error on Branch & Contact Centre/Decide, `consumable-kind` error on Escalate arrears, `context-serves-subdomain` warning on Identity & Access), RiverMart 2 (unchanged: `aggregate-root` + `cross-aggregate-reference` on Wishlist/Cart), StreamLine 4 (unchanged: two `internal-consumable` on Recommendations/BookmarkUpdated, `schema-context` on PlaybackStarted, `policy-complete` on Recertify on SDK release). `bash scripts/verify-all.sh` green end to end (playwright hit "port 4173 already used" on the first run; waited for the port to free and reran clean — see Gates). `npx biome check` clean on `models/northbank/src/workspace.ts`; the `DISCOVERY.md` files and `.ods/northbank.json` are not biome's (confirmed petstore's own generated `.ods/petstore.json` fails the same formatter check pre-existing, unrelated to this change). `git diff --numstat`: `models/northbank/.ods/northbank.json`, `models/northbank/DISCOVERY.md` (+47/-2), `models/northbank/src/workspace.ts` (+53/-91), `models/rivermart/DISCOVERY.md` (+21), `models/streamline/DISCOVERY.md` (+20). Nothing in `packages/`, `pages/`, or any file outside this card's four models touched. Card moved to `review`, `live: false`, `clean-code-swept: true`; not committed yet per the brief — ready for a `fix(models):` commit on request, the lead merges. One flag for review, not a blocker: item 4 is editorial judgement rather than a correction of a stated fact — RiverMart's five-way `money()` split is unsupported by anything in its interviews, and I chose to explain it as-is (each context prices a genuinely different thing) rather than consolidate five contexts and their relationships onto a shared kernel, which felt out of proportion to a documentation gap. If the lead would rather RiverMart's Money actually moved to a kernel, that's a source change I didn't make and would want a steer on first.
