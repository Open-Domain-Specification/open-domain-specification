---
column: done
labels: [backend, docs, models]
priority: high
agent: senior-developer
live: false
clean-code-swept: true
updatedAt: 2026-09-11T09:40:00.000Z
---
# A context modelled at its boundary only; nested unknown fields; a domain service raises its own events

The architect's fifteenth round found the metamodel has no honest state for an owned, coherent context nobody has interviewed yet: every insides-are-knowable rule infers knowability from the absence of `external` or `bigBallOfMud`, so a bank adopting the model for Payments first cannot hold a customer id into its own CRM without inventing the CRM's entities or calling a healthy context a mess, and petstore does the latter (its Identity context is `bigBallOfMud: true` with a discovery note saying it means "modelled at its boundary only"). Decision 28 now names the third kind: `boundaryOnly`. Also: `unknown-field` sees element-level keys only, so `returns: { $ref, reasons, bogus }` loads with no diagnostic and loses both keys on the round trip; and `raises-in-aggregate` walks aggregates only, so a domain service's operation may raise two aggregates' events. Decisions 17, 28 and 29 are amended.

## Checklist

- [x] `BoundedContextSchema.boundaryOnly?: boolean`: ours (team and subdomain as usual), coherent, modelled at its boundary only; it declares consumables, schemas and value objects and no aggregates, policies, processes or context invariants (`boundary-only-is-boundary`, mirroring `external-is-boundary` with its own message); identities into it name the context or one of its schemas; `event-unraised`, `subscription-backed`, `consumption-by-required`, `aggregate-root` and the other insides rules skip it; `mud-needs-acl` does not apply; a context cannot be two of external, mud and boundary-only; DSL `addBoundedContext(name, { boundaryOnly: true })`; loader, `toSchema`, JSON schema; the context map and the tree draw it with its own stereotype; doc and pages label it
- [x] Petstore's Identity context becomes `boundaryOnly` with its discovery note corrected; every model's diagnostics otherwise unchanged
- [x] `unknown-field` reports unknown keys inside `$ref` objects and every other nested object the loader reads (`returns`, `rejects` entries, `schema`, `consumable`, `deadlines`, `relations`, `comments`, `reasons` where an object), with the path; tests for `returns.reasons` and a nested bogus key; the round trip still drops them
- [x] `raises-in-aggregate` also refuses a domain service's operation raising an aggregate's event; an application service's operation stays the one exemption; test
- [x] `apps/docs/docs/3-core/4-validation.md` rows; the strategic page's context flags section names the three kinds; skill references regenerated and the interview playbook asks which kind an uninterviewed context is; `bash scripts/verify-all.sh` green

## Gates

- [x] clean-code-swept — nested unknown-field check is one declarative map (`NESTED_FIELDS`) walked by three small functions rather than a call per site; the `then` key that tripped `noThenProperty` is built by `refObjectFields` instead of suppressed; biome clean on every file touched; the three pre-existing findings elsewhere (`packages/doc/src/service.md.ts`, `packages/pages/src/lib/flow/fullscreen.svelte.ts`, `packages/pages/src/lib/templates/WorkspacePage.test.ts`) were left alone (senior-developer, 2026-09-11T09:40:00.000Z)

## Comments

- **senior-developer** (2026-09-11T09:40:00.000Z): The third context flag. `BoundedContextSchema.boundaryOnly` in `packages/core/src/schema.ts:259-280`, through `BoundedContextAttributes`, the field, the constructor and `toSchema` in `packages/core/src/workspace.ts:987-1215`, and `KNOWN_FIELDS.boundedcontext` in `packages/core/src/workspace-from-schema.ts:117-133`. New rule `boundary-only-is-boundary` at `packages/core/src/validate.ts:5230-5300` with its catalogue entry, mirroring `external-is-boundary`: it refuses aggregates, policies, processes and context invariants, and refuses a context marked two of the three flags — reporting the clash and stopping there, since with the author's own statement ambiguous, listing what the second flag refuses would answer a question they have not asked. `knowableContexts` (`packages/core/src/validate.ts:82-107`) now skips it too, which is what makes `event-unraised`, `subscription-backed`, `aggregate-root`, `root-identity` and `entity-identity` leave it alone in one place; `consumptionByRequired` at `packages/core/src/validate.ts:3509` and `identifiesEntity` at `packages/core/src/validate.ts:420-448` take it explicitly. `mud-needs-acl` needed no change — it reads a `bigBallOfMud` provider — and there is a test pinning that a consumer of a boundary-only context is not asked for a layer.
- **senior-developer** (2026-09-11T09:40:00.000Z): Nested unknown fields. `checkUnknownFields` walked an element's own keys only, so probe P41's `returns: { $ref, reasons, bogus }` loaded silently and lost both. `packages/core/src/workspace-from-schema.ts:234-352` declares `NESTED_FIELDS`, one entry per kind mirroring `schema.ts`, and `checkKeys`/`checkNested` follow it to the leaf; every ref path is the JSON path, index and all (`.../rejects/0/reasons`). Two paths were corrected on the way: a consumption is now reported at `.../consumes/<i>` rather than `.../consumes`, and a relationship — the one thing a file writes with no ref of its own — at `#/relationships/<i>`, which wired up two `KNOWN_FIELDS` entries that had never been called. Eight tests at `packages/core/src/workspace-from-schema.test.ts:616-780`, including the round trip still dropping the nested key.
- **senior-developer** (2026-09-11T09:40:00.000Z): `raises-in-aggregate` now walks aggregates and domain services alike (`packages/core/src/validate.ts:4295-4335`), the application service staying the one exemption; probe P6b's transfer service is the test at `packages/core/src/validate.test.ts:4893-4950`. It caught five real instances in the reference models: petstore's `DispatchPlanner` raising the Shipment aggregate's `ShipmentPlanned` (`models/petstore/src/workspace.ts:1018-1046`), and RiverMart's `BuyBoxService`, `RiskScorer` and `AuctionService` raising four aggregates' events (`models/rivermart/src/workspace.ts`). In each case the event's own description said whose fact it was — a buy box is the outcome of comparing every offer, an auction is about a results page — so the event moved to the service that produces it rather than the aggregate gaining an invented operation. Written up in both DISCOVERY records.
- **senior-developer** (2026-09-11T09:40:00.000Z): Petstore's Identity context is `boundaryOnly` (`models/petstore/src/workspace.ts:118-128`) and its `User` aggregate is gone: what the context states is its four endpoints, the three facts they publish, the `User` schema the read answers with and the `UserStatus` value that schema carries, with the glossary term embodied by the schema (`models/petstore/src/workspace.ts:1190-1275`). The discovery note that claimed `bigBallOfMud` meant "modelled at its boundary only" is corrected at `models/petstore/DISCOVERY.md:162-170` with a revision section at the foot. Petstore validates to zero diagnostics; the other four models are unchanged apart from RiverMart's four moved events.
- **senior-developer** (2026-09-11T09:40:00.000Z): Surfaces. `BOUNDARY_ONLY_STEREOTYPE` («boundary only») in `packages/graphviz/src/role-labels.ts:46-53`, drawn on the Graphviz map with a dashed egg (`packages/graphviz/src/context-map.ts:53-90`), on the interactive map in `packages/pages/src/lib/flow/ContextNode.svelte:33-63`, in the legend at `packages/pages/src/lib/flow/legend.ts:127-136`, as a plain (not warning) keyword in `packages/pages/src/lib/molecules/ContextLockup.svelte:16-22` and `packages/pages/src/lib/templates/ContextPage.svelte:167`, as a callout in `packages/doc/src/boundedcontext.md.ts:135` and as a tree label in `apps/ods-vscode/src/tree.ts:322`. Two pages tests that found petstore's mud context now read RiverMart's, since petstore has none. Two e2e specs needed a word: `e2e/diagrams-context.spec.ts:26-36` scopes its stereotype assertion to `.port.stereotype`, because a node draws its own stereotype line under the same class, and `e2e/relationship.spec.ts:291-297` filters on the flag word petstore's Identity now carries.
- **senior-developer** (2026-09-11T09:40:00.000Z): Docs and skill. New `boundary-only-is-boundary` row and four amended rows in `apps/docs/docs/3-core/4-validation.md`; the context flags section of `apps/docs/docs/3-core/2-strategic-design.md:26-60` now names the three kinds and says they are mutually exclusive; the tree in `apps/docs/docs/3-core/index.md:43-44`; a boundary-only context and an assertion in `apps/docs/tests/strategic.example.test.ts`, which the page embeds. The interview playbook asks it at `packages/skill/skill/references/interview-playbook.md:45-58` — "is that a mess nobody can read, or is it fine and we simply have not looked at it yet?" — with rows in the DSL API and translation table and the petstore example corrected; `model-reference.md` and `validation-rules.md` regenerated. Card 133's files and sentences were not touched. `bash scripts/verify-all.sh` green end to end on the first uninterrupted run.
