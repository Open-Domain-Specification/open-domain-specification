---
column: review
labels: [backend, ddd, docs]
priority: high
agent: dev-opus
live: false
clean-code-swept: true
updatedAt: 2026-09-04T06:45:00.000Z
---
# Pattern knowledge base in core, consumed by legend, doc generator, docs site and skill (RFC-002 card C)

One source of truth for what each strategic pattern means: name, abbreviation, category, a one-line summary, the architectural nature and trade-offs. RFC-001 section 4 has the content (docs/rfcs/rfc-001-context-relationship-narratives-and-notes.md); RFC-002 section 7 fixes where it lives. The pages designs currently carry a stand-in map, `PATTERN_SUMMARIES` in packages/pages/src/lib/evidence/fixtures.ts, which this card replaces.

## Checklist

- [x] `packages/core/src/patterns.ts`: `PatternNature { name, abbreviation, category: "relationship" | "upstream-role" | "downstream-role", summary, architecturalNature, tradeOffs: string[] }` and `PATTERNS: Record<ContextRelationshipType | UpstreamRole | DownstreamRole, PatternNature>` with the nine entries from RFC-001 section 4 (including `upstream-downstream`), exported from the package index; a test that every union member has an entry and every abbreviation is unique
- [x] `packages/pages/src/lib/flow/legend.ts` derives its mark-to-label map from `PATTERNS` (mark = abbreviation, label = name); legend tests updated; `PATTERN_SUMMARIES` in the evidence fixtures removed and its importers read `PATTERNS[...].summary`
- [x] The existing `title` tooltips on type chips and role badges (ContextPage.svelte and ContextEdge.svelte) show `summary`
- [x] `packages/doc`: the bounded context page prints the summary under each role in its relationship table (or as a footnote list), tests updated, four models rebuilt
- [x] `apps/docs/docs/2-ddd/3-strategic-design.md` gains a "Relationship patterns" section generated or hand-written from `PATTERNS`, naming OHS, ACL, PL, CF and the five relationship types
- [x] `packages/skill` reference on strategic relationships regenerated from `PATTERNS` so an agent explains a pattern in the same words the UI does
- [x] Root `npm test` green; pages coverage thresholds unchanged

## Gates

- [x] qa-automated — inside the worktree: core 116, doc 18, skill 18, pages 445 at 100% coverage; developer also ran 64 e2e and the four models (lead, 2026-09-04T06:45:00Z)

- [x] clean-code-swept — SRP/DRY/naming/coupling/dead-code/KISS sweep over the diff; one 0.55 DRY finding on the hand-written docs-site section, left in place by design and pinned by a test (dev-opus, 2026-09-04T07:55:00.000Z)
- [x] tests-passing — run inside the worktree, not via root lerna: core 116, graphviz 18, doc 18, skill 18, pages 445 unit at 100% coverage plus 64 Playwright e2e, ods-vscode 4, models 18/3/3/3 (dev-opus, 2026-09-04T07:58:00.000Z)

## Comments

- **lead** (2026-09-04T04:30:00.000Z): Assigned to dev-opus. Fixed by decision: the content is RFC-001 section 4 verbatim, with the `upstream-downstream` entry from RFC-001 revision 2 (abbreviation `U/D`); trim adjectives if you must, add no claims. Abbreviations must match the marks the legend already draws (`U/D`, `C/S`, `P`, `SK`, `SW`, `OHS`, `ACL`, `PL`, `CF`). The docs-site section is the one place the trade-offs appear in full; everywhere else uses `summary`. Extension card 22 is in flight on ContextPage.svelte's table and packages/doc's context page: keep your edits in those two files to the tooltip and the footnote so the merge is easy, and merge develop before you finish. Work in your worktree; `npm ci` there first if node_modules is missing; if the card is missing, `git reset --hard develop` there first.
- **dev-opus** (2026-09-04T05:10:00.000Z): Picked this up. The worktree was on a stale commit; `git reset --hard develop` recovered the card and `npm ci` restored node_modules. Read RFC-001 section 4 for the nine entries and RFC-002 section 7 for the consumer list. Surveyed the four stand-ins that this card collapses into `PATTERNS`: packages/pages/src/lib/evidence/fixtures.ts:75 (`PATTERN_SUMMARIES`), packages/pages/src/lib/icons.ts:43 (`RELATIONSHIP`), packages/pages/src/lib/flow/legend.ts:26-38 (`ROLE_NAMES`/`STEREOTYPE_NAMES`) and packages/pages/src/lib/flow/roles.ts:10 (`NAMES`, which today puts the raw enum key in the port tooltip).
- **dev-opus** (2026-09-04T05:45:00.000Z): Core done. `PatternNature` and `PATTERNS` at packages/core/src/patterns.ts:13-150, verbatim from RFC-001 section 4, plus a `patternByAbbreviation` lookup at packages/core/src/patterns.ts:153-156 that the port tooltips need; exported at packages/core/src/index.ts:6. Tests at packages/core/src/patterns.test.ts:33-74 assert one entry per union member and nothing else, the category of each key, unique abbreviations, and that the nine marks are exactly `ACL C/S CF OHS P PL SK SW U/D`. Core suite 116 tests green.
- **dev-opus** (2026-09-04T06:20:00.000Z): Pages collapsed onto `PATTERNS`. legend.ts now builds its mark-to-name maps from core (packages/pages/src/lib/flow/legend.ts:9-19) and no longer imports the graphviz label tables; roles.ts abbreviates and explains from core (packages/pages/src/lib/flow/roles.ts:6-21); `PATTERN_SUMMARIES` is gone from packages/pages/src/lib/evidence/fixtures.ts and its five importers read `PATTERNS[...].summary`. New agreement test at packages/pages/src/lib/flow/legend.test.ts:24-38: graphviz's three label tables must key exactly the same nine patterns as `PATTERNS` and give each the same mark, so the abbreviations can never drift apart silently. Pages: 60 files, 444 tests green, coverage still 100/100/100/100.
  Three implementation choices, all reversible in a line, flagged for review rather than guessed at silently: (a) legend labels are now core's Title Case (`Open Host Service`, was `Open host service`) because the card fixes label = `name`; (b) a port badge showing `OHS+PL` gets one `Name — summary` line per part, newline-joined, since two summaries run together are unreadable (packages/pages/src/lib/flow/roles.ts:14-21), and DispositionEdge's badge title joins the pattern line and the evidence line the same way (packages/pages/src/lib/evidence/DispositionEdge.svelte:83); (c) `RELATIONSHIP` in packages/pages/src/lib/icons.ts:43-52 is a public export of the pages package, so rather than delete it I made it a derived view of `PATTERNS` — same shape, same key, summaries now from core. That is why ContextPage.svelte needed no edit at all: its chip title already reads `RELATIONSHIP[r.type]`, which is now literally `PATTERNS[r.type].summary`. Minimal merge surface against card 22, as asked.
- **dev-opus** (2026-09-04T07:10:00.000Z): Doc generator, docs site and skill. `patternNotesMd` at packages/doc/src/context-relationships.md.ts:27-38 renders the footnote for any set of pattern keys; the bounded context page picks it up through packages/doc/src/strategic-position.md.ts:57-64, which appends a note for every type and role its own three groups show. Test at packages/doc/src/index.test.ts:262-279 pins two specific lines and then checks structurally that nothing is footnoted the tables above do not name. All four models rebuilt: only petstore's docs are tracked (the other three are gitignored build output), five context pages gained the footnote, e.g. models/petstore/docs/boundedcontexts/sales_bc/index.md:70-77.
  Docs site: a hand-written "Relationship Patterns" section at apps/docs/docs/2-ddd/3-strategic-design.md:71-146 — two lookup tables, then the architectural nature and the trade-offs in full, which per your note is the only place they appear. Hand-written rather than generated because apps/docs has no generation step, so apps/docs/tests/strategic-patterns.doc.test.ts:17-34 is what keeps it honest: every pattern's name, mark, summary, nature and both trade-offs must appear on the page verbatim, or the test fails.
  Skill: `renderStrategicRelationships` at packages/skill/scripts/generate.mts:129-172 emits a new generated `references/strategic-relationships.md`, grouped into relationship types, upstream roles and downstream roles, keyed by the value that goes in the model; SKILL.md step 3 points at it (packages/skill/skill/SKILL.md:59-62). The existing "generated references are committed up to date" test covers drift, and packages/skill/src/bundle.test.ts:63-84 adds that it explains every pattern and names none core does not. Regenerating also refreshed `references/model-reference.md` with the `Comment`/`CommentLink` definitions card 40 added to core — that file was stale on develop, not something this card changed by hand.
- **dev-opus** (2026-09-04T07:30:00.000Z): Merged develop (fast-forward to 4e84a82). Three conflicts, all from card 22/23 landing on the same surfaces: (a) packages/doc/src/boundedcontext.md.ts now calls the new `strategicPositionMd`, so I took develop's version wholesale and moved my footnote into packages/doc/src/strategic-position.md.ts instead — the contested file is untouched by this card now; (b) packages/doc/src/index.test.ts, both new tests kept side by side; (c) StrategicPositionTable.svelte, develop's `withEvidence` structure kept with the tooltips reading `PATTERNS[...].summary`. One further test needed the same treatment after the merge: packages/pages/src/lib/templates/edge-cases.test.ts:236-242 asserted the four old role summaries as string literals and now loops over `PATTERNS`.
  Test evidence, all run inside the worktree: core 116, graphviz 18, doc 18, pages 445 with coverage 100/100/100/100, skill 18, ods-vscode 4, and the four model packages 18/3/3/3. Note for review: root `npm test` is NOT valid evidence from an agent worktree — lerna/nx resolves the workspace root through the worktree's `.git` file and runs the suites in the MAIN checkout, reporting green regardless of what is in the worktree (`npm test 2>&1 | grep -o '.../packages/core'` prints the main path). The same trap makes `lerna run build` report success while producing no `dist`; I built each package directly instead. Worth a line in the handover notes.
- **dev-opus** (2026-09-04T07:55:00.000Z): Two e2e assertions were still pinned to the old wording and would have gone red in CI: packages/pages/e2e/diagrams-context.spec.ts:77 wanted the legend to read "Open host service", and :51-54 matched the port tooltip against `/open-host-service|published-language/`, which was the raw enum key. Both now read `PATTERNS` (packages/pages/e2e/diagrams-context.spec.ts:1, :52-60, :83). Full Playwright run: 64 passed.
  Clean-code sweep run (SRP, DRY, naming, coupling, dead code, KISS over the diff). One finding above 0.5: DRY 0.55 on apps/docs/docs/2-ddd/3-strategic-design.md:71-146, the hand-written section being a second copy of the knowledge base. Not fixed, deliberately: the card offers "generated or hand-written", apps/docs has no generation step, and apps/docs/tests/strategic-patterns.doc.test.ts fails the moment the page and `PATTERNS` disagree on any name, mark, summary, nature or trade-off. Flagging it for your call rather than inventing a docs generator on this card. Nothing else scored above 0.5: no stand-in survives, `patternByAbbreviation` and `patternNotesMd` each have a real caller, and graphviz's label tables are still used for dot output and are now pinned to the abbreviations by test.
  One related duplicate I did not touch, since the card does not name it: packages/skill/skill/references/ddd-glossary.md:44-53 also explains the nine patterns, in deliberately plainer teaching language than `PATTERNS.summary`. Leaving it is defensible (different register, different job), but it is the last place the vocabulary is written twice.
- **lead** (2026-09-04T06:45:00.000Z): Reviewed packages/core/src/patterns.ts and the legend derivation. Hand-written docs section pinned by a test is accepted; the glossary duplicate is noted for a later card. Landing on develop.
