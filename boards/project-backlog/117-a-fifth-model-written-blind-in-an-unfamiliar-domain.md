---
column: done
labels: [models]
priority: medium
agent: developer
live: false
clean-code-swept: true
updatedAt: 2026-09-06T16:00:00.000Z
---
# A fifth model written blind in an unfamiliar domain

The architect's tenth round named a systemic risk: the validator's exemptions were shaped by what the four reference models needed (eight helpers under `reaction-cycle` alone), and nothing shows they generalise. The cheapest test is a fifth model in a domain unlike the four, written by someone who has not read them, counting the rules that must be worked around or amended to validate clean. The domain is an outpatient clinic: GP referrals arriving from an external practice system, triage, scheduling with slots and cancellations, a laboratory as an external context that answers with results, a records context that holds the patient's identity, a regulator that is a published language. Runs after card 116. The author is free to put an outbound consumption on a domain service and to have an operation answer with two shapes, and records the diagnostic met; the report counts both, because decisions 17 and 18 name them as their reopening conditions.

## Checklist

- [x] `models/clinic` built like the others (DSL workspace, `DISCOVERY.md` interview, tests, `.ods` output, registered wherever the four are), written without reading `models/petstore`, `models/rivermart`, `models/streamline` or `models/northbank`; the skill in `packages/skill` and the docs are the only guide
- [x] Every diagnostic met on the way is journalled in the card's Comments as either a modelling mistake fixed, a rule worked around and how, or a rule the author believes wrong, with the shape that triggered it
- [x] The finished model validates clean or on purpose, with each on-purpose diagnostic explained in `DISCOVERY.md`
- [x] `bash scripts/verify-all.sh` green, including the new model in the gate's list

## Gates

- [x] clean-code-swept — read back the finished `models/clinic/src/workspace.ts` end to end against the skill's conventions before marking the card for review; no drive-by changes outside `models/clinic`, `models/_shared` (untouched) and `scripts/verify-all.sh`'s model list (developer, 2026-09-06T16:00:00.000Z)
- [x] verify-all — `bash scripts/verify-all.sh` green end to end, including `clinic: 7 tests passed` in the summary alongside the four reference models and the full pages e2e suite (297 passed) (developer, 2026-09-06T16:10:00.000Z)

## Comments

- **developer** (2026-09-06T14:00:00.000Z): Claimed the card. Reset the worktree to `origin/develop` (`d696990`, "card 117 dispatched"), ran `npm install`, and read the brief's whole reading list: `.claude/skills/repodoc-workflow/SKILL.md`, `packages/skill/skill/SKILL.md` and everything under `packages/skill/skill/references/`, and `apps/docs/docs/**`. Deliberately did not open `models/petstore`, `models/rivermart`, `models/streamline`, `models/northbank` or `decisions/`. Looked only at `models/_shared` (fully, it is shared plumbing, not one of the four) and `models/streamline/package.json`, `tsconfig.json`, `vitest.config.ts` and `src/generate.ts` to copy the package wiring, per the brief.
- **developer** (2026-09-06T14:20:00.000Z): Wrote the interviews in `models/clinic/DISCOVERY.md` first, in the practice manager's, triage nurse's, scheduler's and records officer's own words, before writing any model element. Two things the interviews name up front as freedoms the card grants: the nurse says the assessment itself (not a front desk) calls out to Records, and the scheduler describes an operation answering with two things that happened, neither a refusal.
- **developer** (2026-09-06T14:50:00.000Z): Modelled the three external contexts in `models/clinic/src/workspace.ts:43-168`: GP Practice System (publishes `Referral Submitted`, published-language, own message schema), Laboratory (`Order Test` open-host-service, `Test Result Reported` published-language, answering later as the brief says), and Clinical Coding Regulator (a `Clinical Code` value object with its own invariant, no service, no operations at all — "provides nothing to call" taken literally).
- **developer** (2026-09-06T15:05:00.000Z): Modelled Patient Records (`workspace.ts:174-317`): a `Patient` root, an `External Identifier` child entity with two kinds (`GP Practice Reference`, `Lab Reference`) each holding its own `identifies` into the external system it names, matching the records officer's account of "what other systems know the patient by". Ran the build for the first time here and hit the first real diagnostic — see below.
- **developer** (2026-09-06T15:05:30.000Z): **Diagnostic met — modelling mistake, fixed.** `cross-context-relation` fired on `referralEntity.uses(clinicalCodeVO, ...)` (`workspace.ts:428` at the time): a `uses` relation is an entity relation, and entity relations never cross a bounded context, even to a value object — only `identifies` does, and a value object is borrowed cross-context through the attribute's own `valueobject:` link, not through a relation on the map. Removed the relation and left a comment explaining why in its place (`workspace.ts:428-433`); the attribute's `valueobject: clinicalCodeVO` link, backed by the `conformist` relationship to the regulator, was already enough. This was me mis-reading `attribute-relation-coherence`'s advice to declare a `uses` relation for every value-typed attribute as if it applied regardless of which context declared the value; `model-reference.md`'s value-object-shape and cross-context-relation entries say otherwise once read together.
- **developer** (2026-09-06T15:20:00.000Z): Modelled Triage (`workspace.ts:335-527`): the `Referral Case` aggregate, the `Triage Assessment` domain service, and the two application services `Referral Intake` (ACL-consumes the GP's `Referral Submitted`) and `Lab Ordering` (ACL-consumes the lab's `Order Test` and `Test Result Reported`).
- **developer** (2026-09-06T15:20:30.000Z): **Diagnostic met — rule worked around on purpose, per the card's own grant.** `domain-service-consumes-inside` fires on `Triage Assessment` consuming Records' `Get Patient Summary` directly (`workspace.ts:541-555`). The nurse's account was explicit: "it's the assessment that goes and gets it... not the reception desk", so the consumption sits on the domain service exactly where she put it, not on an application service invented to keep the rule quiet. Left standing; explained in `DISCOVERY.md`'s "What the model could not say" and in the pinned diagnostic in `models/clinic/src/workspace.test.ts`. This is the outbound-consumption-on-a-domain-service freedom the card names against decision 17's reopening condition.
- **developer** (2026-09-06T15:35:00.000Z): Modelled Scheduling (`workspace.ts:531-664`): `Clinic Schedule` (a `Clinic Session` root with `Slot` children), `Booking` (its own aggregate, holding the session's and slot's ids via `identifies` rather than a cross-aggregate relation, since a booking reaches a slot that is a child, not a root), the `Scheduling Desk` application service, and an `Appointment Lifecycle` process with a deadline, matching the scheduler's "we hold that open... we need to remember whether a cancellation ever came in."
- **developer** (2026-09-06T15:35:30.000Z): **Freedom recorded — not a validation diagnostic, but named by the card as counting toward the tally (decision 18's reopening condition).** `Offer Slot` (`workspace.ts:596-604`) can genuinely come back two ways that both happened — booked, or waitlisted — and neither is a refusal. The model has one `returns` and no shape for "a second thing that happened, not a refusal", so `Patient Waitlisted` is carried under `rejects` for want of anywhere better to put it. No rule catches this because none of the model's rules check whether a `rejects` entry is honestly a refusal; the compromise is recorded in `DISCOVERY.md`'s "What the model could not say" rather than left silent.
- **developer** (2026-09-06T15:45:00.000Z): Wired Records back to Triage (`workspace.ts:667-682`): a `Register Patient On Referral` policy consuming Triage's `Referral Registered`, and the second, opposite-direction relationship between the two contexts (Triage downstream of Records for the patient lookup, Records downstream of Triage for the registered-case fact) — both legitimate per `relationship-duplicate`'s "two contexts that each depend on the other... is one directed relationship each way, and both are kept", and not a `relationship-cycle` since one edge is event-only.
- **developer** (2026-09-06T15:50:00.000Z): `npm run build` in `models/clinic` (`node src/generate.ts`) validates with exactly one diagnostic, the deliberate `domain-service-consumes-inside`. Wrote `models/clinic/src/workspace.test.ts`: pins that one diagnostic, checks every non-external context has a team, a glossary term exists somewhere, at least one process and several policies exist, the workspace round-trips through `Workspace.fromSchema`, and the generated doc site is internally consistent (`assertDocSite` from `models/_shared`, read in full as allowed shared plumbing). `npx vitest run --config vitest.config.ts` — 7 tests passed.
- **developer** (2026-09-06T15:55:00.000Z): Registered `clinic` in the gate: `scripts/verify-all.sh:76` — the model-build loop now reads `for model in northbank petstore rivermart streamline clinic`. Ran the grep the brief names (`grep -rn "streamline" --include=*.json --include=*.sh --include=*.ts --include=*.mts . | grep -v node_modules | grep -v dist | grep -v models/streamline`) and found three further kinds of hit, deliberately left alone — see Decisions Needed in the final report to Lead: `models/_shared/src/relationship-types.test.ts` (asserts the *four* reference/stress-test models jointly cover all five relationship types — card 117 says this model is unlike those four on purpose, not a fifth member of that set); and `packages/pages`'s fixtures, e2e helpers and UI tests (`fixtures.ts`, `ImportScreen.test.ts`, `AttributeTable.test.ts`, `e2e/helpers.ts`) and `apps/ods-ui/package.json`, which are the pages app's own example-model conventions, outside the paths this brief names and not something this card's checklist asks for by its own wording ("registered in the gate's model list").
- **developer** (2026-09-06T16:00:00.000Z): Final tally — **mistakes fixed: 1** (`cross-context-relation` from a `uses` relation to another context's value object); **rules worked around on purpose: 2** (`domain-service-consumes-inside` on `Triage Assessment`'s consumption of Records; the non-diagnostic `rejects`-for-a-non-refusal compromise on `Offer Slot`, both named by the card against decisions 17 and 18); **rules believed wrong: 0** — every rule met either caught a real modelling mistake or was the exact shape the card said to leave standing and explain. Set `column: review`, `live: false`, `clean-code-swept: true`. `bash scripts/verify-all.sh` run separately; see the developer's final report to Lead for its evidence.
