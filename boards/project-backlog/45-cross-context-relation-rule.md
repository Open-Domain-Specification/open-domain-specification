---
column: done
live: false
clean-code-swept: true
labels: [backend, ddd]
priority: high
agent: dev-opus
updatedAt: 2026-09-06T16:00:00.000Z
---
# A relation never crosses a bounded context

Implements [decision 14](../../decisions/14-cross-context-relations-are-identities.md): a new `cross-context-relation` error, petstore moved to identity-only, docs and skill following.

## Checklist

- [x] `packages/core/src/validate.ts`: `cross-context-relation` errors when a relation's target entity belongs to another bounded context, message naming both contexts, fix naming the identity attribute to hold instead; catalogue entry; tests including the same-context case that must still pass
- [x] Petstore: remove `orderRoot.references(petRoot, ...)` at workspace.ts:452, keep `petId`, and make sure the Sales to Catalog dependency still reads on the consumable map; petstore validates clean; the other three models checked for the same pattern
- [x] Decision 08's table and decision 03 cross-referenced from the new rule's why; `docs/` DDD page on aggregates mentions identity-only references
- [x] Skill references regenerated from the catalogue; the interview's Phase E asks "which id does it hold?" for a foreign aggregate
- [x] Pages: the aggregate relation map shows no cross-context edge for petstore; `assertDocSite` green; root suites green inside the worktree

## Gates

- [x] qa-automated — Optimus re-ran core and the petstore and rivermart suites in the worktree; developer ran all ten projects, 255 e2e and the docs build with the nx cache skipped (Optimus, 2026-09-06T16:00:00Z)

- [x] tests — `npx lerna run test --skip-nx-cache` green for all 10 projects: core 208, doc 4 files, graphviz 6 files, pages 646 at 100% statements/branches/functions/lines, skill 26, the four model suites incl. `assertDocSite`, extension 1 file. Pages e2e 255 passed. Docusaurus build clean, no broken links (dev-opus, 2026-09-06T16:05:00.000Z)
- [x] clean-code-swept — full audit, two parallel passes over SRP/DRY/KISS and NAMING/COUPLING/DEAD, plus an inline BOUNDARY/PANIC read; nothing scored above 0.4, no markers written (dev-opus, 2026-09-06T16:05:00.000Z)

## Comments

- **lead** (2026-09-06T10:00:00.000Z): Assigned to dev-opus. Fixed by decision 14; read it first. No schema change. Card 44 runs in parallel and owns `schema.ts`, `workspace.ts`, the `schema-context` rule and the petstore operations; you own the `cross-aggregate-reference`/`cross-context-relation` rules and petstore line 452. Merge develop before your final run. Work in your worktree; `npm ci` if node_modules is missing; if the card is missing, `git reset --hard develop` there first.
- **dev-opus** (2026-09-06T12:00:00.000Z): Picked this up. Read decision 14, decision 08's crossing table and decision 03. Reset the worktree to develop (the card was not in the base commit) and ran `npm ci`. Landmarks: the rule set and catalogue at packages/core/src/validate.ts:64-86 and :325-420, the catalogue completeness fixture at packages/core/src/rule-catalog.test.ts:6-51 (every catalogued rule must fire on it), petstore's relation at models/petstore/src/workspace.ts:452, and the pages e2e that asserts the `for-pet` edge at packages/pages/e2e/diagrams-relation.spec.ts:75.
- **dev-opus** (2026-09-06T13:05:00.000Z): Rule written. `crossContextRelation` at packages/core/src/validate.ts:89-108 walks the same `relationsOf` generator as `crossAggregateReference` and compares `relation.source.aggregate.boundedcontext` with the target's; the message names both contexts and the identity to hold ("Order" in "Sales BC" references "Pet" in "Catalog BC"; ... hold "Pet"'s identity as an attribute on "Order" instead). Catalogue entry at packages/core/src/validate.ts:353-362, its `why` citing decision 03 for each context owning its own model and decision 08's crossing table for `EntityRelationSchema.target`, its `fix` naming the identity attribute and pointing the dependency at the consumable map. Two tests at packages/core/src/validate.test.ts:64-108: the first proves a relation across aggregates *inside* one context still passes while the same relation across contexts errors, the second that a value object's `uses` is caught too. The catalogue completeness fixture gained a cross-context reference at packages/core/src/rule-catalog.test.ts:26-28. Core: 208 tests, 207 passing; the one failure is the subject of the question below.
- **dev-opus** (2026-09-06T13:20:00.000Z): QUESTION for lead: the card scopes the model work to petstore:452, but with the rule live the pattern is everywhere -- 16 relations, in all four models and in core's own rich fixture. Full scan: **petstore 2** (`Order`->`Pet`, workspace.ts:452; `Shipment`->`Order`, workspace.ts:680); **northbank 6** (Mandate->Customer, PaymentInstruction->Account, Card->Account, LoanApplication->Customer, Loan->Account, ServiceRequest->Customer); **rivermart 6** (CartLine->Offer, OrderLine->Offer, FulfilmentOrder->Order, AdGroup->Product, Case->Order, and `Case` *includes* `OrderLine` across contexts); **streamline 2** (PlaybackSession->Title, Signal->Title); plus `invoice.references(order, "bills")` at packages/core/src/makeTestWs.ts:232, which is why packages/core/src/validate.test.ts:17 ("only warns about the deliberately unserved reporting context") now fails. Three things make this more than a mechanical sweep. (1) Twelve of the sixteen sources already carry the identity attribute, so deleting the relation is all it takes; four do not -- northbank's `Loan` has no `accountId` (loan_id, application_id, principal, apr, status), rivermart's `AdGroup` has no `productId` (ad_group_id, keywords, bid), rivermart's `OrderLine` has no `offerId` (it has `sku`, which may or may not be meant to stand for the offer), and the core fixture's `Invoice` has no attributes at all. (2) rivermart's `Case includes OrderLine` looks like the planted `cross-aggregate-reference` that models/rivermart/src/workspace.test.ts:14-18 declares deliberate; it will now raise two errors, so either the plant moves to a same-context target or `cross-context-relation` joins that model's deliberate list. (3) Both of petstore's `references` relations are cross-context, so removing them leaves the demonstration reference with no `references` relation at all -- and packages/pages/e2e/diagrams-relation.spec.ts:66-83 asserts a `.references` association edge labelled `for-pet` on the Order relation map. Options: **(a)** sweep all sixteen plus the core fixture, regenerate `docs/` and `.ods/` for the four models (`npm run build` per model), and add one same-context `references` to petstore so the model still demonstrates the relation kind and the e2e keeps an association to assert; **(b)** do petstore:452 only as the card says, which leaves fifteen errors standing and the three other model suites red, so the rule could not ship as an error. I recommend (a). It needs four naming decisions from you: the new attribute on northbank's `Loan` (I would use `accountId: string`, matching `Card`'s `account_id`), on rivermart's `AdGroup` (`productId: string`), whether rivermart's `OrderLine.sku` already is the offer identity or wants an explicit `offerId`, and what the core fixture's `Invoice` should hold (I would add `Order Id` with type `OrderId`, mirroring `Order`). Plus your call on rivermart's plant, and on where petstore's surviving same-context `references` should go -- I would put it inside Sales, but that is a modelling choice, not mine. Blocked until answered.
- **lead** (2026-09-06T13:30:00.000Z): ANSWER. Option (a): sweep all sixteen relations and the core fixture, regenerate every model's `docs/` and `.ods/`. Attribute names for the four sources that lack an identity: northbank `Loan.accountId`, rivermart `AdGroup.productId`, rivermart `OrderLine.offerId` (keep `sku`), core fixture `Invoice` gains `id` (identity) and `orderId`; each described as "the only thing that crosses the boundary", as petstore's `petId` is. Rivermart's planted `cross-aggregate-reference` must demonstrate exactly one rule: move it to two aggregates inside one context so it no longer trips `cross-context-relation`, and keep `workspace.test.ts` asserting the single deliberate error. Petstore keeps a same-context `references` by adding a small second aggregate to Fulfilment BC: `Carrier` (root with `id` identity and `name`), with `Shipment` holding `carrierId` and `references` Carrier as `shipped-by`, cardinality `1`; `e2e/diagrams-relation.spec.ts:66-83` moves its association-edge assertion to the Shipment relation map and the `shipped-by` label. Sales orders stay anonymous (decision on separate-ways from Identity stands).
- **dev-opus** (2026-09-06T14:40:00.000Z): Sweep done to your answer; all four models validate to exactly their declared deliberate list and core is 208/208. **Petstore**: the `for-pet` relation is gone from models/petstore/src/workspace.ts:450, `petId` and its "only the id crosses the boundary" description stay at :428-432; new `Carrier` aggregate in Fulfilment at models/petstore/src/workspace.ts:652-665 (root with `id` identity and `name`), `Shipment` gains `carrierId` at :675 and `references` it as `shipped-by` cardinality `1` at :690, and the `fulfils` relation to Sales' Order is replaced by a described `orderId` at :669-673. `workspace.validate()` returns `[]`. **NorthBank**: six relations deleted (models/northbank/src/workspace.ts:625, :984, :1459, :1621, :1672, :2095), each replaced by a comment naming the identity already held; `Loan` gains `accountId` at :1658-1662. Diagnostics are exactly separate-ways, consumable-kind, context-serves-subdomain -- the list at models/northbank/src/workspace.test.ts:14-18. **RiverMart**: five relations deleted (models/rivermart/src/workspace.ts:748, :912, :1437, :1776, :1913), `OrderLine` gains `offerId` beside the kept `sku` at :890-894 and `AdGroup` gains `productId` at :1773-1777. The planted error moved inside Cart & Checkout: `caseRoot.includes(orderLine, ...)` is gone and `cart.includes(wishlistItem, "saves-for-later", "*")` at models/rivermart/src/workspace.ts:815-821 takes its place, Cart and Wishlist being the one existing same-context aggregate pair with a child entity to copy in; diagnostics are exactly aggregate-root, cross-aggregate-reference, role-coherence, so it still demonstrates the single rule. **StreamLine**: both relations deleted (models/streamline/src/workspace.ts:816, :1139), `titleId` already held on each source. **Core fixture**: `invoice.references(order, "bills")` is gone; `Invoice` gains `Id` (identity) and `Order Id` at packages/core/src/makeTestWs.ts:243-248, and a `Basket` aggregate in Ordering BC at :176-186 carries the same-context `references` the derived-map and round-trip tests need -- applying to core the pattern you set for petstore's Carrier, since three tests there exist to exercise a relation crossing an aggregate. Re-pointed at packages/core/src/derived-maps.test.ts:151 and :161-168 and packages/core/src/round-trip.test.ts:115-123; edge counts and the namespace assertion are unchanged, only the aggregate they start from.
- **dev-opus** (2026-09-06T16:05:00.000Z): Docs, skill and pages finished. **Docs**: the aggregates section at apps/docs/docs/2-ddd/2-building-blocks-of-a-shared-language.md:66-73 now says a cluster reaches another by identity only and that across a context there is no relation at all, linking the two rules; the rule table at apps/docs/docs/3-core/4-validation.md:16 gains the `cross-context-relation` row, and the worked example at apps/docs/tests/validation.example.test.ts:18-20 -- whose `Order -> Price` was already cross-context -- says so in its comment and its inline snapshot picks up the second diagnostic. **Skill**: `npm run generate` re-rendered packages/skill/skill/references/validation-rules.md:23-29 from the catalogue; Phase E at packages/skill/skill/references/interview-playbook.md:83-89 now asks "is that other cluster inside this same part of the business, or somewhere else?" first and, for another context, "which id does it hold?" with the dependency picked up in Phase F; the excerpt at packages/skill/skill/examples/petstore.md:33-56 moves to `shipmentRoot.references(carrierRoot, ...)` and gains a section showing the identity-only attribute. **Pages**: packages/pages/e2e/diagrams-relation.spec.ts:49 keeps the `uses` and cardinality-port assertions on the Order map and now asserts the legend has no "open arrow" there, and a new test at :80-104 makes the association assertions against the Shipment map and the `shipped-by` label, with "open arrow" in that legend. **Verification**: merged develop (four docs/board commits, no code, no conflicts), `npx nx reset` then a full `--skip-nx-cache` build and test -- nx replays cached output in a worktree without writing `dist/`, so both had to skip it. All 10 projects green, pages 646 unit at 100% coverage and 255 e2e, four `assertDocSite` runs green, docusaurus builds with no broken links. Regenerated `docs/` and `.ods/` for all four models; petstore gains models/petstore/docs/boundedcontexts/fulfilment_bc/aggregates/carrier/ as a new page. Two things for your review, both outside this card: apps/docs has three failing snapshot tests (tests/doc.example, tests/model.example, tests/tactical.example) that also fail on a clean tree -- they are missing the `comments`/`disposition` fields and the Health section from card 40, and apps/docs has no `test` script so `lerna run test` never runs them; and biome still reports the two pre-existing `noThenProperty` errors at packages/core/src/workspace.ts:1785 and :1801. Neither is mine and I left both alone. Moving to review.
- **optimus-prime** (2026-09-06T16:00:00.000Z): Landing. The sweep is exactly as ruled; the core fixture's Basket aggregate applying the Carrier pattern is a good call. The three stale apps/docs snapshots and the missing docs test script become a card.
