---
column: review
labels: [backend, ddd]
priority: high
agent: ironhide
live: false
clean-code-swept: true
updatedAt: 2026-09-06T20:30:00.000Z
---
# Validator rules the model states but never enforces

From the Antigravity baseline (scratchpad agy-baseline.md, issues 9, 10, 11, 13, 14, 15, 16) and run 2 (agy-run2.md, issues 15, 17, 18). Each is a rule with a catalogue entry (why, fix), tests for the passing and failing shapes, and the reference models checked and corrected.

## Checklist

- [x] `root-identity` (error): an aggregate's root entity declares at least one `identity: true` attribute
- [x] `value-object-shape` (error): a value object's attributes are never `identity: true`, and a value object's relations are never `includes`
- [x] `aggregate-tree` (error): within an aggregate, `includes` forms a tree from the root: no cycles, no entity included by two parents, `includes` targets entities only, `uses` targets value objects only (decision 10's conventions)
- [x] `relationship-roles-backed` (warning): a directed relationship's declared upstream roles are each carried by at least one consumable crossing from upstream to downstream between those two contexts, and its downstream roles by at least one consumption; and a crossing consumption's pattern is declared on the relationship
- [x] `mud-needs-acl` (warning): a consumption from a context marked `bigBallOfMud` whose pattern is `conformist` or absent
- [x] `attribute-relation-coherence` (warning): an attribute typed by a value object without a `uses` relation to it, a `uses` relation without an attribute, and an array-typed attribute (`X[]`) whose relation cardinality is `1` or `0..1`
- [x] `role-coherence` no longer warns on a consumption between two contexts that declare a `partnership` or `shared-kernel` relationship: symmetric partners carry no upstream or downstream role (Antigravity run 2, issue 15)
- [x] `term-in-context` (error): a glossary term's `embodiedBy` names an element of the term's own context (run 2, issue 17)
- [x] `separate-ways` also walks policy subscriptions: a policy `on` an event of a context the policy's context has gone separate ways from is the same error as a consumption (run 2, issue 18)
- [x] Skill references regenerated; `docs/` validation page lists the new rules; four models validate with the intended diagnostics only (petstore clean)
- [x] `invariant-in-aggregate` (error): every element an invariant constrains belongs to the invariant's own aggregate (lead, run 3)
- [x] `aggregate-tree` also warns on an orphan: a non-root entity no `includes` or `references` chain reaches from the root (lead, run 3)
- [x] `attribute-relation-coherence` also warns when an attribute with `valueobject` set types itself as neither the value object's name nor that name with `[]` (lead, run 3)

## Gates

- [x] qa-automated — Optimus re-ran core 241, the four model suites and skill 26 in the worktree after merging develop and regenerated the models' docs and .ods (Optimus, 2026-09-06T20:30:00Z)

- [x] clean-code-swept — swept the new rules in packages/core/src/validate.ts; two findings fixed at source (a repeated map-append inlined twice, extracted to `append` at validate.ts:179; a doc comment on `orphanEntities` that did not say what the function returns). Biome clean on every touched file. (ironhide, 2026-09-06T18:40:00.000Z)
- [x] tests-passing — packages/core build green and 241 vitest tests green; petstore 18, northbank 3, rivermart 3, streamline 3; @open-domain-specification/skill 26. (ironhide, 2026-09-06T18:40:00.000Z)

## Comments

- **lead** (2026-09-06T12:00:00.000Z): Assigned to dev-opus after cards 44 and 45 land (the lead will say); they own `schema-context`, `returns-on-operation` and `cross-context-relation`. Fixed by decision: severities as listed; a rule never fires on a model element outside the file it is in; messages name both elements. Work in your worktree with absolute paths; `npm ci` if node_modules is missing; if the card is missing, `git reset --hard develop` there first.
- **ironhide** (2026-09-06T13:00:00.000Z): Claimed. Cards 44 and 45 have landed: `returns-on-operation` is at packages/core/src/validate.ts:242-261 and `cross-context-relation` at packages/core/src/validate.ts:88-108, so the ten items build on those. Read decisions 10, 14, 15, 17 and the whole catalogue at packages/core/src/validate.ts:333-470. Baseline before any edit: core 211 tests green. Plan is one rule at a time, each with its passing and failing shape in src/validate.test.ts, then the completeness fixture in src/rule-catalog.test.ts, then the four models.
- **ironhide** (2026-09-06T16:20:00.000Z): Eight new rules and two amendments in packages/core/src/validate.ts, each with a catalogue entry carrying its own why and fix. `root-identity` at validate.ts:127, `value-object-shape` at :151, `aggregate-tree` at :191 with its two walks at :246 (orphans) and :273 (cycles), `attribute-relation-coherence` at :308, `invariant-in-aggregate` at :381 over the helper at :367, `relationship-roles-backed` at :453, `mud-needs-acl` at :501, `term-in-context` at :522. The two amendments: `roleCoherence` at validate.ts:418 now skips a pair the workspace declares partners or shared-kernel through `symmetricallyRelated` at :404 (run 2, issue 15), and `separateWays` at :546 walks policy `on` lists as well as consumptions (run 2, issue 18). Two constraints held throughout: every diagnostic's `ref` is an element of the file the rule read, and every message names both elements. `term-in-context` decides membership by ref prefix rather than a lookup, so it cannot reach outside the context it is checking.
- **ironhide** (2026-09-06T16:55:00.000Z): Tests. Each rule has its quiet shape and its failing shapes in packages/core/src/validate.test.ts — `root-identity` at :253, `value-object-shape` at :279, `aggregate-tree` at :309 (six cases: clean, wrong relation kinds, two parents, cycle, orphan, and a `references` counting as reachability), `invariant-in-aggregate` at :393, `attribute-relation-coherence` at :423 (five cases, one per shape the card and run 3 name), `relationship-roles-backed` at :488, `mud-needs-acl` at :580, the partnership exemption at :637, `term-in-context` at :684, the policy half of `separate-ways` at :713. The completeness fixture in packages/core/src/rule-catalog.test.ts:58-116 gained a `Tree` aggregate, a `Coherence` aggregate, a foreign glossary term and a big-ball-of-mud context `C` with an upstream-downstream relationship whose declared roles nothing carries, so every new rule and both of `aggregate-tree`'s severities fire there.
- **ironhide** (2026-09-06T17:30:00.000Z): The rules found real defects in the fixtures and the reference models, which is the point of the card. packages/core/src/makeTestWs.ts:173-178 declared the order total as Money on `Order` and the `uses` relation on `Order Line`; both now carry the attribute and the relation, which moves two counts in packages/core/src/derived-maps.test.ts:142 and :168. Petstore had two: `InventoryView` was a root with no attributes at all (models/petstore/src/workspace.ts:810-822 now gives it the status it counts, as its identity, and the count) and `Shipment` used `TrackingNumber` with nothing typed by it (models/petstore/src/workspace.ts:704-707). Petstore validates clean again.
- **ironhide** (2026-09-06T17:50:00.000Z): The three stress models. NorthBank had 19 attribute/relation incoherences, all the same shape — an entity that `uses` a value object without an attribute typed by it, or the reverse — fixed across models/northbank/src/workspace.ts (customer address, consent purpose and scope, account overdraft, `OverdraftLimit` to Money, ledger value date, `LedgerAccount` to `AccountNumber`, payment payee and execution date, scheme format, fraud case status, loan term and decision, the three credit-decision inputs, the reporting period, and the service-request channel and status). StreamLine had 16 of the same, fixed across models/streamline/src/workspace.ts. Both are back to exactly the three diagnostics their tests declare deliberate.
- **ironhide** (2026-09-06T18:10:00.000Z): RiverMart needed judgement rather than a mechanical fix. Nine attribute/relation incoherences fixed in models/rivermart/src/workspace.ts. Three others were rules catching real modelling errors: the `Vendor` glossary term of Seller Onboarding was embodied by the Vendor Purchasing context (models/rivermart/src/workspace.ts:726-730) — the definition already says the word is a false friend, so the term now embodies nothing and says so; the Offers to Order Management relationship declared open-host-service and conformist while its own description says the coupling is identity only and nothing crosses, so the roles are now empty; and the deliberate two-root `Wishlist` cascaded into `root-identity`, so `WishlistItem` now declares its `productId` as its identity and only `aggregate-root` fires. The deliberate `role-coherence` warning at models/rivermart/src/workspace.ts:1744 is gone on purpose: it sat on a consumption between Warehouse and Last Mile, and those two share a kernel, so run 2's issue 15 is exactly why the rule is now quiet there. models/rivermart/src/workspace.test.ts:8-19 drops it from `deliberate` and says why. Flagged to the lead: no model plants a `role-coherence` warning any more, so that rule's only coverage is the catalogue fixture and validate.test.ts.
- **ironhide** (2026-09-06T18:40:00.000Z): Docs and skill. apps/docs/docs/3-core/4-validation.md lists the eight new rules and, while there, the two the table had always been missing (`schema-context` and the opt-in `comments-required`). `npm run generate` in packages/skill rewrote skill/references/validation-rules.md from `RULE_CATALOG`; its own minimal example tripped `attribute-relation-coherence` for real — `Order` used `Address` with no attribute typed by it — so both halves of the example gained the attribute (packages/skill/skill/examples/minimal.workspace.ts:43-46 and the matching entry in minimal.ods.json), which keeps the DSL and JSON forms the same model. Everything green: core build plus 241 tests, petstore 18, northbank 3, rivermart 3, streamline 3, skill 26. Moving to review.
- **optimus-prime** (2026-09-06T20:30:00.000Z): Rulings: (1) dropping RiverMart's planted role-coherence warning is right, the rule is covered by its own tests; (2) emptying the Offers to Order Management roles is right, honesty over invented traffic; (3) `on` only is right, card 48 owns `then`. The 44 attribute and relation incoherences the new rule found across the models are the point of the card. Landing.
