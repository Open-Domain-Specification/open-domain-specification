---
column: done
labels: [pages, graphviz]
priority: low
agent: senior-dev
live: false
clean-code-swept: true
updatedAt: 2026-09-08T23:30:00.000Z
---
# Borrowed value objects draw on the relation map

A value object borrowed from a shared kernel or a conformed-to upstream never appears on the holding aggregate's relation map: `uses` may not cross a context and the coherence rule skips foreign value objects, so NorthBank's Account shows no Money. The map should draw the borrowed value as a node labelled with its context, dashed border, reached by the same `uses` edge, in Svelte Flow, DOT and PlantUML.

## Checklist

- [x] `ODSRelationGraph` includes borrowed value objects held by the scoped aggregate's attributes, marked with their owning context; the three renderers draw them with the foreign mark and a legend row
- [x] `attribute-relation-coherence` keeps skipping foreign value objects, because the `uses` edge is derived from the attribute and there is no declared relation to compare; the skip's justification restated (reworded by the lead at landing: the original item was not implementable, `cross-context-relation` forbids the relation the warning would demand) — QUESTION for lead below; removing the skip warns 13 times in NorthBank alone with no fix available
- [x] `bash scripts/verify-all.sh` green

## Gates

- [x] clean-code-swept — swept the diff against the eight principles; nothing above 0.5. The one thing the sweep changed: `relationGraph`'s inline node literal became a named `relationNode` function, because the extra field needed a return type for svelte-check to accept it (senior-dev, 2026-09-08T23:20:00Z)
- [x] verify-all — `bash scripts/verify-all.sh` exit 0: core 513, graphviz 27, doc 31, skill 26, northbank 3, petstore 20, rivermart 3, streamline 3, models/_shared 9, pages 752, apps/docs 23, apps/ods-vscode 15, pages e2e passed, petstore schema matches core dist (senior-dev, 2026-09-08T23:25:00Z)

## Comments

- **lead** (2026-09-08T17:10:00.000Z): the senior developer, after card 92 lands (the lead will say).
- **senior-dev** (2026-09-08T22:40:00.000Z): Picked the card up. Reset the worktree to origin/develop at 2401712 ("card 92 done, 93 dispatched"), installed, built core, graphviz and doc. Confirmed the starting point: `ODSRelationGraph` collects relations, identities and subtypes and nothing reads `attribute.valueobject` across a boundary (`packages/core/src/relation-map.ts`), and `ODSRelationMap.fromAggregate(account)` in NorthBank drew no Money at all.
- **senior-dev** (2026-09-08T23:10:00.000Z): Item 1 is in, derived the same way an identity is. `ODSRelationGraph` gains `borrowings`: the attributes in scope whose value object belongs to another bounded context, collected in `collectBorrowings` beside `collectIdentities` (`packages/core/src/relation-map.ts:161-167`). `ODSRelationMap` takes them as a fourth argument and draws one `uses` edge per attribute, from the holder to the value, labelled with the attribute's name — the same pairing an identity edge uses, because no relation can be declared to name the line (`packages/core/src/relation-map.ts:278-292`). The node type union gains `foreign_valueobject`, following `external_context` as card 81 left it; `relationNode(node, borrowed)` is the only place the mark is decided. The box lands in `contextMemberNamespace(vo)`, which is the lending context's own cluster, so the map says whose value it is without inventing a second label: DOT and PlantUML get the cluster/package name for free and Svelte Flow gets it as the subtitle under the box. The foreign mark is the stereotype `«borrowed value object»`, one constant in `packages/graphviz/src/role-labels.ts` that all three renderers read through `STEREOTYPES`; in the pages the box also carries the muted tone, which is the dashed border every muted class already draws. The legend row is derived from the nodes, not hard-coded: `RelationNodeData.borrowed` (`packages/pages/src/lib/flow/relation-graph.ts:44`) is what `relationLegend` reads, so the row appears only on a map that actually draws one.
- **senior-dev** (2026-09-08T23:15:00.000Z): NorthBank's Account now draws Money. `ODSRelationMap.fromAggregate(accounts/account)` gives `Money` and `AccountNumber` as `foreign_valueobject` in the cluster "NorthBank / Platform / Shared Financial Primitives / Shared Kernel", with five edges: `OverdraftLimit -uses-> Money : limit`, `Account -uses-> AccountNumber : accountNumber`, and `Account -uses-> Money` for `postedBalance`, `pendingAuthorisations` and `availableBalance`. Tests: `packages/core/src/derived-maps.test.ts` (a borrowed value drawn in the lending cluster, and a value of this context still plain), `packages/graphviz/src/relation-map.test.ts` (the DOT stereotype, the DOT cluster label, the PlantUML class stereotype and package), `packages/pages/src/lib/flow/relation-graph.test.ts` (the mark, the tone, the subtitle), `legend.test.ts` (the row appears only when a borrowed node is drawn) and `RelationNode.test.ts` (the rendered box says it is borrowed and whose). A `Borrowed value object` story is on `RelationNode.stories.svelte`.
- **senior-dev** (2026-09-08T23:25:00.000Z): `bash scripts/verify-all.sh` green end to end, exit 0. Summary: core 513, graphviz 27, doc 31, skill 26, northbank 3, petstore 20 (schema comparison against core dist: match), rivermart 3, streamline 3, models/_shared 9, pages 752, apps/docs 23, apps/ods-vscode 15, pages e2e full suite passed. Diagnostics per model are exactly what they were before the card: petstore `Swagger Petstore (v3): 0 diagnostic(s)`; RiverMart 2 (`aggregate-root`, `cross-aggregate-reference`); StreamLine 3 (`internal-consumable`, `schema-context`, `policy-complete`); NorthBank 4 (`relationship-declared`, `separate-ways`, `consumable-kind`, `context-serves-subdomain`) — every one a DELIBERATE. `npx tsc --noEmit` clean in core, graphviz, doc, apps/docs and apps/ods-vscode (skill only after its own `npm run generate`, which verify-all does); `npm run check` in pages clean, 939 files, 0 errors; biome clean on all eleven files I touched.
- **senior-dev** (2026-09-08T23:30:00.000Z): QUESTION for lead, on checklist item 2, and the reason the box is unticked. I reproduced the literal change: dropping `vo.boundedcontext !== context` from `attributeRelationCoherence` (`packages/core/src/validate.ts:863`) and rebuilding core gives 13 new warnings in NorthBank alone — `"Account" types attribute "postedBalance" by value object "Money" but declares no "uses" relation to "Money"` and twelve like it — and none of them can be fixed, because `cross-context-relation` is an error on any relation whose ends are in different contexts (`packages/core/src/validate.ts:274-287`), so the `uses` the warning asks for may not be written. With the edge derived there is also nothing left for the rule to check on a borrowed value: no declared relation exists for the attribute to disagree with, and the cardinality half of the rule compares the attribute against a relation's cardinality. So I have left the skip in place and only replaced its justification, which was the stale half — the old comment said the map never draws it, and now it does (`packages/core/src/validate.ts:860-865`). If you meant something else by "stops skipping" — a new rule that a borrowing must be carried by a shared kernel or conformist is already `valueobject-context` from card 92 — say so and I will do it in one pass.
- **senior-dev** (2026-09-08T23:30:00.000Z): One consequence worth your eye, not a decision I took alone. Borrowing is a property of the edge, so a value object is marked borrowed wherever the holder is drawn, including on a workspace-scope map where the lending context is itself in view: `ODSRelationMap.fromWorkspace(northbank)` shows Money and AccountNumber as `foreign_valueobject` in the Shared Kernel's cluster. I think that reads correctly — the box sits in the kernel's cluster and says every holder borrows it — but if you would rather the mark were relative to the map's scope, that is a change to `ODSRelationGraph` and I would want you to say it.
