---
column: done
labels: [backend, ddd, breaking]
priority: high
agent: senior-developer
live: false
clean-code-swept: true
updatedAt: 2026-09-10T02:20:00.000Z
---
# A stateless operation has a contract; a postcondition reads the request; a pair may hold two named agreements

Codex review run 7, issues 1, 2 and 11, each reproduced. Card 100 refused `precondition` and `postcondition` on a context invariant so it could never claim to hold after its operation, and left a freight quotation service, which stores nothing, with no home for "weight must be positive" and "the quote respects the tariff". A postcondition may reach the answer's attributes but not the request's, so "every returned itinerary arrives by the requested time" cannot name the requested time. And `relationship-duplicate` refuses a second directed relationship in one direction between a pair, which decision 15 said to reopen when one pair in one direction needs two dispositions: a negotiated fulfilment API and a tolerated legacy feed from the same warehouse are that case.

## Checklist

- [x] A context invariant may carry `precondition` or `postcondition`: both are checks, before or after the operation they name, and neither claims to hold at rest; `context-invariant-is-checked` refuses only a context invariant with no flag that names no guard, and the page says "checked before" or "checked after"; the quotation example validates with both rules on the context; decision 27 amended by the lead
- [x] A postcondition's admissible targets include the guarded operation's request schema and everything it composes, as a precondition's do; the review's itinerary example (answer attribute constrained against request attribute) validates; decision 19 amended by the lead
- [x] `ContextRelationshipSchema.name?: string`: a second directed relationship between one pair in one direction is allowed when both carry distinct names; the ref of a named relationship appends the name; `relationship-duplicate` refuses only unnamed duplicates; `findRelationship` and every reader of the pair key follow; the context map draws two lines labelled by name; the reference models unchanged unless one already narrates two agreements; decision 15 amended by the lead
- [x] `bash scripts/verify-all.sh` green; diagnostics per model unchanged or explained; the review's three probes rerun

## Gates

- [x] clean-code-swept — swept the diff (33 files, ~700 lines added) against the eight principles. Two things it changed. `inGuardedRequest` is now `inGuardedShapes`: it was already answering about the answer as well as the request for a precondition, and now that both flags reach the same three shapes the old name named a third of what it did. And `contextInvariantGuarded` and `contextInvariantIsChecked` were two rules with one job left between them once both flags became legal, so they are one — see the comment on the merge below. Three things it looked at and left. `relationshipKey` still builds a string rather than a tuple, because every other key in this file is a string and the `~` separator is the ref's own. `ContextRelationship.nameId` is a getter of two lines rather than an inline `snakeCase(this.name ?? "")` at both sites, because the ref and the duplicate key have to agree on it and a getter is what makes them. And the context map's edge id gained a fourth `|`-joined part rather than becoming a structured key, because the map's edges are keyed by string everywhere and changing that is a bigger move than this card. (senior-developer, 2026-09-10T02:10:00Z)
- [x] verify-all — `bash scripts/verify-all.sh` exit 0: core 642, graphviz 28, doc 31, skill 26, northbank 3, petstore 20, rivermart 3, streamline 3, models/_shared 9, pages 761, apps/docs 23, apps/ods-vscode 15, pages e2e passed, petstore schema matches core dist (senior-developer, 2026-09-10T02:15:00Z)

## Comments

- **the lead** (2026-09-09T23:30:00.000Z): senior-developer, now; `feat!`.
- **senior-developer** (2026-09-10T00:20:00.000Z): Picked it up; worktree reset to origin/develop at 5459e43 ("Codex run 7 rulings; card 103"), installed. Read the card, the three amendments (decision 15's two-agreements entry, decision 19's third, decision 27's third), and decisions 03 and 26 for the ref grammar. Baseline diagnostics before any change, all DELIBERATE: `petstore: 0`, `northbank: 4`, `rivermart: 2`, `streamline: 4`. Each of the three issues reproduced against that baseline — see the probe block below for what each did before.
- **senior-developer** (2026-09-10T00:55:00.000Z): Issue 1, the stateless operation's contract. `context-invariant-is-checked` no longer reads the flags to refuse them; it reads them to know that a flagged rule has already been asked for its guard elsewhere. QUESTION for lead, decided narrowly and flagged rather than blocked: the checklist says the rule "refuses only a context invariant with no flag that names no guard", and `context-invariant-guarded` refused exactly the no-guard case for every context invariant, flagged or not. Keeping both would report the unflagged, unguarded rule twice under two ids. So `context-invariant-guarded` is gone and `context-invariant-is-checked` carries its check, narrowed to the unflagged rule; a flagged one with no guard is reported once by `precondition-names-operation` or `postcondition-names-operation`, and one whose guard is another context's operation by `invariant-in-context`, so nothing that used to be caught is now let through. If you would rather keep the `context-invariant-guarded` id and retire `context-invariant-is-checked` instead, it is a rename and two message strings. The invariant page reads the flags back as "Checked before" and "Checked after" on the guards heading, with its own lead and empty text for each; a context rule with neither flag still says "Checked by".
- **senior-developer** (2026-09-10T01:15:00.000Z): Issue 2, the postcondition that reads the request. `guardedSchemas` pushes the guarded operation's `schema` for a postcondition as it already did for a precondition, so the two now reach the same three shapes — request, answer, refusals — and composition follows into everything those compose. The two flags still differ where it matters: which of them a rule sets is what the page says about when the rule holds, and `postcondition-names-operation` still refuses a rule marked both. The refusal message for a postcondition changed one word ("takes, returns or rejects with"), and the test that asserted the old refusal is now the review's itinerary case: `Arrives By The Requested Time` names `Itinerary.arrivesAt` and `Itinerary Request.arriveBy` and validates. A postcondition naming a shape its guard never carries is still refused, and there is a test for that too.
- **senior-developer** (2026-09-10T01:40:00.000Z): Issue 11, two agreements per pair and direction. `name?: string` is on both members of `ContextRelationshipSchema` — the card names the union, and a pair could as easily hold two partnerships worth telling apart — and it flows through `ContextRelationshipAttributes`, `DirectedRelationshipOptions`, `SymmetricRelationshipOptions`, `toSchema`/`fromSchema` and the JSON schema. The ref appends the name in snake case (`#/relationships/warehouse~upstream-downstream~sales~legacy_feed`), through a `nameId` getter that the duplicate key also reads, so "Legacy Feed" and "legacy feed" are one name to both and cannot silently share a ref. `relationshipKey` keys on the name, so two distinct names stand and an unnamed or same-named pair is still an error, with the message now saying which repair to make. Every reader of the pair key follows: the context map's edge id carries the name, so the pair draws one line per agreement; Graphviz labels the line with the stereotype and the name on a second row; the pages map puts `U/D · Legacy Feed` on the stereotype badge and matches each line to its own intent, so the two dispositions no longer collide. No reference model narrates two agreements between one pair in one direction — I grepped all four — so all four are unchanged, as the checklist allows.
- **senior-developer** (2026-09-10T02:00:00.000Z): Docs and skill followed the three changes, because leaving them would have put the text and the code back into the disagreement the last two reviews reported: the skill's ref grammar gains the named-relationship form and its example, `dsl-api.md` gains `name` on `upstreamOf`/`partnerOf` and rewrites `bc.addInvariant`, `SKILL.md` and `ddd-glossary.md` stop saying a context's rule is checked "before acting" only, and `apps/docs`' validation table and tactical-design page drop the retired rule and gain the quotation example. `references/model-reference.md` and `references/validation-rules.md` are generated and were regenerated by the skill build.
- **senior-developer** (2026-09-10T02:18:00.000Z): `bash scripts/verify-all.sh` green end to end, exit 0. Summary: core 642 tests passed, graphviz 28, doc 31, skill 26, northbank 3, schema comparison (petstore vs core dist) match, petstore 20, rivermart 3, streamline 3, models/_shared 9, pages 761, apps/docs 23, apps/ods-vscode 15, pages e2e (full suite) passed. `npx tsc --noEmit` clean in core, graphviz, doc, skill, pages, all four models, models/_shared, apps/docs and apps/ods-vscode; `npm run check` in pages clean, 939 files, 0 errors, 0 warnings; biome clean on every file I touched (22 source files). Diagnostics per model, unchanged from the baseline and every one a DELIBERATE: `petstore: 0`. `rivermart: 2` — `[error] aggregate-root`, `[error] cross-aggregate-reference`. `northbank: 4` — `[error] consumable-kind`, `[error] separate-ways`, `[warning] context-serves-subdomain`, `[warning] relationship-declared`. `streamline: 4` — two `[error] internal-consumable`, `[error] schema-context`, `[warning] policy-complete`. The three probes, rerun against the built core dist:

```
probe 1 — stateless operation's contract on the context: 0 error(s)
probe 2 — postcondition reads the request: 0 error(s)
probe 3 — two named agreements: 0 relationship-duplicate diagnostic(s)
  #/relationships/warehouse~upstream-downstream~sales~fulfilment_api
  #/relationships/warehouse~upstream-downstream~sales~legacy_feed
  map draws 2 line(s):
    Warehouse → Sales [upstream-downstream] "Fulfilment API"
    Warehouse → Sales [upstream-downstream] "Legacy Feed"
  unnamed duplicate still refused: relationship-duplicate
```

  Probe 1 is the freight quotation service with no aggregate, stating `Weight Is Positive` (precondition) and `Quote Respects The Tariff` (postcondition) on the context; before the change both were `context-invariant-is-checked` errors. Probe 2 is the review's itinerary, a postcondition naming `Itinerary.arrivesAt` and `Itinerary Request.arriveBy`; before, the request attribute was an `invariant-in-aggregate` error. Probe 3 is the warehouse's negotiated API beside its tolerated legacy feed; before, the second was a `relationship-duplicate` error and the map drew one line.
