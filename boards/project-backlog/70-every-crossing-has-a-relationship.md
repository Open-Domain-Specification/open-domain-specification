---
column: done
labels: [backend, ddd]
priority: high
agent: ironhide
live: false
clean-code-swept: true
updatedAt: 2026-09-07T14:40:00.000Z
---
# Every crossing has a declared relationship, and no two relationships collide

Prowl's review, findings 4, 6 and 10. Decision 03 made relationships explicit and decision 08 promised a rule that a consumption of another context needs a matching relationship; no such rule exists, the context map just draws an implied edge (probe 3). An `identifies` into another context draws nothing on any map and needs no relationship (NorthBank's Fraud case identifies a customer with no Fraud/Customer relationship). And two relationships of one type in one direction between a pair share a ref and the second is unreachable (probe 6).

## Checklist

- [x] `relationship-declared` (warning): a consumption across contexts, or an `identifies` across contexts, with no relationship joining the two contexts in the right direction (a symmetric relationship counts either way); fix text names the two contexts; the implied edge on the context map stays as the visual hint
- [x] The context map draws an identity-only dependency as an implied edge like a consumption's, with its own legend row; decision 14's sentence "reads on the consumable map" corrected by the lead to the context map
- [x] `relationship-duplicate` (error): two relationships of the same type and direction between one pair; fix text says roles go on one relationship
- [x] RiverMart's role-less relationship invented to record an identity dependency re-read: keep it with roles if the dependency is real, or let `relationship-declared` and the implied edge carry it
- [x] Root suites green inside each package in build order; pages at 100% with `npm run check` clean

## Gates

- [x] clean-code-swept — swept my own diff. It found one thing: `attributeOwnersIn` was private to `validate.ts` and the new map code needed the same walk, so it moved to `packages/core/src/identity-crossings.ts:9-15` beside `identityCrossings` and `validate.ts` imports it rather than keeping a second copy (ironhide, 2026-09-07T14:40:00.000Z)
- [x] verify-all — green end to end: core 345, graphviz 21, doc 29, skill 26, northbank 3, petstore 18, rivermart 3, streamline 3, models/_shared 9, pages 710, apps/docs 22, apps/ods-vscode 9, pages e2e (diagram-panel-fit) passed, schema comparison (petstore vs core dist) match (ironhide, 2026-09-07T14:40:00.000Z)

## Comments

- **optimus-prime** (2026-09-07T10:00:00.000Z): Ironhide, after card 69 lands (the lead will say); `feat`. Decision 08 is marked Proposed by the lead and its table corrected; do not touch it.
- **ironhide** (2026-09-07T14:40:00.000Z): Both rules are in. `relationship-declared` (`packages/core/src/validate.ts:779-825`, catalogued at `:1854-1863`) walks the two kinds of crossing — a consumption of another context's consumable, and an identity naming another context's entity — and asks for a relationship pointing the way the crossing runs. A partnership or shared kernel satisfies it either way round, since neither end is upstream of the other; `separate-ways` satisfies nothing, and the `fix` says so in as many words, because it is the declaration that the two contexts do *not* integrate. The direction test is `relationshipJoins` at `:750-765`. It reports once per undeclared pair and direction rather than once per crossing: one relationship clears them all, so one warning is what a reader can act on. `relationship-duplicate` (`:845-863`, catalogued at `:1863-1872`) is an error because a relationship's ref is its two contexts and its type, so a second copy is unreachable and everything written on it — description, comments, disposition — is lost the moment it is written. A symmetric type has no direction, so participants either way round count as the same one (`relationshipKey`, `:827-834`). Tests at `packages/core/src/validate.test.ts:2370-2559`.
- **ironhide** (2026-09-07T14:40:00.000Z): The map. `ODSContextMap` now implies an edge from an identity as well as from a consumption (`packages/core/src/context-map.ts:111-125`), and `ODSContextMapEdge.implied` is no longer a boolean but `"consumption" | "identity" | false` (`:205`, with `ImpliedBy` at `:177`), so every renderer can say what put the edge there. Identities are walked last, so a pair that already exchanges something keeps its consumption edge, which says strictly more — the identity travels on that traffic. The context holding the identity is the downstream end: it is the one shaped by the other's model. The enumeration lives in the new `packages/core/src/identity-crossings.ts`, shared by the map and the rule so neither can drift from the other. Graphviz draws it dashed under an `«id»` stereotype rather than `U/D` (`packages/graphviz/src/context-map.ts:72-101`, label at `role-labels.ts:28-36`), because an implied, role-less edge has no relationship type to claim and what the reader wants is where the dependency came from; that also matches the `«identifies»` the relation map already draws on the same fact. Pages follows (`packages/pages/src/lib/flow/context-graph.ts:100-112`) and the legend gains its own row, `dashed «id» — Identity dependency`, kept apart from `dashed — Implied relationship` (`legend.ts:87-106`). The doc relationship table now reads `upstream-downstream (implied by identity)`. Decision 14's Decision bullet said the consumable map is where that dependency reads; corrected to the context map in one line, with an amendment saying why (the Consequences section already said the context map, so the record now agrees with itself).
- **ironhide** (2026-09-07T14:40:00.000Z): The models. `relationship-declared` raised seventeen warnings across the four reference models on its first run — NorthBank 7, RiverMart 5, StreamLine 5, Petstore 0 — and every one was a real gap. Sixteen are now fixed by declaring the relationship the prose supports; the seventeenth is deliberate and is covered below. NorthBank: `Ledger → Accounts` is a genuine published-language/conformist supply, since Accounts takes `EntryPosted` as published (`models/northbank/src/workspace.ts:2613-2619`), plus five identity-only relationships at `:2692-2727`. RiverMart: five more at `models/rivermart/src/workspace.ts:2464-2492`. StreamLine: five at `models/streamline/src/workspace.ts:2163-2197`. Petstore was already clean. The identity-only ones carry empty role lists, which is the honest reading: nothing is exchanged, so neither end plays a part in an exchange, and `relationship-roles-backed` has nothing to complain about. That answers the RiverMart re-read on the checklist: `orderBC.downstreamOf(offersBC)` (`models/rivermart/src/workspace.ts:2458-2463`) stays exactly as it was, and it is no longer an invention — it is the first example of what the new rule requires, which the comment above it now says. One warning survives on purpose: NorthBank's deliberate violated separate ways between Channels and Credit Decisioning now raises `relationship-declared` as well as `separate-ways`, which is the rule working, and it is listed in the model's `deliberate` array with the reason (`models/northbank/src/workspace.test.ts:14-23`).
- **ironhide** (2026-09-07T14:40:00.000Z): Two things I saw and left alone, for the lead to schedule if they are worth it. First, decision 03's Decision section still says only "Consumptions between two contexts with no explicit relationship produce an implied upstream/downstream edge"; identities do too now, and that record is the lead's to amend, not mine. Second, `narrative.ts`'s implied aside is hard-coded to "Implied by consumptions" (`packages/core/src/narrative.ts:150-152`); no caller feeds it an implied link today — both pass real `ContextRelationship`s — so nothing is wrong on screen, but the sentence would be false if one ever did. Also noticed, unrelated to this card: petstore's fulfilment subdomain page draws `Catalog BC → Sales BC` as implied even though the workspace declares that relationship, because `ODSContextMap.fromScope` keeps only relationships involving a context in the subdomain's scope while the consumption walk reaches further out. The page has been wrong that way since decision 03; it is a scoping bug, not this card's.
