---
column: done
labels: [backend, ddd, breaking]
priority: high
agent: senior-dev
live: false
clean-code-swept: true
updatedAt: 2026-09-09T00:20:00.000Z
---
# An answer is named by its origin; a precondition is stated, not inferred

Codex review run 4, issues 1 and 4, both card 92's. A reactor names an answer by its schema, so when `AuthorisePayment` and `RefundPayment` both reject with `PaymentDeclined` the reaction walk connects both to the cart-reopening policy and nothing warns. And card 92 made "constrains an operation" mean "is a precondition", which conflates what kind of rule something is with which operation keeps it: `PostEntry` must produce balanced postings and the balance rule stays true afterwards.

## Checklist

- [x] An answer has a ref of its own: `<operation ref>/returns` and `<operation ref>/rejects/<schema id>`; a policy's `on` and a process's `on` and `ends` name answers by those refs, never by a bare schema; `getReactionTriggerByRef` resolves them; the DSL takes `op.returned()` and `op.rejected(schema)` (or the closest fit, say which on the card); `toSchema`/`fromSchema`; the ref grammar documented
- [x] The reaction walk and the flow map step from exactly the named operation to its answer to the reactor; `consumable-kind` refuses an answer of an operation the reactor's context does not consume and a rejection the operation does not declare; test with the review's two-operation case, which must connect only the one named
- [x] `InvariantSchema.precondition?: boolean`: a precondition is checked before the operation it names runs and is not kept true afterwards; an invariant that names an operation without the flag is kept by that operation and stays true after it; `invariant-in-aggregate` and the invariant page say which by the flag, not by whether an operation is named; `precondition-names-operation` (error) when the flag is set and no operation is named; the five reference preconditions set the flag; NorthBank's `DailyLimit` re-read against its own description
- [x] Decisions 23 and 27 amended by the lead; you confirm the mechanics match
- [x] `bash scripts/verify-all.sh` green; diagnostics per model unchanged or explained

## Gates

- [x] clean-code-swept — swept the diff against the eight principles; nothing above 0.5. Two things the sweep changed: `answersOf(operation)` in `reaction-walk.ts` became a one-line alias of the new `Consumable.answers` and was deleted rather than kept as indirection, and `answeredBy(bc, schema)` was deleted outright — it existed only to guess which operations might have answered with a shape, and an answer now names its operation, so both callers (the policy and process pages) read `answer.operation`. `ReactionChain.answerers` and `answerersOf` went the same way (senior-dev, 2026-09-09T00:10:00Z)
- [x] verify-all — `bash scripts/verify-all.sh` exit 0: core 521, graphviz 27, doc 31, skill 26, northbank 3, petstore 20, rivermart 3, streamline 3, models/_shared 9, pages 752, apps/docs 23, apps/ods-vscode 15, pages e2e passed, petstore schema matches core dist (senior-dev, 2026-09-09T00:15:00Z)

## Comments

- **lead** (2026-09-08T23:10:00.000Z): the senior developer, now; `feat!`. Precise; do not widen.
- **senior-dev** (2026-09-08T23:40:00.000Z): Picked it up. Reset the worktree to origin/develop at 0d94d51 ("Codex run 4 rulings; card 94; decisions 23, 27 amended"), installed. Read both amendments and confirmed the mechanics match what they say: decision 23's third amendment asks for `<operation>/returns` and `<operation>/rejects/<schema>` as refs of their own with the walk stepping from exactly that operation, and decision 27's second amendment asks for `precondition: true` stated rather than inferred, naming the operation it guards. I did not stage issue 1 as a running model; I read it off the code, which says it plainly enough that staging it would have added nothing. `ReactionChain.answerers` was `Map<DataSchema, Consumable[]>` and `stepsFrom` pushed a step to every listener of every shape the node answered with, so two operations sharing one rejection shape both stepped to the one reactor; and `consumable-kind` asked only whether *some* operation the context consumes answers with that shape, so nothing warned. The two tests I added are that case written down, and they are what a reader should check the fix against. Issue 4 needs no reproduction — it is what `invariant-in-aggregate`'s own text asserted.
- **senior-dev** (2026-09-09T00:00:00.000Z): Item 1. New `Answer` class in `packages/core/src/workspace.ts`: an operation, a shape and whether it is a refusal, with `ref` `<operation ref>/returns` or `<operation ref>/rejects/<schema id>`, plus `name` (the shape's), `origin` ("Authorise Payment rejects with Payment Declined", which is what diagnostics say), `boundedcontext` (the operation's — an answer comes from where the call went) and `declared`. `ReactionTrigger` is now `Consumable | Answer`; a bare `DataSchema` no longer typechecks as one, which is what forced every call site into the open. The DSL is exactly what you asked for: `op.returned()` and `op.rejected(schema)`, cached per operation so naming one twice names one object — the walk keys its listeners by identity, so that had to hold. `returned()` throws when the operation returns nothing (there is no answer to name, and an answer with no shape would infect every surface's types); `rejected(schema)` is deliberately permissive, because a refusal the operation never declares is a modelling mistake `consumable-kind` has to be able to report rather than a crash at load. `Workspace.getAnswerByRef` resolves both shapes against what the operation actually declares, wired into `getByRef` by the `rejects` segment and by a final `returns` — a consumable whose own id is `returns` still resolves as a consumable, because `provides` is matched first. `toSchema`/`fromSchema` need no special case: the ref is what is written and `getReactionTriggerByRefOrThrow` reads it. Ref grammar: two rows and a paragraph in `packages/skill/scripts/generate.mts`, regenerated into the bundle.
- **senior-dev** (2026-09-09T00:05:00.000Z): Item 2. `ReactionChain` no longer keeps an answerers map at all: `stepsFrom` reads `node.answers` and looks each one up in `listeners`, so the step runs from exactly the operation named. `ODSFlowMap.enter` and the ending-answer branch step to `answer.operation` instead of searching for operations that answer with a shape. `consumable-kind` now reports two distinct things, both with the answer's origin in the message: a rejection the operation never declares, and an answer of an operation nothing in the reactor's context consumes. The review's case is tested twice — `packages/core/src/derived-maps.test.ts` ("connects only the operation the reactor named, where two share a shape": Refund Payment rejects with the same Payment Declined and draws no edge to the process) and `packages/core/src/validate.test.ts` ("keeps two operations' refusals apart even when the shape is shared": two different `Answer` objects, no new diagnostic).
- **senior-dev** (2026-09-09T00:10:00.000Z): Item 3. `InvariantSchema.precondition?: boolean` and `Invariant.precondition`, absent meaning false; `precondition-names-operation` (error) fires when the flag is set and no operation is named. `invariant-in-aggregate`'s prose, its catalogue `why`, the schema's `constrains` doc and the invariant page all say the same thing now: naming an operation says which operation keeps the rule, and `precondition` says whether the rule survives it. The page picks its wording from `i.precondition`, never from `guarded.length` — the `guard` block became `precondition` and the plain `aggregate` block says balanced postings are still balanced once the posting is made. Five reference preconditions set the flag, each with a line saying what it was checked against and who can move it: NorthBank `FundsAvailableAtInitiation` and `AuthWithinAvailableBalance`, Petstore `ApproveOnlyWhenAvailable`, StreamLine `SessionNeedsEntitlement` and `AdsOnlyOnAdSupportedPlan`. The sixth aggregate invariant that names an operation, Petstore's `SoldNotReopen`, deliberately does not: a sold pet's status is the aggregate's own and stays sold on every save, so `ChangePetStatus` is named for responsibility. Its comment now says so.
- **senior-dev** (2026-09-09T00:12:00.000Z): NorthBank's `DailyLimit`, re-read against its own description, is not a precondition and I have not flagged it. Its sentence is "instructions from one account never exceed the daily limit in total" — a property of the context's own instructions, as true after `InitiatePayment` as before it, and everything it counts is this context's to read in one place. That is the opposite of `FundsAvailableAtInitiation` two lines below it, which reads a balance Accounts owns and may move the next second. `InitiatePayment` is named on both, which is exactly the conflation the card is about: the same guard, two different kinds of rule. Comment added above it saying so, so the next reader does not "fix" it.
- **senior-dev** (2026-09-09T00:15:00.000Z): `bash scripts/verify-all.sh` green end to end, exit 0. Summary: core 521 tests passed, graphviz 27, doc 31, skill 26, northbank 3, schema comparison (petstore vs core dist) match, petstore 20, rivermart 3, streamline 3, models/_shared 9, pages 752, apps/docs 23, apps/ods-vscode 15, pages e2e (full suite) passed. Diagnostics per model unchanged, every one a DELIBERATE: `Swagger Petstore (v3): 0 diagnostic(s)`; `RiverMart: 2` — `[error] aggregate-root` on `cart_&_checkout/aggregates/wishlist`, `[error] cross-aggregate-reference` on `cart_&_checkout/aggregates/cart/entities/cart`; `StreamLine: 3` — `[error] internal-consumable` on `recommendations/services/recommendations_api`, `[error] schema-context` on `playback/.../playback_started`, `[warning] policy-complete` on `devices/policies/recertify_on_sdk_release`; `NorthBank: 4` — `[warning] relationship-declared` and `[error] separate-ways` on `branch_&_contact_centre/services/channels_app`, `[error] consumable-kind` on `lending/policies/escalate_arrears`, `[warning] context-serves-subdomain` on `identity_&_access`. `npx tsc --noEmit` clean in core, graphviz, doc, all four models, apps/docs and apps/ods-vscode; `npm run check` in pages clean, 939 files, 0 errors, 0 warnings; biome clean on every file I touched.
- **senior-dev** (2026-09-09T00:20:00.000Z): Two things for your eye, neither a decision I took alone. First, the pages: an answer has no page of its own, so a row that renders one links its name to the shape and names the operation in the Provider column, and the context column is now the operation's context rather than the schema's — that is where the answer comes from. The Kind column says `answer` or `rejection`. On the context page's dense reaction rows there is no provider column, so the origin is the link's tooltip. Second, `answeredBy` is gone from the core's public surface along with `answersOf` and `ReactionChain.answerersOf`; nothing outside this repo can be using them yet, and keeping a function whose whole job was to guess an answer's origin would have left the defect a caller could still reach for.
