---
column: review
labels: [models, ddd]
priority: medium
agent: developer
live: false
clean-code-swept: true
updatedAt: 2026-09-09T21:10:00.000Z
---
# The reference models say postcondition and starting command where those are true

Card 99 gave the model postconditions, commands that start a process and completions a reactor may wait on, and left the four reference models to be re-read. Invariants that are really guarantees about an answer (NorthBank's `AuthWithinAvailableBalance` is the obvious candidate) become postconditions; processes that begin on a command begin on it; a process waiting for a payload-free call's success waits on its completion rather than a fabricated event.

## Checklist

- [x] Every invariant in the four models re-read against decision 19's amendments: a rule about what an operation answers with becomes `postcondition: true` and names the answer's attributes; a rule checked before the call stays a precondition
- [x] Every process re-read: one that begins on a command names it in `starts`; one that waits for a returns-less call's success waits on `op.completed()` and the invented completion event, if any, comes out
- [x] DISCOVERY.md gains a line per model naming what changed and why; `.ods/` regenerated; each model's diagnostics unchanged or explained
- [x] `bash scripts/verify-all.sh` green

## Gates

- [x] clean-code-swept — swept the diff (5 files) against the eight principles. It is two model edits and four documentation entries, no new abstractions: `startOnboarding` is a plain capture of an operation the DSL already returned, and the postcondition's `constrains` target is `cardApprovalSchema.attributes.get("amount")!`, the non-null assertion the rest of both models already use for this exact lookup (`variant.attributes.get("sku")!` and four others in rivermart alone). Nothing else in the diff repeats a shape or invents one (developer, 2026-09-09T21:09:00Z)
- [x] verify-all — `bash scripts/verify-all.sh` exit 0: core 626, graphviz 27, doc 31, skill 26, northbank 3, petstore 20, rivermart 3, streamline 3, models/_shared 9, pages 757, apps/docs 23, apps/ods-vscode 15, pages e2e (297) passed, petstore schema matches core dist (developer, 2026-09-09T21:10:00Z)

## Comments

- **the lead** (2026-09-09T18:30:00.000Z): developer, after card 100 lands; models only.
- **developer** (2026-09-09T20:50:00.000Z): Picked it up; worktree reset to origin/develop at 2d3d7a7 ("card 100 done; decision 29; card 102; 101 and 102 dispatched"), `npm install` clean, `packages/core`, `packages/graphviz` and `packages/doc` all build clean. Read the card, decisions 19, 23 and 27 with every 2026-09-09 amendment, decision 29, card 99 and card 100 in full (including their DECISIONS NEEDED comments and probe logs), `Invariant`, `Process` and `Consumable.completed()` in `packages/core/src/workspace.ts`, and `guardedSchemas`/`inGuardedRequest` in `packages/core/src/validate.ts`. Card 102 is core-only and dispatched alongside this one; I have not touched `packages/core`.
- **developer** (2026-09-09T21:00:00.000Z): Every invariant in all four models, listed by what it constrains, checked against two questions: does it name an operation, and does that operation return anything. Ninety-five invariants total (northbank 40, petstore 6, rivermart 22, streamline 26 — one, `SingleCurrencyPerEntry`, constrains nothing and is out of scope for either flag). Of those, only nine name an operation at all: `DailyLimit` and `FundsAvailableAtInitiation` (northbank, `InitiatePayment`), `AuthWithinAvailableBalance` (northbank, `AuthoriseCard`), `OneOpenApplicationPerCustomer` (northbank, `SubmitApplication`), `ApproveOnlyWhenAvailable` (petstore, `ApproveOrder`), `OneActiveOfferPerSellerSku` (rivermart, `PublishOffer`), `SessionNeedsEntitlement` and `WithinStreamLimit` (streamline, `StartPlayback`), `OneActiveSubscriptionPerHousehold` (streamline, `StartSubscription`). `DailyLimit`, `OneOpenApplicationPerCustomer`, `OneActiveOfferPerSellerSku`, `WithinStreamLimit` and `OneActiveSubscriptionPerHousehold` are context invariants, which decision 27's second 2026-09-09 amendment already refuses either flag on — nothing to reconsider. Of the remaining four aggregate preconditions, three guard an operation that returns nothing (`InitiatePayment`, `ApproveOrder`, `StartPlayback`): a postcondition needs an answer to be a guarantee about, and there is none to name, so all three stay preconditions exactly as they read. `AuthWithinAvailableBalance` is the exception the card names: `AuthoriseCard` returns `CardAuthorisationApproved` with the amount now held, so "an authorisation is approved only if the balance covers it" is truthfully "every approval AuthoriseCard makes is within the balance", a guarantee about that answer and not a check on the request (the balance isn't in the request at all — it's read from another context through the ACL). Changed to `postcondition: true`, description reworded as the guarantee, and `constrains` moved from the `Authorisation` entity to `CardAuthorisationApproved.amount`, the attribute the guarantee is actually about. No other invariant in any model qualified; this was the one candidate, as the card's own wording ("the obvious candidate") suggested.
- **developer** (2026-09-09T21:05:00.000Z): All five processes re-read for both halves of the second checklist item. `starts`: northbank's "Customer onboarding" started on `OnboardingStarted`, an `internal: true` event raised by `StartOnboarding` and heard by nothing else in the model — invented for the process to hear, which is exactly what decision 23's third amendment (card 99) lets the model stop doing. Changed to `.starts(startOnboarding)`, the command itself; `OnboardingStarted` and its `raises` come out. The other four `starts` events — `PaymentInitiated` and `AuthoriseCard`'s process (northbank), `OrderPlaced` (petstore), `CartCheckedOut` and `OrderPlaced` (rivermart), `MasterDelivered` (streamline) — are all `published-language` with real external readers or, for `MasterDelivered`, cross a context boundary and so can only ever be an event (decision 23's amendment lets a command start a saga only in the process's *own* context). None of the five is the invented-bridge shape; none moved. `op.completed()`: every event a process's `on` or `ends` names was checked against the operation that raises it for whether that operation has no `returns` and whether the event has any consumer besides the waiting process. All of them — `PartyMatched`, `SubmitToScheme`'s own answer (already `.returned()`/`.rejected()`), `ScoreTransaction`'s answer (already `.returned()`), `PetStatusChanged`, `ShipmentDispatched`, `StockShort`, `PaymentAuthorised`/`AuthorisePayment`'s rejection (already the answer), `EncodingCompleted` — are real, independently meaningful published facts, not a returns-less call's own success renamed. None is the shape `op.completed()` was built for; none moved.
- **developer** (2026-09-09T21:08:00.000Z): DISCOVERY.md, one entry per model. NorthBank gets the substantive one: `## Revision (card 101)` covers both changes above, names the three `published-language` events that stayed as they were and why, and says why `AuthWithinAvailableBalance` differs from `FundsAvailableAtInitiation` even though they read alike. Its event-storming table row for `OnboardingStarted` and the "Recorded as:" paraphrase of the onboarding process both now say `StartOnboarding`. Petstore, RiverMart and StreamLine each gain a short `## Revision (card 101): re-read, nothing moved` naming what was checked and why nothing qualified, since the checklist asks for a line per model and three of the four found nothing to change — I did not find a "nothing changed" precedent in these three files' own history (cards 99 and 100 left no trace in them at all when nothing moved), so if the lead would rather these three stayed silent the way the others did, that's a one-comment revert. `.ods/northbank.json` regenerated via `node src/generate.ts`; the other three models are untouched so their `.ods/` output is unchanged and was not regenerated. NorthBank's diagnostics are the same four DELIBERATEs as before (`relationship-declared` warning + `separate-ways` error on Branch & Contact Centre/Decide, `consumable-kind` error on Escalate arrears, `context-serves-subdomain` warning on Identity & Access) — `northbank/src/workspace.test.ts` asserts that exact set and passed unchanged.
- **developer** (2026-09-09T21:10:00.000Z): `bash scripts/verify-all.sh` green end to end. Summary: core 626 tests passed, graphviz 27, doc 31, skill 26, northbank 3, schema comparison (petstore vs core dist) match, petstore 20, rivermart 3, streamline 3, models/_shared 9, pages 757, apps/docs 23, apps/ods-vscode 15, pages e2e (full suite, 297) passed. `npx biome check` clean on every file I touched (`models/northbank/src/workspace.ts`; the four `DISCOVERY.md` files and `.ods/northbank.json` are not biome's). `git diff --stat`: `models/northbank/.ods/northbank.json` (21 lines), `models/northbank/DISCOVERY.md` (+28/-2), `models/northbank/src/workspace.ts` (+21/-21 net, two changes), `models/petstore/DISCOVERY.md` (+10), `models/rivermart/DISCOVERY.md` (+11), `models/streamline/DISCOVERY.md` (+10). Nothing in `packages/`, `pages/`, or any other model's `src/workspace.ts` touched. Not yet committed — the brief says the lead merges and I never push; ready for a `fix(models):` commit on request or the lead can take it from here.
