---
column: done
labels: [backend, docs, breaking]
priority: high
agent: senior-developer
live: false
clean-code-swept: true
updatedAt: 2026-09-11T14:05:00.000Z
---
# A process hears the answer of the call its start made; an external consumer is not asked how it protects itself; the surfaces say what the newest amendment says

The architect's sixteenth round found no rule doing the opposite of its decision on a shape a first model would write, and these. A process that starts on a command whose handler makes the first outbound call cannot wait on that call's answer, because an answer routes only to a reactor that issued the call and the process issued nothing yet; the clean form costs one more internal operation and event, and RiverMart's `Hold attempt` passes only because a second `by` happens to exist. Decision 23 now says the operation that starts a process is the process's own first step, so the process hears the answers of the calls that operation made. The shortest workaround for that gap, a process that starts on and issues the same operation, validated clean though it spawns instances, because `reEntersWhileAlive` accepted a ring closed by the start step. `role-coherence` asks an external consumer of our event for a downstream role, which is its inside (decision 28). The JSON schema requires `attributes` on `Entity` and `DataSchema` while the loader accepts their absence, so the two authoring surfaces disagree. And five hand-written sentences drifted after the latest amendments (a boundary-only context's schema as an identity target; borrowing routes in `SKILL.md`; an external invariant on its own event; optional collections; the specialisation table row), `SKILL.md` still tells an author to default to conformist against decision 03, the `identifies` comment does not mention boundary-only, and the generator renders a symmetric relationship's participants as "array of unknown". Decisions 11, 15, 23 and 28 are amended.

## Checklist

- [x] `routesTo` and `hearsAnswerOf`: a process hears the answer of a call made `by` an operation that starts it (through the local `by` chain from that operation), across a boundary still one hop; `consumable-kind` follows; tests for the checkout saga whose start command's handler calls `Pay` (clean) and for a process starting on a different command (still refused); RiverMart's `Hold attempt` unchanged and still clean
- [x] `reEntersWhileAlive`: a ring closed by the process's own `starts` step is not re-entry while alive; a process that starts on and issues the same operation is reported as spawning instances; test
- [x] `role-coherence` asks no downstream role of an external or boundary-only consumer; test for the partner carrier subscribing to our event
- [x] `EntitySchema.attributes` and `DataSchemaSchema.attributes` optional in the schema so the JSON schema and the loader agree; JSON schema regenerated; test that ajv and the loader accept the same file
- [x] The sentences corrected: `apps/docs/docs/3-core/3-tactical-design.md` (identity targets, near line 195) and the `identifies` comment in `schema.ts` (lines 53-64) name external, mud and boundary-only contexts and their schemas; `packages/skill/skill/SKILL.md` borrowing routes (near 104-106), external invariant (97-103), optional collections (72-75), and the default-conformist guidance (171, 185) which now says a customer-supplier downstream writes no role and a conformist is the downstream with no say; `apps/docs/docs/3-core/2-strategic-design.md` external invariants (68-72); `references/dsl-api.md` line 18; the `specialisation-in-boundary` row of the validation table; `generate.mts` renders a symmetric relationship's participants as two refs
- [x] The drift test pins the current wording positively for the facts that drifted twice (borrowing routes, external invariants, identity targets, optional collections), so a future amendment that changes the fact fails the test until the sentence follows
- [x] Skill references regenerated; every model's diagnostics unchanged; `bash scripts/verify-all.sh` green

## Gates

- [x] clean-code-swept — reaction-walk's caller list and the two lifecycle predicates named for what they ask; `npx @biomejs/biome check` clean on every file touched; `bash scripts/verify-all.sh` green end to end (senior-developer, 2026-09-11T14:05:00.000Z)

## Comments

- **senior-developer** (2026-09-11T14:05:00.000Z): Claimed the card and read the ruled amendments (decisions 23, 28, 11, 03, 16, 14) and the architect's round-16 probes before touching anything.
- **senior-developer** (2026-09-11T14:05:00.000Z): A process's start is its own first step. `packages/core/src/reaction-walk.ts:310-360` adds `firstSteps`, the operations whose outbound calls are a reactor's: a policy's `issues`, and for a process its `issues` plus the own-context operations named in `starts`. `callersFor` and `routesTo` read that list, so a `by` naming the starting operation, and the local `by` chain leading from it, route the answer home; the start's *own* answer is deliberately not routed, because whoever called it is waiting for that. `consumable-kind` follows through `hearsAnswerOf` and its message now says "an operation it issues or starts on" for a process (`packages/core/src/validate.ts:4209`). Seven tests in `packages/core/src/validate.test.ts:9954-10129`: the clean checkout saga, the step drawn from the start, the local chain, a process started by a different command (still refused), the boundary staying one hop, the start's own answer still refused, and the two-`by` RiverMart shape. Five of the seven fail without the change.
- **senior-developer** (2026-09-11T14:05:00.000Z): `reEntersWhileAlive` (`packages/core/src/validate.ts:4681-4700`) now asks about the start before the answer: a node the process only starts on closes the ring by making an instance, whatever else routes through it, so the shortest workaround — start on and issue one operation — is reported instead of validating clean. A trigger that is both `starts` and `on` stays exempt. `processThroughTranslatingLayer` and `isProcessLifecycleThroughLayer` collapsed into `lifecycleProcessOf` and `spawnsInstances` (`packages/core/src/validate.ts:4775-4818`), which were one question asked twice: the spawning message now fires for a policy-free ring too, which is what `apps/docs/docs/3-core/4-validation.md:78` already said it did. Tests at `packages/core/src/validate.test.ts:10131-10218`.
- **senior-developer** (2026-09-11T14:05:00.000Z): `role-coherence` asks no downstream role of an `external` or `boundaryOnly` consumer (`packages/core/src/validate.ts:2596-2607`): how it takes our fact in is its inside. A mud is ours and is still asked. Test for the partner carrier at `packages/core/src/validate.test.ts:10220-10298`.
- **senior-developer** (2026-09-11T14:05:00.000Z): `EntitySchema.attributes` and `DataSchemaSchema.attributes` are optional (`packages/core/src/schema.ts:78-90` and `packages/core/src/schema.ts:630-641`), which is what the loader always accepted and what card 104 says of every map. JSON schema regenerated into `models/petstore/.ods/schema.json`; `packages/skill/src/json-schema-agrees.test.ts` loads one document through ajv and through `Workspace.fromSchema` and pins that neither definition requires `attributes`. `ajv` added as a devDependency of `packages/skill` for it.
- **senior-developer** (2026-09-11T14:05:00.000Z): The five drifted sentences corrected — identity targets in `apps/docs/docs/3-core/3-tactical-design.md:188-201` and `packages/core/src/schema.ts:53-70` (external, mud and boundary-only contexts; schemas of the first and last only); borrowing routes, the external invariant, optional collections and the default-conformist guidance in `packages/skill/skill/SKILL.md:72-76`, `97-105`, `106-110`, `175-176` and `190-193`; external invariants in `apps/docs/docs/3-core/2-strategic-design.md:68-76` and `packages/skill/skill/references/dsl-api.md:18`; the `specialisation-in-boundary`, `role-coherence`, `consumable-kind` and `reaction-cycle` rows of `apps/docs/docs/3-core/4-validation.md`. `specialisation-in-boundary`'s own message named two borrowing routes where `mayBorrowFrom` allows three, so it is corrected too (`packages/core/src/validate.ts:641-646`). `packages/skill/scripts/generate.mts:71-80` renders a tuple as its positions, so a symmetric relationship's `participants` reads `[{ "$ref": string }, { "$ref": string }]` rather than "array of unknown".
- **senior-developer** (2026-09-11T14:05:00.000Z): `packages/skill/src/validator-drift.test.ts:159-270` adds `currentFacts`, pinning the current wording positively for the four facts that drifted twice, in every file that states them; `references/dsl-api.md` joins the checked corpus. Verified one pin fails when the sentence is reverted.
- **senior-developer** (2026-09-11T14:05:00.000Z): `models/rivermart/src/workspace.ts` is untouched and still clean; its `Hold attempt` now routes the acquirer's answer through both `AuthorisePayment` (the start) and `RetryHold`, so `models/rivermart/src/workspace.test.ts:79-93` says so — the model passed before only because that second `by` happened to exist. Every model's diagnostics are byte-identical to the baseline. `bash scripts/verify-all.sh` green end to end: core 814, skill 62, pages 769, e2e 297.
