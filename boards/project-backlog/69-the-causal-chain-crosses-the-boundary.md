---
column: review
labels: [backend, ddd]
priority: high
agent: ironhide
live: false
clean-code-swept: true
updatedAt: 2026-09-07T13:20:00.000Z
---
# The causal chain crosses the boundary

Prowl's review, findings 3 and 9: under decision 17 a policy issues a local operation that "calls out through the ACL", and there the flow map dead-ends and `reaction-cycle` stops, because neither follows a consumption. A ring through two contexts' calls under a partnership validates clean (probe 5). Card 55's `by` is the missing link: a consumption made by a local operation says that operation's effect continues at the consumable. And nothing stops a local operation from listing another context's event under `raises` (probe 2), which lets an author fake the link.

## Checklist

- [x] The flow map and `reaction-cycle` follow a consumption whose `by` names an operation: local operation, then the consumed operation, then what it raises, and on; the walker is shared; a ring through two contexts is reported as a reaction cycle with both contexts named
- [x] `raises-in-context` (error): an operation raises only events of its own provider's context; DDD reason in the doc comment (a context publishes its own facts)
- [x] Decision 21's "not a call graph" sentence amended by the lead to: `by` is the one causal link across a boundary, and the flow map reads it; you confirm the mechanics match
- [x] `partnership-backed` requires traffic in at least one direction (decision 20, second amendment); its summary, why and fix say why the other direction is not demanded; then, in the same card, the four things card 63 left waiting come out together: the `partnership-backed` entries in RiverMart's and StreamLine's `deliberate` arrays, their two DELIBERATE comments, both DISCOVERY.md section 7 bullets, and the header counts (RiverMart "two deliberate findings", StreamLine "three")
- [x] Tests: the two-context ring from the review's probe 5 is reported; a chain that ends at a foreign event with no further policy is not
- [x] Root suites green inside each package in build order; pages at 100% (the flow map fixture may change) with `npm run check` clean

## Gates

- [x] clean-code-swept — swept my own diff; the one thing it found was `ODSFlowMap.walk` computing a node it did not use on a revisit (`packages/core/src/flow-map.ts:47-56`), now guarded first (ironhide, 2026-09-07T13:20:00.000Z)
- [x] verify-all — green end to end: core 329, graphviz 20, doc 29, skill 26, northbank 3, petstore 18, rivermart 3, streamline 3, models/_shared 9, pages 708, apps/docs 22, apps/ods-vscode 9, pages e2e passed, schema comparison match (ironhide, 2026-09-07T13:20:00.000Z)

## Comments

- **optimus-prime** (2026-09-07T10:00:00.000Z): Ironhide, after card 61 lands (the lead will say); `feat`.
- **ironhide** (2026-09-07T12:40:00.000Z): The chain is one walker now: `packages/core/src/reaction-walk.ts:31-82` (`ReactionChain`) holds the four steps — policy to its commands, operation to what it raises, event to the policies it wakes, and operation to the operation it calls through a consumption whose `by` names it. `packages/core/src/flow-map.ts:35-53` walks it to draw the map (starting at what wakes each policy, so the edges come out in causal order), and `packages/core/src/validate.ts:1493-1520` walks the same graph for `reaction-cycle`, whose message now names every context on a ring that crosses one. The `by` step is found from the operation's own provider (`reaction-walk.ts:75-81`), because `consumption-by-resolves` already guarantees the caller is the consumer, so no index is needed and the scope of the map makes no difference to it. Only a consumed *operation* continues the chain: consuming an event is a subscription, not something the consumer causes. `raises-in-context` is at `validate.ts:1424-1459` with the DDD reason in the doc comment, catalogued at `validate.ts:1876-1885`. `partnership-backed` now asks for traffic in at least one direction (`validate.ts:981-1012`), and its summary, why and fix say why the other direction is not demanded. Tests: the probe 5 ring and its two negatives at `packages/core/src/validate.test.ts:2143-2234`, `raises-in-context` at `validate.test.ts:1460-1527`, the flow map's `by` step at `packages/core/src/derived-maps.test.ts:289-347`. Core is 329 green.
- **ironhide** (2026-09-07T12:45:00.000Z): Card 63's four leftovers are out together: `models/rivermart/src/workspace.test.ts:8-22` (header rewritten, `partnership-backed` off the `deliberate` array), `models/rivermart/src/workspace.ts:30-34` ("two deliberate findings") and `:2565-2572` (the DELIBERATE comment is now a plain note), `models/rivermart/DISCOVERY.md:353-372`; and the same four in StreamLine at `models/streamline/src/workspace.test.ts:8-21`, `models/streamline/src/workspace.ts:30-34` and `:2188-2194`, `models/streamline/DISCOVERY.md:277-296`. Both models drop exactly one diagnostic and nothing new appears: RiverMart 3 → 2, StreamLine 4 → 3, both suites green. NorthBank is unchanged and green. Docs that stated the old behaviour: `apps/docs/docs/3-core/4-validation.md:29,42-44` (the `partnership-backed` row, the new `raises-in-context` row, the `reaction-cycle` row) and `apps/docs/docs/4-graphviz/4-flow.md:3-9`. Decision 21's amendment gained one sentence a reader needs — the two things that take no step, and the rule that stops `raises` faking the link.
- **ironhide** (2026-09-07T12:50:00.000Z): QUESTION for lead. The change works, and the first thing it found is a real one in petstore, which is why `verify-all` is not green. `reaction-cycle` now reports: `"PetStatusChanged" -> "Approve when pet available" -> "ApproveOrder" -> "OrderApproved" -> "Reserve pet on approval" -> "ReservePet" -> "ReservePetForOrder" -> "PetStatusChanged"`, through Catalog BC and Sales BC. It is not a false positive: Catalog publishes one `PetStatusChanged` for three different facts (`models/petstore/src/workspace.ts:311-316`), Sales' "Approve when pet available" reacts to all of them and only its prose says "to available" (`:745-752`), and reserving raises the same event again (`:344-352`, `:437-465`). The ring is exactly the shape the rule's own `fix` names — a policy reacting to too broad an event — and it was invisible before this card. Petstore's suite asserts it has no diagnostics at all (`models/petstore/src/workspace.test.ts:281`), so something has to give, and what Catalog publishes is not my call. Two options. **(a) — my recommendation:** split the fact. `ReservePet` raises a new `PetReserved` and `MarkPetSold` a new `PetSold`; `ChangePetStatus` keeps `PetStatusChanged` for the catalogue's own relisting edits, which is the only one Sales' policy ever meant. The ring breaks honestly, the demonstration model stays clean, and petstore ends up teaching the lesson the rule exists to teach. Cost: two events plus their schemas in `models/petstore/src/workspace.ts`, the regenerated `.ods/schema.json`, docs and flow-map SVGs. **(b)** Leave the model and let petstore carry its first deliberate finding, changing `workspace.test.ts` to a `deliberate` array like the other three models. Cheap, but it costs petstore the "validates clean" property the docs lean on. There is a third thing I could do and will not without being told to: `PetApp`'s two public operations re-declare `.raises(petStatusChanged)` (`models/petstore/src/workspace.ts:456`, `:465`) for a fact the aggregate operation behind them already raises, and dropping those two lines also breaks the ring — but only by accident, because `petApp.consumes(reservePet, {})` (`:468-469`) names no `by`; add the `by` decision 21 asks for and the ring is back. Whether a service that fronts an aggregate operation should restate its `raises` at all is a real question, and a separate one. Everything else in the card is done: core 329, RiverMart/StreamLine/NorthBank 3 each, graphviz 20, doc 29, skill 26, pages 706 at 100% statements/branches/functions/lines, apps/docs 22 after the one inline snapshot update. Say which and I will land it; nothing is committed yet.
- **ironhide** (2026-09-07T13:20:00.000Z): Option (a), as the lead ruled. Catalog publishes three facts under three names now: `PetReserved` off `ReservePet` and `PetSold` off `MarkPetSold`, both carrying `PetId` because the id is all the news there is, while `PetStatusChanged` keeps the from/to payload and now means only the catalogue's own edit, e.g. relisting a returned pet (`models/petstore/src/workspace.ts:311-336`, `:362-379`). PetApp's two open-host fronts raise the split facts to match (`:475`, `:484`), and `petApp.consumes(reservePet)` and `consumes(markPetSold)` now name their `by` (`:485-490`), so the flow map follows the front into the aggregate operation behind it. Sales' "Approve when pet available" listens to `PetStatusChanged` and `OrderPlaced` as before, and its description says out loud that it does not listen to `PetReserved` — the fact its own chain produces (`:765-771`). Inventory's projection takes and reacts to all three, because it counts all three (`:1055-1058`, `:1070-1079`). Petstore is back to zero diagnostics and its DISCOVERY.md carries the why under Accepted (`models/petstore/DISCOVERY.md:100-106`). `verify-all` is green end to end; the summary is under Gates. Regenerated `models/petstore/.ods/` and `models/petstore/docs/`; the schema comparison against the core dist still matches, since no schema shape changed.
