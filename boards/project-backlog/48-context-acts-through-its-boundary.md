---
column: done
labels: [backend, ddd]
priority: high
agent: ironhide
live: false
clean-code-swept: true
updatedAt: 2026-09-07T00:30:00.000Z
---
# A context acts through its own boundary

Implements [decision 17](../../decisions/17-a-context-acts-through-its-own-boundary.md): rules `policy-in-context`, `aggregate-not-public`, `domain-service-internal`, and the petstore restructured so PetApp is the open host.

## Checklist

- [x] The three rules with catalogue entries and tests; `policy-in-context` checks `then` only, and `on` is treated as a consumption by `separate-ways` (decision 17, amended; run 3 issue 13)
- [x] Decision 08's crossing table shows `PolicySchema.on` may cross and `then` may not; the loader's cross-file rule matches when WorkspaceSet lands (note in extension card 07)
- [x] Petstore: Pet's `ReservePet`/`MarkPetSold` internal; PetApp provides the public operations (open-host-service) that consume them; Sales policies name Sales' own operations; Fulfilment's policy likewise; petstore validates clean; the other three models corrected where the shape repeats
- [x] Docs: the DDD strategic page and the skill's interview say what an aggregate offers stays inside the context; skill references regenerated
- [x] Consumable map and `assertDocSite` green for four models
- [x] packages/pages follows the new refs: unit suite at 100%, `browse` and `diagrams-consumable` green

## Gates

- [x] qa-automated — Optimus re-ran core 249, four models, skill 26, pages 660 at 100% and the browse, consumable-map and relationship e2e in the worktree after merging develop (Optimus, 2026-09-07T00:30:00Z)

- [x] clean-code-swept — `npx biome check` clean over all sixteen changed sources; the two boundary rules share one `operationsStayInside` helper rather than repeating the walk, and no rule duplicates card 47's `separateWays` (ironhide, 2026-09-04T12:10:00.000Z)

## Comments

- **lead** (2026-09-06T12:00:00.000Z): Assigned to dev-opus after cards 44, 45 and 47 land (the lead will say). Work in your worktree with absolute paths; `npm ci` if node_modules is missing; if the card is missing, `git reset --hard develop` there first.
- **ironhide** (2026-09-04T09:00:00.000Z): Claimed. Worktree was on the v0.4.0 tag, reset to develop (83e4cdc) so card 48 and card 47's rules are present. Read decision 17 and its amendment, decision 08's corrected crossing table, and packages/core/src/validate.ts:545-590 where card 47's `separateWays` already walks a policy's `on` as a consumption; I will build on that rather than add a second walk.
- **ironhide** (2026-09-04T09:40:00.000Z): Surveyed all four models against decision 17 before writing a line of the validator. Aggregate operations carrying an upstream pattern: petstore 3 (`Pet/ReservePet`, `Pet/MarkPetSold`, `Order/DeliverOrder`), northbank 15, rivermart 11, streamline 9. Policy `then` crossing a context: petstore 3, northbank 4, rivermart 3, streamline 1. Domain service offering outward: northbank's `Fraud/TransactionScorer` only. That is the shape the card describes and it is what the three rules will catch. QUESTION for lead below on the one place where decision 17's wording is wider than that.
- **ironhide** (2026-09-04T10:30:00.000Z): Three rules landed in packages/core/src/validate.ts:637-720 with catalogue entries at :1013-1042. `policyInContext` checks `then` only; `operationsStayInside` at :667 is shared by `aggregateNotPublic` and `domainServiceInternal` so the two read the same and differ only in the provider they walk and the word in the message. I did not touch `separateWays` — card 47 already walks a policy's `on` as a consumption at :566-588, and the new test at src/validate.test.ts:912 pins that an `on` across a context stays legal. Tests: src/validate.test.ts:854-1040 (9 cases across three describes), fixture extended at src/rule-catalog.test.ts:60-83 so each new rule fires. src/makeTestWs.ts:23-27 changed the plain fixture's S1 from a domain service to an application service — it offered `open-host-service` outward and was the one fixture the new rules caught. `npm run build && npx vitest run` in packages/core: 249 passed, 13 files. Decision 08's crossing table already carried the corrected `on`/`then` rows on develop, so that item needed no edit.
- **ironhide** (2026-09-04T10:35:00.000Z): QUESTION for lead, not blocking — I have implemented the narrow reading and want it on the record. Decision 17 says an aggregate's consumables "may not carry an upstream pattern and may not be consumed from another context". Read literally that includes events, and every model publishes its domain events straight off the aggregate with `published-language`: 13 such events in petstore, 22 northbank, 28 rivermart, 16 streamline, with ~64 cross-context consumptions and ~50 policy subscriptions behind them. Making those legal would mean moving every published event off its aggregate onto an application service, which breaks the `raises` link (an operation on the aggregate raises the event; a service cannot raise it) and so needs a schema change — explicitly out of this card's scope. I therefore implemented the rules over an aggregate's and a domain service's **operations** only, and left events alone; the catalogue text says so in as many words, and a test pins it. That matches this card's bullet 3, which names only operations. If you want events routed through application services too, it is a one-line change in `operationsStayInside` plus a card of its own for the four models — say the word and I will write the follow-up card rather than fold it into this one.
- **ironhide** (2026-09-04T11:05:00.000Z): Petstore restructured. `Pet`'s `ReservePet` and `MarkPetSold` are internal at models/petstore/src/workspace.ts:286-317 and PetApp provides the open-host `ReservePetForOrder`/`MarkPetSoldForOrder` that consume them at :383-414 (the ADR-017 comment moved with the public operation, where the reader now meets it). `Order`'s `DeliverOrder` went internal the same way and OrderApp's `ConfirmDelivery` fronts it at :566-580 and :609-618. Sales' two policies now name OrderApp's own `ReservePet`/`MarkPetSold` at :642-660, which are the operations that call out through the ACL. Fulfilment gained `ShipmentApp` with `ReportDelivery` at :838-852 so its policy names an operation of its own context. Names follow decision 17 literally — Pet keeps `ReservePet` and `MarkPetSold` as its internal transitions — so the public pair needed distinct names. Petstore validates clean: `npx vitest run --config vitest.config.ts` 18 passed, including `validates clean` and `assertDocSite`.
- **ironhide** (2026-09-04T11:20:00.000Z): The other three models had the same shape 35 more times, and each correction is the same move: the operations a context offers outward go onto an application service, and where a policy reached into a neighbour it now names a local operation that consumes theirs. streamline: seven new application services (EncodingAPI, PlaybackAPI, EdgeAPI, DevicesAPI, ProfilesAPI, BillingAPI, AdsAPI) and Catalogue's `RequestEncode` for the one crossing policy. rivermart: PaymentsAPI, WarehouseAPI and CaseAPI added, `SuspendSeller`, `AddToCart`, `Checkout`, `PlaceOrder`, `CancelOrder` and `RequestReturn` moved onto the services that already existed, and three local operations (`RequestAuthorisation`, `PlaceOrderForCart`, `RequestRefund`). northbank: ScreeningApp, LedgerApp, PaymentsApp, SchemeGatewayApp, FraudApp, CardsApp, LendingApp, DecisioningApp, ChannelsApp and IdentityApp added, the consent and freeze operations moved onto the app services already there, and five local operations for the Payments Hub and Lending policies; the hub's and Lending's outbound consumptions moved onto their app services so the consumption and the operation that makes it sit on the same element. Each model's suite is green with only its declared deliberate diagnostics: petstore 18 passed and 0 diagnostics, northbank 3 passed and its 3, rivermart 3 passed and its 2, streamline 3 passed and its 3. All four `.ods/` and petstore's `docs/` regenerated with `npm run build` per package.
- **ironhide** (2026-09-04T11:28:00.000Z): Docs and skill. apps/docs/docs/2-ddd/3-strategic-design.md:55-70 gains "A Context Acts Through Its Own Boundary", which says what an aggregate or domain service offers stays inside and that a policy may react across a boundary but not act across it; apps/docs/docs/3-core/4-validation.md:28-30 lists the three new rules in the table. packages/skill/skill/references/interview-playbook.md:95-98 says an aggregate's and a domain service's operations stay inside, and :109-112 corrects the old "either side may live in another context" to `on` may cross and `then` may not. `npm run generate` regenerated references/validation-rules.md from RULE_CATALOG and the bundle; `npx vitest run --config vitest.config.ts` in packages/skill: 26 passed.
- **ironhide** (2026-09-04T11:32:00.000Z): FOR THE LEAD, out of my scope and needs a decision. The petstore restructure changes ids the pages package asserts on, and packages/pages is on this card's OUT list, so I left it red and did not touch it. Three tests fail that pass before my change, all from the same cause — the crossing consumable is now `reserve_pet_for_order`/`mark_pet_sold_for_order` on `pet_app` rather than `reserve_pet` on the `pet` aggregate: packages/pages/src/lib/evidence/derive.test.ts:189 (expects the old ids and reads `disposition` off `reserve_pet`), packages/pages/src/lib/molecules/ConsumesTable.test.ts:30 (its first fixture row is now a same-context consumption with no pattern, so the Protection cell has no keyword) and packages/pages/src/lib/templates/templates.test.ts:161. Two more failures in that package, src/site.test.ts and src/lib/templates/edge-cases.test.ts:97, fail on develop before my change too. The harnesses at packages/pages/src/lib/templates/petstore.harness.ts:33, Ref.harness.svelte:10, Lockup.harness.svelte:23 and DataTable.harness.svelte:79 also name the old refs. Say the word and I will fix them under this card or write the follow-up.
- **lead** (2026-09-04T11:45:00.000Z): (1) Agreed and decision 17 is being amended to say so: the three rules cover operations only; an aggregate raises its events and the context publishes them, no routing through a service. Implementation stands. (2) Fix packages/pages under this card: the three failing tests and the four harnesses that name the old refs, changing only refs and fixture names, nothing else in pages. Also run src/site.test.ts and edge-cases.test.ts:97; if they fail because the regenerated petstore.json or the fixture no longer match, fix those two the same way and say so; if the cause is anything else, leave them and report. Then pages unit at 100% and `npx playwright test browse diagrams-consumable` green.
- **ironhide** (2026-09-04T12:10:00.000Z): packages/pages follows the new refs. The three that broke on my change: packages/pages/src/lib/evidence/derive.test.ts:189-200 now expects `reserve_pet_for_order`/`mark_pet_sold_for_order` and reads the disposition and comments off those; packages/pages/src/lib/molecules/ConsumesTable.test.ts:28-30 asserts `.keyword.mono` exists rather than that the first Protection cell carries it, because the petstore's first consumption is now a same-context one that declares no protection and the old selector matched it; packages/pages/src/lib/templates/petstore.harness.ts:35-37 points `operation` at `pet_app/provides/reserve_pet_for_order`, which is where the ADR-017 disposition and comment now live. The four harnesses follow: DataTable.harness.svelte:78-92 and its test at DataTable.test.ts:90, Ref.harness.svelte:10, Lockup.harness.svelte:23. One model edit came with it — models/petstore/src/workspace.ts:383-414 gives PetApp's two public operations `.raises(petStatusChanged)`, the same way AddPet, UpdatePet, DeletePet and UploadImage already raise the Pet aggregate's events from the service; without it the boundary operation drew no Raises section and the template story had nothing to show. Petstore regenerated and still validates clean, 18 passed.
- **ironhide** (2026-09-04T12:12:00.000Z): The two pre-existing failures, as asked. edge-cases.test.ts:90-97 was fixture drift exactly as you guessed: it drew petstore's `inventory_bc/inventory_projection/inventory_view` to assert "no identity attribute marked" and "No attributes.", and card 47 gave that entity a `status` identity and two attributes, so neither sentence could draw. No petstore entity has zero attributes any more, so I pointed it at the hand-built edge-case workspace that exists for exactly these branches and added one bare entity there, packages/pages/src/lib/fixtures.ts:86-89 — that is the one addition beyond a rename, and I am flagging it rather than burying it. src/site.test.ts is a different animal and nothing to do with the models: `exportSite` copies `packages/pages/app/assets`, which only exists after `npm run build` in that package, and the worktree had never been built. It passes after a build; no code or fixture change. e2e/diagrams-consumable.spec.ts:20-42 also had to follow the operation from the Pet aggregate to PetApp, since the lollipop it asserts on is the open-host port and Catalog now offers it from the application service.
- **ironhide** (2026-09-04T12:14:00.000Z): Counts. packages/pages `npm run test:unit`: 94 files, 660 passed, 0 failed, 0 skipped. `npx playwright test browse diagrams-consumable`: 12 passed. packages/core: 249 passed. petstore 18, northbank 3, rivermart 3, streamline 3, skill 26. `npx biome check` clean over the sixteen changed sources.
- **optimus-prime** (2026-09-07T00:30:00.000Z): Landing. Rules over operations only, as decision 17 now says; the models restructured 38 aggregate operations, 19 foreign consumptions and 8 crossing policies into application-service boundaries. The edge-case fixture gaining a bare entity is the right fix.
