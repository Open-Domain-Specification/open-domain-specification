---
column: review
labels: [backend, ddd, breaking]
priority: med
agent: ironhide
live: false
clean-code-swept: true
updatedAt: 2026-09-05T01:55:00.000Z
---
# Schemas compose; invariants may constrain operations

Implements [decision 18](../../decisions/18-schemas-compose.md) and [decision 19](../../decisions/19-invariants-may-constrain-operations.md).

## Checklist

- [x] `AttributeSchema.schema?: { $ref }`, mutually exclusive with `valueobject` (`attribute-one-shape` rule); `schema-context` covers it; JSON schema regenerated
- [x] `InvariantSchema.constrains` accepts consumables of the invariant's aggregate; `invariant-constrains` rule updated; petstore's `SoldNotReopen` names `ChangePetStatus`
- [x] Pages: schema page links nested schemas; invariant page "Guarded by"; consumable page lists invariants; doc generator follows; skill interview asks "is that a shape of its own?"
- [x] `aggregate-tree` reads instances, not types: a self-include and a type included by two wholes are legal; a ring through two or more types stays an error (lead's mid-card addition, review run 7)
- [x] `attribute-one-shape` also keeps a payload shape out of the model: only a schema's attribute may name a schema (lead's answer to the card's second question)
- [x] Root suites green inside the worktree

## Gates

- `clean-code-swept`: swept 2026-09-05. In the validator, `attributeOwnersIn(context)` is now the one walk over everything that carries attributes; `attributeOwnersOf(workspace)` and `sharedKernelBacked`'s `attributesOf` both read it instead of repeating the schemas/value-objects/entities traversal. Removed the `ownerOf` helper, which existed only to widen an `Attribute` to its owner and no longer typechecked once `Constrainable` gained `Consumable`; `scopeOf` says the same thing in one place. Removed the `parents` index and the two-parent diagnostic from `aggregate-tree`, which are dead under the instance reading. Gave the doc generator's attribute list a `fromPath` so it can link, rather than adding a second renderer beside it. biome and tsc clean on every file touched; the two `noThenProperty` errors in packages/core/src/workspace.ts and the findings in packages/pages (fullscreen.svelte.ts, hover-placement.ts, WorkspacePage.test.ts) are pre-existing and untouched, confirmed by re-running biome on a stashed tree.
- `qa-automated`: core `npm run build` + 294 passed (13 files); graphviz 18; doc 24; skill 26 after `npm run generate`; pages `npm run build` + 663 passed across 94 files, coverage 100/100/100/100; ods-vscode `npm run build` + 9. Models, each `npm run build` then `npx vitest run`: petstore 18 (0 diagnostics), rivermart 3 (3 deliberate), streamline 3 (4 deliberate), northbank 3 (3 deliberate). apps/docs is 21 passed | 1 failed, the pre-existing stale `doc.example.test.ts` (card 52), left red. `npx tsc --noEmit` clean in all twelve package directories.

## Comments

- **lead** (2026-09-06T12:00:00.000Z): Assigned to dev-opus after card 49 lands (the lead will say). Work in your worktree with absolute paths; `npm ci` if node_modules is missing; if the card is missing, `git reset --hard develop` there first.
- **ironhide** (2026-09-05T00:25:00.000Z): Claimed. Reset the worktree to origin/develop (7b4d2b3, the card 49 merge) and ran `npm ci` — the worktree had no node_modules, and without one it resolves `@open-domain-specification/core` up to the main checkout.
- **ironhide** (2026-09-05T00:32:00.000Z): Naming. Both the card and decision 19 call the rule `invariant-constrains`; no such rule exists. The rule that reads `constrains` is `invariant-in-aggregate` (packages/core/src/validate.ts), so that is the one I widened. I did not rename it: the id is stable, it is quoted in the docs table, in the skill's generated `validation-rules.md` and in every diagnostic, and renaming it is not what either decision asks for. Flagging rather than deciding.
- **ironhide** (2026-09-05T00:40:00.000Z): Decision 18 in the model. `AttributeSchema.schema?: { $ref }` (packages/core/src/schema.ts), `AttributeOptions.schema?: DataSchema` and the `Attribute.schema` field with its `toSchema` (packages/core/src/workspace.ts), resolved on load beside `valueobject` in `addAttributes` (packages/core/src/workspace-from-schema.ts) — a `DataSchema` exists after the first pass, so nothing about the load ordering had to move. JSON schema regenerated by `npm run build`. Collections stay in the type string, as the decision says; there is no list construct.
- **ironhide** (2026-09-05T00:48:00.000Z): Two rules for it. `attribute-one-shape` (error) fires when one attribute names both a value object and a schema: the two are different claims — a value object is a concept this context models, a schema a payload it publishes — and an attribute making both leaves a reader unable to say which the field is. It reports at the attribute's own ref rather than the owner's, because that is what the author edits. `schema-context` was extended to nested schemas with the same shared-kernel exception it already makes for `schema` and `returns`, and `shared-kernel-backed` now counts a nested schema as something crossing a kernel, since decision 18 explicitly allows a partner's schema and the backing rule would otherwise call a real sharing empty. Tests in packages/core/src/validate.test.ts cover attribute-one-shape, the nested schema-context case and the kernel case.
- **ironhide** (2026-09-05T00:55:00.000Z): Decision 19. `Constrainable` gained `Consumable` (packages/core/src/workspace.ts), `getConstrainableByRef` resolves one, and `scopeOf` in the validator maps a consumable to its provider when that provider is an aggregate and to nothing when it is a service — a service is not a boundary anything is saved inside, so an invariant naming an application service's operation still errors, and the fix text tells the author to name the aggregate's own operation behind it. Two derived readings so the surfaces do not each re-scan: `Invariant.guarded` is its consumable targets, and `Consumable.invariants` is the rules that name it, searched inside the consumable's own context because nothing else can reach it. Petstore's `SoldNotReopen` now names `ChangePetStatus` as well as the Pet (models/petstore/src/workspace.ts); the `.constrains` call sits below the operation because the invariant is declared above it, with a comment saying why.
- **ironhide** (2026-09-05T01:05:00.000Z): Reference models. Decision 18 exists because a payload with a nested shape had to be flattened, and five attributes across two models were doing exactly that as free-text types. RiverMart's `OrderPlaced.lines` and `ReturnRequested.lines` and Vendor Purchasing's `PurchaseOrderReceived.lines` now name `OrderLine`, `ReturnLine` and `PurchaseOrderLine` schemas of their own contexts; NorthBank's `PostEntry.postings` and `EntryPosted.postings` both name one `PostingLine`, which is the case the decision is best at — one shape, written once, carried by the request and by the fact. I deliberately left the three remaining `{...}[]` types alone: StreamLine's `Artwork.images`, `Ladder.rungs` and `Invoice.lines` hang off value objects and an entity, not off schemas, and a published payload shape has no business inside domain internals. All four models still validate to exactly their declared lists.
- **ironhide** (2026-09-05T01:15:00.000Z): Surfaces. Pages: the attribute table links a nested schema the way it already links a value object, so a reader walks a payload into its parts without leaving the table (packages/pages/src/lib/molecules/AttributeTable.svelte); the invariant page splits its targets — what the rule holds true of stays under Constrains, the operations that have to uphold it draw a new "Guarded by" section (packages/pages/src/lib/templates/InvariantPage.svelte); the consumable page gains an Invariants section listing the rules it must uphold (packages/pages/src/lib/templates/ConsumablePage.svelte). Stories: `transitionInvariant` and `guardedOperation` routes added to the petstore harness so both new sections draw full rather than empty, and a "Nested shape" story on the schema page drawn against RiverMart, since the petstore has no payload with a shape inside it and I was not going to invent one. Doc generator: the attribute list links a nested schema to its context's Schemas section, and the Provides table gained a "Guarded by" column, which is where a consumable's invariants can read given a consumable has no page of its own. Pages coverage held at 100% without loosening anything.
- **ironhide** (2026-09-05T01:30:00.000Z): Optimus's mid-card addition, review run 7: `aggregate-tree` was reading the type graph for a claim that is about instances. A Category whose parts are Categories is the composite pattern and still a tree per instance, and a Waypoint included by both a FreightLeg and an AccessorialLeg still belongs to one leg at a time; neither is a model error. The two-parent diagnostic is gone entirely, a self-include never enters the cycle graph, and a ring through two or more distinct types stays an error — with no type you can name as the one that holds the other, there is no whole to start an instance tree from, and the message now says that instead of "never a cycle". Reachability from the root is untouched. Doc comment at packages/core/src/validate.ts, catalogue entry beside it, docs row in apps/docs/docs/3-core/4-validation.md, and three tests: the self-include, the two-parent case and the two-type cycle. The completeness fixture keeps a two-parent entity and a self-include that are now expected to stay quiet.
- **ironhide** (2026-09-05T01:45:00.000Z): Skill and docs. The interview asks "is that a shape of its own?" when a payload field is described as a thing with parts, with the `valueobject`-or-`schema` rule attached, and the invariant question follows up with "which operation makes that change?" when the answer is about a transition rather than a value (packages/skill/skill/references/interview-playbook.md); `dsl-api.md` and `translation-table.md` carry both constructs; `model-reference.md` and `validation-rules.md` regenerate from core, so `attribute-one-shape` and the reworded rules came through `npm run generate`. apps/docs: the tactical page says what an attribute may point at and that an invariant may name its aggregate's operations, and `tests/tactical.example.test.ts` — the file that page embeds — now builds both constructs and records them in its snapshots.
- **ironhide** (2026-09-05T01:50:00.000Z): Two things for the lead. First, no attribute in any model names a schema across a shared kernel, so the exception decision 18 borrows from decision 16 is covered by unit tests only; that is honest, but worth knowing. Second, nothing in the model stops an entity's or a value object's attribute naming a schema — decision 18 talks about payloads throughout, and an entity typed by a published payload shape looks like a modelling error to me, but forbidding it is a rule neither decision asked for and I did not invent one.
- **ironhide** (2026-09-05T01:52:00.000Z): QUESTION for lead: (a) decision 19 and this card both name a rule `invariant-constrains` that does not exist; I widened `invariant-in-aggregate` and left the id alone — say if you want the rename and I will take the docs, the skill's generated reference and the diagnostics with it. (b) Should a rule forbid an entity's or a value object's attribute from naming a schema, so a nested shape stays a payload construct? I recommend yes, as a one-line addition to `attribute-one-shape` or a rule of its own, but it is a modelling decision and belongs to you.
- **lead** (2026-09-05T02:00:00.000Z): (a) Keep `invariant-in-aggregate`; the lead amends decision 19 and this card's wording on develop. (b) Yes to the guard, as one clause inside `attribute-one-shape`, error severity, message saying which to use.
- **ironhide** (2026-09-05T02:10:00.000Z): Both answers actioned. The id is untouched. `attribute-one-shape` now has a second clause: an attribute of an entity or a value object may name a value object but never a schema (packages/core/src/validate.ts). The doc comment gives the reason rather than the mechanics — a schema is a payload shape at the context's boundary, and what a domain object holds is part of the model, so letting a schema inside would put the vocabulary of the wire into the model the boundary exists to protect; composition with a schema is a schema's own business. The message names the owner, the attribute and the schema and says an entity or value object names a value object instead; the catalogue's `why` and `fix` carry the same reason, so the docs row and the skill's regenerated `validation-rules.md` say it too. One test covers all three cases the lead asked for: a schema nesting a schema stays silent, and the entity and the value object each report one error at the attribute's own ref. The `both shapes` clause `continue`s before the new one, so an attribute making both claims still reports once and reports the more specific problem. Nothing in the four models trips it — every nested schema there is on a schema — and the interview playbook, translation table and DSL table now say only a schema's attribute may use `schema`.
