---
column: done
labels: [backend, ddd, breaking]
priority: high
agent: dev-opus
live: false
clean-code-swept: true
updatedAt: 2026-09-06T18:00:00.000Z
---
# Operations declare what they return

Implements [decision 13](../../decisions/13-operations-declare-what-they-return.md): `returns?: { $ref }` on operation consumables, across core, models, doc, pages and the skill.

## Checklist

- [x] `packages/core/src/schema.ts`: `returns?: { $ref: string }` on `ConsumableSchema`, documented as operation-only; `Consumable` in workspace.ts carries `returns`, `toSchema`/`fromSchema` round-trip it, `provides(...)` accepts it
- [x] Rules: `returns-on-operation` errors when an event carries `returns`; `schema-context` covers `returns` as it covers `schema`; catalogue entries with why and fix; tests
- [x] Petstore: `GetPetSummary` (workspace.ts:361-365) returns a `PetSummary` schema matching its description; `GetInventory` (:818) returns its status-count schema; description contradictions fixed; other models where a query exists
- [x] JSON schema regenerated; skill references regenerated; interview playbook gains "and what comes back?" for operations
- [x] Pages: ConsumablePage shows a "Returns" definition row and a second attribute table only when set; ConsumableSubsection shows the row too (lead, 2026-09-06T16:00); ProvidesTable unchanged; unit at 100%, e2e on petstore GetPetSummary
- [x] Doc generator prints Returns under the operation; `assertDocSite` green for four models
- [x] Root suites green inside the worktree (absolute paths; the nx cache replays the main tree)

## Gates

- [x] qa-automated — the lead re-ran core 211, petstore 18 and pages 653 at 100% in the worktree after merging develop; developer ran doc 24, skill 26, graphviz 18, the four models, ods-vscode 9 and pages e2e 257 (the lead, 2026-09-06T18:00:00Z)

- [x] clean-code-swept — full audit over 51 files, two reviewers across the eight principles; highest finding 0.45, nothing above the 0.5 fix threshold; the one thing I introduced and fixed was an unnecessary `export` on `returnsLinkMd`. The sweep's 0.4 finding — Returns missing from `ConsumableSubsection` — was put to the lead and closed by their instruction of 2026-09-06T16:00; the three files added after the sweep are biome-clean and change no logic beyond that row (dev-opus, 2026-09-06T16:05:00.000Z)

## Comments

- **lead** (2026-09-06T10:00:00.000Z): Assigned to dev-opus. Fixed by decision 13; read it first. This is a `feat!:` change. Card 45 runs in parallel and owns the `cross-aggregate-reference` rule and petstore line 452; you own `schema.ts`, `workspace.ts`, the `schema-context` rule, the new `returns-on-operation` rule and the petstore operations. Merge develop before your final run. Work in your worktree; `npm ci` if node_modules is missing; if the card is missing, `git reset --hard develop` there first.
- **dev-opus** (2026-09-06T11:00:00.000Z): Claimed. Worktree was on a stale commit; reset to develop (a183ee6) and ran `npm ci`. Read decision 13. Starting at `ConsumableSchema` (packages/core/src/schema.ts:139-165) and the `schema-context` rule (packages/core/src/validate.ts:201).
- **dev-opus** (2026-09-06T12:10:00.000Z): Core done. `returns?: { $ref }` on `ConsumableSchema` (packages/core/src/schema.ts:158-163), `Consumable.returns` plus constructor and `toSchema` (packages/core/src/workspace.ts:1010-1011, 1035, 1067), resolved in `addProvides` (packages/core/src/workspace-from-schema.ts:36-47); `provides(...)` needed no change since it forwards `ConsumableAttributes` (packages/core/src/workspace.ts:864, 939). `schema-context` now checks the returned schema as well (packages/core/src/validate.ts:206-213) and the new `returnsOnOperation` rule errors on an event with `returns` (packages/core/src/validate.ts:225-243), catalogued with why and fix at packages/core/src/validate.ts:400-407. Fixture `Place Order` gained `returns: orderSummary` (packages/core/src/makeTestWs.ts:212) so the round-trip covers a distinct sent and returned schema (packages/core/src/round-trip.test.ts:72-88); rule tests at packages/core/src/validate.test.ts:129-176 and the catalogue fixture at packages/core/src/rule-catalog.test.ts:44-49. 209 core tests green.
- **dev-opus** (2026-09-06T13:30:00.000Z): Petstore, skill and pages done. `PetSummary` (models/petstore/src/workspace.ts:238-247) on `GetPetSummary` and `InventoryCounts` (models/petstore/src/workspace.ts:819-826) on `GetInventory`; both descriptions rewritten so they no longer promise a shape the schema contradicts (models/petstore/src/workspace.ts:370-371, 833). Skill references regenerated — `returns` in model-reference.md:115 and the new rule in validation-rules.md:55-61 — and the hand-written three updated: dsl-api.md:34, interview-playbook.md:98-100, translation-table.md:44. ConsumablePage gains the fact row (packages/pages/src/lib/templates/ConsumablePage.svelte:88-90), the `returns` section in `sectionsFor` (:8-9) and the second attribute table (:113-122); `PETSTORE_REFS.query` (packages/pages/src/lib/templates/petstore.harness.ts:36-38) drives a new story and two tests, one for a query and one for an operation with no returns (packages/pages/src/lib/templates/templates.test.ts:171-196). Pages 648 tests, 100% statements/branches/functions/lines. Core 209 green, petstore 18 green including `assertDocSite`.
- **dev-opus** (2026-09-06T13:35:00.000Z): QUESTION for lead, three of them, all reached at once; blocked until answered. Noted the card-45 heads-up and will re-run model generation only after `git merge develop`.

  **(1) Doc generator — where does Returns go?** The doc has no consumable page; `providesTableMd` (packages/doc/src/consumables.md.ts:34-47) is one table with columns Name, Type, Internal, Pattern, Description, Schema, Raises, and a bullet list of comments beneath it. "prints Returns under the operation" reads two ways. (a) An eighth column, `Returns`, beside `Schema`, linked the same way `schemaLinkMd` links (packages/doc/src/consumables.md.ts:7-13). (b) A line beneath the row, in the bullet block where `commentsMd` already writes. I recommend (a): it is symmetric with Schema and Raises, it costs nothing for the ~40 consumables that have no returns (a `-`, as Raises already does for events), and (b) buries the response shape below the fold of a wide table. (a) does widen an already wide table.

  **(2) Does a returned schema count as a carrier?** `DataSchema.consumables` (packages/core/src/workspace.ts:1701-1711) matches only `c.schema === this`, and it feeds the SchemaPage "Carried by" table (packages/pages/src/lib/templates/SchemaPage.svelte:29) and the doc's Schemas "Used by" column (packages/doc/src/boundedcontext.md.ts:35-40). As it stands the regenerated petstore says `| PetSummary | ... | - |` and `| InventoryCounts | ... | - |` (models/petstore/docs/boundedcontexts/catalog_bc/index.md:42, models/petstore/docs/boundedcontexts/inventory_bc/index.md:35-38) and PetSummary's page reads "Nothing carries this schema yet" — a self-contradiction of exactly the kind decision 13 exists to remove. Options: (a) widen `consumables` to `c.schema === this || c.returns === this`, so the getter answers "who depends on this shape", with the "Kind" column left as is. (b) same, but the carriers table gains a column saying whether it is sent or returned. (c) leave it, and accept that a returns-only schema looks unused. I recommend (a): it is one predicate, it keeps the tables' shape (so ProvidesTable and the doc columns stay put), and it makes the page honest. (b) is more precise but adds a column the card did not ask for; (c) publishes a page that contradicts the model.

  **(3) How far across the reference models?** The card names GetPetSummary and GetInventory, then "other models where a query exists". Queries with no `returns` today: petstore FindPetsByStatus, GetPetById, GetOrderById, GetUserByUsername (models/petstore/src/workspace.ts:331, 336, 556, 904); northbank GetCustomer, GetAvailableBalance (models/northbank/src/workspace.ts:450, 736); rivermart ListProduct, GetProduct, SearchProducts, GetOffer, GetOrder, GetSponsoredResults, GetCustomer (models/rivermart/src/workspace.ts:328, 343, 413, 567, 1031, 1846, 2039); streamline GetTitle, GetHomepageRows, GetEntitlement (models/streamline/src/workspace.ts:595, 1182, 1444). Sixteen operations, each needing a new `DataSchema` I would have to invent from its description. Options: (a) every one of them, so no reference model shows a query that hides its answer. (b) the two named on the card only. (c) the two named plus one query per model, so each model demonstrates the field once. I recommend (c): it teaches the construct in all four models and keeps the diff reviewable, where (a) is sixteen invented shapes across three models I would be guessing at and (b) leaves three of the four models silent about a construct they now all support. Say the word and (a) is fine, it is just long.
- **lead** (2026-09-06T14:00:00.000Z): ANSWERS. (1) An eighth column, Returns, beside Schema in the provides table, symmetric with Raises. (2) Yes: a returned schema is carried by the operation that returns it; widen `DataSchema.consumables` to `c.schema === this || c.returns === this`, and the schema page's "carried by" says which way (payload or returns) if that is cheap, otherwise just lists it. (3) The two named plus one query per other model, each with a DataSchema that follows its existing description; leave the rest without `returns`, which is honest. Land after card 45; merge develop first and keep both sides in petstore.
- **dev-opus** (2026-09-06T14:40:00.000Z): Lead's three answers actioned. (1) Doc gains an eighth `Returns` column beside `Schema`; `schemaLinkMd` split so `returnsLinkMd` links the same way (packages/doc/src/consumables.md.ts:7-32), header at :50-59. Test fixture gained an `Order Receipt` returned by `Approve Order` and both row assertions grew a column (packages/doc/src/index.test.ts:145-149, 160, 187-197); an event prints `-` there, as it already does for Raises. 24 doc tests green. (2) `DataSchema.consumables` now matches `c.schema === this || c.returns === this` (packages/core/src/workspace.ts:1701-1715), so PetSummary and InventoryCounts stop reading as unused. Direction was cheap, so the SchemaPage carriers table gains a "Carried as" column printing `payload`, `returns` or `payload, returns` (packages/pages/src/lib/templates/SchemaPage.svelte:31-46, 76-77) and its lead now says "whether they send it or answer with it" (:66). Covered three ways: PetId all-payload and the new PetSummary returns-only (packages/pages/src/lib/templates/templates.test.ts:199-233), and the both-ends branch by an `Echoing Operation` over a new `Echoed Schema` in the edge-case fixture (packages/pages/src/lib/fixtures.ts:109-113, 121-129; packages/pages/src/lib/templates/edge-cases.test.ts:190-197) — `Unused Schema` stays uncarried so the empty state keeps its test. Core `lookups.test.ts:31-48` now asserts both ends of Order Summary. (3) One query per other model: northbank `CustomerDetails` on `GetCustomer` (models/northbank/src/workspace.ts:376-396, 470-477), rivermart `ProductDetail` on `GetProduct` (models/rivermart/src/workspace.ts:307-321, 357-364), streamline `TitleDetail` on `GetTitle` (models/streamline/src/workspace.ts:539-558, 613-620); each description rewritten to name what goes in and what comes back. All three typecheck. Core 209, doc 24, skill 26, pages 650 at 100% across the board. Holding here: models are NOT regenerated and develop is NOT merged, per the lead's instruction to wait for card 45.
- **dev-opus** (2026-09-06T15:30:00.000Z): Merged develop (ab599f9, card 45 at e0f9f8d) and finished. The merge took no conflicts — card 45's relation removals, its new attributes and its Carrier aggregate sit in different regions of the model files from my schemas and `returns`; verified both sides survive in models/petstore/src/workspace.ts:240-245 (PetSummary), :377 (returns), :668 (their Carrier) and :852 (InventoryCounts). All four models regenerated; petstore reports 0 diagnostics and the other three report only their pre-existing DELIBERATE ones. Added the e2e the card asked for at packages/pages/e2e/browse.spec.ts:141-172: it walks GetPetSummary's two facts and two tables, follows the Returns link to PetSummary and asserts the carriers table names the operation back with `returns`. apps/docs gained the two paragraphs that keep the published docs true to the model — the `returns` half of a consumable at apps/docs/docs/3-core/3-tactical-design.md:44-53 and the new rule in the table at apps/docs/docs/3-core/4-validation.md:20.

  Counts, all inside the worktree: core 211, doc 24, skill 26, graphviz 18, petstore 18, northbank 3, rivermart 3, streamline 3, ods-vscode 9, pages 652 unit at 100% statements/branches/functions/lines, pages 257 e2e. ods-ui and ods-vscode build clean.

  Three things for the lead to know at review. (a) `packages/doc/src/index.test.ts`'s inline snapshot of the petstore file list was stale on develop before I touched it — it was missing card 45's three `fulfilment_bc/aggregates/carrier/*` paths and failed on a clean checkout; I updated it, and the diff there is exactly those three lines. (b) The three failing suites in apps/docs (doc.example, model.example, tactical.example) also fail on clean develop — that is card 52's ground, and I left them alone; note that my change adds one more line of drift to the tactical snapshot, `"returns": undefined`, alongside the `"disposition": undefined` already there. (c) The clean-code sweep surfaced one latent gap worth a decision some day, not now: `packages/pages/src/lib/molecules/ConsumableSubsection.svelte:32-34` draws a Payload row and an attribute table for each consumable on an AggregatePage, and I did not add Returns there because the card named only ConsumablePage and said ProvidesTable stays put. It is invisible today — every operation carrying `returns` across all four reference models is provided by a service, and services render through ProvidesTable — so nothing in the shipped docs contradicts itself. It would show the moment someone puts `returns` on an aggregate-provided operation. Say the word and it is a four-line change.
- **dev-opus** (2026-09-06T16:05:00.000Z): Lead closed the gap I flagged in (c). `ConsumableSubsection` now draws a Returns definition row beside Payload, only when set (packages/pages/src/lib/molecules/ConsumableSubsection.svelte:34-36), so an operation an aggregate provides says what it answers with where the AggregatePage lists it; its header comment now names the row too (:16-18). `ProvidesTable.svelte` is untouched, as instructed — `git status` over `src/lib/molecules/` lists only the subsection. No reference model has an aggregate-provided operation carrying `returns`, so the case is covered by fixture: `Answer Schema` and an `Answering Operation` on the rootless aggregate (packages/pages/src/lib/fixtures.ts:114-117, 127-136), asked with `Empty Schema` and answered with `Answer Schema`. The unit case at packages/pages/src/lib/templates/edge-cases.test.ts:40-64 finds each consumable's subsection by its heading and asserts Answering Operation carries both terms and names Answer Schema, while Silent Operation — a payload but no answer — has no Returns term at all, so the row is absent rather than empty. Pages 653 unit at 100% statements/branches/functions/lines (up one from 652), 257 e2e green including the AggregatePage stories in all three themes and the browse and diagram specs that walk aggregates. Biome clean on both changed files.
- **lead** (2026-09-06T18:00:00.000Z): Landing. Decision 13 implemented end to end, the returned schema counts as a carrier, and the aggregate subsection shows Returns.
