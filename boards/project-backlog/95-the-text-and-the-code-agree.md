---
column: done
labels: [backend, ddd, breaking]
priority: high
agent: ironhide-deep
live: false
clean-code-swept: true
updatedAt: 2026-09-09T02:10:00.000Z
---
# The text and the code agree; a process owns its deadlines; the kernel needs no invented subdomain

Prowl's fourth review, each item probed. Four places where a rule or decision promises what the code does not do: a one-operation consumer "is its own `by`" but the walk never infers it; `conformist-backed` says "consumes nothing it publishes" and checks only schema-carrying consumptions, so a payload-less event fails it; a standard declared as a published language is warned unbacked because `relationship-roles-backed` backs an upstream role by consumption only; and `invariant-in-aggregate` accepts an unheld value object of its own context while its comment says it refuses. One phantom node: a process branching on a local operation's answer must declare a consumption nobody makes. One model bend: NorthBank credits its Scheme Gateway with raising the scheme's answers and never declares the scheme. And a per-instance deadline costs five declarations through a Clock context that decision 28 meant for calendar events.

## Checklist

- [x] `consumable-kind` accepts an answer of an operation the reactor itself issues (a local call-and-branch), as well as one the context consumes; the reaction walk steps from the issued operation to its answer; test with the review's probe P1a which must validate clean with no consumption
- [x] The reaction walk treats a consumer that provides exactly one operation as that operation's caller when `by` is empty (`callsOut` infers it), so the rule text is true; RiverMart's `CaseAPI` consumption reaches the chain; test with probe P3
- [x] `conformist-backed` counts any consumption of the upstream's consumables, schema or not, and any borrowed schema or value object; the catalogue text matches; the `models/_shared` assertion that every cross-context event carries a schema comes out, because it masked this
- [x] `relationship-roles-backed` backs a `published-language` upstream role by a schema or value object the downstream borrows as well as by a schema-carrying consumption; test with probe P5 (an external standards body with a borrowed schema) which must validate clean
- [x] A process may declare its own deadlines: `ProcessSchema.deadlines?: { [id]: { name, description, after: string } }`, each behaving as an event the process raises to itself and may `on` or `ends` on; the reaction walk and flow map draw a deadline as a step from the process to itself labelled with `after`; no Clock context needed for a per-instance timer; RiverMart's 30-minute authorisation expiry from card 92 becomes the Checkout process's own deadline and the scheduler operation comes out; decision 23 amended by the lead
- [x] A shared kernel context (a context whose relationships are all `shared-kernel` and that has two or more sharers) is exempt from `context-serves-subdomain`, because it serves its sharers' subdomains; NorthBank's invented "Shared Financial Primitives" subdomain comes out and the kernel context keeps its team; decision 16 amended by the lead
- [x] RiverMart's `RiskAssessment.subjectId` follows decision 15's own answer: `orderId` and `sellerId`, each optional and identifying its target, with a prose invariant that exactly one is set
- [x] NorthBank declares the payment scheme as an external context that raises `SchemeSettlementConfirmed` and `SchemeRejected`, consumed by the Scheme Gateway, whose `SubmitToScheme` stops claiming to raise them; `AuthoriseCard` declares what it returns and rejects with; DISCOVERY.md says so
- [x] `invariant-in-aggregate` refuses a value object nobody in the aggregate holds whether it is the context's own or borrowed, as the comment says; the models re-read for any invariant that relied on the gap
- [x] `Consumption.path` qualifies the first `by` caller by kind so a policy and an operation with one id do not collide; `consumption-once` compares refs; test with probe P7
- [x] Decisions 15, 16, 17, 21, 23, 28 amended by the lead; you confirm the mechanics match
- [x] `bash scripts/verify-all.sh` green; diagnostics per model unchanged or explained; probes P1a, P3, P4, P5, P6d, P7 rerun and reported

## Gates

- [x] clean-code-swept — swept the diff against the eight principles; nothing above 0.5. Three things the sweep changed. `Process.on`/`ends` guarded a foreign deadline through `events.map(this.own, this)`, which used `map` for its side effect and a `thisArg` for no gain; it is now a plain loop over `refuseForeignDeadline`. `conformsInSubstance` had three branches (borrow, call, schema-carrying consumption) and the fix collapsed two of them into one, so the helper `callCrosses` is no longer read there — it is still read by `shared-kernel-backed`, so it stays. And `stepsFrom`'s deadline step carried an `as Reactor` cast that a `: ReactionStep` return annotation removes. `getDeadlineByRefOrThrow` and `getProcessTriggerByRefOrThrow` exist because every other lookup has the pair and `lookups.test.ts` asserts every OrThrow variant by name; both are now in that test rather than dead (ironhide-deep, 2026-09-09T02:00:00Z)
- [x] verify-all — `bash scripts/verify-all.sh` exit 0: core 538, graphviz 27, doc 31, skill 26, northbank 3, petstore 20, rivermart 3, streamline 3, models/_shared 9, pages 753, apps/docs 23, apps/ods-vscode 15, pages e2e passed, petstore schema matches core dist (ironhide-deep, 2026-09-09T02:05:00Z)

## Comments

- **optimus-prime** (2026-09-09T00:20:00.000Z): Ironhide-deep, justified by the reach: a new process element, four rule corrections, two model surgeries and six decisions. After card 94 lands (the lead will say); `feat!`.
- **ironhide-deep** (2026-09-09T00:40:00.000Z): Picked it up; worktree reset to origin/develop at d4cd361 ("card 94 done, 95 dispatched"), installed. Read the six amendments and confirm the mechanics match what they say, item by item. 15: the polymorphic-identity entry's own answer is two optional attributes each identifying its own target plus a prose invariant, and it now cites a model that does that. 16: exempt a context whose relationships are all shared kernel with two or more sharers. 17 is a note, not an amendment — two named costs of the boundary drawn twice — and asks for no mechanism; nothing to implement, and I have changed nothing there. 21: the walk infers the caller when the consumer provides exactly one operation and `by` is empty. 23: an answer of an operation the reactor issues is a trigger without a consumption, and a process declares `deadlines`, each an event it raises to itself after a stated interval, `on` or `ends`. 28: `relationship-roles-backed` backs an upstream published-language role by a borrowed schema or value object. I ran the reviewer's three probe files first, against the card-94 build, adapted only for the new `op.returned()`/`op.rejected()` API and for this worktree's path; all six named probes reproduced exactly as the review describes them, and that run is the baseline everything below is measured against.
- **ironhide-deep** (2026-09-09T01:00:00.000Z): The four rules. `consumable-kind` now accepts an answer whose operation the reactor issues, as well as one the reactor's context consumes — the message names both ways out. `conformsInSubstance` counts any consumption crossing from the upstream, schema-carrying or not, plus the borrowing it already counted: what is conformed to is the upstream's language and a name is language, so `NightlyBatchCompleted` with no payload backs the role. That is one fewer branch, not one more: the old "calls one of its operations" and "consumes something carrying its schema" are both just "consumes something it provides". `relationship-roles-backed` backs a published-language upstream role by the downstream borrowing a schema or value object, which is the mirror of what it already did for conformist, and the unbacked message says so. `invariant-in-aggregate` asks a value object one question and no other — does anything inside this aggregate hold one — instead of reading the scope first, which answered "inside the boundary" for every value the context owned; that is the sentence its own comment has made since card 89. Catalogue `summary`, `why` and `fix` rewritten for all four, and `validation-rules.md` regenerated from them.
- **ironhide-deep** (2026-09-09T01:15:00.000Z): Deadlines. A `Deadline` is an element of the process: `name`, `description`, `after` (free text, like an attribute's type), ref `<process ref>/deadlines/<id>`, `ProcessSchema.deadlines?` keyed by id, `Process.addDeadline(name, { description, after })`. `ReactionTrigger` stays `Consumable | Answer` for a policy and a new `ProcessTrigger` adds `Deadline` for a process's `on` and `ends` — a policy is stateless and has no instance to run a clock for, so the type says so rather than a rule saying it afterwards. Nothing else may name one: `Process.on`/`ends` throw on another process's deadline, where `Consumable.returned()` throws on an operation that returns nothing, because it is a sentence with no meaning rather than a model with a mistake in it. The walk steps from the process to the process carrying the deadline; `reaction-cycle` was already exempting a ring through one process and no other reactor, so the self-loop is a lifecycle and not a warning, and there is a test saying so. The flow map draws the loop labelled `after 30 minutes`, or `after 30 minutes (ends)` when it is what completes the instance, through a new `after` on the edge and `flowEdgeLabel`. No node: there is no provider it could hang under. `relationship-declared` skips it, because it crosses nothing.
- **ironhide-deep** (2026-09-09T01:25:00.000Z): The walk and the ref. `callsOut` infers the caller when the consumer provides exactly one operation and `by` is empty, which is the sentence `consumption-by-required` already acts on when it stays quiet. RiverMart's `CaseAPI` is the case the review named: it provides `OpenCase` alone, and `chain.after(OpenCase)` now reaches `RequestReturn` and `GetCustomer` as well as `GetOrder`, with `reachedEvents` reaching `ReturnRequested`. It changed one existing test's premise rather than breaking it: `raises-restated` said nothing about a front with no `by`, and with a single-operation consumer the chain does carry the fact, so the restatement is now reported; the "no `by`, no chain" case is kept with a second operation on the service, which is where the model really has nothing to say. `Consumption.path` puts the caller's collection in the segment — `provides/archive`, `policies/decide` — so Petstore's "Reserve Pet" policy and "Reserve Pet" operation no longer produce one ref for two consumptions, and `consumption-once` compares callers by ref, which is what the path is built from, so the two read the same thing.
- **ironhide-deep** (2026-09-09T01:40:00.000Z): The models. RiverMart: `RiskAssessment.subjectId` is `orderId` and `sellerId`, each optional and identifying its own target, with `OneSubject` saying in prose that exactly one is set; the Checkout process declares "Authorisation expiry" after 30 minutes and ends on it, and `ExpireAuthorisations`, `AuthorisationExpired` and the consumption of it are gone. NorthBank: "Shared Financial Primitives" is gone and the Shared Kernel context keeps its team; the Payment Scheme is an external context publishing `SchemeSettlementConfirmed` and `SchemeRejected` in its own `SchemeResponse` shape, the gateway consumes both through the operation that waits, `SubmitToScheme` returns `SchemeSettlement` and rejects with `SchemeRefusal` instead of claiming to raise the scheme's events, and `AuthoriseCard` returns `CardAuthorisationApproved` and rejects with `CardAuthorisationDeclined`. Both DISCOVERY.md files carry a card-95 revision saying all of it. `models/_shared`'s cross-context-event schema assertion is out, with a paragraph saying why it masked the `conformist-backed` gap. Every model's diagnostics are unchanged, one for one, ref for ref.
- **ironhide-deep** (2026-09-09T02:05:00.000Z): `bash scripts/verify-all.sh` green end to end, exit 0. Summary: core 538 tests passed, graphviz 27, doc 31, skill 26, northbank 3, schema comparison (petstore vs core dist) match, petstore 20, rivermart 3, streamline 3, models/_shared 9, pages 753, apps/docs 23, apps/ods-vscode 15, pages e2e (full suite) passed. Diagnostics per model unchanged, every one a DELIBERATE: `Swagger Petstore (v3): 0 diagnostic(s)`; `RiverMart: 2` — `[error] aggregate-root` on `cart_&_checkout/aggregates/wishlist`, `[error] cross-aggregate-reference` on `cart_&_checkout/aggregates/cart/entities/cart`; `StreamLine: 3` — `[error] internal-consumable` on `recommendations/services/recommendations_api`, `[error] schema-context` on `playback/.../playback_started`, `[warning] policy-complete` on `devices/policies/recertify_on_sdk_release`; `NorthBank: 4` — `[warning] relationship-declared` and `[error] separate-ways` on `branch_&_contact_centre/services/channels_app`, `[error] consumable-kind` on `lending/policies/escalate_arrears`, `[warning] context-serves-subdomain` on `identity_&_access`. `npx tsc --noEmit` clean in core, graphviz, doc, skill, all four models, models/_shared, apps/docs, apps/ods-vscode and pages; `npm run check` in pages clean, 940 files, 0 errors, 0 warnings; biome clean on every one of the twenty files I touched.
- **ironhide-deep** (2026-09-09T02:08:00.000Z): The six probes, rerun against this build. Every one flips and nothing else moved.

```
=== P1a process waits on answer of LOCAL op it issues (no consumption declared): 0 diagnostic(s)
  steps from ValidateInstruction: [ 'Lifecycle (answer Verdict)' ]

=== P3 one-op consumer without by: 0 diagnostic(s)
  chain after DoIt: [ 'ChangeThing' ]  reachedEvents: [ 'ThingChanged' ]

=== P4 aggregate invariant on unheld same-context VO: 1 diagnostic(s)
  [error] invariant-in-aggregate: Invariant "Positive" of aggregate "Note" constrains "Money", which is a value object of bounded context "C" that nothing in "Note" holds; ...

=== P5 standard as external context, borrowed schema, published-language declared: 0 diagnostic(s)

=== P6d Clock: after roles and relationship: 0 diagnostic(s)

=== P7 policy and operation share an id: 0 diagnostic(s)
  c1.ref: #/boundedcontexts/a/services/app/consumes/boundedcontexts~b~aggregates~t~provides~happened/provides/reserve_pet
  c2.ref: #/boundedcontexts/a/services/app/consumes/boundedcontexts~b~aggregates~t~provides~happened/policies/reserve_pet  same? false  findConsumption(c2.ref)===c1? false
```

P4 is the one that gains a diagnostic, which is the point of it: the rule now refuses what its comment always said it refused. P6d was a warning that the Clock model was unbacked and is now clean — the Clock still validates, for the calendar events decision 28 kept it for — and the per-instance case it was standing in for is a deadline now.
- **ironhide-deep** (2026-09-09T02:10:00.000Z): Three things for your eye. First, the one judgement I had to make that the card did not spell out. Moving `SchemeSettlementConfirmed` and `SchemeRejected` to the scheme leaves the Payments Hub with nothing to wait on, and there were two ways to give it one: let the hub subscribe to the scheme's events directly, or let the gateway answer the call. I took the second, because the hub lead said "we submit to the scheme **through the gateway**" and a Payment Scheme -> Payments Hub relationship would contradict that in the one place the map is meant to be read. It uses card 94's machinery and needed two schemas: `SchemeRefusal` on the gateway (instruction, scheme reference, and the scheme's status reason passed through) and `SchemeResponse` on the scheme, which mirrors CardCo's `CardCoAuthorisationMessage` exactly. Say the word and I will swap it for the direct subscription instead. Second, "30 minutes" for RiverMart's expiry is your figure from the card, not DISCOVERY's — the interview says a hold "expires if not captured" and gives no number — so I recorded it in the card-95 revision as coming from the card. Third, `invariant-in-context` has the same disagreement between its comment and its code that `invariant-in-aggregate` had: it short-circuits on `contextOf(target) === bc` before the held check, so a context invariant on the context's own unheld value object is accepted while the comment says "a value nobody in the context holds is refused (card 89)". It is not on the checklist and no model relies on it either way, so I left it. It wants a card, or a line on the next one.
