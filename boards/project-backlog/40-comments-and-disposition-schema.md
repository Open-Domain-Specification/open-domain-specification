---
column: review
labels: [backend, ddd, breaking]
priority: high
agent: dev-opus
live: false
clean-code-swept: true
updatedAt: 2026-09-04T04:10:00.000Z
---
# Comments and disposition on strategic intents (RFC-002 card D)

Pin the evidence layer in core. Every strategic intent (a context relationship, a consumable, a consumption) can carry `comments` and a `disposition`, inline on the element. See docs/rfcs/rfc-002-intent-and-evidence.md sections 3 and 6, and the provisional types the designs used in packages/pages/src/lib/evidence/fixtures.ts (which this card replaces with the real ones).

## Checklist

- [x] `packages/core/src/schema.ts`: `CommentLinkKind = "code" | "contract" | "adr" | "runbook" | "dashboard"`, `CommentLink { kind, url, label? }`, `Comment { text, link? }`, `Disposition = "by-design" | "tolerated" | "refactor"`; `comments?: Comment[]` and `disposition?: Disposition` on `DirectedContextRelationshipSchema`, `SymmetricContextRelationshipSchema`, `ConsumableSchema` and `ConsumptionSchema`
- [x] Workspace model (`packages/core/src/workspace.ts`): the three element classes expose `comments` and `disposition`; `toSchema` and `fromSchema` round-trip them; DSL options on `upstreamOf`, `downstreamOf`, `relatesTo`, `provides`/`consumes` accept them
- [x] `dispositionOf(element)` helper in core returning `"by-design"` when unset, and `intentsWithoutComments(workspace)` returning every strategic intent with no comments (the health report and the future rule both use it)
- [x] JSON schema regenerated (`dist/workspace.schema.json`); the skill reference regenerated if it documents relationship options
- [x] The four reference models gain real comments: petstore at least the shared kernel (refactor, two comments as in the Storybook fixture), Sales to Inventory (tolerated) and Catalog to Sales (two comments); each other model at least two intents
- [x] Core coverage stays at its thresholds; `npm test` at root green; `assertDocSite` still green for the four models

## Gates

- [x] qa-automated — lerna run test with the nx cache skipped: core 107, doc 16, pages 442, four model suites incl. assertDocSite, extension 4, all green (lead, 2026-09-04T04:10:00Z)

- [x] clean-code-swept — six-lens sweep over the diff; nothing above 0.5, no TODOs added, no refactor loop needed (dev-opus, 2026-09-04T11:10:00.000Z)
- [x] tests — core 107 tests in 10 files with evidence.ts at 100%; `npx lerna run test --skip-nx-cache` green for all 10 projects including the four model suites and their `assertDocSite` checks (dev-opus, 2026-09-04T11:20:00.000Z)

## Comments

- **lead** (2026-09-04T03:00:00.000Z): Assigned to dev-opus. Fixed by decision: inline on the element, not a separate index keyed by ref; no lifecycle fields of any kind (no dates, no verified-by, no status beyond the three dispositions); `by-design` is the meaning of an absent disposition and is never written to JSON by the DSL. Names are exactly as in the checklist so pages can swap its provisional types for core's. This is a `feat!:` change: no backwards compatibility, update every reference model and generated artefact in the same tree. Do not touch pages, doc or the extension; card 22 on the extension board runs in parallel on pages. Tests that prove it: core unit suite with coverage, a round-trip test on a relationship with comments and a disposition, and the four model suites. Work in your worktree; `npm ci` there first if node_modules is missing; if the card is missing, `git reset --hard develop` there first.
- **dev-opus** (2026-09-04T08:00:00.000Z): Picked this up. The worktree was on an older commit so the card file was missing; reset it to develop and ran `npm ci`. Read RFC-002 sections 3 and 6, the provisional types at packages/pages/src/lib/evidence/fixtures.ts:18-46 and the petstore overlay at packages/pages/src/lib/evidence/fixtures.ts:124-207 — I will reuse that comment text verbatim in the petstore model so the designs and the real data say the same thing. Targets in core: packages/core/src/schema.ts:100-170, packages/core/src/workspace.ts:969-1044 (Consumable), packages/core/src/workspace.ts:1361-1390 (Consumption), packages/core/src/workspace.ts:1392-1490 (relationship options and class), packages/core/src/workspace-from-schema.ts:366-399 (relationship rehydration).
- **dev-opus** (2026-09-04T09:10:00.000Z): Schema types are in: `CommentLinkKind`, `CommentLink`, `Comment` and `Disposition` at packages/core/src/schema.ts:100-133, and `comments?`/`disposition?` on `ConsumableSchema` (packages/core/src/schema.ts:163-167), `ConsumptionSchema` (packages/core/src/schema.ts:177-181), `DirectedContextRelationshipSchema` (packages/core/src/schema.ts:216-220) and `SymmetricContextRelationshipSchema` (packages/core/src/schema.ts:230-234). The model side: a new packages/core/src/evidence.ts holds `EvidenceOptions`, the `Evidenced` interface, `normaliseDisposition` (packages/core/src/evidence.ts:37-41), `dispositionOf` (packages/core/src/evidence.ts:44-48) and `intentsWithoutComments` (packages/core/src/evidence.ts:54-72), exported from packages/core/src/index.ts:4. `Consumable` (packages/core/src/workspace.ts:990-991, :1013-1014, :1047-1048), `Consumption` (packages/core/src/workspace.ts:1380-1381, :1392-1393, :1405-1406) and `ContextRelationship` (packages/core/src/workspace.ts:1465-1466, :1472-1473, :1493-1512) now carry them, and packages/core/src/workspace-from-schema.ts:383-384 and :398-399 rehydrate them. Two judgement calls I made, both directly implied by your comment, say if you disagree: (1) `by-design` is normalised to `undefined` at construction (packages/core/src/evidence.ts:36-40) rather than dropped in `toSchema`, so nothing downstream ever sees the redundant value and the JSON never carries it; `dispositionOf` is the accessor. (2) `intentsWithoutComments` walks relationships plus every consumable and consumption, including `internal: true` consumables, because the card's description enumerates "a consumable" without qualification. Evidence: packages/core/src/evidence.test.ts:1-188, 9 new cases; core suite 105 passed, evidence.ts at 100% statements/branches/functions.
- **dev-opus** (2026-09-04T09:15:00.000Z): QUESTION for lead: the checklist names `upstreamOf`, `downstreamOf`, `relatesTo` and `provides`/`consumes`, but there is no `relatesTo` on `BoundedContext` — `relatesTo` is the entity/value-object relation helper (packages/core/src/workspace.ts:1102-1107 and :1206-1211). The symmetric relationship helpers are `partnerOf`, `sharesKernelWith` and `separateWaysFrom`, and each takes `description?: string` positionally (packages/core/src/workspace.ts:724-745). The petstore shared kernel the checklist asks for is a `sharesKernelWith` call (models/petstore/src/workspace.ts:905-908), so one of them has to change shape and that is a public API decision. Options: (a) widen the second parameter to `description?: string | SymmetricRelationshipOptions`, so the twelve existing call sites across the four models are untouched and only the ones gaining evidence pass an object; (b) replace it with a single `options: SymmetricRelationshipOptions = {}`, matching how the directed helpers already work, and rewrite all twelve call sites to `{ description: "..." }`. I recommend (b): the card is a `feat!:` with no backwards compatibility, and it leaves one rule in the DSL — every relationship helper takes an options object — instead of a union parameter that every future option has to thread through. I have already added `SymmetricRelationshipOptions` at packages/core/src/workspace.ts:1418-1421 and `EvidenceOptions` on `ContextRelationshipAttributes` (packages/core/src/workspace.ts:1423-1440), so `workspace.addRelationship` already accepts evidence for symmetric relationships either way; only the three sugar helpers are waiting on this. Blocked until answered — the four reference models and the regenerated JSON schema all follow from it.
- **dev-opus** (2026-09-04T10:20:00.000Z): Lead answered (b), plus: keep the by-design normalisation, and exclude `internal: true` consumables from `intentsWithoutComments` with a test. Both done. The three symmetric helpers now take `SymmetricRelationshipOptions` (declared at packages/core/src/workspace.ts:1424-1427, used at packages/core/src/workspace.ts:724-757), so every relationship helper in the DSL takes an options object; the thirteen call sites are rewritten — packages/core/src/makeTestWs.ts:266-268 and the four models. `intentsWithoutComments` skips internal consumables at packages/core/src/evidence.ts:69-71, covered by packages/core/src/evidence.test.ts:103-114, and packages/core/src/evidence.test.ts:163-177 pins all three symmetric helpers on the new shape.
- **dev-opus** (2026-09-04T10:45:00.000Z): The four reference models carry real evidence. Petstore takes the Storybook wording verbatim so the designs and the shipped model agree: Catalog→Sales customer-supplier with two comments (models/petstore/src/workspace.ts:921-945), Sales→Inventory `tolerated` with the conformist comment (models/petstore/src/workspace.ts:948-963), and the Catalog/Inventory shared kernel `refactor` with the kernel and ADR-014 comments (models/petstore/src/workspace.ts:967-988); the two remaining relationships move to the options object at models/petstore/src/workspace.ts:992-995 and :998-1000. It also exercises the consumable and consumption paths in real data: `ReservePet` marked `refactor` with ADR-017 (models/petstore/src/workspace.ts:277-296), the `GetPetSummary` operation (models/petstore/src/workspace.ts:357-399) and its ACL consumption (models/petstore/src/workspace.ts:570-582). Northbank has the money kernel and a `refactor` on the Channels/Decisioning separate ways that the model already admitted contradicts itself (models/northbank/src/workspace.ts:2344-2365 and :2371-2393); rivermart the tracking kernel as `tolerated` and the Search/Ads partnership (models/rivermart/src/workspace.ts:2194-2216 and :2219-2232); streamline the manifest kernel as `tolerated` and the joint certification partnership (models/streamline/src/workspace.ts:1814-1836 and :1838-1851).
- **dev-opus** (2026-09-04T10:55:00.000Z): Generated artefacts and the skill reference. `packages/core/dist/workspace.schema.json` regenerated by the package's own `postbuild`: it now defines `Comment`, `CommentLink`, `CommentLinkKind` and `Disposition`, and `comments`/`disposition` appear on `DirectedContextRelationshipSchema`, `SymmetricContextRelationshipSchema`, `ConsumableSchema` and `ConsumptionSchema`; `dist/` is gitignored, so the artefact is the build's, not a committed file. The skill reference documented the old positional signatures, so packages/skill/skill/references/dsl-api.md:20-24 and :33-34 gain the evidence options and packages/skill/skill/references/dsl-api.md:43-49 explains the pair once; packages/skill/skill/references/translation-table.md:20-22 moves the three symmetric rows onto the options object; packages/skill/skill/examples/petstore.md:91-97 is rewritten and :99-124 adds a worked evidence example.
- **dev-opus** (2026-09-04T11:10:00.000Z): Line-number correction to my 09:10 entry: biome reflowed the `ContextRelationship` implements clause, so its citations are now packages/core/src/workspace.ts:1473-1474, :1480-1481 and :1503-1517; `Consumable` and `Consumption` are unmoved. Clean-code sweep run over the diff on all six lenses; nothing scored above 0.5, so no TODOs were added and no `refactor` loop was needed. The only findings were two DRY notes at 0.4 — the `comments`/`disposition` constructor pair (packages/core/src/workspace.ts:995-996, :1385-1386, :1473-1474) and the `toSchema` pair (packages/core/src/workspace.ts:1052-1053, :1410-1411, :1504-1507) repeat across the three classes. I left them: each is two lines, and a shared `initEvidence`/`evidenceToSchema` helper would add an indirection to save four lines. Say if you would rather have the helper.
- **dev-opus** (2026-09-04T11:20:00.000Z): Full verification. Core: 10 files, 107 tests, evidence.ts at 100% statements/branches/functions/lines and no threshold configured in packages/core/vitest.config.ts:9-11, so nothing regressed. `npx lerna run test --stream --skip-nx-cache` green for all 10 projects — nx replays cached output with main-repo paths, so the cache had to be skipped to prove the worktree; the four model suites (which call `assertDocSite`, models/petstore/src/workspace.test.ts:286) all pass. `npx @biomejs/biome check` clean on everything I touched; the two remaining `noThenProperty` errors at packages/core/src/workspace.ts:1745 and :1761 are pre-existing (confirmed against a stashed tree). Moving to review.
- **lead** (2026-09-04T04:10:00.000Z): Reviewed packages/core/src/evidence.ts, the schema additions and the petstore comments. The two 0.4 DRY notes stay as they are. Landing on develop as feat!.
