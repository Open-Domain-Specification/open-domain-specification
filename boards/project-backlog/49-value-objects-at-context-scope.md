---
column: review
labels: [backend, ddd, breaking]
priority: high
agent: ironhide
live: false
clean-code-swept: true
updatedAt: 2026-09-05T01:20:00.000Z
---
# Value objects belong to the context; shared kernels share them

Implements [decision 16](../../decisions/16-value-objects-belong-to-the-context.md). Supersedes backlog card 46.

## Checklist

- [x] Schema, workspace model, DSL: `BoundedContext.addValueObject`; aggregates and schemas reference context value objects; `AggregateSchema.valueobjects` removed; JSON schema regenerated
- [x] Shared-kernel exception in `schema-context` and in the cross-file rules for exactly the two declaring contexts
- [x] `shared-kernel-backed` (warning): two contexts declaring a shared kernel share at least one value object or schema across it (run 4, issue 13)
- [x] Four reference models moved; `models/_shared` `money()` removed; petstore's PetStatus declared once in Catalog and referenced by Inventory through their shared kernel
- [x] Doc generator, pages (context page Value objects section; aggregate page lists the ones used), skill reference and interview updated
- [x] `relationship-cycle` says what a static model can say: a ring of calls is a ring of models each shaped around the next, not a runtime wait; both repairs offered, partnership first
- [x] Root suites green inside the worktree; pages coverage at 100%

## Gates

- `clean-code-swept`: swept 2026-09-05. Folded the doc generator's two copies of the aggregate/value-object relation into one module, `packages/doc/src/lib/value-objects.ts`, read from both ends by the aggregate page and the context page. In the validator, `modelMembersOf` is now the single walk over everything that carries attributes and relations, and `saysWhatItPointsAt` names in one place which relations `aggregate-tree` owns and which belong to `cross-aggregate-reference` and `cross-context-relation`. Gave `Entity` a `boundedcontext` getter so nothing has to reach through `aggregate.boundedcontext` to find out where a relation end lives. Removed one now-unused local in the validator tests. biome and tsc clean on every file touched; the two `noThenProperty` errors in packages/core/src/workspace.ts and the two lint findings in packages/pages (fullscreen.svelte.ts, WorkspacePage.test.ts) are pre-existing and untouched.
- `qa-automated`: core `npm run build` + 288 passed (13 files); doc 24; skill 26 after `npm run generate`; graphviz 18; pages `npm run build` + 660 passed across 94 files, coverage 100/100/100/100; ods-vscode `npm run build` + 9. Models, each `npm run build` then `npx vitest run`: petstore 18 (0 diagnostics), northbank 3 (3 deliberate), rivermart 3 (3 deliberate), streamline 3 (4 deliberate). apps/docs is 21 passed | 1 failed, the pre-existing stale `doc.example.test.ts` (card 52); see the journal.

## Comments

- **lead** (2026-09-06T12:00:00.000Z): Assigned to dev-opus after card 48 lands (the lead will say); this is the largest change and must not overlap the rule cards. `feat!:`. Work in your worktree with absolute paths; `npm ci` if node_modules is missing; if the card is missing, `git reset --hard develop` there first.
- **ironhide** (2026-09-05T00:05:00.000Z): Claimed. Reset the worktree to origin/develop (5d65141) and ran `npm ci`, which mattered more than usual here: without a node_modules of its own the worktree resolves `@open-domain-specification/core` up to the main checkout, so the models were building against another tree's core.
- **ironhide** (2026-09-05T00:35:00.000Z): The move itself. `ValueObjectSchema` now hangs off `BoundedContextSchema.valueobjects` (packages/core/src/schema.ts:98), `AggregateSchema.valueobjects` is gone, `valueObjectRef` takes a context and an id (packages/core/src/schema.ts:470), and the ref shape is `#/boundedcontexts/<bc>/valueobjects/<vo>`. In the model, `ValueObject` holds a `boundedcontext` instead of an `aggregate` (packages/core/src/workspace.ts:1204-1240), `BoundedContext.addValueObject` is the DSL entry (packages/core/src/workspace.ts:791-799), and `Entity` gained a `boundedcontext` getter so both ends of a relation can say which context they are in without callers reaching through the aggregate. Loading and dumping moved with it in packages/core/src/workspace-from-schema.ts:189-197 and :283-296, and the visitor now walks a context's value objects (packages/core/src/visitor.ts:137-140) rather than an aggregate's.
- **ironhide** (2026-09-05T00:45:00.000Z): Five rules had to be re-scoped, and each one is a judgement worth recording rather than a mechanical edit. `cross-aggregate-reference` (packages/core/src/validate.ts:94-129) now fires only when both ends are entities: a value object belongs to the whole context, so holding one crosses no aggregate boundary. `aggregate-tree` (packages/core/src/validate.ts:311-360) splits in two — the "uses points at a value object, includes at an entity" half now reads every relation the new helper `saysWhatItPointsAt` says it owns, and the tree, cycle and orphan half stays over the aggregate's own entities. `attribute-relation-coherence` (packages/core/src/validate.ts:439-497) asks for a matching `uses` only when the value object is in the same context, because a relation may not cross a boundary and a value reached over a shared kernel therefore has no relation to match. `value-object-shape` walks the context. `invariant-in-aggregate` (packages/core/src/validate.ts:515-542) now accepts a value object of the invariant's own context: it carries no state of its own and is saved as part of whichever aggregate holds one. That last one is the only rule I loosened, and it was forced — the four models constrain value objects from their aggregates 14 times.
- **ironhide** (2026-09-05T00:50:00.000Z): The shared-kernel exception is `sharesKernelWith` at packages/core/src/validate.ts:538-549, read by `schema-context` (packages/core/src/validate.ts:1057-1090) for both `schema` and `returns`. The cross-file half of the checklist has nothing to change yet: decision 08's `WorkspaceSet` is not implemented — there is no set loader and no crossing-table rule in core — so the exception exists in the one rule that can enforce it today, and decision 08's table is a document, not code. Flagging rather than inventing the loader.
- **ironhide** (2026-09-05T00:55:00.000Z): `shared-kernel-backed` at packages/core/src/validate.ts:765-790, with `borrowsFrom` reading the two ways a kernel is actually shared: an attribute anywhere in one context typed by a value object the other declares, or a consumable carrying the other's schema as `schema` or `returns`. Catalogue entry alongside it; tests at packages/core/src/validate.test.ts:1300-1352 cover the empty kernel, both ways of filling it, and a partnership staying quiet, plus the schema-context exception at packages/core/src/validate.test.ts:190-215. The completeness fixture gained a context D sharing an empty kernel with B (packages/core/src/rule-catalog.test.ts:157-159).
- **ironhide** (2026-09-05T01:00:00.000Z): Models. All four now declare their value objects on the context; `money()` is gone from models/_shared/src/index.ts and each model that needs Money has a five-line local helper taking a `BoundedContext`, because a value object is still one per context. Three shared kernels were claims the model did not back, and each model's own prose said what to do. Petstore: Inventory's `InventoryView.status` was a free-text union restating Catalog's PetStatus, and now points at it (models/petstore/src/workspace.ts:906-916) — one definition, counted rather than restated. NorthBank: Ledger declared its own Money and AccountNumber next to a comment saying they were "the same library" as Accounts', so it now holds Accounts' own (models/northbank/src/workspace.ts:824-830); the two `uses` relations went, because a relation may not cross a context and the attribute's `valueobject` is the whole of the link. That turned `SingleCurrencyPerEntry` into an invariant reaching into Accounts, so it now constrains the posting amount inside its own aggregate (models/northbank/src/workspace.ts:895-902). RiverMart: Last Mile held a copy of TrackingLabel described as "the same barcode ... the warehouse prints" and now holds the warehouse's (models/rivermart/src/workspace.ts:1750-1756). StreamLine had no crossing at all, so `ResolveEdge` now answers with an `EdgeManifest` schema whose one attribute is Playback's `StreamManifest` (models/streamline/src/workspace.ts:1073-1084), which is what the kernel's comment claims the two sides share. Every model still validates to exactly its declared deliberate list.
- **ironhide** (2026-09-05T01:05:00.000Z): Pages and the doc generator. The context page gains a Value objects section between Model and Integration (packages/pages/src/lib/templates/ContextPage.svelte:238-263) — name, attribute count, which aggregates hold it, description — and the aggregate table's Value objects column now counts the ones that aggregate holds. The aggregate page lists the value objects it holds rather than owns (packages/pages/src/lib/templates/AggregatePage.svelte:44), over `valueObjectsOf` in packages/pages/src/lib/elements.ts:56-70; the value object page is now a page of the context, crumbs and all, and collects invariants from every aggregate of that context (packages/pages/src/lib/templates/ValueObjectPage.svelte:35-45). Routing moved in packages/pages/src/lib/resolve.ts:23 and :67-70. In the generated docs the context page gains a Value objects table and the aggregate page links each value object to it. The VS Code tree lists Value Objects under the context (apps/ods-vscode/src/tree.ts:276-278) and search does the same. Skill: `bc.addValueObject` in the DSL and translation tables, the ref grammar in the generator, the minimal example in both modes, and a new interview question in phase E — "which values does this context define once, and which of these things carry them?" — with the shared-kernel note attached (packages/skill/skill/references/interview-playbook.md:72-77).
- **ironhide** (2026-09-05T01:10:00.000Z): Optimus's mid-card addition, from the sixth review run: `relationship-cycle` claimed a runtime wait a static model cannot know. Reworded to what the model can say — an upstream-downstream relationship means the downstream shapes its model around the upstream's, so a ring of calls is a ring of models each shaped around one shaped around itself, and none can change first. The message now offers both repairs in the order Optimus gave: declare a partnership where two of them really do move as one, or reverse a dependency by turning that call into an event. Doc comment at packages/core/src/validate.ts:672-688, message at packages/core/src/validate.ts:717-723, catalogue entry at packages/core/src/validate.ts:1395-1404, docs row at apps/docs/docs/3-core/4-validation.md:24. What the rule detects is unchanged, and the three reference models still find no ring of calls.
- **ironhide** (2026-09-05T01:15:00.000Z): Two things outside this card that the run turned up, both reported rather than absorbed. First, apps/docs is not on this card's list but its example tests build models with the DSL, so four of them had to move to `bc.addValueObject`; running them updated four inline snapshots, and three of those snapshots also carried drift from cards 47, 48 and 53 (new diagnostics in `validation.example.test.ts`, nothing to do with value objects) that nobody had re-recorded. I let the update stand because the recorded output is now true, but it is card 52's ground and the lead should know it moved. Second, `doc.example.test.ts` still fails there, and that one is untouched by this card: its expected workspace page has been missing the Health section since card 26 added it. Left red for card 52.
