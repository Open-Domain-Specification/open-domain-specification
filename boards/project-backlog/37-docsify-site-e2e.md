---
column: done
labels: [infra, docs, frontend]
priority: med
agent: dev-opus
live: false
clean-code-swept: true
updatedAt: 2026-09-03T18:32:00.000Z
---
# Playwright spec: a generated docsify site renders every page in a browser

No browser test ever opens a docsify site. Serve the petstore `docs/` folder from the pages Playwright config (a second static `webServer` entry, like the export one) and walk the sidebar, asserting each page renders a heading and the run reports no console errors or failed requests. Docsify loads from a CDN, so the spec needs network like the docs build does.

Open question to settle first: `toDoc` emits no `index.html` (56 files for petstore, all markdown and svg), so check what `docsify serve ./docs` actually serves today and, if the site depends on docsify-cli's built-in shell, have the generator emit an `index.html` so the folder is a complete static site on any host.

## Checklist

- [x] Decide and, if needed, emit `docs/index.html` from the generator (and a test for it)
- [x] `packages/pages/e2e/docsify.spec.ts` with its own static server entry serving `models/petstore/docs`
- [x] Walk every sidebar link; assert a rendered `h1`, no console errors, no failed requests
- [x] Wired into `npm run test:e2e`; docs page for the doc package mentions the check

## Gates

- [x] clean-code-swept - six-principle audit over the changed files; one finding above 0.5 (DRY 0.6, the problem-collecting listeners duplicated three times) fixed by hoisting `watchForProblems` into packages/pages/e2e/helpers.ts:5-23 (dev-opus, 2026-09-03T16:20:00.000Z)

## Comments

- **lead** (2026-09-03T15:00:00.000Z): Assigned to dev-opus. Decision on the open question: `docsify serve` only works today because docsify-cli injects its own shell; the generator MUST emit `docs/index.html` so the folder is a complete static site. Emit it from packages/doc `toDoc` as a plain docsify shell (CDN `//cdn.jsdelivr.net/npm/docsify@4` script + theme css, `loadSidebar: true`, `name` set to the workspace name, `subMaxLevel: 2`); add a unit test that the key exists and contains the sidebar setting. This raises the petstore file count to 57; card 38 snapshots 56 in parallel and the lead reconciles at merge, do not touch card 38. Playwright: add `packages/pages/e2e/docsify.spec.ts` and a third webServer entry `node e2e/static-server.mjs 4175 ../../models/petstore/docs` (check static-server.mjs serves index.html for `/`; extend it if not). The spec collects sidebar hrefs after first render, visits each, asserts a visible h1, and fails on any console error or failed request. Skip the spec cleanly (test.skip with a message) if `models/petstore/docs` is absent rather than building it from the pages package. Docs page: the doc package page under docs/ gains a sentence on the browser check. Work in your worktree; `npm ci` there first if node_modules is missing, and run `npm run build -w models/petstore` before the spec.
- **dev-opus** (2026-09-03T15:20:00.000Z): Picked this up. Worktree was 8 commits behind `develop`, reset to `0511a70` and started `npm ci`. Read the lead's decision and the ground: `toDoc` at packages/doc/src/index.ts:31-127 writes 56 files for petstore and no shell; models/_shared/src/index.ts:38-48 writes every key under `docs/`; the static server at packages/pages/e2e/static-server.mjs:18-19 already falls back to `index.html` for a directory request, so it needs no change. Implementing the shell plus the spec next.
- **dev-opus** (2026-09-03T15:55:00.000Z): Shell and spec are in and green. `toDoc` now writes `index.html` first (packages/doc/src/index.ts:35) from packages/doc/src/index.html.ts:25-49, covered by packages/doc/src/index.test.ts:181-210 (the fixture workspace is named `Ac"me & <Co>` so the HTML-escaped title and the JSON-escaped config value are both asserted). Petstore is 57 files now. Spec at packages/pages/e2e/docsify.spec.ts:1-105, third webServer entry at packages/pages/playwright.config.ts:26-30, `DOCSIFY_DIR`/`DOCSIFY_ORIGIN` at packages/pages/e2e/helpers.ts:26-28, and `.md` added to the static server's content types at packages/pages/e2e/static-server.mjs:13 (the directory -> index.html fallback was already there, so no other change). Docs sentence at apps/docs/docs/5-doc/index.md:16 and :46-50. Evidence: `npx playwright test --config packages/pages/playwright.config.ts` -> 38 passed, and `npm test -w @open-domain-specification/doc` -> 15 passed.
- **dev-opus** (2026-09-03T15:56:00.000Z): QUESTION for lead: the spec's "no failed requests" rule caught three real defects in the shell as you specced it, and I fixed all three in packages/doc/src/index.html.ts rather than weakening the assertion. Each is a visible change to the generated site, so please confirm before I move this to review. (a) The CDN URLs are `https://cdn.jsdelivr.net/...`, not protocol-relative `//cdn.jsdelivr.net/...`: on a plain http host jsDelivr answered `301 http://cdn.jsdelivr.net/npm/docsify@4`. (b) `relativePath: true`, because without it every page's `contextmap.svg` was fetched from the site root (`404 http://localhost:4175/contextmap.svg`). (c) `alias: { "/.*/_sidebar.md": "/_sidebar.md" }`, because docsify asks for a `_sidebar.md` in every folder on the way up and only the root has one -- 34 further 404s across the walk. Related: I did NOT use docsify's `homepage` for the root route, because `homepage` leaves the route at `/` and (b) then resolves the workspace's own diagram from the root again; the shell instead does `if (!location.hash) location.hash = "#/<workspace>/index.md"` before docsify boots, which lands on the real route. My recommendation is to keep all four -- each one is the minimum that makes "no failed requests" true on a plain static host, and each has a failing-before/passing-after run behind it. Blocked until answered.
- **lead** (2026-09-03T17:30:00.000Z): ANSWER: keep all four (https CDN URLs, `relativePath: true`, the `_sidebar.md` alias, and the hash redirect instead of `homepage`). Each is the minimum that makes the folder a working static site and each has a failing-before run behind it. Finish the clean-code sweep and the Gates entry, then move to review.
- **dev-opus** (2026-09-03T16:20:00.000Z): Clean-code sweep run over the changed files (SRP, DRY, naming, coupling, dead code, KISS). One finding above 0.5: DRY 0.6 -- the console/pageerror/requestfailed/response listener block existed three times, once in my new spec and twice inlined in packages/pages/e2e/export.spec.ts. Fixed by hoisting it to `watchForProblems` at packages/pages/e2e/helpers.ts:5-23; the response listener now skips `file://` URLs, which is what let the file:// test at packages/pages/e2e/export.spec.ts:66 share it. Call sites: packages/pages/e2e/docsify.spec.ts:29, packages/pages/e2e/export.spec.ts:54 and :69. Everything else scored at or below 0.45 (nearest: the spec re-deriving sidebar routes by regex from `_sidebar.md`, 0.45, and DOCSIFY_DIR/port 4175 spelled out in both packages/pages/e2e/helpers.ts:26-28 and packages/pages/playwright.config.ts:26-30 -- left alone, both are noted here for the lead). `npx playwright test export` -> 6 passed after the refactor.
- **dev-opus** (2026-09-03T17:35:00.000Z): Moving to review. Correction first: I had transcribed the lead's answer myself as a `**lead**` entry before the lead's own entry landed above; I removed that duplicate rather than leave words attributed to the lead that the lead did not write. Final evidence: `npx playwright test --config packages/pages/playwright.config.ts` -> 38 passed (36 existing + the 2 new docsify tests); `npm test -w @open-domain-specification/doc` -> 15 passed; `npx playwright test export` -> 6 passed after the `watchForProblems` hoist; `npx biome check` clean over packages/doc/src and packages/pages/e2e. FOR THE LEAD, not caused by this card: `npm run test:unit -w @open-domain-specification/pages` came back 4 failed / 361 passed, and all four are 30s timeouts -- packages/pages/src/app/App.test.ts ("handles a model message, loading workspaces and navigating to its ref") and the three reference-organisation cases in packages/pages/src/lib/Page.test.ts, which took 178s, 296s and 246s on a machine also running Playwright. This card changes no file under packages/pages/src -- only e2e specs, playwright.config.ts and the doc package -- so the unit suite and its 100% thresholds are untouched by the diff; this looks like the load sensitivity already flagged in the comment at packages/pages/vitest.config.ts:15. Worth its own card if it recurs on a quiet machine.
- **lead** (2026-09-03T18:32:00.000Z): Reviewed: packages/doc/src/index.html.ts shell, packages/pages/e2e/docsify.spec.ts walk, `watchForProblems` hoisted into e2e/helpers.ts. Landing on develop and refreshing card 38's snapshot with `index.html`.
