---
column: done
labels: [docs, backend]
priority: high
agent: developer
live: false
clean-code-swept: true
updatedAt: 2026-09-06T14:20:00.000Z
---
# The record says what it means

A five-whys on ten reviewer contradictions (owner's instruction, 2026-09-10) found the same root cause behind most: the reasoning lives in `decisions/`, which the docs site never links and the skill never explains; the docs site and skill never say the model has preferences that are not DDD's laws; rule messages name a refusal without the replacement the record intends; and several schema comments and decision sentences say the opposite of what the record means. The decisions are corrected (15, 16, 23, 27, 28 on 2026-09-10). This card fixes what a developer reads. Runs after card 116, which touches the validator.

## Checklist

- [x] `apps/docs/docs/3-core/3-tactical-design.md`: a paragraph "What the model leaves out on purpose" near the top listing the preferences (no delivery flag, no coordination fields on a process, no modules, no actors, no read-model element, no operations on a value object, an entity has one home, a context invariant records the check not the store) with a link to the repository's `decisions/15` and the folder; §Events and operations gains "type is kind, not delivery; a queued command is still an operation" and "an operation people call through a screen is consumed by nobody, and that is normal"; §Processes documents `deadlines` (`after`, `from`) and says all-or-any of `on` and what it undoes are prose on purpose; the context-invariant lines say "records who checks it, not how strongly the store holds it"; §Schemas says an external context's schemas are also the kinds it publishes, which `identifies` may name; §Value objects says a value provides no operations; a new short §Read models (query service, `returns` the view's shape, a policy writes it); the cross-context invariant line gains the wallet-and-escrow example
- [x] `apps/docs/docs/3-core/2-strategic-design.md`: §Context flags says what an external context may state (schemas, value invariants, a contract on its own operation); the shared-kernel lines say a pairwise kernel shares values and schemas directly and a jointly owned entity is an aggregate of a kernel context both consume
- [x] `apps/docs/docs/3-core/index.md`: the Domain Hierarchy tree refreshed (Process, deadlines, context invariants, `external`, `bigBallOfMud`, `returns`, `rejects`, `reasons`) and a pointer to `decisions/`
- [x] `packages/core/src/schema.ts` comments: `PolicySchema.on` drops "synchronous because the operation is"; the Process description says how long it waits is a deadline; `DataSchemaSchema` says on an external context a schema is also a published kind; regenerated references follow
- [x] `packages/core/src/validate.ts` messages and fix text: `relationship-cycle` fix gains the queued-command clause; `context-invariant-is-checked` why says who checks, not how strongly the store holds; `external-is-boundary` message and fix name the schema-plus-`identifies` route; `cross-context-relation` and `specialisation-in-boundary` fix name the kernel-context route for a kernel pair; `consumption-by-reactor` message carries the projection clause its fix already has; `invariant-in-context` message names the policy-or-process route; the docs rule table follows
- [x] `packages/skill/skill/references/interview-playbook.md` Phase F asks "who asks it to do that, a person at a screen, another part, a schedule?" and records the answer in the description; `translation-table.md` gains rows for "the clerk does X" and "we share Product with them"; `SKILL.md` gains one line saying the omissions are preferences, not DDD's laws, and where the list is
- [x] `bash scripts/verify-all.sh` green

## Gates

- [x] clean-code-swept — prose-only and doc-comment-only changes to schema.ts/validate.ts, no rule logic touched; message/fix/why edits verified against validate.test.ts and the fivewhys probes (developer, 2026-09-06T14:20:00.000Z)

## Comments

- **developer** (2026-09-06T14:20:00.000Z): Read the card, decisions/15 (both 2026-09-10 amendments), 16, 23, 27, 28 and the repodoc-workflow skill. Reset the worktree to `394f37d` ("card 116 done; card 120 dispatched") and ran `npm install`.
- **developer** (2026-09-06T14:20:00.000Z): `packages/core/src/validate.ts`: added the queued-command clause to `relationship-cycle`'s fix (validate.ts:5089), the who-checks/not-the-store sentence to `context-invariant-is-checked`'s why (validate.ts:5025), the schema-plus-`identifies` route to `external-is-boundary`'s aggregate message and catalogue fix (validate.ts:4599-4620,5401), the kernel-context route to `cross-context-relation` and `specialisation-in-boundary`'s fix text (validate.ts:4876,4929), the projection clause to `consumption-by-reactor`'s message (validate.ts:3219), and the policy-or-process route to `invariant-in-context`'s message (validate.ts:1557). Updated the five pinned strings in `packages/core/src/validate.test.ts` to match; `npx vitest run packages/core/src/validate.test.ts` — 412 passed.
- **developer** (2026-09-06T14:20:00.000Z): `packages/core/src/schema.ts`: dropped "synchronous because the operation is" from `PolicySchema.on` (schema.ts:135), added the deadline sentence to `ProcessSchema`'s description (schema.ts:174), and added the published-kind sentence to `DataSchemaSchema`'s description (schema.ts:70-73). Rebuilt core (`npm run -w packages/core build`) and regenerated the skill bundle (`npm run generate` in packages/skill) so `model-reference.md` and `validation-rules.md` follow.
- **developer** (2026-09-06T14:20:00.000Z): Docs: `apps/docs/docs/3-core/3-tactical-design.md` gains the "What the model leaves out on purpose" section (3-tactical-design.md:15-35), the value-object/schemas/events/processes/context-invariant sentences the card names, and a new §Read models (3-tactical-design.md:319-327). `apps/docs/docs/3-core/2-strategic-design.md` §Context flags and the shared-kernel paragraph (2-strategic-design.md:32-48,74-86). `apps/docs/docs/3-core/index.md` Domain Hierarchy tree refreshed with Process/deadlines/context invariants/external/bigBallOfMud/returns/rejects/reasons and a decisions/ pointer (index.md:38-63). `npx docusaurus build` (apps/docs) succeeded; `npx vitest run` (apps/docs) — 23 passed, including `validation-rules.doc.test.ts`.
- **developer** (2026-09-06T14:20:00.000Z): Skill: `packages/skill/skill/references/interview-playbook.md` Phase F gains the "who asks it to do that" follow-up (interview-playbook.md:120-124); `translation-table.md` gains the "the clerk does X" and "we share Product with them" rows; `SKILL.md` gains the preferences-not-laws line pointing at decision 15 (SKILL.md:83-87). `npm run generate && npm run build` in packages/skill; `npx vitest run` — 26 passed.
- **developer** (2026-09-06T14:20:00.000Z): Ran `bash scripts/verify-all.sh` twice (first run failed only because `packages/skill` had never been built in this fresh worktree, so `verify-esm-builds.mjs` could not find `packages/skill/dist/index.mjs`; built it and reran). Second run green end to end — package test counts: core 723, graphviz 28, doc 31, skill 26, northbank 3, petstore 23, rivermart 6, streamline 3, models/_shared 9, pages 764 + svelte-check, apps/docs 23, apps/ods-vscode 15, pages e2e full suite 297, ESM builds ok. Each model's own validation diagnostics matched the pre-card-120 baseline exactly, in count and rule id: NorthBank 3 (separate-ways, consumable-kind, context-serves-subdomain), Petstore 0, RiverMart 2 (aggregate-root, cross-aggregate-reference), StreamLine 4 (internal-consumable×2, schema-context, policy-complete) — only message text moved, as the card allows.
