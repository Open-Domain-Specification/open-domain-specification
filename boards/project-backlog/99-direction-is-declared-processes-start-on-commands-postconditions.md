---
column: review
labels: [backend, ddd, breaking]
priority: high
agent: senior-developer-deep
live: false
clean-code-swept: true
updatedAt: 2026-09-09T16:20:00.000Z
---
# Direction is what the author declared; a process may start on a command and end on a completion; a postcondition; preconditions follow composition

Codex review run 6, issues 2, 6, 7 and 8, each reproduced. Card 98 taught `schema-context` and `relationship-roles-backed` that upstream is who dictates the language, but `relationship-declared` still demands a relationship pointing from provider to consumer and `role-coherence` still asks the provider for an upstream role, so NorthBank had to declare a reverse relationship and call CardCo a conformist. A process may only start on an event, though a command starts a saga, and a payload-free call has no completion to wait on. A guarantee about an answer, every returned itinerary meets the requested deadline, can only be filed as a precondition. And a precondition may name a request field but not a field of a schema the request composes.

## Checklist

- [x] `relationship-declared` is satisfied by a declared relationship joining the two contexts in either direction; the direction is the author's strategic claim, not the call's; `role-coherence` reads roles by the declared direction, asking the declared upstream for an upstream role and the declared downstream for a downstream one, whichever side provides the consumable; NorthBank's reverse CardCo relationship and its "conformist" claim come out; decision 03 noted by the lead
- [x] `ProcessSchema.starts` may name an operation of the process's own context, the command that creates an instance; the reaction walk steps from that operation to the process; `process-starts` accepts it; decision 23 amended by the lead
- [x] An operation without `returns` has a completion a reactor may wait on: `op.completed()` names an answer with no shape, ref `<operation ref>/completed`; `consumable-kind` accepts it; the flow map labels it "completes"; decision 13 amended by the lead
- [x] `InvariantSchema.postcondition?: boolean`: a postcondition holds of an operation's answer and may constrain attributes of the schema the guarded operation returns or rejects with; `precondition` and `postcondition` are exclusive; `postcondition-names-operation` (error); the invariant page says which; decision 19 amended by the lead
- [x] `inGuardedRequest` follows schema composition: an attribute of any schema reachable from the guarded operation's request, return or rejection through `attribute.schema` is admissible for a precondition or postcondition; test with a request composing lines whose amount is constrained
- [x] `bash scripts/verify-all.sh` green; diagnostics per model unchanged or explained; the review's probes rerun

## Gates

- [x] clean-code-swept — swept the diff (32 files, ~1170 lines added) against the eight principles. Two things it changed. The three pages that draw a reaction's triggers each had their own copy of "what does an answer link to", and my first cut added a third copy of "which kind of answer is this" beside them; both are now `answerRef` and `answerKeyword` in `molecules/element-kind.ts`, which all three already import for `kindOf`, so a completion cannot come out as a schema link on one page and a call link on another. And `Consumable.answerFor` keyed its cache by re-deriving the key from `(schema, rejection)`, which cannot express a third kind of answer; the key is now the caller's, one string per kind, so `returned`, `rejected` and `completed` each say what they are asking for. Three things it looked at and left. `inGuardedRequest` is now a poor name — it has reached the answer as well as the request since card 97, and now reaches composition — but the card names the function and renaming it churns four call sites and two doc comments for no reader; flagged below instead. `schemaAttributeRefusal` grew a third branch and stays a flat if-chain, which reads better than a table of three. And `Answer.description` falls back to the operation's description for a completion, which is the only honest thing a shapeless answer has to say; a blank cell in the process page's Description column was the alternative (senior-developer-deep, 2026-09-09T16:05:00Z)
- [x] verify-all — `bash scripts/verify-all.sh` exit 0: core 590, graphviz 27, doc 31, skill 26, northbank 3, petstore 20, rivermart 3, streamline 3, models/_shared 9, pages 756, apps/docs 23, apps/ods-vscode 15, pages e2e passed, petstore schema matches core dist (senior-developer-deep, 2026-09-09T16:15:00Z)

## Comments

- **the lead** (2026-09-09T13:00:00.000Z): senior-developer-deep; `feat!`.
- **senior-developer-deep** (2026-09-09T14:00:00.000Z): Picked it up; worktree reset to origin/develop at acd7ff9 ("card 99 dispatched"), installed. Read the card, the 2026-09-09 amendments and notes on decisions 03, 13, 19 and 23, and decisions 14, 17, 21, 25, 27 for context. All four items reproduced against that baseline before any change: NorthBank's truthful `CardCo upstream of Cards` relationship alone left `relationship-declared` warning and `role-coherence` asking the provider for an upstream role and the processor for a downstream one; an operation in a process's `starts` was a `consumable-kind` error ("reacts to X, which is an operation, not an event"); `op.returned()` on a returns-less operation threw at the DSL and there was no other answer to name; and a precondition naming a field of a schema the request composes was refused by `invariant-in-aggregate`.
- **senior-developer-deep** (2026-09-09T14:30:00.000Z): Direction, and the one thing the amendment does not spell out. `relationshipJoins` no longer reads the arrow at all: any relationship but separate-ways joins the pair, because the arrow is the author's claim about who dictates the model and not a statement about which way the traffic runs. Its diagnostic key drops the direction with it — one relationship either way now clears every crossing between a pair, and the rule's own comment already said one warning is what a reader can act on, so keeping two would have been two warnings one edit clears. `role-coherence` reads the declared direction first through a new `declaredUpstream`: where the provider is the declared upstream, or the pair declares nothing and the implied edge reads the provider as upstream, it asks exactly what it asked before; where the **consumer** is the declared upstream it asks nothing of either end. That last part is mine and is worth the lead's eye. The card says the rule should ask "the declared upstream for an upstream role and the declared downstream for a downstream one, whichever side provides the consumable" — but a `Consumable.pattern` is typed `UpstreamRole` and a `Consumption.pattern` `DownstreamRole`, so in the reversed case neither field can hold the role its side's position implies. Widening those two fields is an API change and not on this card, so the narrowest truthful reading is: the roles live on the relationship there, and `relationship-roles-backed` is what checks them — which is exactly what it already does for NorthBank's `published-language` and `anti-corruption-layer`. Flagged in DECISIONS NEEDED.
- **senior-developer-deep** (2026-09-09T14:50:00.000Z): A command starts a saga. `starts` still holds consumables, so the reaction walk and the flow map needed no code at all — a starting operation is a listener key like an event, and `chain.stepsFrom(operation)` already returns the process; there are tests for both so it stays true. What did need care is that a process's `starts` feeds four subscription rules through `subscribedEvents`, and none of them means anything about a command: nobody subscribes to a command, it is issued. So `startingCommands(reactor)` names them and `subscribedEvents` excludes them, which is what lets `consumable-kind` stop reporting the kind error and keeps `separate-ways`, `internal-consumable`, `subscription-consumed` and `partnership-backed` from inventing a crossing. Whose operation it has to be is `process-in-context`'s, not `process-starts`': that rule is already the one about what a process may name outside its own context, and it now says "starts on X, an operation of Y; the command that creates an instance is this context's own, though an event that starts one may cross". `process-starts` only widened its sentence.
- **senior-developer-deep** (2026-09-09T15:10:00.000Z): The completion. `Answer.schema` is now optional and a completion is the answer with none: ref `<operation ref>/completed`, name "completes", origin "X completes", `many` false, and `declared` true only for an operation with no `returns` — so waiting on the completion of an operation that answers with a shape is a `consumable-kind` error telling the author to wait for that shape, and waiting on an event's completion is one saying nobody calls an event. Three messages rather than one, in `undeclaredAnswer`, because they are three different mistakes. `Consumable.completed()` does not throw where `returned()` does, for the reason `rejected()` does not: a model with a mistake in it still has to load and be validated, and a JSON document can name the ref. `Consumable.answers` yields it, which is what makes the walk step from the operation to whoever waited, so the flow map draws `Activate Line -> Provisioning [completes]` with no node in between and `[completes (ends)]` where the process ends on it — the card's "the flow map labels it completes" comes out of `Answer.name`, so core, graphviz and the pages graph all say it without three copies of the word.
- **senior-developer-deep** (2026-09-09T15:30:00.000Z): The postcondition, and where the exclusivity went. `InvariantSchema.postcondition?: boolean` beside `precondition`; the invariant page gains a fourth `KIND` entry, so the header keyword reads "postcondition" and the Guarded-by lead says the operation is what holds it, since the answer is saved nowhere. Reach: `inGuardedRequest` now asks `guardedSchemas(invariant)`, which is the guarded operations' `schema`, `returns` and `rejects` for a precondition and `returns` and `rejects` only for a postcondition — a postcondition is a guarantee about the answer, and what the caller sent is not what the operation promises — closed under `attribute.schema` by `composedSchemas`, cycle-safe. `schemaAttributeRefusal` says which of the three reasons a schema attribute was refused. The exclusivity is reported by `postcondition-names-operation` rather than by a rule of its own, and that is a judgement I want confirmed: the card names one new rule, and an invariant marked both is precisely a postcondition that is not one, so the rule's summary reads "names at least one operation it guards, and is not also marked a precondition". A separate `invariant-one-kind` would be the house style (`attribute-one-shape` is the precedent) but is a second rule the card does not ask for. Flagged in DECISIONS NEEDED.
- **senior-developer-deep** (2026-09-09T15:50:00.000Z): NorthBank, and nothing else in the models. `cardsBC.upstreamOf(cardCoBC, ...)` — the second relationship card 98 wrote so that the arrow pointed the way the call ran — is gone, and with it the `pattern: "conformist"` on CardCo's consumption of `AuthoriseCard`. Both said the opposite of the truth: a conformist takes the other side's model as it stands, and CardCo is the side whose model is taken. `AuthoriseCard` keeps `pattern: "open-host-service"`, because who provides is not who is upstream and the bank really does offer it as an open host; the comments and DISCOVERY.md's new card-99 revision say so. Nothing else moved: the four deliberate diagnostics are the same four, and the relationship count drops from 14 to 13. I did not go looking for invariants in the reference models that are really postconditions or for commands that really start a saga — that is a re-reading of three DISCOVERY.md files and is not on this card; noted as out of scope.
- **senior-developer-deep** (2026-09-09T16:18:00.000Z): `bash scripts/verify-all.sh` green end to end, exit 0. Summary: core 590 tests passed, graphviz 27, doc 31, skill 26, northbank 3, schema comparison (petstore vs core dist) match, petstore 20, rivermart 3, streamline 3, models/_shared 9, pages 756, apps/docs 23, apps/ods-vscode 15, pages e2e (full suite) passed. `npx tsc --noEmit` clean in core, graphviz, doc, skill, pages, all four models, models/_shared, apps/docs and apps/ods-vscode; `npm run check` in pages clean, 939 files, 0 errors, 0 warnings; biome clean on every file I touched. Diagnostics per model, all unchanged and every one a DELIBERATE: `petstore: 0`. `rivermart: 2` — `[error] aggregate-root` on Wishlist, `[error] cross-aggregate-reference` on Cart. `northbank: 4` — `[warning] relationship-declared` and `[error] separate-ways` on Branch & Contact Centre consuming Decide, `[error] consumable-kind` on Escalate arrears, `[warning] context-serves-subdomain` on Identity & Access. The separate-ways pair still warns `relationship-declared`, which is the point of the one exclusion the widened rule keeps. `streamline: 4` — two `[error] internal-consumable`, `[error] schema-context`, `[warning] policy-complete`.
- **senior-developer-deep** (2026-09-09T16:20:00.000Z): The four probes, rerun against this build. Each was reproduced failing on the card-98 baseline first (see the 14:00 comment); each is now clean, with nothing else reported.

```
## probe 1 — CardCo calls Cards in CardCo's format; one relationship, CardCo upstream,
##           published-language / anti-corruption-layer; no downstream role on the consumption
  (clean: no relationship-declared, no role-coherence)

## probe 2 — a process starting on Open Claim, an operation of its own context
  (clean: 0 diagnostics)

## probe 3 — a process waiting on Activate Line's completion; Activate Line returns nothing
  (clean: 0 diagnostics)

## probe 4 — a precondition constraining Quote Line.amount, reached from Quote Request
##           through the composed `lines` attribute
  (clean: 0 diagnostics)
```

The same four before the change, on the card-98 build: probe 1 gave `[warning] relationship-declared` plus two `[warning] role-coherence`; probe 2 gave `[error] consumable-kind — Process "Claim Handling" reacts to "Open Claim", which is an operation, not an event`; probe 3 could not be written at all, since `returned()` threw and no other answer existed; probe 4 gave `[error] invariant-in-aggregate — ... constrains "Quote Line.amount", which is an attribute of schema "Quote Line", which no operation this precondition guards takes, returns or rejects with`.
