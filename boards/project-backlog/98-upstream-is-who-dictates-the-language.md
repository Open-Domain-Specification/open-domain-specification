---
column: done
labels: [backend, ddd, breaking]
priority: high
agent: senior-dev-deep
live: false
clean-code-swept: true
updatedAt: 2026-09-09T09:40:00.000Z
---
# Upstream is who dictates the language; a subscriber is a reactor; an aggregate-initiated call has a front

the architect's fifth review, each item probed. Every strategic rule equates upstream with the provider of the consumable, but DDD's upstream is who dictates the model: a card processor, an EDI partner or an HL7 orderer calls us in its own format and we translate behind an anti-corruption layer, and that truthful form is a `schema-context` error today, so NorthBank inverts the call into an event and nothing consumes `AuthoriseCard`. `subscription-backed` lets an operation stand in as the subscriber of a foreign event, and eight consumptions in the models use it, so the reaction walk goes dark there. The single-operation caller inference draws RiverMart's `CaseAPI` making a call its comment says the aggregate makes. Plus the corrections below.

## Checklist

- [x] `schema-context` lets a consumable carry a foreign schema when the carrier's context is downstream of the schema's owner with `conformist` **or** `anti-corruption-layer` in its roles (the ACL translates the caller's language at the boundary); `relationship-roles-backed` backs an upstream role by a schema the downstream carries in any of `schema`, `returns` or `rejects`, as well as by a schema-carrying consumption (this also fixes the returns-only published language); NorthBank's Cards: `AuthoriseCard` typed by CardCo's schema, CardCo upstream of Cards with an anti-corruption layer, CardCo's feed consuming `AuthoriseCard` with `by`, the inverted `AuthorisationRequested` event out; DISCOVERY.md says so; decision 03 amended by the lead
- [x] On a consumption of an event, `by` names policies and processes only (`consumption-by-reactor`, error), and `subscription-backed` clears an event only when a reactor of the consumer's context listens to it; the eight operation-as-subscriber consumptions in the models become a policy issuing a local operation the guarded operation reads, or come out where the interview names none; NorthBank's `Decide` comment about a two-way partnership goes
- [x] RiverMart's `CaseAPI` gains the front `ResolveCase` that makes the return request, with `by` naming it, per decision 17; the comment saying the aggregate makes the call is rewritten to decision 17's form; decision 21 noted by the lead: there is no opt-out, the caller is named
- [x] A kernel context's aggregate operations may be consumed by the contexts that share the kernel: `aggregate-not-public` exempts a consumption by a sharer of a shared-kernel context; test with the review's P4; decision 16 amended by the lead
- [x] `DeadlineSchema.from?: { $ref }`: the trigger the interval counts from, one of the process's own `starts` or `on` entries; absent means from the start; the flow map labels the loop with `after` and, when set, `from`; decision 23 amended by the lead
- [x] `identifies` may name a `bigBallOfMud` context as it may an external one; `identifies-entity` accepts it
- [x] `relationship-duplicate` also refuses `upstream-downstream` and `customer-supplier` between one pair in one direction (one directed relationship per direction per pair); `external-is-boundary` refuses `external` together with `bigBallOfMud`
- [x] Decisions 03, 08, 13, 15, 16, 17, 21, 23 amended by the lead; you confirm the mechanics match
- [x] `bash scripts/verify-all.sh` green; diagnostics per model unchanged or explained; the review's probes P1, P3, P4, P5, P6, P10 rerun and reported

## Gates

- [x] clean-code-swept — swept the diff (34 files, ~1700 lines) against the eight principles; nothing above 0.5 left. Two things the sweep changed. `flow-map.ts` formatted "how long, and from what" twice, once for the edge key and once for the label, so the phrase is now one `waitOf(edge)` both read — the key and the label can no longer drift, which is the whole reason two limits of one instance draw two loops. And the first cut of the ACL exception was a context-level predicate, `mayCarrySchemaFrom(workspace, carrier, owner)`, which excused a foreign shape on everything the downstream published: StreamLine's deliberate `schema-context` error went quiet under it, which is how I caught it. It is now `translatesForCaller(consumable, caller)` — the shape is excused on the one consumable the caller itself consumes — and there is a test for the fact it published to everyone. Three things it looked at and left: `schemaContext` still emits an `internal-consumable` warning inside itself (pre-existing, and moving it is a rule's-worth of churn for no reader); "after X from Y" is phrased by hand in core, the doc generator and the process page, which is the triplication card 95 accepted for "after X" and which no single helper fits, since two take a `Deadline` and one takes edge strings; and `packages/doc/src/service.md.ts` has two unused imports biome warns about, which are outside this diff (senior-dev-deep, 2026-09-09T09:20:00Z)
- [x] verify-all — `bash scripts/verify-all.sh` exit 0: core 571, graphviz 27, doc 31, skill 26, northbank 3, petstore 20, rivermart 3, streamline 3, models/_shared 9, pages 754, apps/docs 23, apps/ods-vscode 15, pages e2e passed, petstore schema matches core dist (senior-dev-deep, 2026-09-09T09:30:00Z)

## Comments

- **lead** (2026-09-09T06:40:00.000Z): senior-dev-deep, justified by the reach: the strategic rules' central assumption, two rule corrections, a deadline field, and surgery in two models. After card 97 lands (the lead will say); `feat!`.
- **senior-dev-deep** (2026-09-09T07:00:00.000Z): Picked it up; worktree reset to origin/develop at c536e0b ("card 97 done, 98 dispatched"), installed. Ran the reviewer's probe first, against the card-97 build and adapted only for this worktree's paths, so everything below is measured against that baseline: P1 warned `relationship-roles-backed` (published language backed only by a `returns` shape), P3 with an anti-corruption layer gave `schema-context` **and** an unbacked-role warning while the conformist variant was clean, P4 gave two `aggregate-not-public` errors, P5 and P6 were clean where they should not have been, P10 refused an `identifies` naming a big ball of mud. The census found **ten** consumptions of a foreign event with an operation in `by`, not eight: six in NorthBank, one in RiverMart, three in StreamLine. The two the card's count leaves out are the Scheme Gateway's, where `SubmitToScheme` was named as the subscriber of the scheme's two response events (card 95 wrote them that way); they are the same defect and I have treated them as such rather than leaving two errors behind. All ten are listed in the model comment below.
- **senior-dev-deep** (2026-09-09T07:30:00.000Z): The rules, and where the mechanics needed a decision the amendments do not spell out. (1) `schema-context`: decision 03's amendment says a downstream may carry the upstream's schema "behind an anti-corruption layer as well as as a conformist". Taken at the context level that excuses the shape on everything the downstream publishes, and it silently cleared StreamLine's deliberate `schema-context` error — `PlaybackStarted` carrying Catalogue's `TitleRef` — because Playback declares an ACL toward Catalogue. So the exception is narrowed to the boundary the layer actually translates: the consumable has to carry the upstream's shape **and** be consumed by that upstream. NorthBank's `AuthoriseCard` qualifies, a fact published to everyone does not. Attributes keep the old predicate: an attribute is inside the model and past the layer. (2) The same narrowing is what backs the ACL role in `relationship-roles-backed`, and that half is mine, not the amendment's: where the caller is upstream nothing crosses from upstream to downstream at all, so with the rule unchanged NorthBank's truthful Cards/CardCo pair warned that the layer was unbacked. Conformist keeps `borrowsFrom`. (3) The upstream half is the amendment's: `carriesUpstreamRole` now reads `schema`, `returns` or `rejects` on a crossing consumable, and `borrowsFrom` reads `rejects` beside the other two, which together clear P1.
- **senior-dev-deep** (2026-09-09T07:50:00.000Z): The other four rules. `consumption-by-reactor` (error) refuses an operation in the `by` of a consumption of an event, mirroring `consumption-by-operation`: an operation is issued rather than woken, so naming one says a thing that does nothing when the fact arrives is what takes it in. `subscription-backed` no longer counts `by` as backing, so only a reactor of the consumer's context clears it; the two rules are one statement from two sides. `aggregate-not-public` exempts a consumer that shares a kernel with the aggregate's context — by `sharesKernelWith`, the same predicate `shared-kernel-backed` reads when it counts a kernel call as the sharing, so the two rules can no longer recommend and refuse the same model; the upstream-role half is untouched, because an open host is offered to everyone. `identifies-entity` accepts a `bigBallOfMud` context beside an external one: a legacy account key is the id nobody can trace to an entity, and demanding one invites an invented aggregate. `relationship-duplicate` keys every directed type on the pair and the direction, so an upstream-downstream and a customer-supplier the same way round collide — customer-supplier is a flavour of upstream/downstream, not a second joint — while the two directions and the symmetric types stay as they were. `external-is-boundary` refuses a context marked external and mud at once. Catalogue `summary`, `why` and `fix` rewritten for all seven, the docs site's hand-written rule table updated, and `validation-rules.md` regenerated.
- **senior-dev-deep** (2026-09-09T08:10:00.000Z): `DeadlineSchema.from?: { $ref }`, and no new rule for it. A deadline is created before the process has any triggers, so the anchor is linked in the loader's second pass and `Deadline.countsFrom(trigger)` refuses anything that is not one of the process's own `starts` or `on` entries, and refuses the deadline itself — refused where it is written, exactly as a deadline belonging to another process is (decision 23's fourth amendment set that precedent). The flow map's loop reads `after 30 minutes from PaymentAuthorised`, the process page's Waits-for and Ends tables say the same beside the deadline's name, and the doc generator's trigger cell does too, so the three surfaces agree. No reference model anchors one: RiverMart's authorisation expiry starts when the instance asks for the hold, which is the start of the instance, and giving it an anchor would state something DISCOVERY.md does not. The pages fixture grows a "Timed Process" for the surface instead, in the same spirit as the other edge cases there.
- **senior-dev-deep** (2026-09-09T08:40:00.000Z): The models. NorthBank/Cards: `AuthoriseCard` carries CardCo's `CardCoAuthorisationMessage` and answers in the bank's own shapes; CardCo's feed provides `RequestAuthorisation` (internal — it is what the processor offers its merchants, not us) and consumes `AuthoriseCard` with `by` naming it; two relationships, CardCo upstream with a published language and Cards downstream with an ACL, Cards upstream with an open host that CardCo conforms to; the `AuthorisationRequested` event and its consumption are gone, and so is `CardAuthorisationRequest`, the translated shape nothing at the boundary carried. The chain now runs `RequestAuthorisation → AuthoriseCard → CardAuthorised, GetAvailableBalance, ScoreTransaction`, where before `chain.after(AuthorisationRequested)` was `[]` and `AuthoriseCard.consumptions` was empty. The ten subscriptions: NorthBank's `CustomerVerified` gains "Note a verified customer" → `RecordVerifiedCustomer`, which `OpenAccount` reads; `CardAuthorised` gains "File a card authorisation" → `RecordCardAuthorisation`, which the scorer reads; the scheme's two response events gain "Match the scheme's response" → `RecordSchemeResponse`, which the waiting submission is answered from; `ApplicationSubmitted` comes out, with the two-way-partnership comment. RiverMart: `SellerActivated` gains "Keep the seller list in step" → `RecordActiveSeller`, the same shape Offers already uses for the catalogue's SKUs. StreamLine: `MasterDelivered` gains "Note a delivered master" (the studio lead's "the catalogue and the encoding pipeline both react", finally written down), `DeviceCertified` gains "Keep the certified device list", `BookmarkUpdated` gains "Freshen continue watching". RiverMart's `CaseAPI` gains the front `ResolveCase`, which consumes the aggregate's `ResolveCase` and Returns' `RequestReturn`, both named in `by`; the comment saying the aggregate makes the call is rewritten. All three DISCOVERY.md files carry a card-98 revision saying it.
- **senior-dev-deep** (2026-09-09T09:35:00.000Z): `bash scripts/verify-all.sh` green end to end, exit 0. Summary: core 571 tests passed, graphviz 27, doc 31, skill 26, northbank 3, schema comparison (petstore vs core dist) match, petstore 20, rivermart 3, streamline 3, models/_shared 9, pages 754, apps/docs 23, apps/ods-vscode 15, pages e2e (full suite) passed. `npx tsc --noEmit` clean in core, graphviz, doc, skill, pages, all four models, models/_shared, apps/docs and apps/ods-vscode; `npm run check` in pages clean, 939 files, 0 errors, 0 warnings; biome clean on every file I touched. Diagnostics per model: `Swagger Petstore (v3): 0`; `RiverMart: 2` — `[error] aggregate-root` on `cart_&_checkout/aggregates/wishlist`, `[error] cross-aggregate-reference` on `cart_&_checkout/aggregates/cart/entities/cart`; `NorthBank: 4` — `[warning] relationship-declared` and `[error] separate-ways` on `branch_&_contact_centre/services/channels_app`, `[error] consumable-kind` on `lending/policies/escalate_arrears`, `[warning] context-serves-subdomain` on `identity_&_access`. All three unchanged, every one a DELIBERATE. `StreamLine: 4`, one more than before and the only change: `[error] internal-consumable` on `recommendations/services/recommendations_api` (as before) and now also on the policy `recommendations/policies/freshen_continue_watching`, plus `[error] schema-context` on `playback/.../playback_started` and `[warning] policy-complete` on `devices/policies/recertify_on_sdk_release`. The unagreed bookmark dependency is now reported at both ends, because the subscription has to name the reaction that takes it in and the reaction reads Playback's internal event; both statements are true and section 7 of StreamLine's DISCOVERY.md says so.
- **senior-dev-deep** (2026-09-09T09:38:00.000Z): The six probes, rerun against this build. Every one flips and nothing else moved.

```
## P1 published-language backed only by a returns shape
  (clean)

## P3 inbound call typed by the caller's schema, Cards declares anti-corruption-layer
  (clean)

## P3 inbound call typed by the caller's schema, Cards declares conformist
  (clean)

## P4 kernel aggregate operation consumed by two sharers (decision 16 note)
  (clean)

## P5 two directed relationships of different type, same direction
  [error] relationship-duplicate: "A" and "B" declare both a upstream-downstream and a
  customer-supplier relationship with "A" upstream; customer-supplier is a flavour of
  upstream/downstream, so one direction between one pair carries one directed
  relationship, and two of them disagree about how the pair stands

## P6 external + bigBallOfMud
  [error] external-is-boundary: Bounded context "X" is marked both external and a big ball
  of mud; a mud context is the enterprise's own, however unreadable, and an external one is
  somebody else's system, so a context is one or the other

## P10 identifies naming a bigBallOfMud context
  (clean)

## census: consumptions of a foreign event with an operation in `by`
  northbank 0, rivermart 0, streamline 0, petstore 0 (was 6, 1, 3, 0)

## northbank chains
  chain after CardCo.RequestAuthorisation -> ["AuthoriseCard"]
  consumers of Cards.AuthoriseCard -> ["CardCo Authorisation Feed"]
  chain after Cards.AuthoriseCard -> ["CardAuthorised","GetAvailableBalance","ScoreTransaction"]

## rivermart chains
  chain after CaseAPI.ResolveCase -> ["ResolveCase","RequestReturn"]
  chain after CaseAPI.OpenCase -> ["CaseOpened","GetOrder","GetCustomer"]
```

- **senior-dev-deep** (2026-09-09T09:40:00.000Z): QUESTION for lead, two of them, neither blocking — the work is done and green either way. (1) Decision 03's amendment says the ACL lets a downstream "carry the upstream's schema", and I have implemented it narrower: only on a consumable the upstream itself consumes, because the wide reading cleared a deliberate error in StreamLine. If you want the wide reading instead, StreamLine's `PlaybackStarted` stops being a `schema-context` case and that model needs another one. (2) The same amendment says nothing about what backs the **downstream** ACL role, and NorthBank's truthful Cards/CardCo pair warns without it, so I made the caller-facing shape back it. Both are in decision 03's territory rather than mine; say the word and I will write them into the record, or change them.
