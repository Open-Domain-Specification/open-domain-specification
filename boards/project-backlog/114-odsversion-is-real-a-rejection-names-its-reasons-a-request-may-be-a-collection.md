---
column: review
labels: [backend, docs]
priority: medium
agent: senior-developer
live: false
clean-code-swept: true
updatedAt: 2026-09-06T12:08:08.000Z
---
# `odsVersion` is real; a rejection names its reasons; a request may be a collection; the `by` comment tells the truth

The architect's ninth round found one thing broken and three costs the model's own reasoning cuts against. Five decisions say `odsVersion` bumps on a breaking change; it has been `1.0.0` since the first commit and nothing reads it, so a file written against an older metamodel fails as `unresolved-ref` or rule errors rather than as a version mismatch. An answer is keyed by shape, so an acquirer's one error shape with a `code` field gives a process one rejection branch whatever the code says, and the only alternative, one schema per reason, misstates the contract the way decision 13 refused for lists. A `returns` may be a collection and a request may not, though decision 13's argument is the same for both; petstore leaves `createUsersWithList` out for that reason. And the `by` doc comment on `ConsumptionSchema` says "optional detail, not a call graph" while the rule asks for it wherever the consumer has two operations. Decisions 13, 25 and 29 are amended. Runs after card 113, which touches the same files.

## Checklist

- [x] `ODS_VERSION` constant in core, set to `2.0.0`; `toSchema` writes it; the loader reports `ods-version` (error) when the file's `odsVersion` is missing or its major differs, with fix text saying to regenerate from the DSL, and still loads what it can (decision 29); every model's `.ods` output regenerated; the JSON schema's `odsVersion` description says what the number means and who bumps it; docs and skill references follow
- [x] `rejects` entries may carry `reasons?: string[]`, the enumerated outcomes of that shape as the contract states them (ISO 8583 response codes, a provider's decline codes); an answer `<op>/rejects/<schema>/<reason>` exists for each, alongside the shape-level answer that hears them all; DSL `op.rejected(schema, reason?)`; the reaction walk, `answers-by-origin`, `consumable-kind` and the flow map treat a reason answer as a rejection of that operation; the JSON round trip keeps it
- [x] `ConsumableSchema.schema` becomes `{ $ref, many? }` like `returns`; DSL `schema: { of, many }` or the equivalent already used for `returns`; pages and docs that print a payload say "list of" the way they do for answers; petstore models `createUsersWithList` with a `many` request
- [x] `ConsumptionSchema.by` doc comment says what the rule asks: absent means the whole consumer, which is fine for a consumer with one operation, and a consumer with two or more names which of them makes the call so the reaction walk has its causal link (decision 21)
- [x] RiverMart's external payment provider states the decline outcomes of `hold_funds` as reasons and the payments process branches on one of them; `apps/docs/docs/3-core/4-validation.md` rows; tests for each item
- [x] `bash scripts/verify-all.sh` green

## Gates

- [x] clean-code-swept — the diff read back end to end: no dead field left behind (`WorkspaceAttributes.odsVersion` removed rather than ignored, `Consumable.rejects` kept as a derived getter so no reader duplicates it), every new field carries the reasoning for it in a doc comment, and `biome check` clean on every file touched (senior-developer, 2026-09-06T12:08:08.000Z)

## Comments

- **senior-developer** (2026-09-06T12:08:08.000Z): `ODS_VERSION` is a constant of the library, `packages/core/src/schema.ts:765-776`, and no longer something an author sets: `WorkspaceAttributes` drops `odsVersion` and `Workspace.odsVersion` is the constant (`packages/core/src/workspace.ts:47-55,148-154`), so `toSchema` always writes the version this core reads. The loader compares the major and records the mismatch on the workspace (`packages/core/src/workspace-from-schema.ts:1002-1017`, `packages/core/src/workspace.ts:174-182`); `ods-version` (error) reports it at `#/odsVersion`, which is the key itself in the file, so the extension squiggles the number without a change of its own (`packages/core/src/validate.ts:4515-4548`, catalogue at `packages/core/src/validate.ts:4610-4620`). It still loads what it can, and a test pins that the mismatched file's other diagnostics are exactly the clean file's (`packages/core/src/workspace-from-schema.test.ts:405-471`). Every model's `.ods` output regenerated; NorthBank's and StreamLine's diffs are the version line alone.
- **senior-developer** (2026-09-06T12:08:08.000Z): A refusal now carries the outcomes the contract enumerates. `rejects` entries are `{ $ref, reasons? }` (`packages/core/src/schema.ts:396-410`), the model keeps them as `Rejection`s with `rejects` a derived getter so every existing reader is untouched (`packages/core/src/workspace.ts:1363-1377,1489-1500`), and `op.rejected(schema, reason?)` makes the answer (`packages/core/src/workspace.ts:1518-1536`). A reason answer is a ref of its own, `<op>/rejects/<schema>/<reason>`, resolved in the one place ref shapes are read (`packages/core/src/workspace.ts:503-540,700-720`) and declared only where the contract enumerates it, so an unknown reason is an `unresolved-ref` from a file and a `consumable-kind` error from the DSL. The walk and the flow map needed no new wiring — an answer already names the operation it comes back from — and the reason is part of the answer's name so two outcomes of one shape draw two edges (`packages/core/src/workspace.ts:1727-1746`). Covered in `packages/core/src/workspace.test.ts:649-813`, `packages/core/src/derived-maps.test.ts:1110-1265` and `packages/core/src/workspace-from-schema.test.ts:473-538`.
- **senior-developer** (2026-09-06T12:08:08.000Z): A request may be a list: `ConsumableSchema.schema` is `{ $ref, many? }` (`packages/core/src/schema.ts:370-381`) and the DSL takes `schema: { of, many }` (`packages/core/src/workspace.ts:1377-1384`). Docs print it "many X" beside the answer's (`packages/doc/src/consumables.md.ts:14-27`) and the pages say "Payload, many" where they say "Returns many" (`packages/pages/src/lib/molecules/ConsumableSubsection.svelte:36`, `packages/pages/src/lib/templates/ConsumablePage.svelte:138,154-164`). Petstore models `createUsersWithList` on that (`models/petstore/src/workspace.ts:1265-1280`). The `by` comment now says what `consumption-by-required` asks (`packages/core/src/schema.ts:426-440`), and so do the docs (`apps/docs/docs/3-core/3-tactical-design.md:200-212`).
- **senior-developer** (2026-09-06T12:08:08.000Z): RiverMart's acquirer publishes `ProviderDecline` with its three decline codes, and Payments branches on the one it can do something about: a `Hold attempt` process that retries on `issuer_unavailable` through a `RetryHold` operation of its own, which is what the consumption's `by` names so the answer routes to it (`models/rivermart/src/workspace.ts:1500-1512,1535-1580,1618-1632`). RiverMart's diagnostics are the two deliberate ones and nothing else; petstore still validates clean. `bash scripts/verify-all.sh` green end to end: core 710, graphviz 28, doc 31, skill 26, northbank 3, petstore 22, rivermart 6, streamline 3, _shared 9, pages 764, docs 23, extension 15, e2e 297, schema comparison match.
- **senior-developer** (2026-09-06T12:08:08.000Z): One thing for the lead. The DSL now spells the two collection fields differently — `schema: { of, many }` and `returns: { schema, many }` — because the card sanctioned either for `schema` and aligning `returns` was not in the brief. The wire format is the same shape for both (`{ $ref, many }`); it is only the DSL that reads two ways. Worth a follow-up card to settle on one spelling, which is a five-call-site change.
