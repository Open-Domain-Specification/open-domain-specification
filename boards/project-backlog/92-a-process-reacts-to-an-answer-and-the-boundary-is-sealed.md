---
column: done
labels: [backend, ddd, breaking]
priority: high
agent: senior-dev-deep
live: false
clean-code-swept: true
updatedAt: 2026-09-08T22:35:00.000Z
---
# A process reacts to an answer; the boundary is sealed for value objects; decision 17 is enforced

the architect's third review, each item probed. Decision 16 says a value object borrowed across contexts is allowed only through a shared kernel or a conformist and `schema-context` never reads `attribute.valueobject`, so an insurer's Claims typing a reserve by Policy Admin's Money with no relationship validates clean. A process can wait only on events, so RiverMart publishes `PaymentDeclined` as an event the Checkout process waits for, against decision 25's own example, and NorthBank describes a synchronous verdict and models it as two published events. A `by` naming a policy or process on an operation consumption is accepted and silently breaks the reaction walk, which petstore relies on; domain services consume foreign consumables in two models against decision 17. Plus the smaller items below.

## Checklist

- [x] `valueobject-context` (error): an attribute's `valueobject` in another context is allowed only through a shared kernel with that context or a conformist relationship toward it, the same predicate `schema-context` uses; `relationship-declared` counts a value object borrowing as a crossing; test with the review's probe (a root typed by another context's Money with no relationship) which must error
- [x] A process's `on` and `ends`, and a policy's `on`, may name a schema that an operation of the same context's consumptions returns or rejects with, meaning "when that answer comes back"; `consumable-kind` accepts it; the reaction walk steps operation to its returns and rejects to the reactor; the flow map draws the answer as an edge from the operation; decision 23 amended by the lead; RiverMart's `PaymentDeclined` becomes a rejection of `AuthorisePayment` the Checkout process waits on, and NorthBank's `ScoreTransaction` returns a verdict schema its process reacts to, both with DISCOVERY.md lines
- [x] `consumption-by-operation` (error): `by` on a consumption of an operation names operations only; a reactor issues a local operation that makes the call (decision 17); petstore's `orderApp.consumes(getPetSummaryOp, { by: [orderFulfilment] })` gains the local operation and its comment is rewritten to say why the step is real
- [x] `domain-service-consumes-inside` (error): a domain service consumes only its own context's consumables; NorthBank's `KycScreening` and StreamLine's `Ranker` move their foreign consumptions to their application services with `by`
- [x] `subscription-backed` (warning): a consumed event that no policy or process of the consumer's context reacts to and that names no `by` is a claim with nothing under it; the eleven in the models gain their reactor or come out
- [x] `invariant-in-aggregate`'s why-text and the invariant page distinguish a guard: an invariant that constrains an operation is a precondition checked when that operation runs, not a rule true again after every save; decision 27 noted by the lead
- [x] A value object's relations target value objects only (`uses`); a relation from a value object to an entity is refused by `value-object-shape` with the reason; `cross-aggregate-reference` no longer has the gap
- [x] `entity-identity` skips a big ball of mud like the other three rules
- [x] `returns` of a list is an answer schema whose attribute holds the list, as RiverMart's `SearchResults` does; petstore's `FindPetsByStatus` returns a `Pets` schema; decision 13 noted by the lead
- [x] RiverMart demonstrates a deadline: Payments' application service `ExpireAuthorisations`, called by a scheduler, raises `AuthorisationExpired` after `Authorisation.expiresAt`, and the Checkout process ends on it; DISCOVERY.md says so
- [x] `InvariantSchema` doc comment says an aggregate invariant constraining a value object means that aggregate's instances of it; RiverMart's `PaymentIntent.orderId` becomes optional per its own comment; Last Mile's relationship description stops naming the retired role-coherence warning
- [x] Decisions 13, 15, 16, 17, 23, 25, 27 amended by the lead; you confirm the mechanics match
- [x] `bash scripts/verify-all.sh` green; diagnostics per model unchanged or explained; the review's probes P2, P3a, P4, P5, P6 rerun and reported

## Gates

- [x] clean-code-swept — swept the diff against the eight principles; one finding above 0.5 (the reaction chain's answerers were scoped too narrowly for a one-context flow map) fixed rather than tagged, and two names and a dead parameter tidied with it (senior-dev-deep, 2026-09-08T22:15:00Z)
- [x] verify-all — `bash scripts/verify-all.sh` exit 0: core 511, graphviz 26, doc 31, skill 26, northbank 3, petstore 20, rivermart 3, streamline 3, models/_shared 9, pages 749, apps/docs 23, apps/ods-vscode 15, pages e2e passed, petstore schema matches core dist (senior-dev-deep, 2026-09-08T22:20:00Z)

## Comments

- **lead** (2026-09-08T17:10:00.000Z): senior-dev-deep, justified by the reach: a new reaction step, five rules, three models and seven decisions. After card 91 lands (the lead will say); `feat!`.
- **senior-dev-deep** (2026-09-08T18:00:00.000Z): Picked the card up. Reset the worktree to origin/develop at cd23a4d ("card 91 done"), installed, built core and reran the review's probes against this worktree's dist to see the starting point: P2 clean (a foreign Money borrowed with no relationship, `packages/core/src/validate.ts:2280-2337` never reads `attribute.valueobject`), P3a clean (`by: [policy]` on an operation consumption, `packages/core/src/validate.ts:1966-2000`), P4 reports only the entity control and not the value object's relation into another aggregate's child (`packages/core/src/validate.ts:199-222`), P5 warns `entity-identity` inside a big ball of mud (`packages/core/src/validate.ts:341-359`), P6 clean (a domain service consuming a foreign operation). probe2 lists the eleven unbacked event consumptions: three in RiverMart, four in StreamLine, four in NorthBank. Working the checklist in order.
- **senior-dev-deep** (2026-09-08T21:00:00.000Z): Every item but the verify run is in. The answer step: `ReactionTrigger = Consumable | DataSchema` in `packages/core/src/workspace.ts:2185`, held by a policy's `on` and a process's `on` and `ends` (`starts` stays events only — an answer is what a caller gets back, so something was already waiting for it). `ReactionChain` keeps its listeners keyed by trigger and adds one step: an operation to whoever waits on what it returns or rejects with, `packages/core/src/reaction-walk.ts:105-131`, with `answersOf`, `operationsCalledBy` and `answeredBy` beside it so validate, the flow map and the pages read one definition of "an answer this context hears". The schema gets no node on the flow map: it is drawn as an edge from the operation, labelled with the shape, `packages/core/src/flow-map.ts:45-136`, and `flowEdgeLabel` (`packages/core/src/flow-map.ts:212-221`) is what all three renderers label with — graphviz at `packages/graphviz/src/flow-map.ts:58-72`, the pages graph at `packages/pages/src/lib/flow/flow-graph.ts:88-95`, and `FlowEdge.svelte` already drew a label. `consumable-kind` accepts an answer only when an operation the reactor's own context consumes answers with it (`packages/core/src/validate.ts:2470-2489`); a shape nothing answers with is refused with the fix naming what to write. Five rules in all: `valueobject-context` (`packages/core/src/validate.ts:2359-2385`), `consumption-by-operation` (`:2035-2062`), `domain-service-consumes-inside` (`:2352-2389`), `subscription-backed` (`:2181-2218`), and `value-object-shape` grown a second half so a value reaching an entity is refused where `cross-aggregate-reference` could never see it (`:395-419`). `entity-identity` now walks `knowableAggregatesOf` (`:376`), so a big ball of mud is exempt as it is from the other three; card 90's test that asserted the opposite is rewritten with the reason in it (`packages/core/src/validate.test.ts:4423-4446`).
- **senior-dev-deep** (2026-09-08T21:05:00.000Z): Model surgery, all of it the truthful shape the interviews already describe. RiverMart: `PaymentDeclined` stops being a published event and becomes the schema `AuthorisePayment` rejects with, which the Checkout process waits on (`models/rivermart/src/workspace.ts:1330-1420`); the consumption of the event goes, because the answer arrives down the call the orchestrator already declares. The deadline the process never had is `ExpireAuthorisations` on Payments' application service raising `AuthorisationExpired`, which the checkout ends on (`models/rivermart/src/workspace.ts:1355-1420`, `:1520`) — a scheduler calls it and nothing in the model consumes it, which is what an operation called from outside looks like (decision 28). NorthBank: `ScoreTransaction` returns `TransactionVerdict` and raises only `TransactionFlagged`; `TransactionCleared` is gone, because nobody publishes a clearance (`models/northbank/src/workspace.ts:1580-1700`). `KycScreening` held nothing but the outbound call, so `ScreenCustomer` moved to the onboarding application service and the domain service is gone (`models/northbank/src/workspace.ts:525-537`, `:648`). StreamLine's `Ranker` hands its two subscriptions to `RecommendationsAPI` with the reacting policy in `by` (`models/streamline/src/workspace.ts:1478-1500`). Petstore's process gained `CheckPetAvailable`, the operation that makes the availability call (`models/petstore/src/workspace.ts:748-800`), and `FindPetsByStatus` answers with a `Pets` schema whose attribute holds the list (`models/petstore/src/workspace.ts:283-300`). The eleven unbacked subscriptions: two gained a reactor the interviews state (Offers keeping its SKU list, Recommendations opening a taste profile), one joined the policy that already accumulates (Reporting and the nightly batch), and the rest name the operation the fact is for. Each model's DISCOVERY.md carries a revision section saying what changed and which sentence of which interview it comes from.
- **senior-dev-deep** (2026-09-08T22:10:00.000Z): Clean-code sweep found one thing worth fixing and I fixed it rather than tagging it: `ReactionChain` registered the operations that answer with a shape only from the contexts in scope, so a flow map drawn for one context — which is what the process page and the context page draw — could not name the neighbour's operation the answer came from. The chain now registers the operations a context *calls* as well (`packages/core/src/reaction-walk.ts:93-124`), and a test pins it (`packages/core/src/derived-maps.test.ts:645-657`). The near-twin names went with it: `chain.answerersOf(schema)` is what is in scope, `answeredBy(bc, schema)` is what one context's calls answer with, and the pages use the second. `PolicyPage`'s `withKind` parameter became dead once both its tables named the kind, so it is gone rather than left unreachable.
- **senior-dev-deep** (2026-09-08T22:20:00.000Z): `bash scripts/verify-all.sh` green end to end. Summary: core 511, graphviz 26, doc 31, skill 26, northbank 3, petstore 20 (schema comparison against core dist: match), rivermart 3, streamline 3, models/_shared 9, pages 749, apps/docs 23, apps/ods-vscode 15, pages e2e full suite passed. Diagnostics per model are exactly what they were before the card: petstore 0; RiverMart 2 (`aggregate-root`, `cross-aggregate-reference`); StreamLine 3 (`internal-consumable`, `schema-context`, `policy-complete`); NorthBank 4 (`relationship-declared`, `separate-ways`, `consumable-kind`, `context-serves-subdomain`) — every one of them a DELIBERATE, and the eleven `subscription-backed` warnings and petstore's `consumption-by-operation` error the new rules first reported are all answered in the models. `npx tsc --noEmit` clean in core, doc, graphviz and each model; `npm run check` in pages clean (939 files, 0 errors); biome clean on every file I touched (the three pre-existing offenders — `packages/doc/src/service.md.ts`, `packages/pages/src/lib/flow/fullscreen.svelte.ts`, `packages/pages/src/lib/templates/WorkspacePage.test.ts` — I left alone).
- **senior-dev-deep** (2026-09-08T22:25:00.000Z): The review's probes, rerun against this worktree's dist. **P2** (a root typed by another context's Money with no relationship): now 2 diagnostics — `[error] valueobject-context: "Inv" in "B" types attribute "total" by value object "Money" from "A"; ... wants a shared kernel with "A" or a conformist relationship toward it`, and `[warning] relationship-declared` for the same crossing. It was silent. **P3a** (`by: [policy]` on an operation consumption): `[error] consumption-by-operation: "BApp" says its consumption of "Op1" is made by policy "P"; a policy issues an operation of its own context and that operation makes the call, so name that operation here`. It was silent; the probe's own filter names `consumption-by-resolves`, so the line above is the same probe with the filter dropped. **P4** (a value object referencing a child of another aggregate): `[error] value-object-shape: Value object "Payee" references entity "Mandate"; a value is a value of something and nothing is reached through it ... hold "Mandate"'s id as an attribute with identifies instead`. `cross-aggregate-reference` still reports only the entity control, and now says so in its own doc comment: the gap is closed on the shape of a value object, where it belongs. **P5** (a big ball of mud with an unidentified child): 0 diagnostics, was 1. **P6** (a domain service consuming a foreign operation): `[error] domain-service-consumes-inside: Domain service "Calc" consumes "Op1" from "A" ... let an application service of "B" make the call and hand "Calc" what it needs`. It was silent. probe2's eleven unbacked event consumptions are 0 in all four models; the two services that provide nothing and exist to hold consumptions stay, which is the cost decision 17's second amendment names.
- **senior-dev-deep** (2026-09-08T22:30:00.000Z): Mechanics against the seven decisions, checked one by one. 13's note — a list answer is a schema whose attribute holds the list, petstore's `Pets` ✓. 15 — no delivery flag, no correlation or deadline field: the deadline is an operation raising an event, which is decision 28's own shape ✓. 16's third amendment — `valueobject-context` uses `mayBorrowFrom`, the predicate `schema-context` uses, and `relationship-declared` counts the borrowing ✓; the relation map is card 93's. 17's second amendment — `by` on an operation names operations, a domain service consumes inside, a consumed event nothing reacts to warns ✓. 23's second amendment — `on` and `ends` of a process and `on` of a policy may name an answer, and the walk and the map read it as a step from the operation ✓. 25's note — RiverMart's process waits on the rejection as the answer it is ✓. 27's third note — the rule's why-text and the invariant page say which of the two an aggregate's invariant is by whether it names a guard ✓. One thing I would put to the lead rather than decide: `starts` takes events only, because an answer is what a caller gets back and something must already have been waiting for it; the decision does not say so and I have written the reasoning into `ProcessSchema.starts` and `Process`.
