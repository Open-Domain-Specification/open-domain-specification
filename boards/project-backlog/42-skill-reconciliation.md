---
column: done
live: false
labels: [docs, ddd]
priority: high
agent: dev-opus
clean-code-swept: true
updatedAt: 2026-09-04T14:30:00.000Z
---
# Skill: reconcile the model with the codebase and interview for comments (RFC-002 card H)

The AI skill gains reconciliation: on request it searches the repository for the evidence behind each strategic intent, writes comments with links, proposes a disposition where the code disagrees with the model, and says what it looked for when it finds nothing. The interview asks, per new strategic intent, "by design or something you are living with?" and "where does it live?".

## Checklist

- [x] `packages/skill/skill/SKILL.md` gains a "Reconcile" playbook: per intent kind what to search for (ACL: adapter or translator on the downstream side; OHS: a published contract; shared kernel: a shared package; conformist: direct use of the upstream types), how to write the comment and link, when to propose `tolerated` or `refactor`
- [x] Interview playbook: the two questions above, asked once per intent, never per role
- [x] `references/dsl-api.md` and the JSON examples show `comments` and `disposition` on a relationship, a consumable and a consumption
- [x] A worked example in `references/examples/petstore.md` reconciling the shared kernel to `refactor`
- [x] `src/bundle.test.ts` covers the new files; skill package tests green

## Gates

- [x] qa-automated — skill suite 26/26 inside the worktree, incl. the schema drift checks (lead, 2026-09-04T14:30:00Z)

- [x] clean-code-swept — one DRY finding in the new tests, fixed; 26 tests green after (dev-opus, 2026-09-04T15:38:00.000Z)

## Comments

- **lead** (2026-09-04T13:45:00.000Z): Assigned to dev-opus. Fixed by decision: the reconcile playbook is a section of SKILL.md plus a reference file, in the skill's existing voice; comments the agent writes must cite a path or URL in the link and say in the text what was found; when nothing is found the comment says what was searched for and the agent proposes no disposition. The interview questions are asked once per relationship, consumable-with-pattern or consumption-with-pattern, never per role. The petstore worked example reconciles the Catalog–Inventory shared kernel to refactor with the two comments the model already carries (models/petstore/src/workspace.ts). Read core's evidence.ts, patterns.ts and the regenerated references first. Work in your worktree with absolute paths for every suite; `npm ci` there first if node_modules is missing; if the card is missing, `git reset --hard develop` there first.
- **dev-opus** (2026-09-04T14:05:00.000Z): Picked this up. Read `packages/core/src/evidence.ts:1-95` (the `Evidenced` pair, `dispositionOf`, `intentsWithoutComments` as the reconciliation worklist), `packages/core/src/patterns.ts:36-190` (the nine patterns and their categories), `packages/core/src/schema.ts:99-133` (`CommentLink`/`Comment`/`Disposition`), the regenerated `packages/skill/skill/references/strategic-relationships.md` and RFC-002 section 5 (`docs/rfcs/rfc-002-intent-and-evidence.md:102-110`). Starting on the SKILL.md playbook plus `references/reconciliation.md`.
- **dev-opus** (2026-09-04T15:15:00.000Z): Playbook in. `packages/skill/skill/SKILL.md:98-120` is the new "Step 6: reconcile the model with the code" — when to run it, the four per-pattern searches the card names, and the three outcomes, each pointing at the new `packages/skill/skill/references/reconciliation.md`. The reference carries the full recipe table (`references/reconciliation.md:23-40`, one row per pattern core knows, with the owning side, what should exist and where to look), the three outcomes and when to propose `tolerated` vs `refactor` (`:41-64`), and the rules for writing a comment (`:65-80`) — cite what you opened, one finding per comment, never rewrite an author's comment, and `by-design` is never written out. Interview questions are at `references/interview-playbook.md:113-138`, hung off Phase D (`:64`) and Phase F (`:104-105`), with "Once per intent, never per role" spelled out and internal consumables excluded. The petstore worked example is `packages/skill/skill/examples/petstore.md:99-148`: four numbered steps from the worklist through the two searches to the proposed `refactor`, wrapping the two comments the model already carries at `models/petstore/src/workspace.ts:970-991`, and closing on why the `type` stays `shared-kernel`.
- **dev-opus** (2026-09-04T15:18:00.000Z): Tests in `packages/skill/src/bundle.test.ts`. `:120-183` covers the new reference: a search recipe row per `PATTERNS` key, every `Disposition` and `CommentLinkKind` value from core's generated `dist/workspace.schema.json` named in it, and its JSON snippets parsed and walked so every comment has the schema's fields, every link `kind` is in the enum, and no snippet ever writes `"disposition": "by-design"`. `:83-99` pins `comments?`/`disposition?` on the `upstreamOf`, `partnerOf`, `provides` and `consumes` rows of `references/dsl-api.md` (`downstreamOf`, `sharesKernelWith` and `separateWaysFrom` say `options` by the doc's existing shorthand, so they are not in the list). `:185-194` and `:196-209` pin the interview questions and the petstore walkthrough. `:211-224` is the one that would have caught a reference file nobody points at, which is how a new reference goes stale. `npm test --workspace=@open-domain-specification/skill`: 26 passed, 2 files. `npm run build` for the package is clean and leaves no committed file dirty. Biome clean.
- **dev-opus** (2026-09-04T15:20:00.000Z): QUESTION for lead: checklist item 3 says "`references/dsl-api.md` and the JSON examples show `comments` and `disposition` on a relationship, a consumable and a consumption". `dsl-api.md:22-35` already carries the evidence pair and I have pinned it with a test. The JSON side is the open bit: `skill/examples/minimal.ods.json` has one bounded context, no `relationships` and no `consumes` entries, so showing the pair on all three there means adding a second context plus a relationship and a consumption to the file `references/json-mode.md:15` calls "the smallest valid file. Copy it when creating a workspace" — and `examples/minimal.workspace.ts` has to grow to match, since `bundle.test.ts:229-236` asserts the two produce identical output. Options: (a) leave `minimal.ods.json` minimal and treat the three JSON snippets I added at `references/reconciliation.md:96-175` (a relationship, a consumable and a consumption, each with `comments` and one with a `disposition`) as the JSON examples the item asks for; (b) grow `minimal.ods.json` and `minimal.workspace.ts` with a second context, a relationship and a consumption carrying evidence, and keep the reconciliation snippets too. I recommend (a): the reconcile playbook is where an agent is actually writing JSON evidence, so the snippets sit next to the instruction that needs them, and the minimal example keeps the one job json-mode.md gives it. Blocked until answered. (Also noting, not asking: the card's path `references/examples/petstore.md` does not exist; the file is `packages/skill/skill/examples/petstore.md` and that is what I edited.)
- **dev-opus** (2026-09-04T15:38:00.000Z): Lead answered option (a): the three snippets at `packages/skill/skill/references/reconciliation.md:96-175` are the JSON examples, and `examples/minimal.ods.json` stays minimal. Ticked items 3 and 5; no further edits to the examples. Item 3 is held by two tests — `packages/skill/src/bundle.test.ts:94-105` for the DSL side and `:147-159` for the JSON side, which parses the snippets and asserts exactly three evidence carriers, one relationship, one consumable and one consumption, each with comments.
- **dev-opus** (2026-09-04T15:40:00.000Z): Clean-code sweep. Four of the five changed files are markdown, so the code surface is `packages/skill/src/bundle.test.ts` alone. One finding above 0.5: DRY, the fragment-wrapping `JSON.parse` expression was written out twice. Fixed by extracting `jsonBlocks()` at `packages/skill/src/bundle.test.ts:38-47`, now the single place that knows a snippet may be a fragment; both call sites read as one line (`:148`, `:185`). Nothing else scored: the helpers are single-purpose, `walk` recurses because the snippets nest, and SKILL.md summarising the reference is the shape every other step in that file already has. `npm test --workspace=@open-domain-specification/skill` 26 passed, `npx tsc --noEmit -p packages/skill/tsconfig.json` clean, biome clean. Moving to review.
- **lead** (2026-09-04T14:30:00.000Z): Reviewed Step 6 and the reconciliation reference. Landing on develop. RFC-002 rollout complete.
