---
column: done
labels: [backend, ddd, breaking]
priority: medium
agent: ironhide
live: false
clean-code-swept: true
updatedAt: 2026-09-05T02:20:00.000Z
---
# A consumption names the operations that make it

The sixth review run (issue 6) is right that a consumption declared on a service says every operation of that service depends on the consumable, when usually one or two do. StreamLine's subscription service consumes a payment gateway; listing entitlements does not. The atlas view is unchanged, the consumer node still depends on the provider, but the evidence beneath it should be able to say which of the consumer's own operations or policies make the exchange.

`ConsumptionSchema.by?: Array<{ $ref: string }>`: the consumer's own consumables (operations) or policies that make this exchange. Absent means the whole consumer, which stays the default and the common case. This is optional detail, not a call graph; nothing derives sequence diagrams from it. The seventh review run (issue 4) asks for the same link from the other side, which provided operation invokes which consumption, and `by` answers it.

## Checklist

- [x] `by?: Array<{ $ref }>` on `ConsumptionSchema`; workspace model, DSL (`consumes(consumable, { by: [...] })`), `toSchema`/`fromSchema`, JSON schema regenerated
- [x] `consumption-by-resolves` (error): every `by` ref resolves to a consumable or policy of the consumer node itself, not of another node or context
- [x] Reference models: StreamLine's subscription service names the operations behind its payment gateway consumption; petstore names one; the core fixture `makeTestWs.ts` carries one, so the pages fixture shows it
- [x] Consumable page and context page list, under each consumption, the operations that make it; the consumable map's edge hover shows them; doc generator prints them; skill reference and interview ("which operations of this service actually make that call?")
- [x] Decision 21 written: a consumption belongs to the consumer and may name the operations behind it
- [x] Root suites green inside the worktree; pages at 100%

## Gates

- `clean-code-swept`: swept 2026-09-05. The doc generator had `consumptionSection` written out twice, byte-identical, on the aggregate page and the service page; rather than add the new **Made by** line in two places I moved it to one `packages/doc/src/consumptions.md.ts` that both import, so the shape of a consumption on a page is stated once. Nothing else was in the way: the new field threads through existing seams (`getConsumptionCallerByRef` sits beside `getConstrainableByRef`, the map edge carries names as the patterns already do). biome and tsc clean on every file touched. Pre-existing and deliberately untouched: the two `noThenProperty` errors in `packages/core/src/workspace.ts` (`Policy.then`, which decision 09 keeps as a method), unchanged from develop.
- `qa-automated`: in build order, inside each package directory with absolute paths. core `npm run build` + 307 passed (13 files); graphviz build + 19 (6 files); doc build + 26 (4 files); skill `npm run generate` via build + 26 (2 files); pages `npm run build` + 667 passed across 94 files, coverage 100/100/100/100, `npm run check` 922 files 0 errors 0 warnings; ods-vscode `npm run build` clean. Models, each `npm run build` then `npx vitest run`: petstore 18 (0 diagnostics), streamline 3 (4 diagnostics, its own `deliberate` list), northbank 3 (3), rivermart 3 (3). `cmp models/petstore/.ods/schema.json packages/core/dist/workspace.schema.json` silent. apps/docs 21 passed | 1 failed — the pre-existing `doc.example.test.ts` Health section, card 52, left red as instructed. `npx tsc --noEmit -p tsconfig.json` clean in core, doc, the four models and apps/docs.

## Comments

- **optimus-prime** (2026-09-07T02:10:00.000Z): Ironhide, after card 54 lands (the lead will say); schema changes are serialised. `feat!:`. Work in your worktree with absolute paths; `git reset --hard origin/develop` there first.
- **ironhide** (2026-09-05T01:20:00.000Z): Claimed. Reset the worktree to origin/develop (00d7d7f, the sprint doc commit on top of the card 54 merge) and ran `npm install` there, since a fresh worktree has no node_modules and would otherwise resolve every cross-package import up to the main checkout.
- **ironhide** (2026-09-05T01:30:00.000Z): The field and the DSL. `ConsumptionSchema.by?: { $ref }[]` (packages/core/src/schema.ts); `ConsumptionAttributes.by?: ConsumptionCaller[]` and `Consumption.by` with its `toSchema` (packages/core/src/workspace.ts), so the DSL reads exactly as the card asked: `orderApp.consumes(reservePetForOrder, { pattern: "anti-corruption-layer", by: [reservePetForApproved] })`. `ConsumptionCaller = Consumable | Policy` is the new alias, with `getConsumptionCallerByRef`/`OrThrow` beside the existing `getConstrainableByRef` pair, because a `by` ref may land on either kind. Loading needed no new pass: consumptions are already linked after every consumable is added, and policies exist from the first pass. An empty `by` serialises as absent, so a model that says nothing is byte-identical to before.
- **ironhide** (2026-09-05T01:35:00.000Z): The rule. `consumption-by-resolves` (error) at the consumer's ref, in three shapes: a consumable provided by another node, a policy of another context, and — the one the card implies with "consumables (operations)" — an event. An event is something that has already happened, so it calls nothing; I included it rather than leaving a nonsense `by` legal, and said so in the message and in decision 21. The reason in the doc comment and the catalogue is the DDD one: a consumption belongs to the consumer, so what makes it is the consumer's own, and no node may declare behaviour it does not own. It parses no text.
- **ironhide** (2026-09-05T01:40:00.000Z): The models, and one honest note on the card's wording. StreamLine has no payment gateway context — the prose in the card is the illustration, not the model — so I set `by` on the two consumptions where the same thing is true of Billing: `subscriptionAgg.consumes(householdCreated, { by: [awaitPlan] })` and `subscriptionAgg.consumes(discRentalInvoiced, { by: [addDiscCharge] })`. Each is read by exactly one reaction; renewing, dunning and answering entitlement never touch either. Both consumptions moved below the policy that makes them, since a policy has to exist before it can be named. Petstore names one, the operation case: `orderApp.consumes(reservePetForOrder, { by: [ReservePet] })` — placing, reading or deleting an order never calls Catalog. Its twin `markPetSoldForOrder` is deliberately left plain, so one model shows both the named form and the default, and the core fixture carries the policy case (`invoiceAgg.consumes(orderPlaced, { by: [invoiceOnOrderPlaced] })`), which is what the pages fixture then shows.
- **ironhide** (2026-09-05T01:50:00.000Z): The reading surfaces. Core's consumable map edge carries the names (`ODSConsumptionMapEdge.by`), which is what lets every renderer show them without re-walking the model; the graphviz test's hand-built edge gained `by: []` rather than the type being loosened. Doc: a `- **Made by**: ...` line under each consumption on the aggregate and service pages, and a **Made By** column on the context page's Consumptions table, `-` where it is the whole consumer. Pages: the same column on `ConsumesTable`, which is what the consumable page, the context page, the aggregate page and the service page all draw, with a `whole consumer` keyword in the default case; the consumable map's edge shows "Made by X, Y" on hover of the line, as an SVG title on the edge label, and there is a story for it. Skill: `dsl-api.md` carries the option, `translation-table.md` a row for the phrases that produce it, the interview asks "which operations of this service actually make that call?" and is told to take "all of it" for an answer and never to guess a call graph, the petstore example shows it, and `model-reference.md`/`validation-rules.md` came through `npm run generate`. apps/docs: a Consumptions section on the tactical page, a row in the validation table, and `tests/tactical.example.test.ts` — the file that page embeds — now declares a consumption with `by`.
- **ironhide** (2026-09-05T01:55:00.000Z): Two test-suite notes. `rule-catalog.test.ts` requires every catalogued rule to fire on its everything-wrong fixture, so that fixture now names a provider's own operation as what makes a consumer's consumption. `apps/docs/tests/__snapshots__/model.example.test.ts.snap` gained one `"by": undefined` line, the same way `comments` and `disposition` already appear; regenerated with `-u`, no other snapshot moved. The only red left in the tree is `doc.example.test.ts`'s Health section, which is card 52's and untouched.
- **ironhide** (2026-09-05T02:00:00.000Z): Decision 21 written as `decisions/21-a-consumption-names-the-operations-behind-it.md`, status Accepted, with the reason the card gave: the atlas is unchanged — same nodes, same edge — the evidence beneath it may say which operations make the exchange, and nothing derives a sequence diagram, a call graph or an ordering from it. It records that `by` is a set and not a sequence, that absent and empty mean the same thing so there is one way to say "all of it", and rejects the mirror field on the provider (a `calls` back-link would store the same fact where the provider cannot know it).
- **ironhide** (2026-09-05T02:20:00.000Z): Card moved to `review`, `live: false`. Nothing here needs the lead before merge; the gates lines carry the counts.
