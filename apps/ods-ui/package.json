{
	"name": "ods-ui",
	"version": "0.1.9",
	"description": "The ODS viewer published at open-ds.io: the pages viewer bundle as a deployable site",
	"private": true,
	"type": "module",
	"scripts": {
		"clean": "npx rimraf dist",
		"build": "node scripts/build.mjs",
		"depcheck": "npx depcheck"
	},
	"dependencies": {
		"@open-domain-specification/pages": "^0.1.9"
	}
}
