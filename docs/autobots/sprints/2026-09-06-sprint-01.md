# Sprint 01 — the metamodel survives external review

Goal: the Antigravity prompt returns no issue that is not a written decision. Roadmap milestone 3.

## Tasks (in dependency order)

| Task | Card | Owner | Status | Note |
| --- | --- | --- | --- | --- |
| Operations declare `returns` | project 44 | dev-opus (carried over from before the persona switch) | merging develop and regenerating models | decision 13 |
| Cross-context relations are identities; sweep four models and the core fixture; Carrier aggregate in petstore | project 45 | dev-opus (carried over) | shipped e0f9f8d | decision 14 |
| Validator rules the model states but never enforces (7 rules) | project 47 | Ironhide | queued behind 44/45 | baseline issues 9-11, 13-16 |
| A context acts through its own boundary (3 rules; petstore restructure so PetApp is the open host) | project 48 | Ironhide | queued behind 47 | decision 17; baseline 3, 7 |
| Value objects at context scope; shared kernel shares them | project 49 | Ironhide | queued behind 48 | decision 16; feat!; baseline 4, 5 |
| Schemas compose; invariants constrain operations | project 50 | Ironhide | queued behind 49 | decisions 18, 19; baseline 8, 12 |
| Adversarial pass over the new rules against the four models | new card after 50 | Ratchet | queued | the rules must fail on the shapes they guard and pass on the models |
| Re-run the Antigravity prompt after each landing; diff against the baseline | none | Optimus | run 2 in progress after card 45 | scratchpad agy-baseline.md, agy-run2.md |
| Docs app snapshots stale and never run in CI | project 52 | Bumblebee-lite | queued for sprint end | found by card 45 |

Riskiest assumption, tested first: that the two in-flight schema and rule changes (44, 45) merge cleanly through petstore. Order of landing is 45 then 44.

## Decisions made this sprint

13 returns; 14 cross-context relations; 15 what is left out and why (`on` is any-of); 16 value objects at context scope; 17 a context acts through its boundary; 18 schemas compose; 19 invariants constrain operations. All under `decisions/`.

## Shipped

- Card 45: `cross-context-relation` rule; sixteen relations across four models and the core fixture became identity attributes; petstore Carrier and core Basket aggregates keep a same-context `references` demonstrated.

## Carried

Nothing yet.
